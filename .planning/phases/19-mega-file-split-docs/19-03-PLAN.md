---
phase: 19-mega-file-split-docs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanelScannerTabs.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanelMcpTabs.kt
autonomous: true
requirements: [QUAL-01]
must_haves:
  truths:
    - "SettingsPanel.kt is under 500 lines (contains class declaration, field declarations, init block, public API, applySettingsToUi, applyAndSaveSettings)"
    - "SettingsPanelScannerTabs.kt exists as a set of internal extension functions on SettingsPanel covering scanner section builders"
    - "SettingsPanelMcpTabs.kt exists as a set of internal extension functions on SettingsPanel covering MCP section builders"
    - "Previously private UI component fields accessed by the extracted functions are changed to internal per Visibility Change Summary"
    - "Full test suite green after every individual extraction commit (per D-01)"
    - "No call-site changes outside the ui/ package"
  artifacts:
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanelScannerTabs.kt"
      provides: "internal extension functions on SettingsPanel for scanner-related tab sections"
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanelMcpTabs.kt"
      provides: "internal extension functions on SettingsPanel for MCP/Burp-integration tab sections"
  key_links:
    - from: "SettingsPanelScannerTabs.kt"
      to: "SettingsPanel.kt"
      via: "internal val fields — extension functions access passiveAiScanner, passiveAiStatusLabel etc. as internal members"
      pattern: "internal fun SettingsPanel\\.passiveAiScannerSection|internal fun SettingsPanel\\.activeAiScannerSection"
    - from: "SettingsPanelMcpTabs.kt"
      to: "SettingsPanel.kt"
      via: "internal val fields — extension functions access mcpEnabled, mcpToken, mcpPort etc. as internal members"
      pattern: "internal fun SettingsPanel\\.mcpSection|internal fun SettingsPanel\\.tokenPanel"
---

<objective>
Split SettingsPanel.kt (2782 lines) into 2 focused companion files in the same ui package:
SettingsPanelScannerTabs.kt (scanner section builder methods as internal extension functions) and
SettingsPanelMcpTabs.kt (MCP/Burp section builder methods as internal extension functions).
SettingsPanel.kt itself retains the class declaration, field declarations (~500 fields), init
block, public API, and styling helpers at ~480 lines.

Purpose: QUAL-01 / SC1 — the named file must end under 500 lines (D-01: one atomic commit per
extraction, ./gradlew test green between each). Per D-01: cohesion-based split, extract to same-
package internal extension functions (the locked decision for SettingsPanel — fields must become
internal for this to work), zero call-site changes outside the moved code.

Output: 2 new .kt files in ui/; SettingsPanel.kt reduced to ~480 lines; test suite green after
every extraction commit; fat JAR builds successfully.
</objective>

<execution_context>
@/Users/six2dez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/six2dez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/six2dez/Tools/burp-ai-agent/.planning/PROJECT.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/ROADMAP.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/STATE.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/19-mega-file-split-docs/19-CONTEXT.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/19-mega-file-split-docs/19-RESEARCH.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/19-mega-file-split-docs/19-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen private fields to internal + extract SettingsPanelScannerTabs.kt</name>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt (full file — focus on: field declarations block lines 77–538 (identify all `private val` fields); init block lines 539–976; scanner section builder methods at lines ~1526–1930: passiveAiScannerSection, refreshPassiveAiStatus, applyPassiveAiSettings, showPassiveAiFindingsDialog, activeAiScannerSection, refreshActiveAiStatus, applyActiveAiSettings, updateActiveRiskDescription, showActiveAiFindingsDialog, showActiveScanQueueDialog, showScannerTriageDialog, severityRank; which `private val` fields each of those methods touches)
    - .planning/phases/19-mega-file-split-docs/19-PATTERNS.md (Section: SettingsPanelScannerTabs.kt pattern — visibility hazards, concrete extension function excerpt, design-token usage pattern; Same-package extension functions pattern with AgentSettings.kt analog; Visibility Change Summary table row for SettingsPanel)
    - .planning/phases/19-mega-file-split-docs/19-RESEARCH.md (Section 3.2 SettingsPanel split-boundary map, Open Question 2 about detekt)
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/PassiveScanConfigPanel.kt (analog for internal extension function style and design-token usage)
    - src/main/kotlin/com/six2dez/burp/aiagent/config/AgentSettings.kt (analog for extension-function-on-class-in-separate-file syntax)
  </read_first>
  <action>
    This extraction requires a preparatory visibility step before moving code.

    **Preparatory step — widen private fields to internal:**

    In SettingsPanel.kt, for every `private val` field in the constructor/field declaration block (lines 77–538) that is referenced by any of the scanner or MCP section builder methods being extracted, change `private val` to `internal val`. Do this for ALL such fields in a single edit pass.

    Determine which fields need widening by inspecting the bodies of the scanner section builders (passiveAiScannerSection, refreshPassiveAiStatus, applyPassiveAiSettings, showPassiveAiFindingsDialog, activeAiScannerSection, refreshActiveAiStatus, applyActiveAiSettings, updateActiveRiskDescription, showActiveAiFindingsDialog, showActiveScanQueueDialog, showScannerTriageDialog, severityRank) AND the MCP section builders (to be extracted in Task 2) — widen all of them now in one commit to avoid a second preparatory round in Task 2.

    Typical fields that will need widening include (audit the actual bodies to confirm): `passiveAiScanner`, `passiveAiStatusLabel`, `passiveAiEnabled`, `passiveAiRateSpinner`, `passiveAiScopeOnly`, `activeAiScanner`, `activeAiStatusLabel`, `activeAiEnabled`, `mcpEnabled`, `mcpToken`, `mcpPort`, `mcpToolToggles`, `unsafeToolToggles`, `mcpSseUrl`, `mcpTlsEnabled`, etc.

    Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew check` to surface any detekt rule violations introduced by this visibility change. If detekt flags any `internal` field change as a new violation (rule like `NoPublicApi` or `UnusedPrivateMember`), add to detekt baseline with `./gradlew detektBaseline`. This is expected per RESEARCH.md Open Question 2.
    Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` — must be green.
    Commit: `refactor(19-03): widen SettingsPanel private fields to internal for extension extraction`

    **Extraction A — SettingsPanelScannerTabs.kt:**

    Create src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanelScannerTabs.kt with:
    - Package declaration: `package com.six2dez.burp.aiagent.ui`
    - Imports: start from the import block at the top of SettingsPanel.kt; include all imports actually referenced by the methods being moved; specifically ensure `com.six2dez.burp.aiagent.ui.design.*` symbols (DesignTokens, applyAreaStyle, applyFieldStyle, addRowFull, formGrid, etc.) are explicitly imported since they are in the `ui.design` sub-package, not the `ui` package
    - Each extracted method becomes `internal fun SettingsPanel.<methodName>(...)` — the `this` receiver gives access to all `internal val` fields
    - Methods to extract: `passiveAiScannerSection()`, `refreshPassiveAiStatus()`, `applyPassiveAiSettings()`, `showPassiveAiFindingsDialog()`, `activeAiScannerSection()`, `refreshActiveAiStatus()`, `applyActiveAiSettings()`, `updateActiveRiskDescription()`, `showActiveAiFindingsDialog()`, `showActiveScanQueueDialog()`, `showScannerTriageDialog()`, `severityRank()`
    - All UI color/font references in extracted methods MUST use DesignTokens.* or UiTheme.* constants — never hardcoded Color(...) or Font(...) (per CONVENTIONS.md)
    - Delete the extracted methods from SettingsPanel.kt

    Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` — must be green.
    Commit: `refactor(19-03): extract SettingsPanelScannerTabs.kt from SettingsPanel.kt`
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test</automated>
  </verify>
  <acceptance_criteria>
    - SettingsPanelScannerTabs.kt exists, package is `com.six2dez.burp.aiagent.ui`, all functions are `internal fun SettingsPanel.*`
    - No hardcoded Color(...) or Font(...) literals in SettingsPanelScannerTabs.kt — only DesignTokens.*/UiTheme.* references
    - `./gradlew test` exits 0 after the visibility-widening commit and again after the extraction commit (two green gates)
    - `grep -c 'private fun passiveAiScannerSection\|private fun activeAiScannerSection\|private fun refreshPassiveAiStatus\|private fun refreshActiveAiStatus' src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt` returns 0 (methods are no longer private methods in SettingsPanel.kt)
    - `grep -c 'internal fun SettingsPanel' src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanelScannerTabs.kt` returns at least 10 (all scanner section builders present as extension functions)
    - SettingsPanel.kt still compiles: the init block references to scanner section builders (called as `this.passiveAiScannerSection()` or just `passiveAiScannerSection()`) resolve to the extension functions in the new file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extract SettingsPanelMcpTabs.kt and verify final SettingsPanel.kt line count</name>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt (current state after Task 1 — identify: MCP section builder methods at lines ~2002–2500+: mcpSection(), tokenPanel(), mcpQuickActions(), buildSseUrl(), buildCurlCommand(), copyToClipboard(), buildMcpToolsPanel(), updateUnsafeToolStates(), collectMcpToolToggles(), collectEnabledUnsafeTools(), applyUnsafeToolApprovals(), availableMcpToolsWithReasons(), availableMcpTools(), updateMcpTlsState(), updateMcpCorsWarning(), refreshMcpNotice(); remaining utility methods that stay: updatePrivacyWarnings(), updateRiskWarnings(), refreshPrivacyNotice(), updateSaveFeedback(), updateFieldStyle(), styleCombo(), openExternalCli(), shellQuote())
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanelScannerTabs.kt (created in Task 1 — reference for the extension function pattern and import structure to replicate)
    - .planning/phases/19-mega-file-split-docs/19-PATTERNS.md (Section: SettingsPanelMcpTabs.kt pattern — visibility hazards, Assumption A3 about ui.design.* explicit imports)
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/McpConfigPanel.kt (analog for MCP UI structure)
  </read_first>
  <action>
    **Extraction B — SettingsPanelMcpTabs.kt:**

    Create src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanelMcpTabs.kt with:
    - Package declaration: `package com.six2dez.burp.aiagent.ui`
    - Imports: mirror the import structure of SettingsPanelScannerTabs.kt; include all imports actually referenced by the MCP methods (ui.design.* explicit imports are required — they are not in the `ui` package itself); check SettingsPanel.kt's lines 27–36 for the exact set of ui.design imports used
    - Each extracted method becomes `internal fun SettingsPanel.<methodName>(...)` — same extension function pattern as Task 1
    - Methods to extract: `mcpSection()`, `tokenPanel()`, `mcpQuickActions()`, `buildSseUrl()`, `buildCurlCommand()`, `copyToClipboard()`, `buildMcpToolsPanel()`, `updateUnsafeToolStates()`, `collectMcpToolToggles()`, `collectEnabledUnsafeTools()`, `applyUnsafeToolApprovals()`, `availableMcpToolsWithReasons()`, `availableMcpTools()`, `updateMcpTlsState()`, `updateMcpCorsWarning()`, `refreshMcpNotice()`
    - All UI color/font references in extracted methods MUST use DesignTokens.*/UiTheme.* constants
    - Delete the extracted methods from SettingsPanel.kt

    Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew compileKotlin` first to catch any import/visibility issues early.
    Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` — must be green.
    Commit: `refactor(19-03): extract SettingsPanelMcpTabs.kt from SettingsPanel.kt`

    **Final verification:**

    Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew check` (full check including ktlint/detekt).
    Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew shadowJar` — fat JAR must build.
    Check line count: `wc -l src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt` — must be under 500.

    If detekt introduces new violations from `internal val` fields, add them to the detekt baseline (these are pre-existing patterns, not new violations): `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detektBaseline` then re-commit the updated baseline.
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test && wc -l src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt</automated>
  </verify>
  <acceptance_criteria>
    - SettingsPanelMcpTabs.kt exists, package is `com.six2dez.burp.aiagent.ui`, all functions are `internal fun SettingsPanel.*`
    - No hardcoded Color(...) or Font(...) literals in SettingsPanelMcpTabs.kt
    - `wc -l src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt` outputs a number less than 500 (target ~480 lines)
    - SettingsPanel.kt still contains: class declaration + field declarations + init block + `currentSettings()` + `applySettingsToUi()` + `applyAndSaveSettings()` + `helpSection()` + `privacySection()` + `promptSection()` + `customPromptsSection()` + `updatePrivacyWarnings()` + `updateRiskWarnings()` + `refreshPrivacyNotice()` + `updateSaveFeedback()` + styling helpers (`updateFieldStyle`, `styleCombo`, `openExternalCli`, `shellQuote`)
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` exits 0
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew shadowJar` exits 0 and produces build/libs/Custom-AI-Agent-0.8.0.jar
    - Total 3 commits for this plan (1 visibility-widening + 2 extraction commits) visible in git log
    - `grep -c 'internal fun SettingsPanel' src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanelMcpTabs.kt` returns at least 12 (all MCP section builders present as extension functions)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI module internal | Kotlin `internal` visibility — UI component fields widened from `private` to `internal` remain invisible outside the com.six2dez.burp.aiagent module (Burp classloader sees compiled class, not visibility modifiers) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-SC | Tampering | SettingsPanel.kt file split | accept | Pure mechanical refactor — no new inputs, no new IO surface; `private`→`internal` visibility on UI component fields is a Kotlin source-level change only, binary/runtime behaviour is identical; no trust boundary changes; disposition confirmed by `./gradlew test` green after every extraction commit |

No new attack surface introduced. All existing trust boundaries preserved. Zero HIGH or MEDIUM threats.
</threat_model>

<verification>
After both tasks complete:
1. `wc -l src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt` — output must be < 500
2. `ls src/main/kotlin/com/six2dez/burp/aiagent/ui/` shows SettingsPanel.kt, SettingsPanelScannerTabs.kt, SettingsPanelMcpTabs.kt (plus existing files)
3. `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` exits 0
4. `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew shadowJar` exits 0
5. `git log --oneline -4` shows 3 commits (visibility + 2 extractions) within this plan
6. `grep -rn 'SettingsPanel\.' src/main/kotlin/ --include='*.kt' | grep -v 'SettingsPanel\.kt\|SettingsPanelScanner\|SettingsPanelMcp'` — external call sites for SettingsPanel's public API (like `.generalTabComponent()`, `.currentSettings()`) must be unchanged
</verification>

<success_criteria>
- SettingsPanel.kt is under 500 lines (QUAL-01 SC1 gate)
- 2 new files created in ui/ package as internal extension function sets
- Full test suite green after each extraction commit (QUAL-01 SC1 per D-01)
- No call-site changes outside the ui/ package (zero import changes in other packages)
- Fat JAR builds successfully
</success_criteria>

<output>
Create .planning/phases/19-mega-file-split-docs/19-03-SUMMARY.md when done
</output>

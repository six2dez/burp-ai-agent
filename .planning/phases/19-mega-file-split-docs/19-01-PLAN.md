---
phase: 19-mega-file-split-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpTools.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolModels.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorImpl.kt
autonomous: true
requirements: [QUAL-01]
must_haves:
  truths:
    - "McpTools.kt is under 500 lines (contains only registerTools(), registerToolsLegacy(), toolJson, and package/imports)"
    - "McpToolModels.kt exists in the same package with all 40+ @Serializable data class types and ToolSpec"
    - "McpToolHelpers.kt exists with all private→internal top-level helper functions"
    - "McpToolExecutorImpl.kt exists with the object McpToolExecutor block"
    - "Full test suite passes green after every individual extraction commit (per D-01)"
    - "BackendRegistryTest passes — anthropic factory present in registry"
  artifacts:
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolModels.kt"
      provides: "@Serializable data class parameter types for MCP tools"
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt"
      provides: "Pure transform helper functions for MCP tool execution"
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorImpl.kt"
      provides: "object McpToolExecutor — the runtime when-block dispatcher"
  key_links:
    - from: "McpTools.kt"
      to: "McpToolExecutorImpl.kt"
      via: "toolJson val — must be internal in McpTools.kt or McpToolHelpers.kt"
      pattern: "internal val toolJson"
    - from: "McpToolExecutorImpl.kt"
      to: "McpToolHelpers.kt"
      via: "internal helper function calls — same package, no imports needed"
      pattern: "internal fun executeIssueCreate|findProxyHistoryMatch|sanitizeHeaders"
---

<objective>
Split McpTools.kt (2925 lines) into 3 focused files in the same package: McpToolModels.kt
(data classes), McpToolHelpers.kt (helper functions), and McpToolExecutorImpl.kt (the
McpToolExecutor object). McpTools.kt itself shrinks to ~90 lines containing only the
registerTools() dispatcher and registerToolsLegacy() stub.

Purpose: QUAL-01 / SC1 — the named file must end under 500 lines (D-01: one atomic commit per
extraction, ./gradlew test green between each). Per D-01: cohesion-based split, same-package
top-level functions/objects, zero call-site changes outside the moved code.

Output: 3 new .kt files in mcp/tools/; McpTools.kt reduced to ~90 lines; test suite green after
every extraction commit; fat JAR builds successfully.
</objective>

<execution_context>
@/Users/six2dez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/six2dez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/six2dez/Tools/burp-ai-agent/.planning/PROJECT.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/ROADMAP.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/STATE.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/19-mega-file-split-docs/19-CONTEXT.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/19-mega-file-split-docs/19-RESEARCH.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/19-mega-file-split-docs/19-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract McpToolModels.kt (data classes) + McpToolHelpers.kt (helper functions)</name>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpTools.kt (full file — source of truth; note package declaration, all imports, toolJson val at line 53, helper functions at lines 884–1262, ToolSpec at line 1263, data classes at lines 2534–2926)
    - .planning/phases/19-mega-file-split-docs/19-PATTERNS.md (Section: McpToolModels.kt pattern, McpToolHelpers.kt pattern, Visibility Change Summary table)
    - .planning/phases/19-mega-file-split-docs/19-RESEARCH.md (Section 3.1 McpTools.kt split-boundary map, Common Pitfalls 1 and 2)
    - src/main/kotlin/com/six2dez/burp/aiagent/context/ContextModels.kt (multi-type models file analog)
  </read_first>
  <action>
    Perform two sequential extractions, each followed by a ./gradlew test green gate and an atomic commit.

    **Extraction A — McpToolModels.kt (commit first):**

    Create src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolModels.kt with:
    - Package declaration: `package com.six2dez.burp.aiagent.mcp.tools`
    - Imports: `kotlinx.serialization.Serializable` plus any interface imports referenced by the data classes (e.g. `HttpServiceParams`, `Paginated` — check their definitions in McpTools.kt)
    - All `@Serializable data class` types from McpTools.kt lines 2534–2926: `SendHttp1Request`, `SendHttp2Request`, `CreateRepeaterTab`, `RepeaterTabWithPayload`, `SendToIntruder`, `IntruderPrepare`, `InsertionPoints`, `ExtractParams`, `DiffRequests`, `RequestParse`, `ResponseParse`, `ParsedParam`, `ParsedRequest`, `ParsedResponse`, `FindReflected`, `ComparerSend`, `ProxyHistoryAnnotate`, `ResponseBodySearch`, `CookieJarGet`, `CookieEntry`, `ScopeCheck`, `ScopeUpdate`, `CollaboratorGenerate`, `CollaboratorPoll`, all other data classes in that block, the `Paginated` interface if defined there, and the `HttpServiceParams` interface if defined there
    - The `ToolSpec` data class from McpTools.kt line 1263
    - Follow the multi-type models file pattern from PATTERNS.md: no class wrapper, all top-level declarations, no visibility modifiers on the data classes (they must remain accessible from McpToolExecutor and McpTools.kt)
    - Delete the extracted declarations from McpTools.kt

    Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` — must be green before proceeding.
    Commit: `refactor(19-01): extract McpToolModels.kt from McpTools.kt` with one-line body.

    **Extraction B — McpToolHelpers.kt (commit second):**

    Create src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt with:
    - Package declaration: `package com.six2dez.burp.aiagent.mcp.tools`
    - Imports: copy the relevant subset of McpTools.kt's import block — keep all imports actually used by the functions being moved
    - The `toolJson` val (line 53 of McpTools.kt): move here and change visibility to `internal` (per Visibility Change Summary in PATTERNS.md: "Referenced by McpToolExecutor moving to McpToolExecutorImpl.kt")
    - All private top-level helper functions from McpTools.kt lines 884–1262: `executeIssueCreate`, `findProxyHistoryMatch`, `withAiIssuePrefix`, `hasEquivalentIssue`, `normalizeHttpRequest`, `truncateIfNeeded`, `ensureAllowedProxyHistoryCount`, `orderedProxyHistory`, `decodeJwt`, `normalizeHashAlgorithm`, `diffLines`, `countOccurrences`, `parseHighlightColor`, `sanitizeHeaders`, `maybeAnonymizeUrl`, `resolveReportPath`, `applyReplacements`, `resolveAuditConfig`, `getActiveEditor`
    - Change each function from `private` to `internal` (per Visibility Change Summary: "Called from McpToolExecutor in new file")
    - `normalizeHttpRequest` is already `internal` — move as-is; `getActiveEditor` is package-internal — move as-is
    - Add AWT-free contract comment per PATTERNS.md: `// AWT-free contract: MUST NOT import java.awt.* or javax.swing.*`
    - Delete the extracted functions and the `toolJson` val from McpTools.kt (removing any now-redundant imports from McpTools.kt too)

    Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` — must be green before proceeding.
    Commit: `refactor(19-01): extract McpToolHelpers.kt from McpTools.kt`
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test</automated>
  </verify>
  <acceptance_criteria>
    - McpToolModels.kt exists at the correct path, package is `com.six2dez.burp.aiagent.mcp.tools`, contains `ToolSpec` and all `@Serializable data class` types, no class wrapper
    - McpToolHelpers.kt exists, `toolJson` is declared `internal val`, all extracted helper functions are `internal`, AWT-free contract comment present
    - McpTools.kt no longer contains the data class block (lines 2534–2926) or the helper functions block (lines 884–1262) or ToolSpec or toolJson — those declarations are only in the new files
    - `./gradlew test` exits 0 after each extraction commit (two green checks, not one at the end)
    - `git log --oneline -3` shows two separate extraction commits
    - `grep -c 'data class\|@Serializable' src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpTools.kt` returns 0 (no data class declarations remain in McpTools.kt after both extractions)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extract McpToolExecutorImpl.kt (object McpToolExecutor) and verify final McpTools.kt line count</name>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpTools.kt (current state after Task 1 — read to see what remains: registerTools(), registerToolsLegacy(), package/imports)
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt (created in Task 1 — verify toolJson is internal here, imports are correct)
    - .planning/phases/19-mega-file-split-docs/19-PATTERNS.md (Section: McpToolExecutorImpl.kt pattern, concrete excerpt of object McpToolExecutor lines 1272–1319)
    - .planning/phases/19-mega-file-split-docs/19-RESEARCH.md (Section 3.1, cross-reference risks for McpToolExecutor + toolJson)
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/HistoryTools.kt (analog for internal fun in same package)
  </read_first>
  <action>
    **Extraction C — McpToolExecutorImpl.kt:**

    Create src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorImpl.kt with:
    - Package declaration: `package com.six2dez.burp.aiagent.mcp.tools`
    - Imports: start from McpTools.kt's full import block (lines 1–51), then prune imports only needed by registerToolsLegacy (which stays in McpTools.kt); add any imports needed exclusively by the executor body that may not already be present
    - The complete `object McpToolExecutor` body from McpTools.kt lines 1272–2533 verbatim — do not alter any logic, only move
    - The `object McpToolExecutor` references `toolJson` — this is now `internal val toolJson` in McpToolHelpers.kt, same package, accessible without import
    - Delete `object McpToolExecutor` from McpTools.kt

    After creating the file, compile-check before running the full test suite: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew compileKotlin` to surface any unresolved reference errors early (e.g. `toolJson`, missing imports, `private` companion constant issues). Fix any compile errors by adjusting imports or visibility as needed — do NOT change logic.

    Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` — must be green.
    Commit: `refactor(19-01): extract McpToolExecutorImpl.kt from McpTools.kt`

    **Final verification:**

    Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew check` (full check including ktlint).
    Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew shadowJar` — fat JAR must build successfully.
    Check line count: `wc -l src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpTools.kt` — must be under 500.
    Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests "*.BackendRegistryTest"` — all BackendRegistryTest tests must pass.
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests "*.BackendRegistryTest" && wc -l src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpTools.kt</automated>
  </verify>
  <acceptance_criteria>
    - McpToolExecutorImpl.kt exists, package is `com.six2dez.burp.aiagent.mcp.tools`, contains `object McpToolExecutor` declaration and full body
    - `wc -l src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpTools.kt` outputs a number less than 500 (target ~90 lines)
    - McpTools.kt no longer contains `object McpToolExecutor` — that declaration only exists in McpToolExecutorImpl.kt
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` exits 0
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew shadowJar` exits 0 and produces build/libs/Custom-AI-Agent-0.8.0.jar
    - BackendRegistryTest passes green (anthropic factory present in registry — SC2 gate)
    - No call-site changes outside the moved code (grep for `McpToolExecutor\.` and `describeTools\|executeTool` in files outside mcp/tools/ — these must call the same identifiers with no modification)
    - Total 3 atomic extraction commits visible in git log for this plan
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| McpTools package internal | Kotlin `internal` visibility — extracted helpers are invisible outside com.six2dez.burp.aiagent.mcp.tools module |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-SC | Tampering | McpTools.kt file split | accept | Pure mechanical refactor — no new inputs, no new IO surface; the external-MCP trust-boundary marker (`McpToolExecutor` when-block routing) moves unchanged to McpToolExecutorImpl.kt; disposition confirmed by `./gradlew test` green after every extraction commit |

No new attack surface introduced. All trust boundaries (external-MCP untrusted-output marker, redaction call sites) are preserved verbatim in the moved code. Zero HIGH or MEDIUM threats.
</threat_model>

<verification>
After both tasks complete:
1. `wc -l src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpTools.kt` — output must be < 500
2. `ls src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/` shows McpTools.kt, McpToolModels.kt, McpToolHelpers.kt, McpToolExecutorImpl.kt (plus existing files)
3. `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` exits 0
4. `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew shadowJar` exits 0
5. `git log --oneline -4` shows 3 refactor commits (one per extraction) within this plan
6. `grep -rn 'import com.six2dez.burp.aiagent.mcp.tools' src/` — no new import statements appear in files outside the mcp/tools package (same-package access does not require imports)
</verification>

<success_criteria>
- McpTools.kt is under 500 lines (QUAL-01 SC1 gate)
- 3 new files created in mcp/tools/ package with correct content groupings
- Full test suite green after each individual extraction (QUAL-01 SC1 "before and after each extraction" requirement per D-01)
- BackendRegistryTest passes, confirming ServiceLoader/registration intact (QUAL-01 SC2)
- Fat JAR builds successfully
- Zero call-site changes outside the moved code
</success_criteria>

<output>
Create .planning/phases/19-mega-file-split-docs/19-01-SUMMARY.md when done
</output>

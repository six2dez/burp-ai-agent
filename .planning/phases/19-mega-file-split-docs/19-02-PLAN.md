---
phase: 19-mega-file-split-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScanner.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerModels.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerHeuristics.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerParsing.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerPrompts.kt
autonomous: true
requirements: [QUAL-01]
must_haves:
  truths:
    - "PassiveAiScanner.kt is under 500 lines after all extractions (contains class + public API + analysis flow + dedup)"
    - "PassiveAiScannerModels.kt exists with PassiveAiFinding, PassiveAiScannerStatus, internal LocalFinding, internal AiIssueItem"
    - "PassiveAiScannerHeuristics.kt exists with runLocalChecks and checkFor*/detect* functions as internal top-level"
    - "PassiveAiScannerParsing.kt exists with parseIssuesFromAiResponse, cleanJsonResponse, parseIssuesJson and related parsing functions as internal top-level"
    - "PassiveAiScannerPrompts.kt exists with buildAnalysisPrompt, buildBatchAnalysisPrompt, buildCompactRequestBody/ResponseBody and related builders as internal top-level"
    - "Full test suite green after every individual extraction commit (per D-01)"
    - "LocalFinding remains internal — AiPassiveScanCheck.kt compiles without error"
  artifacts:
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerModels.kt"
      provides: "data class types for PassiveAiScanner (file-level and formerly inner)"
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerHeuristics.kt"
      provides: "AWT-free local heuristic check functions"
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerParsing.kt"
      provides: "AI response parsing functions"
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerPrompts.kt"
      provides: "Prompt and body builder functions"
  key_links:
    - from: "PassiveAiScanner.kt"
      to: "PassiveAiScannerHeuristics.kt"
      via: "internal fun runLocalChecks — called by PassiveAiScanner.localChecks()"
      pattern: "internal fun runLocalChecks"
    - from: "PassiveAiScannerHeuristics.kt"
      to: "PassiveAiScannerModels.kt"
      via: "LocalFinding data class — returned by heuristic functions"
      pattern: "internal data class LocalFinding"
    - from: "AiPassiveScanCheck.kt"
      to: "PassiveAiScannerModels.kt"
      via: "LocalFinding type — used by external caller via passiveAiScanner.localChecks()"
      pattern: "List<LocalFinding>"
---

<objective>
Split PassiveAiScanner.kt (2566 lines) into 4 focused companion files in the same scanner
package: PassiveAiScannerModels.kt (data classes), PassiveAiScannerHeuristics.kt (local check
functions), PassiveAiScannerParsing.kt (AI response parsing), PassiveAiScannerPrompts.kt (prompt
builders). PassiveAiScanner.kt itself retains the class skeleton, public API, analysis flow, and
dedup logic at ~480 lines.

Purpose: QUAL-01 / SC1 — the named file must end under 500 lines (D-01: one atomic commit per
extraction, ./gradlew test green between each). Per D-01: cohesion-based split, same-package
top-level functions, zero call-site changes outside the moved code.

Output: 4 new .kt files in scanner/; PassiveAiScanner.kt reduced to ~480 lines; all pre-send
tripwire hook points (Phase 15 additions) remain in PassiveAiScanner.kt undisturbed; test suite
green after every extraction commit.
</objective>

<execution_context>
@/Users/six2dez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/six2dez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/six2dez/Tools/burp-ai-agent/.planning/PROJECT.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/ROADMAP.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/STATE.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/19-mega-file-split-docs/19-CONTEXT.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/19-mega-file-split-docs/19-RESEARCH.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/19-mega-file-split-docs/19-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract PassiveAiScannerModels.kt + PassiveAiScannerHeuristics.kt</name>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScanner.kt (full file — identify: file-level data classes at lines 37–54, PassiveAiFinding + PassiveAiScannerStatus; inner classes LocalFinding ~line 2057, AiIssueItem ~line 2215, CachedAiIssues, PendingAnalysis; heuristic block at lines ~2064–2213: runLocalChecks + detectRequestSmuggling + detectCsrf + detectDeserialization + detectUnrestrictedFileUpload; the private companion object constants at ~line 2481 used by heuristics)
    - .planning/phases/19-mega-file-split-docs/19-PATTERNS.md (Section: PassiveAiScannerModels.kt pattern — visibility hazards for LocalFinding/AiIssueItem; PassiveAiScannerHeuristics.kt pattern — companion object constants to redeclare, AWT-free contract comment; Visibility Change Summary table rows for PassiveAiScanner)
    - .planning/phases/19-mega-file-split-docs/19-RESEARCH.md (Section 3.3 PassiveAiScanner split-boundary map, Common Pitfalls 2 and 3)
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiPassiveScanCheck.kt (confirm it references localChecks() return type — LocalFinding must remain internal)
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScanModels.kt (analog for multi-type models file)
  </read_first>
  <action>
    Perform two sequential extractions, each followed by a ./gradlew test green gate and an atomic commit.

    **Extraction A — PassiveAiScannerModels.kt (commit first):**

    Create src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerModels.kt with:
    - Package declaration: `package com.six2dez.burp.aiagent.scanner`
    - No imports required for the data class declarations themselves (they use only primitive types and kotlin types)
    - File-level data classes extracted from PassiveAiScanner.kt lines 37–54: `data class PassiveAiFinding`, `data class PassiveAiScannerStatus` — these are already file-level, move verbatim
    - Inner/private data classes from inside the PassiveAiScanner class body, moved to file-level in this file: `internal data class LocalFinding` (keep `internal` — critical: AiPassiveScanCheck.kt references it), `internal data class AiIssueItem` (keep `internal`), `private data class CachedAiIssues` (if present in the class), `data class PendingAnalysis` (if top-level extractable — check if it is a nested class or companion member)
    - Pattern: no class wrapper, all top-level declarations (per multi-type models file pattern)
    - Delete the extracted declarations from PassiveAiScanner.kt (both file-level ones at lines 37–54 and the inner class ones from the class body)

    Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` — must be green before proceeding.
    Commit: `refactor(19-02): extract PassiveAiScannerModels.kt from PassiveAiScanner.kt`

    **Extraction B — PassiveAiScannerHeuristics.kt (commit second):**

    Create src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerHeuristics.kt with:
    - Package declaration: `package com.six2dez.burp.aiagent.scanner`
    - Imports: only what is needed by the heuristic functions (burp.api.montoya.http.message.requests.HttpRequest, burp.api.montoya.http.message.responses.HttpResponse) — check the actual imports used in the extracted methods
    - AWT-free contract comment: `// AWT-free contract: MUST NOT import java.awt.* or javax.swing.*`
    - All heuristic methods from PassiveAiScanner.kt lines ~2064–2213 as top-level functions: `runLocalChecks`, `detectRequestSmuggling` (or `checkForSqlErrors` etc. — use the actual method names from the source), all `checkFor*` / `detect*` private methods in that block
    - Change each method from `private` to `internal` (per Visibility Change Summary)
    - For any constants from the `private companion object` referenced by these functions (e.g. `LOCAL_FINDING_SKIP_CONFIDENCE`, `REQUEST_BODY_LOCAL_CHECK_MAX_CHARS`, `RESPONSE_BODY_LOCAL_CHECK_MAX_CHARS`): redeclare them as `private const val` at the top of this file (per RESEARCH.md Section 3.3 recommendation — cleanest approach)
    - `LocalFinding` is in PassiveAiScannerModels.kt (same package) — no import needed; verify the compiler resolves it
    - Delete the extracted methods from PassiveAiScanner.kt; also remove the corresponding `private const val` declarations from the companion object in PassiveAiScanner.kt if those constants are only used in the extracted functions

    Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` — must be green before proceeding.
    Commit: `refactor(19-02): extract PassiveAiScannerHeuristics.kt from PassiveAiScanner.kt`
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test</automated>
  </verify>
  <acceptance_criteria>
    - PassiveAiScannerModels.kt exists, package is `com.six2dez.burp.aiagent.scanner`, contains `PassiveAiFinding`, `PassiveAiScannerStatus`, `internal data class LocalFinding`, `internal data class AiIssueItem` as file-level top-level declarations
    - `internal data class LocalFinding` visibility is exactly `internal` — not `private`, not public
    - PassiveAiScannerHeuristics.kt exists, has AWT-free contract comment, all extracted functions are `internal`, no `javax.swing` or `java.awt` imports
    - `./gradlew test` exits 0 after each extraction commit (two separate green gates)
    - `grep -n 'class LocalFinding\|class AiIssueItem' src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScanner.kt` returns empty (types are no longer in the class body)
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew compileKotlin` exits 0 (AiPassiveScanCheck.kt still compiles with LocalFinding in same package)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extract PassiveAiScannerParsing.kt + PassiveAiScannerPrompts.kt and verify final PassiveAiScanner.kt line count</name>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScanner.kt (current state after Task 1 — identify: parsing methods at ~lines 2224–2400: parseIssuesJson, parseIssuesFromAiResponse, cleanJsonResponse, parseIssuesNode, parseNodeIfValid, stripCodeFences, extractBalancedJsonCandidates, sha256Hex; prompt/body builders: buildAnalysisPrompt, buildBatchAnalysisPrompt, buildMetadataSectionPlain, buildCompactRequestBody, buildCompactResponseBody, isJsonBody, compactJsonBody, compactHtmlBody, truncateWithEllipsis; the companion object constants remaining; instance fields referenced by these methods)
    - .planning/phases/19-mega-file-split-docs/19-PATTERNS.md (Section: PassiveAiScannerParsing.kt pattern — jsonMapper visibility issue, api parameter passing; PassiveAiScannerPrompts.kt pattern — companion object constants to redeclare, instance field audit)
    - .planning/phases/19-mega-file-split-docs/19-RESEARCH.md (Section 3.3, Common Pitfall 1 and 2)
    - src/main/kotlin/com/six2dez/burp/aiagent/util/BudgetGuard.kt (analog for AWT-free pure top-level functions)
  </read_first>
  <action>
    Perform two sequential extractions, each followed by a ./gradlew test green gate and an atomic commit.

    **Extraction C — PassiveAiScannerParsing.kt (commit third):**

    Create src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerParsing.kt with:
    - Package declaration: `package com.six2dez.burp.aiagent.scanner`
    - Imports: com.fasterxml.jackson.databind.JsonNode, com.fasterxml.jackson.databind.ObjectMapper, com.fasterxml.jackson.module.kotlin.registerKotlinModule (or whatever the class uses), burp.api.montoya.MontoyaApi (needed if api is passed as parameter to parseIssuesFromAiResponse)
    - AWT-free contract comment: `// AWT-free contract: MUST NOT import java.awt.* or javax.swing.*`
    - `private val jsonMapper = ObjectMapper().registerKotlinModule()` — declare as file-level private val in this file (it was a private class field; extract it here since parsing is the primary consumer)
    - Methods from PassiveAiScanner.kt lines ~2224–2400: `parseIssuesJson`, `parseIssuesFromAiResponse`, `cleanJsonResponse`, `parseIssuesNode`, `parseNodeIfValid`, `stripCodeFences`, `extractBalancedJsonCandidates`, `sha256Hex`
    - `parseIssuesFromAiResponse` currently calls `api.logging().logToError(...)` referencing the `api: MontoyaApi` instance field — change this function signature to accept `api: MontoyaApi` as an explicit parameter; update the single call site in PassiveAiScanner.kt accordingly
    - Change all extracted methods from `private`/mixed to `internal`
    - `AiIssueItem` is in PassiveAiScannerModels.kt (same package) — accessible without import
    - Delete the extracted methods from PassiveAiScanner.kt; if `jsonMapper` was a class field, remove it from the class constructor and update any remaining usages to call the file-level val in PassiveAiScannerParsing.kt (same package — no import needed)

    Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` — must be green before proceeding.
    Commit: `refactor(19-02): extract PassiveAiScannerParsing.kt from PassiveAiScanner.kt`

    **Extraction D — PassiveAiScannerPrompts.kt (commit fourth):**

    Create src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerPrompts.kt with:
    - Package declaration: `package com.six2dez.burp.aiagent.scanner`
    - Imports: com.six2dez.burp.aiagent.supervisor.AgentSupervisor (for BackendInfo type), com.six2dez.burp.aiagent.util.SecurityExcerpts (if used by prompt builders), any Jackson import if buildCompactRequestBody/buildCompactResponseBody uses the ObjectMapper
    - AWT-free contract comment: `// AWT-free contract: MUST NOT import java.awt.* or javax.swing.*`
    - Methods: `buildAnalysisPrompt`, `buildBatchAnalysisPrompt`, `buildMetadataSectionPlain`, `buildCompactRequestBody`, `buildCompactResponseBody`, `isJsonBody`, `compactJsonBody`, `compactHtmlBody`, `truncateWithEllipsis`
    - For `companion object` constants referenced by these methods (e.g. `DEFAULT_REQUEST_BODY_PROMPT_MAX_CHARS`, `MAX_HEADERS_MAX_COUNT`, `PARAM_VALUE_MAX_CHARS`): redeclare the needed constants as `private const val` at the top of this file
    - If `buildAnalysisPrompt` or `buildBatchAnalysisPrompt` references instance fields (e.g. `endpointDedupMinutes` or similar configuration fields), audit each reference: if the value is a simple field read, pass it as a parameter; do NOT reference `this` from top-level functions
    - If `buildCompactRequestBody`/`buildCompactResponseBody` uses `jsonMapper`, reference the file-level `private val jsonMapper` in `PassiveAiScannerParsing.kt` — same package, accessible directly without import
    - Change all extracted methods from `private` to `internal`
    - Delete the extracted methods and any now-redundant companion object constants from PassiveAiScanner.kt

    Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` — must be green before proceeding.
    Commit: `refactor(19-02): extract PassiveAiScannerPrompts.kt from PassiveAiScanner.kt`

    **Final verification:**

    Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew check` (full check including ktlint).
    Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew shadowJar` — fat JAR must build.
    Check line count: `wc -l src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScanner.kt` — must be under 500.
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test && wc -l src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScanner.kt</automated>
  </verify>
  <acceptance_criteria>
    - PassiveAiScannerParsing.kt exists, has AWT-free contract comment, `private val jsonMapper` declared at file level, all extracted functions are `internal`
    - PassiveAiScannerPrompts.kt exists, has AWT-free contract comment, companion object constants that moved here are declared as `private const val`, all extracted functions are `internal`
    - `wc -l src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScanner.kt` outputs a number less than 500
    - PassiveAiScanner.kt still contains the class declaration, constructor, all instance fields, public API methods, analysis flow (analyzeManually, handleAiResponse, handleParsedAiIssues, handleFinding), dedup methods, budget-pausing logic, and companion object with any remaining constants
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` exits 0 after each extraction commit (four separate green gates across Task 1 + Task 2)
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew shadowJar` exits 0
    - Phase 15 pre-send tripwire hook points (SecretTripwire call sites in PassiveAiScanner) remain in PassiveAiScanner.kt — confirm by grep: `grep -n 'SecretTripwire\|tripwire' src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScanner.kt` returns at least one result
    - Total 4 atomic extraction commits visible in git log for this plan
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scanner package internal | Kotlin `internal` visibility — extracted helper functions are invisible outside com.six2dez.burp.aiagent.scanner module |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-SC | Tampering | PassiveAiScanner.kt file split | accept | Pure mechanical refactor — no new inputs, no new IO surface; the Phase 15 pre-send tripwire hook points (SecretTripwire calls in the analysis flow) stay in PassiveAiScanner.kt verbatim; `internal data class LocalFinding` remains `internal` (not widened to public); disposition confirmed by `./gradlew test` green after every extraction commit |

No new attack surface introduced. All trust boundaries preserved. Zero HIGH or MEDIUM threats.
</threat_model>

<verification>
After both tasks complete:
1. `wc -l src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScanner.kt` — output must be < 500
2. `ls src/main/kotlin/com/six2dez/burp/aiagent/scanner/` shows PassiveAiScanner.kt, PassiveAiScannerModels.kt, PassiveAiScannerHeuristics.kt, PassiveAiScannerParsing.kt, PassiveAiScannerPrompts.kt (plus existing files)
3. `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` exits 0
4. `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew shadowJar` exits 0
5. `git log --oneline -5` shows 4 refactor commits (one per extraction) within this plan
6. `grep -n 'internal data class LocalFinding' src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerModels.kt` returns a match (LocalFinding is in the right file with correct visibility)
</verification>

<success_criteria>
- PassiveAiScanner.kt is under 500 lines (QUAL-01 SC1 gate)
- 4 new files created in scanner/ package with correct content groupings
- Full test suite green after each individual extraction (QUAL-01 SC1 "before and after each extraction" per D-01)
- Phase 15 tripwire hook points preserved in PassiveAiScanner.kt (no behavior change)
- LocalFinding remains `internal` — AiPassiveScanCheck.kt compiles without modification
- Fat JAR builds successfully
</success_criteria>

<output>
Create .planning/phases/19-mega-file-split-docs/19-02-SUMMARY.md when done
</output>

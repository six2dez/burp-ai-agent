---
phase: 14-anthropic-backend-token-budget-listener-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/backends/anthropic/AnthropicBackend.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/backends/anthropic/AnthropicBackendFactory.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/backends/BackendRegistry.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/supervisor/AgentSupervisor.kt
  - src/main/resources/META-INF/services/com.six2dez.burp.aiagent.backends.AiBackendFactory
  - src/main/kotlin/com/six2dez/burp/aiagent/config/AgentSettings.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/BackendConfigPanel.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/backends/anthropic/AnthropicBackendTransportRoutingTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/backends/anthropic/AnthropicModelErrorTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/backends/BackendRegistryTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/config/AgentSettingsSecretEncryptionTest.kt
autonomous: true
requirements: [CAP-01]

must_haves:
  truths:
    - "A user can select Anthropic in Settings > Backend, enter an API key (masked, encrypted on save), and edit a Model field defaulting to claude-sonnet-4-6 (SC1 setup)"
    - "AnthropicBackend.send() issues HTTP only via the injected MontoyaHttpTransport to https://api.anthropic.com/v1/messages, so traffic appears in Burp Proxy > HTTP history (SC2)"
    - "AnthropicBackend.kt contains no okhttp3 / OkHttpClient reference on the production path (SC2c)"
    - "send() with a null transport fails fast with the shared MontoyaHttpTransport-unavailable message rather than falling back to OkHttp (SC2b)"
    - "A 400 whose body contains \"model\" surfaces the exact string: Anthropic rejected the model ID — check Settings > Anthropic > Model (SC3)"
    - "anthropicApiKey is encrypted at rest via SecretCipher under KEY_ANTHROPIC_API_KEY and is in the schema-V4 secret-migration list"
    - "AnthropicBackendFactory is registered via both META-INF/services and the BackendRegistry fallback list (id \"anthropic\")"
    - "AgentSettings carries the four new defaulted fields (anthropicModel, anthropicApiKey, tokenBudgetWarnThreshold, tokenBudgetHardCap) so existing positional/named constructions keep compiling"
  artifacts:
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/backends/anthropic/AnthropicBackend.kt"
      provides: "Anthropic Messages API backend (AiBackend + private AnthropicConnection : AgentConnection, UsageAwareConnection); transport-routed, system-as-top-level-field, content[].text parse, usage.input_tokens/output_tokens"
      min_lines: 150
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/backends/anthropic/AnthropicBackendFactory.kt"
      provides: "AiBackendFactory whose create() returns AnthropicBackend()"
      contains: "AnthropicBackendFactory"
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/config/AgentSettings.kt"
      provides: "anthropicModel/anthropicApiKey/tokenBudgetWarnThreshold/tokenBudgetHardCap fields + KEY_* constants + encrypted load/save + migration list entry"
      contains: "KEY_ANTHROPIC_API_KEY"
    - path: "src/main/resources/META-INF/services/com.six2dez.burp.aiagent.backends.AiBackendFactory"
      provides: "ServiceLoader registration line for AnthropicBackendFactory"
      contains: "anthropic.AnthropicBackendFactory"
  key_links:
    - from: "supervisor/AgentSupervisor.kt"
      to: "backends/anthropic/AnthropicBackend.kt"
      via: "when(backendId) \"anthropic\" branch builds x-api-key/anthropic-version headers + transport=httpTransport"
      pattern: "\"anthropic\"\\s*->"
    - from: "backends/anthropic/AnthropicBackend.kt"
      to: "https://api.anthropic.com/v1/messages"
      via: "config.transport.post(...) — the only HTTP call"
      pattern: "transport\\.post\\("
    - from: "config/AgentSettings.kt"
      to: "SecretCipher"
      via: "cipher.encrypt/decrypt under KEY_ANTHROPIC_API_KEY"
      pattern: "cipher\\.(encrypt|decrypt)\\([^)]*KEY_ANTHROPIC_API_KEY"
    - from: "ui/panels/BackendConfigPanel.kt"
      to: "ui/SettingsPanel.kt"
      via: "BackendConfigState.anthropicModel/anthropicApiKey threaded through currentBackendSettings()/applyState() into AgentSettings"
      pattern: "anthropicApiKey"
---

<objective>
Ship the native Anthropic Messages API backend (CAP-01, SC1–SC3) plus ALL new AgentSettings fields for the phase. This is the foundation plan: it owns every `AgentSettings` field addition (the two Anthropic fields AND the two token-budget threshold fields) so that the wave-2 plans (14-02 token budget, 14-03 listener port) never collide on `AgentSettings.kt` or on `SettingsPanel.kt`'s collect/apply flow.

The backend is a near-copy of `OpenAiCompatibleConnection`, diverging only in: endpoint constant, `x-api-key`/`anthropic-version` headers, top-level `system` field (not a system-role message), `content[].text` response parse, `usage.input_tokens`/`usage.output_tokens` extraction, and the SC3 model-rejection error string. The supervisor's `when(backendId)` launch dispatch gains an `"anthropic"` branch that assembles the headers and injects `httpTransport` — without it the backend receives no auth headers (PATTERNS Planner Note 1; absent from RESEARCH/UI-SPEC file lists).

Purpose: Add the highest-demand backend without leaking traffic past Burp (SC2 = no OkHttp on the production path; all HTTP via the injected transport) and with the API key encrypted from the first commit (Phase 12 SEC-01 inheritance).

Output: `backends/anthropic/` package (2 files), supervisor branch, registry + ServiceLoader registration, 4 new defaulted AgentSettings fields with encrypted key + migration, Anthropic settings card, and the Wave 0 tests for SC2a/b/c + SC3 + registration + key round-trip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-anthropic-backend-token-budget-listener-port/14-CONTEXT.md
@.planning/phases/14-anthropic-backend-token-budget-listener-port/14-RESEARCH.md
@.planning/phases/14-anthropic-backend-token-budget-listener-port/14-PATTERNS.md
@.planning/phases/14-anthropic-backend-token-budget-listener-port/14-UI-SPEC.md
@.planning/phases/14-anthropic-backend-token-budget-listener-port/14-VALIDATION.md

Build/test gate: `./gradlew test` ONLY — never `./gradlew ktlintCheck` (pre-existing generateBuildFlags defect, per CLAUDE.md/MEMORY). English-only in all code and comments. ZERO new dependencies (Jackson + kotlinx-serialization + Montoya + okhttp all already present).
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0 RED): Author the failing SC2 + SC3 backend tests</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/backends/anthropic/AnthropicBackendTransportRoutingTest.kt, src/test/kotlin/com/six2dez/burp/aiagent/backends/anthropic/AnthropicModelErrorTest.kt</files>
  <read_first>
    - src/test/kotlin/com/six2dez/burp/aiagent/backends/http/HttpBackendTransportRoutingTest.kt (analog: stubTransportPost() spy helper ~L288-296; null-transport fail-fast test ~L82-118; source-string guard style)
    - .planning/phases/14-anthropic-backend-token-budget-listener-port/14-RESEARCH.md (Code Examples §5 analog send loop; §3 success body; §4 400 error body; SC3 trigger logic)
    - .planning/phases/14-anthropic-backend-token-budget-listener-port/14-PATTERNS.md (Shared Patterns → "Spy-transport test harness")
  </read_first>
  <action>
    Create two test files under src/test/kotlin/com/six2dez/burp/aiagent/backends/anthropic/ that fail to compile/run because AnthropicBackend does not yet exist (RED).

    AnthropicBackendTransportRoutingTest.kt — model on HttpBackendTransportRoutingTest. Use spy(MontoyaHttpTransport(mock<MontoyaApi>(RETURNS_DEEP_STUBS))), reusing the existing stubTransportPost() helper pattern. (SC2a) Launch AnthropicBackend via a BackendLaunchConfig carrying headers mapOf("x-api-key" to "k", "anthropic-version" to "2023-06-01") and the spy transport; call send() with a simple user text; stub post() to return a 200 TransportResponse with a representative Messages body (content[0].text + usage.input_tokens/output_tokens per RESEARCH §3); assert verify(transport).post(eq("https://api.anthropic.com/v1/messages"), any(), any(), any()) and that onChunk received the parsed text. (SC2b) A second test launches with transport = null and asserts the onComplete error message contains "MontoyaHttpTransport unavailable". (SC2c) A third test reads the AnthropicBackend.kt source file from disk and asserts the source contains neither "okhttp3" nor "OkHttpClient" — mirror the HttpBackendTransportRoutingTest source-string guard exactly.

    AnthropicModelErrorTest.kt — (SC3) stub post() to return TransportResponse(400, "{\"type\":\"error\",\"error\":{\"type\":\"invalid_request_error\",\"message\":\"...model: bogus not found...\"}}", isSuccessful=false); call send(); assert the onComplete error message equals exactly "Anthropic rejected the model ID — check Settings > Anthropic > Model" (the SC3 string from CONTEXT.md — do not reword, do not add punctuation).

    These tests define the contract for Task 2. Do NOT write production code in this task.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.backends.anthropic.*" 2>&1 | grep -qiE "compilation|unresolved reference|cannot access|AnthropicBackend" && echo RED-AS-EXPECTED</automated>
  </verify>
  <acceptance_criteria>
    Both test files exist; the test task fails because AnthropicBackend / AnthropicBackendFactory are unresolved references (RED state proven). The source-string guard test asserts absence of "okhttp3" and "OkHttpClient". The SC3 assertion compares against the exact CONTEXT.md string.
  </acceptance_criteria>
  <done>RED proven: `./gradlew test --tests "com.six2dez.burp.aiagent.backends.anthropic.*"` fails on the missing AnthropicBackend symbol, and the two test files encode SC2a/b/c + SC3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement AnthropicBackend + Factory, register it, add the supervisor branch</name>
  <files>src/main/kotlin/com/six2dez/burp/aiagent/backends/anthropic/AnthropicBackend.kt, src/main/kotlin/com/six2dez/burp/aiagent/backends/anthropic/AnthropicBackendFactory.kt, src/main/kotlin/com/six2dez/burp/aiagent/backends/BackendRegistry.kt, src/main/kotlin/com/six2dez/burp/aiagent/supervisor/AgentSupervisor.kt, src/main/resources/META-INF/services/com.six2dez.burp.aiagent.backends.AiBackendFactory</files>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/backends/openai/OpenAiCompatibleBackend.kt (the whole skeleton to copy; transport fail-fast ~L195-200; send loop ~L153-322; extractUsage ~L324-333; shape-only logging ~L229-240)
    - src/main/kotlin/com/six2dez/burp/aiagent/backends/openai/OpenAiCompatibleBackendFactory.kt (8-line factory analog)
    - src/main/kotlin/com/six2dez/burp/aiagent/backends/http/HttpBackendSupport.kt (ConversationHistory addUser/setHistory/snapshot ~L166-207; newCircuitBreaker/isRetryableConnectionError/retryDelayMs/openCircuitError)
    - src/main/kotlin/com/six2dez/burp/aiagent/backends/http/MontoyaHttpTransport.kt (post() signature; it adds Content-Type: application/json itself ~L29)
    - src/main/kotlin/com/six2dez/burp/aiagent/backends/BackendRegistry.kt (import ~L14; fallback built-ins list ~L53-62; ServiceLoader primary path ~L42-44)
    - src/main/kotlin/com/six2dez/burp/aiagent/supervisor/AgentSupervisor.kt (the "perplexity"/"nvidia-nim" branches ~L793-854; BackendLaunchConfig construction shape)
    - src/main/kotlin/com/six2dez/burp/aiagent/backends/BackendTypes.kt (BackendLaunchConfig fields: backendId, displayName, model, headers, requestTimeoutSeconds, transport, etc.)
    - src/main/resources/META-INF/services/com.six2dez.burp.aiagent.backends.AiBackendFactory (the 10 existing lines)
    - .planning/phases/14-anthropic-backend-token-budget-listener-port/14-PATTERNS.md (AnthropicBackend.kt section: DIVERGENCE 1-6, verbatim snippets)
    - .planning/phases/14-anthropic-backend-token-budget-listener-port/14-RESEARCH.md (Code Examples §1-4: endpoint, request shape, success/usage parse, 400 body)
  </read_first>
  <action>
    Create AnthropicBackend.kt in package com.six2dez.burp.aiagent.backends.anthropic. Build AnthropicBackend : AiBackend with override val supportsSystemRole = true, and a private AnthropicConnection : AgentConnection, UsageAwareConnection that is a near-copy of OpenAiCompatibleConnection. Copy the imports verbatim but DO NOT import HeaderParser and DO NOT import anything from okhttp3 (the SC2c guard asserts the file contains neither "okhttp3" nor "OkHttpClient"). launch() builds the connection from config, gets HttpBackendSupport.newCircuitBreaker(), passes config.transport and a coerced timeout; drop the baseUrl plumbing (endpoint is a constant).

    In send(): keep the transport == null fail-fast guard verbatim (throw IllegalStateException with the "MontoyaHttpTransport unavailable; AI HTTP backends require Burp's HTTP stack ..." message). DIVERGENCE 1 — do NOT call conversationHistory.setSystemPrompt; instead setHistory(history) if non-null, addUser(text), snapshot() for user/assistant turns only, then build a Jackson payload mutableMapOf "model" to model, "max_tokens" to (maxOutputTokens ?: 1024) (max_tokens is REQUIRED by Anthropic), "messages" to messages, "stream" to false; if systemPrompt is non-blank set payload["system"] = systemPrompt as the TOP-LEVEL field; if determinismMode set payload["temperature"] = 0.0. Do NOT send response_format and do NOT add tools/tool_choice/cache headers (deferred this phase). DIVERGENCE 3 — define a private const ANTHROPIC_MESSAGES_URL = "https://api.anthropic.com/v1/messages" and make the single HTTP call config.transport.post(ANTHROPIC_MESSAGES_URL, allHeaders, json, timeoutSeconds * 1000). DIVERGENCE 4 — BEFORE the generic non-2xx handler, add: if resp.statusCode == 400 && resp.body.contains("model", ignoreCase = true) call onComplete(IllegalStateException("Anthropic rejected the model ID — check Settings > Anthropic > Model")) and return from the submit block (exact CONTEXT.md SC3 string). DIVERGENCE 5 — parse the body by iterating node.path("content") and concatenating text from blocks whose type == "text". DIVERGENCE 6 — replace extractUsage to read usage.input_tokens / usage.output_tokens (coerced >= 0; return null if both absent) and build TokenUsage. Keep the body-shape-only privacy logging verbatim (model=… messages=N json_bytes=…); never log the JSON, message content, or the key. Reuse the circuitBreaker.tryAcquire()/openCircuitError/isRetryableConnectionError/retryDelayMs retry loop exactly as the analog.

    Create AnthropicBackendFactory.kt mirroring OpenAiCompatibleBackendFactory exactly: package backends.anthropic, class AnthropicBackendFactory : AiBackendFactory { override fun create(): AiBackend = AnthropicBackend() }. (Do NOT follow the Perplexity/NVIDIA factory shape — those delegate with selectors; Anthropic has its own backend class.)

    Register the factory in BOTH places: append the line com.six2dez.burp.aiagent.backends.anthropic.AnthropicBackendFactory to META-INF/services/com.six2dez.burp.aiagent.backends.AiBackendFactory, and add import …backends.anthropic.AnthropicBackendFactory plus AnthropicBackendFactory() to the BackendRegistry fallback built-ins list.

    Add the "anthropic" branch to AgentSupervisor's when(backendId) launch dispatch, modeled on the "perplexity" branch but NOT using HeaderParser.withBearerToken (Pitfall 2). Resolve model from settings?.anthropicModel ?: prefs.getString("anthropic.model") ?: "claude-sonnet-4-6"; resolve apiKey from settings?.anthropicApiKey ?: prefs.getString("anthropic.apiKey").orEmpty(); reuse the existing timeout default the sibling branches use (Defaults.CLI_PROCESS_TIMEOUT_SECONDS). Build BackendLaunchConfig with backendId, displayName = "Anthropic", model, headers = mapOf("x-api-key" to apiKey, "anthropic-version" to "2023-06-01"), requestTimeoutSeconds, embeddedMode, sessionId, determinismMode, env = baseEnv, cliSessionId, transport = httpTransport. Do NOT set baseUrl.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.backends.anthropic.*"</automated>
  </verify>
  <acceptance_criteria>
    `./gradlew test --tests "com.six2dez.burp.aiagent.backends.anthropic.*"` passes (Task 1 tests now GREEN). The SC2c source-string guard passes, proving AnthropicBackend.kt has no okhttp3/OkHttpClient. `grep -E "okhttp3|OkHttpClient" src/main/kotlin/com/six2dez/burp/aiagent/backends/anthropic/AnthropicBackend.kt` returns no matches. `grep -n '"anthropic"' src/main/kotlin/com/six2dez/burp/aiagent/supervisor/AgentSupervisor.kt` shows the new branch. The new ServiceLoader line is present in the resources file.
  </acceptance_criteria>
  <done>Backend compiles and routes only through transport.post to the Anthropic endpoint; SC3 string is exact; factory registered in both paths; supervisor branch builds x-api-key/anthropic-version headers + injects transport.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (GREEN): AgentSettings fields (all 4) + encrypted key + migration; Anthropic settings card + key wiring</name>
  <files>src/main/kotlin/com/six2dez/burp/aiagent/config/AgentSettings.kt, src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/BackendConfigPanel.kt, src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt, src/test/kotlin/com/six2dez/burp/aiagent/backends/BackendRegistryTest.kt, src/test/kotlin/com/six2dez/burp/aiagent/config/AgentSettingsSecretEncryptionTest.kt</files>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/config/AgentSettings.kt (perplexity field block ~L58-61; decrypt-on-load ~L277-281; integer-coerce ~L327; defaultSettings ~L434-437; encrypt-on-save ~L536-539; migration secretKeys list ~L701-710; KEY_* companion block ~L782-786)
    - src/main/kotlin/com/six2dez/burp/aiagent/config/SecretCipher.kt (encrypt ~L52 / decrypt ~L84 — AES-256-GCM, ENC1: envelope, fail-soft)
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/BackendConfigPanel.kt (BackendConfigState ~L26; buildPerplexityPanel ~L471; cards.add(...,"perplexity") ~L202; currentBackendSettings ~L231-280; applyState ~L281; JPasswordField/applyFieldStyle pattern)
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt (currentSettings ~L1025 backendState block; applySettingsToUi backendConfigPanel.applyState ~L1253-1255)
    - src/test/kotlin/com/six2dez/burp/aiagent/backends/BackendRegistryTest.kt (baselineSettings named-arg construction ~L78-95)
    - src/test/kotlin/com/six2dez/burp/aiagent/config/AgentSettingsSecretEncryptionTest.kt (roundTrip_allSevenSecretKeys_encryptedAtRest ~L57)
    - .planning/phases/14-anthropic-backend-token-budget-listener-port/14-PATTERNS.md (AgentSettings.kt + BackendConfigPanel.kt sections, verbatim snippets)
    - .planning/phases/14-anthropic-backend-token-budget-listener-port/14-UI-SPEC.md (Touch Point 1 anatomy + Copywriting Contract: "Model", "API key (Bearer)", default claude-sonnet-4-6, FLAG-14-02 no Base URL row)
  </read_first>
  <action>
    AgentSettings.kt — add four defaulted fields after the perplexity block: val anthropicModel: String = "claude-sonnet-4-6", val anthropicApiKey: String = "", val tokenBudgetWarnThreshold: Int = 0, val tokenBudgetHardCap: Int = 0. (Defaults are mandatory: BackendRegistryTest.baselineSettings() and defaultSettings() must keep compiling — Planner Note 3.) Add KEY_* companion constants: KEY_ANTHROPIC_MODEL = "anthropic.model", KEY_ANTHROPIC_API_KEY = "anthropic.apiKey", KEY_TOKEN_BUDGET_WARN = "tokenBudget.warnThreshold", KEY_TOKEN_BUDGET_CAP = "tokenBudget.hardCap". In load(): anthropicModel from prefs.getString(KEY_ANTHROPIC_MODEL) trimmed, blank-fallback to "claude-sonnet-4-6"; anthropicApiKey via cipher.decrypt(prefs.getString(KEY_ANTHROPIC_API_KEY).orEmpty().trim(), KEY_ANTHROPIC_API_KEY); tokenBudgetWarnThreshold via (prefs.getInteger(KEY_TOKEN_BUDGET_WARN) ?: 0).coerceAtLeast(0); tokenBudgetHardCap likewise. In defaultSettings(): set all four to their defaults. In save(): prefs.setString(KEY_ANTHROPIC_MODEL, ...); prefs.setString(KEY_ANTHROPIC_API_KEY, cipher.encrypt(settings.anthropicApiKey, KEY_ANTHROPIC_API_KEY)); prefs.setInteger(KEY_TOKEN_BUDGET_WARN, settings.tokenBudgetWarnThreshold.coerceAtLeast(0)); prefs.setInteger(KEY_TOKEN_BUDGET_CAP, ...). The thresholds are integers, NOT secrets — never route them through SecretCipher (Pitfall 5). Append KEY_ANTHROPIC_API_KEY to the migrateToSchemaV4 secretKeys listOf so a pre-encryption plaintext value is encrypted idempotently.

    BackendConfigPanel.kt — add private members anthropicModel = JTextField(initialState.anthropicModel) and anthropicApiKey = JPasswordField(initialState.anthropicApiKey); call applyFieldStyle on both in the same init block as the other fields. Add a private fun buildAnthropicPanel(): JPanel modeled byte-for-byte on buildPerplexityPanel: formGrid(), EmptyBorder(DesignTokens.Spacing.sectionPad × 4), addRowFull(panel, "Model", anthropicModel), addRowFull(panel, "API key (Bearer)", anthropicApiKey), addRowFull(panel, "", buildButtonRowPanel(buildTestConnectionButton("anthropic"))), addSpacerRow(panel, DesignTokens.Spacing.sm). Register it next to perplexity: cards.add(buildAnthropicPanel(), "anthropic"). Add anthropicModel/anthropicApiKey to BackendConfigState (default ""), read String(anthropicApiKey.password).trim() and anthropicModel.text.trim() in currentBackendSettings(), and set anthropicApiKey.text / anthropicModel.text in applyState(...) — mirror the Perplexity wiring exactly. Per FLAG-14-02 do NOT add a Base URL row (the endpoint is a fixed backend constant; a Base URL field implies false configurability and widens SSRF surface). All styling via DesignTokens; no Color()/Font()/spacing literals.

    SettingsPanel.kt — thread the new BackendConfigState fields into AgentSettings: in currentSettings() map backendState.anthropicModel → anthropicModel and backendState.anthropicApiKey → anthropicApiKey on the constructed AgentSettings; in applySettingsToUi() include anthropicModel/anthropicApiKey in the backendConfigPanel.applyState(...) call. (Token-budget UI section/fields are NOT added here — 14-02 owns the budget rendering; the two AgentSettings token-budget fields are declared above so 14-02 can read/collect them.)

    Extend BackendRegistryTest.kt — add an assertion that the registry exposes a backend with id "anthropic" (mirror the existing per-backend registration assertions). Extend AgentSettingsSecretEncryptionTest.kt — add anthropic.apiKey to the roundTrip_allSevenSecretKeys_encryptedAtRest case (it becomes an eight-key round-trip): set a non-empty anthropicApiKey, save, assert the raw pref value starts with the ENC1: envelope and that load() decrypts back to the plaintext.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.backends.BackendRegistryTest" --tests "com.six2dez.burp.aiagent.config.AgentSettingsSecretEncryptionTest"</automated>
  </verify>
  <acceptance_criteria>
    `./gradlew test --tests "com.six2dez.burp.aiagent.backends.BackendRegistryTest" --tests "com.six2dez.burp.aiagent.config.AgentSettingsSecretEncryptionTest"` passes. `grep -n "KEY_ANTHROPIC_API_KEY" src/main/kotlin/com/six2dez/burp/aiagent/config/AgentSettings.kt` shows it in the companion constants, the encrypt/decrypt sites, AND the migration secretKeys list. `grep -n "buildAnthropicPanel\|cards.add(buildAnthropicPanel" src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/BackendConfigPanel.kt` shows the card + registration. The Anthropic card has NO "Base URL" row. The four AgentSettings fields all carry defaults (named-arg baselineSettings still compiles).
  </acceptance_criteria>
  <done>All four AgentSettings fields exist with defaults; anthropicApiKey round-trips encrypted under KEY_ANTHROPIC_API_KEY and is in the migration list; Anthropic card renders Model + masked API key + Test connection (no Base URL); registry test confirms id "anthropic".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Burp extension → api.anthropic.com | Outbound AI request carrying the user's prompt + (redacted) traffic context; MUST traverse Burp's HTTP stack so proxy/redaction/cert participation applies |
| User settings UI → Burp Preferences (disk) | The Anthropic API key crosses from a JPasswordField into persisted preferences |
| Anthropic API response → backend parser → chat UI | Untrusted JSON from a third party is parsed and rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information disclosure | AnthropicBackend HTTP path | mitigate | All HTTP via `config.transport.post(...)`; `transport == null` fails fast (SC2b); SC2c source-string guard asserts no `okhttp3`/`OkHttpClient` so no client can bypass Burp's proxy/redaction (#69 trap) — verified by AnthropicBackendTransportRoutingTest |
| T-14-02 | Information disclosure | API key at rest (AgentSettings) | mitigate | `anthropicApiKey` encrypted via `SecretCipher` (AES-256-GCM) under `KEY_ANTHROPIC_API_KEY`; added to schema-V4 migration list; AgentSettingsSecretEncryptionTest asserts ENC1: envelope at rest + decrypt round-trip |
| T-14-03 | Information disclosure | Backend request logging | mitigate | Body-shape-only logging copied verbatim from the OpenAI analog (`model=… messages=N json_bytes=…`); the JSON, message content, and the key are never logged (Phase 12 rule) — enforced by code review against the analog |
| T-14-04 | Tampering / SSRF | Anthropic endpoint configurability | accept | Endpoint is a fixed backend constant (`https://api.anthropic.com/v1/messages`), NOT a user field; FLAG-14-02 mandates NO Base URL row on the card — there is no user-controlled URL to point at an internal host, so SSRF surface is narrower than the OpenAI-compat card by design |
| T-14-05 | Spoofing | Anthropic auth header | mitigate | Supervisor branch sets `x-api-key` + `anthropic-version: 2023-06-01` (never `Authorization: Bearer`, Pitfall 2); the key is sent only over TLS via Burp's stack |
| T-14-06 | Tampering | Untrusted Anthropic JSON response | mitigate | Response parsed defensively with Jackson `path()` (null-safe, no exceptions on missing fields); only `content[].text` is concatenated and `usage.input_tokens`/`output_tokens` read as ints coerced ≥ 0; parsed text flows into the existing chat-render path (no eval, no injection downstream) |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | ZERO new dependencies this phase (Jackson/kotlinx-serialization/Montoya/okhttp all pre-existing in build.gradle.kts, RESEARCH Package Legitimacy Audit = N/A); no install surface, so no slopcheck/blocking checkpoint applies |
</threat_model>

<verification>
- `./gradlew test --tests "com.six2dez.burp.aiagent.backends.anthropic.*"` — SC2a/b/c + SC3 green.
- `./gradlew test --tests "com.six2dez.burp.aiagent.backends.BackendRegistryTest" --tests "com.six2dez.burp.aiagent.config.AgentSettingsSecretEncryptionTest"` — registration + encrypted-key round-trip green.
- `grep -E "okhttp3|OkHttpClient" src/main/kotlin/com/six2dez/burp/aiagent/backends/anthropic/AnthropicBackend.kt` — no output (SC2c).
- Per-wave merge gate: `./gradlew test` full suite green (358+ tests).
- SC1 (live streaming visible through the proxy) is HUMAN-UAT only — out of scope for automated verification this plan; flagged in 14-VALIDATION.md Manual-Only Verifications.
</verification>

<success_criteria>
- A user can select Anthropic, enter a masked API key (encrypted on save), and edit the Model field (default claude-sonnet-4-6) — backend selectable and configurable (SC1 setup; live smoke is HUMAN-UAT).
- AnthropicBackend.send() issues HTTP only via the injected transport to the Anthropic endpoint, contains no OkHttp on the production path (SC2), and fails fast on a null transport.
- A 400 whose body contains "model" surfaces the exact SC3 string.
- anthropicApiKey is encrypted at rest under KEY_ANTHROPIC_API_KEY and in the migration list; the factory is registered via ServiceLoader + fallback.
- All four new AgentSettings fields exist with defaults (no downstream construction breaks).
</success_criteria>

<output>
Create `.planning/phases/14-anthropic-backend-token-budget-listener-port/14-01-SUMMARY.md` when done.
</output>

---
phase: 15-pre-send-secret-tripwire
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScanner.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolContext.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/redact/SecretTripwireHooksTest.kt
autonomous: true
requirements: [PRIV-03]

must_haves:
  truths:
    - "On a tripwire match, PassiveAiScanner emits a secret_tripwire_detect audit event and STILL calls supervisor.send — never blocks (SC4 / SC2)"
    - "On a tripwire match, McpToolContext.redactIfNeeded emits a secret_tripwire_detect audit event and STILL returns the final redacted string — never blocks (SC4 / SC2)"
    - "All three scanner send sites (single L911, batch L1561, sendSingleAnalysis L1647) scan the FINAL post-redaction prompt, not raw metadataText (G1)"
    - "Every detect audit payload carries sessionId + truncated entropy score + shape categories, and NEVER the raw value (SC3)"
    - "The three scanner hooks are committed in THIS phase so Phase 19's split carries them along (G7)"
  artifacts:
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScanner.kt"
      provides: "detect + emitGlobal + proceed at all three supervisor.send sites"
      contains: "secret_tripwire_detect"
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolContext.kt"
      provides: "detect + emitGlobal + proceed inside redactIfNeeded"
      contains: "secret_tripwire_detect"
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/redact/SecretTripwireHooksTest.kt"
      provides: "Per-path detect-payload + proceed assertions (SC4) with stubbed supervisor"
      contains: "class SecretTripwireHooksTest"
  key_links:
    - from: "src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScanner.kt"
      to: "AuditLogger.emitGlobal"
      via: "detect-and-proceed before each supervisor.send"
      pattern: "emitGlobal"
    - from: "src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolContext.kt"
      to: "SecretTripwire.scan"
      via: "scan of the final redacted string in redactIfNeeded"
      pattern: "SecretTripwire.scan"
    - from: "src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScanner.kt"
      to: "supervisor.currentSessionId"
      via: "sessionId for the detect event"
      pattern: "currentSessionId"
---

<objective>
Wire the two non-interactive outbound paths to the SecretTripwire detector (built in 15-01): the three `PassiveAiScanner` send sites and `McpToolContext.redactIfNeeded()`. There is no user to confirm on these paths, so on a tripwire match they audit-log the detection and PROCEED (never block — honors SC2), emitting `secret_tripwire_detect` via the existing static `AuditLogger.emitGlobal`. Together with the chat path (15-02), this satisfies SC4: the tripwire fires (detect + audit) on all three outbound paths.

This plan is file-disjoint from 15-02 (it owns `PassiveAiScanner.kt` + `McpToolContext.kt`; 15-02 owns `ContextPreviewDialog.kt` + `ChatPanel.kt`), so both run in parallel in Wave 2.

Critical sequencing (G7): the three `PassiveAiScanner` hooks MUST land in this phase. Phase 19 (QUAL-01 mega-file split) later moves these exact methods; committing the hooks here first means Phase 19 carries them along. Do NOT defer.

Purpose: This closes the cross-path coverage half of PRIV-03 — the scanner and MCP egress points get the same last-line-of-defense detection as the chat path, audited but never blocking, so automated/agent flows cannot silently leak a surviving secret without a trace.

Output: edits to `PassiveAiScanner.kt` (3 hook sites) and `McpToolContext.kt` (1 hook), plus `SecretTripwireHooksTest.kt` asserting, per path, that the detect event is emitted with the right keys (no raw token) AND the send still proceeds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-pre-send-secret-tripwire/15-RESEARCH.md
@.planning/phases/15-pre-send-secret-tripwire/15-PATTERNS.md
@.planning/phases/15-pre-send-secret-tripwire/15-VALIDATION.md

# Files modified in this plan + the detector built in 15-01 + the audit channel:
@src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolContext.kt
@src/main/kotlin/com/six2dez/burp/aiagent/audit/AuditLogger.kt
@src/test/kotlin/com/six2dez/burp/aiagent/redact/SafeRegexTest.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — create SecretTripwireHooksTest.kt (failing): per-path detect-payload + proceed (SC4)</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/redact/SecretTripwireHooksTest.kt</files>
  <read_first>
    - src/test/kotlin/com/six2dez/burp/aiagent/redact/SafeRegexTest.kt (analog: headless JUnit Jupiter + Assertions)
    - .planning/phases/15-pre-send-secret-tripwire/15-PATTERNS.md (mockito-kotlin stubbing block: mock AgentSupervisor then whenever(currentSessionId()).thenReturn("sess-1"); Shared Patterns audit event shape)
    - .planning/phases/15-pre-send-secret-tripwire/15-VALIDATION.md (SC4 row: one test per hook, stub currentSessionId, capture emitGlobal, assert send still proceeds; SC3 no-leak)
    - src/main/kotlin/com/six2dez/burp/aiagent/redact/SecretTripwire.kt (15-01 — scan + ScanResult consumed by the shared detect-payload builder)
  </read_first>
  <behavior>
    SecretTripwireHooksTest.kt (package com.six2dez.burp.aiagent.redact) — headless. Tests the shared detect-payload builder that both non-interactive hooks use (extract it onto SecretTripwire so the hook bodies are one-liners and the logic is testable without spinning up the scanner/MCP runtime):
    - buildDetectAuditPayload(scan, path, sessionId): for path "passive_scanner" and "mcp", the Map contains "path" (the given value), "sessionId" (the given id, or "none" when null), "shapeCategories" (sorted List of names), "entropyScore" (truncatedScore string).
    - no-leak: the payload string form does NOT contain the raw input token (only category names + a number).
    - sessionId fallback: when the supplied sessionId is null, the payload "sessionId" value is "none".
    - emit-only-on-match contract: a helper (e.g. SecretTripwire.detectAndBuild(scan, ...)) returns null / signals no-emit when scan.matched is false, and a payload when true — proving the hook never emits on a clean payload but the caller still proceeds regardless.
    - mockito-kotlin (already a test dep) MAY be used to stub AgentSupervisor.currentSessionId() if a path-level test instantiates a hook helper; the detect-payload builder itself needs no mocks.
  </behavior>
  <action>
    Create SecretTripwireHooksTest.kt mirroring SafeRegexTest header style. Assert the shared detect-payload builder (buildDetectAuditPayload + detectAndBuild on SecretTripwire, introduced in Task 2/Task 3) produces the SC3 payload shape for both "passive_scanner" and "mcp" paths, applies the "none" sessionId fallback, contains no raw token (no-leak), and signals no-emit on a non-matched scan while the caller proceeds. Use org.mockito.kotlin.mock/whenever only where a stubbed supervisor is needed. Expected to FAIL until the helpers exist (RED).
  </action>
  <verify>
    <automated>test -f src/test/kotlin/com/six2dez/burp/aiagent/redact/SecretTripwireHooksTest.kt && grep -q "class SecretTripwireHooksTest" src/test/kotlin/com/six2dez/burp/aiagent/redact/SecretTripwireHooksTest.kt</automated>
  </verify>
  <done>SecretTripwireHooksTest exists asserting the detect-payload shape (SC3) for both non-interactive paths, the "none" sessionId fallback, no-leak, and no-emit-on-clean; fails pending the helpers (RED). Commit: test(15-03): add failing non-interactive hook detect-payload tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PassiveAiScanner — detect + emitGlobal + proceed at all three supervisor.send sites (G7)</name>
  <files>src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScanner.kt</files>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScanner.kt (redaction boundary: safeMetadataText at line 846; singlePrompt = buildAnalysisPrompt(safeMetadataText, ...) at line 858; send sites: single L911 text=singlePrompt, batch L1561 text=prompt from buildBatchAnalysisPrompt L1543, sendSingleAnalysis L1647 text=prompt from buildAnalysisPrompt L1625; currentSessionId already used at L451; AuditLogger already imported at L13)
    - .planning/phases/15-pre-send-secret-tripwire/15-PATTERNS.md (PassiveAiScanner MODIFY section — identical hook body at all three sites; G1 wrong-bytes warning; G7 sequencing)
    - .planning/phases/15-pre-send-secret-tripwire/15-RESEARCH.md (Code Examples "Non-interactive hook — PassiveAiScanner"; Common Pitfalls Pitfall 1)
    - src/main/kotlin/com/six2dez/burp/aiagent/redact/SecretTripwire.kt (15-01 — scan + the shared detect helpers from Task 1)
  </read_first>
  <action>
    In PassiveAiScanner.kt, immediately BEFORE each of the three supervisor.send(...) calls (single L911 scanning singlePrompt; batch L1561 scanning prompt; sendSingleAnalysis L1647 scanning prompt), insert the identical detect-and-proceed hook: scan the final prompt var with SecretTripwire.scan(...); when matched, call AuditLogger.emitGlobal("secret_tripwire_detect", SecretTripwire.buildDetectAuditPayload(tw, path = "passive_scanner", sessionId = supervisor.currentSessionId() ?: "none")). Then FALL THROUGH to the existing supervisor.send — NO blocking, NO early return (SC2; the hook only logs). CRITICAL (G1/Pitfall 1): scan the FINAL post-redaction prompt (singlePrompt / prompt, built from safeMetadataText redacted at L846-855), NEVER the raw metadataText. Do NOT re-run redaction (G8 — supervisor.send does not redact; the prompt is already final). Reuse the existing currentSessionId() accessor (already called at L451). Add imports: com.six2dez.burp.aiagent.redact.SecretTripwire and com.six2dez.burp.aiagent.redact.Entropy (AuditLogger is already imported at L13). NEVER put the matched token in the payload (G4 — categories + score only). This commits the hooks in Phase 15 (G7) so Phase 19's split carries them along. Run the hooks test + full suite GREEN.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.redact.SecretTripwireHooksTest" && test $(grep -c "secret_tripwire_detect" src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScanner.kt) -eq 3</automated>
  </verify>
  <done>SecretTripwireHooksTest GREEN; PassiveAiScanner has exactly three secret_tripwire_detect emit sites, each scanning the final redacted prompt and falling through to supervisor.send (never blocking). Hooks are in PassiveAiScanner.kt now (G7). Commit: feat(15-03): tripwire detect+proceed at three scanner send sites (SC4).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: McpToolContext.redactIfNeeded — detect + emitGlobal + proceed (null-safe sessionId)</name>
  <files>src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolContext.kt, src/main/kotlin/com/six2dez/burp/aiagent/redact/SecretTripwire.kt</files>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolContext.kt (redactIfNeeded lines 53-57 — the two return branches to fold into a finalText val; supervisor is a nullable field at line 36; PrivacyMode/Redaction/RedactionPolicy already imported lines 10-12)
    - src/main/kotlin/com/six2dez/burp/aiagent/audit/AuditLogger.kt (emitGlobal static channel line 26 — no constructor threading needed; the McpTool.kt:226 precedent uses it)
    - .planning/phases/15-pre-send-secret-tripwire/15-PATTERNS.md (McpToolContext MODIFY section — exact rewrite of redactIfNeeded; supervisor?.currentSessionId() ?: "none")
    - .planning/phases/15-pre-send-secret-tripwire/15-RESEARCH.md (Code Examples "Non-interactive hook — McpToolContext.redactIfNeeded")
  </read_first>
  <action>
    Rewrite redactIfNeeded(raw) in McpToolContext.kt to: compute a finalText val from the two existing branches (OFF returns raw; otherwise Redaction.apply(raw, RedactionPolicy.fromMode(privacyMode), stableHostSalt = hostSalt)); then scan finalText with SecretTripwire.scan(...); when matched, call AuditLogger.emitGlobal("secret_tripwire_detect", SecretTripwire.buildDetectAuditPayload(tw, path = "mcp", sessionId = supervisor?.currentSessionId() ?: "none")) — note supervisor is nullable (field at L36), so use the null-safe call with the "none" fallback; then return finalText (PROCEED — the MCP output is still returned, never blocked, SC2). Do NOT thread an AuditLogger constructor param — use the static emitGlobal (Don't-Hand-Roll; RESEARCH A3). Add imports: com.six2dez.burp.aiagent.audit.AuditLogger, com.six2dez.burp.aiagent.redact.SecretTripwire, com.six2dez.burp.aiagent.redact.Entropy. NEVER put the matched token in the payload (G4). Run the hooks test + full suite GREEN.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.redact.SecretTripwireHooksTest" && grep -q "secret_tripwire_detect" src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolContext.kt && grep -q "supervisor?.currentSessionId" src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolContext.kt</automated>
  </verify>
  <done>SecretTripwireHooksTest GREEN; redactIfNeeded computes finalText, emits secret_tripwire_detect on match with the null-safe sessionId ("none" fallback) + truncated score + categories, and returns finalText (never blocks, SC2). Uses the static emitGlobal (no constructor surgery). Commit: feat(15-03): tripwire detect+proceed in McpToolContext.redactIfNeeded (SC4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner final prompt -> AI backend wire | The post-redaction analysis prompt about to leave Burp for a third-party AI on the automated passive-scan path; no human present, so detection is logged not gated. |
| MCP tool output -> MCP client / agent | The final redacted string returned by `redactIfNeeded` crosses to an MCP consumer; detection is logged not gated. |
| detect event -> audit log (audit.jsonl) | The detection is recorded for attributability; the record must carry no secret. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | Information Disclosure | secret survives redaction and is exfiltrated on the scanner/MCP path | mitigate | Post-redaction tripwire scans the FINAL prompt/`finalText` at each egress point and emits `secret_tripwire_detect`; the detection is auditable even though (correctly) non-blocking. |
| T-15-10 | Information Disclosure | scanner hook reads raw `metadataText` instead of the redacted `singlePrompt`/`prompt` | mitigate | Hooks land AFTER `buildAnalysisPrompt(safeMetadataText, …)` and scan the final prompt var (L911/1561/1647), never the raw `metadataText` (G1/Pitfall 1). |
| T-15-11 | Information Disclosure | detect audit payload leaking the matched token | mitigate | Payload is built by the shared `buildDetectAuditPayload` from `shapeCategories` (names) + `truncatedScore` (number) + `sessionId` only; SecretTripwireHooksTest asserts no input-token substring (SC3; CLAUDE.md audit-defaults). |
| T-15-12 | Denial of Service (of the pentest/agent workflow) | a tripwire match aborting an automated send | mitigate | Both hooks log-and-proceed: the scanner falls through to `supervisor.send`; `redactIfNeeded` returns `finalText`. No code path drops the send/output on a match (SC2). |
| T-15-13 | Tampering / Repudiation | hooks deferred to Phase 19 and lost during the mega-file split | mitigate | The three scanner hooks are committed in Phase 15 (G7); Phase 19's split carries them along. The `grep -c == 3` verify gate proves all three landed. |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | None — zero external packages added (detector is in-repo from 15-01; audit channel and supervisor accessor already exist). No install task → no legitimacy checkpoint applies; Package Legitimacy Audit in 15-RESEARCH.md confirms no registry dependency. |
</threat_model>

<verification>
- `./gradlew test --tests "*SecretTripwireHooksTest"` green (SC4 per-path detect-payload + proceed; SC3 no-leak + "none" fallback).
- `./gradlew test` full suite green (no regression in the 399+ existing scanner/MCP tests).
- `PassiveAiScanner.kt` contains exactly three `secret_tripwire_detect` emit sites (`grep -c == 3`), each before a `supervisor.send` with no early return (proceed).
- `McpToolContext.kt` contains `secret_tripwire_detect` and the null-safe `supervisor?.currentSessionId`, and `redactIfNeeded` still returns the final text.
- Build/test via `./gradlew test`, NOT `ktlintCheck` (generateBuildFlags defect, MEMORY.md / 15-RESEARCH.md).
</verification>

<success_criteria>
- All three scanner send sites and `redactIfNeeded` detect a tripwire match on the FINAL redacted payload, emit `secret_tripwire_detect`, and PROCEED (never block) — SC4 + SC2.
- Every detect payload carries sessionId + truncated entropy score + categories, never the raw value (SC3).
- The three scanner hooks are committed in Phase 15 (G7).
</success_criteria>

<output>
Create `.planning/phases/15-pre-send-secret-tripwire/15-03-SUMMARY.md` when done.
</output>

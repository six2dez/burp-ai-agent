---
phase: 15-pre-send-secret-tripwire
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/components/ContextPreviewDialog.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/redact/SecretTripwireGateTest.kt
autonomous: true
requirements: [PRIV-03]

must_haves:
  truths:
    - "When the post-redaction contextJson contains a tripwire match, the preview dialog banner escalates to RISK and the affirmative button reads 'Send anyway' (SC5)"
    - "Cancel remains the default-focused option even when a tripwire match is present — Enter never silently sends past a secret (SC2 / G5)"
    - "ContextPreviewDialog.confirm() still returns Boolean and its single caller (ChatPanel.kt) is unchanged in signature (G3 / FLAG-15-03)"
    - "Choosing 'Send anyway' writes a secret_tripwire_allow audit event with sessionId + truncated entropy score + shape categories, and NEVER the raw value (SC3)"
    - "The clean path (no tripwire match) is byte-for-byte today's behavior: 'Send'/'Cancel', Send -> true"
  artifacts:
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/ui/components/ContextPreviewDialog.kt"
      provides: "Tripwire RISK banner + 'Send anyway'/Cancel gate (Boolean-preserving)"
      contains: "SecretTripwire.scan"
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt"
      provides: "secret_tripwire_allow audit emit after createSession with real session id"
      contains: "secret_tripwire_allow"
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/redact/SecretTripwireGateTest.kt"
      provides: "Pure gate-decision logic test: RISK+label+default-Cancel selection (SC5) + allow-event payload shape (SC3)"
      contains: "class SecretTripwireGateTest"
  key_links:
    - from: "src/main/kotlin/com/six2dez/burp/aiagent/ui/components/ContextPreviewDialog.kt"
      to: "SecretTripwire.scan"
      via: "in-dialog self-scan replacing SecretShapes.findSurviving (line 60)"
      pattern: "SecretTripwire\\.scan"
    - from: "src/main/kotlin/com/six2dez/burp/aiagent/ui/components/ContextPreviewDialog.kt"
      to: "SubtleNotice.Level.RISK"
      via: "banner escalation on match"
      pattern: "Level\\.RISK"
    - from: "src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt"
      to: "AuditLogger.emitGlobal"
      via: "allowlist audit after createSession"
      pattern: "emitGlobal\\("
---

<objective>
Wire the interactive ChatPanel send path to the SecretTripwire detector (built in 15-01): extend the existing `ContextPreviewDialog.confirm()` into a warn-with-confirmation gate (SC5) — escalate the Phase 13 survived-secret banner from WARN to RISK and relabel the affirmative to "Send anyway" only when a tripwire match is present, keeping Cancel as the default focus and preserving the `Boolean` return contract. Emit the `secret_tripwire_allow` audit event (SC3) in `ChatPanel.startSessionFromContext` after the session is created, so the event carries a real session id.

This is the only path with a human to confirm. It is a minimal delta on ONE existing UI component per 15-UI-SPEC.md (Delta 1 + Delta 2); no new modal, no new component, no new design token.

This plan is file-disjoint from 15-03 (it owns `ContextPreviewDialog.kt` + `ChatPanel.kt`; 15-03 owns `PassiveAiScanner.kt` + `McpToolContext.kt`), so both run in parallel in Wave 2.

Purpose: This delivers the user-visible half of PRIV-03 — the warn-with-confirmation gate and the auditable allowlist decision — without ever hard-blocking a send (SC2).

Output: edits to `ContextPreviewDialog.kt` and `ChatPanel.kt`, plus a pure logic test `SecretTripwireGateTest.kt` covering the gate decision (RISK level + "Send anyway" label + Cancel default when matched) and the allow-event payload shape. The Swing render (red banner, button layout) is human-UAT per 15-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-pre-send-secret-tripwire/15-RESEARCH.md
@.planning/phases/15-pre-send-secret-tripwire/15-PATTERNS.md
@.planning/phases/15-pre-send-secret-tripwire/15-UI-SPEC.md
@.planning/phases/15-pre-send-secret-tripwire/15-VALIDATION.md

# Files modified in this plan + the detector built in 15-01:
@src/main/kotlin/com/six2dez/burp/aiagent/ui/components/ContextPreviewDialog.kt
@src/main/kotlin/com/six2dez/burp/aiagent/ui/components/SubtleNotice.kt
@src/main/kotlin/com/six2dez/burp/aiagent/audit/AuditLogger.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — create SecretTripwireGateTest.kt (failing): gate-decision logic + allow-event payload shape</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/redact/SecretTripwireGateTest.kt</files>
  <read_first>
    - src/test/kotlin/com/six2dez/burp/aiagent/redact/SafeRegexTest.kt (analog: headless JUnit Jupiter + Assertions; no AWT)
    - .planning/phases/15-pre-send-secret-tripwire/15-UI-SPEC.md (Delta 1 banner-level table + Delta 2 gate table — the exact RISK/label/default-Cancel contract to assert)
    - .planning/phases/15-pre-send-secret-tripwire/15-VALIDATION.md (SC5 row: "branch picks RISK + 'Send anyway' + default Cancel when matched"; SC3 row: allow-event payload keys, NOT the input token)
    - src/main/kotlin/com/six2dez/burp/aiagent/redact/SecretTripwire.kt (15-01 — ScanResult fields consumed by the gate)
  </read_first>
  <behavior>
    SecretTripwireGateTest.kt (package com.six2dez.burp.aiagent.redact) — pure logic, headless, no Swing instantiation. The dialog's gate decision is extracted into pure helpers on SecretTripwire (or a small companion) so it is unit-testable without AWT:
    - When ScanResult.matched is true: the banner level selected is SubtleNotice.Level.RISK-equivalent (assert via a pure enum/string the helper returns, e.g. a GateDecision with bannerRisk=true), the affirmative label is "Send anyway", and the default option index points at Cancel (index 1, never 0).
    - When ScanResult.matched is false: bannerRisk=false (banner hidden/advisory), affirmative label is "Send", default option index 1 (Cancel) unchanged.
    - SC3 allow-event payload builder: given a matched ScanResult and a sessionId, the produced audit payload Map contains keys "path"=="chat", "sessionId", "shapeCategories" (a sorted List of category names), and "entropyScore" (the truncatedScore string); and the payload's string form does NOT contain a raw secret token (assert no input-token substring).
  </behavior>
  <action>
    Create SecretTripwireGateTest.kt mirroring SafeRegexTest header/assertion style. It exercises the pure gate-decision helper(s) and the allow-payload builder that Task 2/Task 3 will introduce (e.g. SecretTripwire.gateDecision(scan): GateDecision returning bannerRisk: Boolean + affirmativeLabel: String + cancelIsDefault: Boolean; and a buildAllowAuditPayload(scan, sessionId): Map&lt;String, Any?&gt; used by ChatPanel). Do NOT instantiate JOptionPane/JPanel/Swing in the test (keep it headless). Assert the SC5 branch picks RISK + "Send anyway" + Cancel-default when matched, and the SC3 payload map shape + no-leak. Expected to FAIL until the helpers exist (RED).
  </action>
  <verify>
    <automated>test -f src/test/kotlin/com/six2dez/burp/aiagent/redact/SecretTripwireGateTest.kt &amp;&amp; grep -q "class SecretTripwireGateTest" src/test/kotlin/com/six2dez/burp/aiagent/redact/SecretTripwireGateTest.kt</automated>
  </verify>
  <done>SecretTripwireGateTest exists asserting the RISK/label/Cancel-default decision (SC5) and the allow-payload keys + no-leak (SC3); fails pending the helpers (RED). Commit: test(15-02): add failing gate-decision + allow-payload tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ContextPreviewDialog gate — RISK banner + 'Send anyway' (Boolean-preserving) + gate helper</name>
  <files>src/main/kotlin/com/six2dez/burp/aiagent/ui/components/ContextPreviewDialog.kt, src/main/kotlin/com/six2dez/burp/aiagent/redact/SecretTripwire.kt</files>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/components/ContextPreviewDialog.kt (the file to edit: self-scan block lines 59-72; options + showOptionDialog lines 91-103; Cancel default = options[1] at line 101 — DO NOT change to options[0])
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/components/SubtleNotice.kt (Level.RISK already wired: enum line 25, subtleDanger line 105, accentDanger line 111; setMessage line 69; hideNotice line 87)
    - .planning/phases/15-pre-send-secret-tripwire/15-UI-SPEC.md (Delta 1 banner copy table; Delta 2 gate table; FLAG-15-01 two-state collapse permitted; FLAG-15-02 relabel-in-place preferred)
    - .planning/phases/15-pre-send-secret-tripwire/15-PATTERNS.md (ContextPreviewDialog MODIFY section — exact insertion points)
  </read_first>
  <action>
    In ContextPreviewDialog.kt: replace the self-scan at lines 59-72. Swap SecretShapes.findSurviving(contextJson) for SecretTripwire.scan(contextJson) (the contextJson arg is ALREADY the final post-redaction payload, redacted in ContextCollector — G2; do NOT re-redact, G8). When scan.matched, call survivedNotice.setMessage(SubtleNotice.Level.RISK, html) with the Delta 1 copy (named-shape variant "A value matching a known secret shape ($shapes) survived redaction. Review before sending." when shapeCategories is non-empty; the high-entropy variant "A high-entropy value that may be a secret survived redaction. Review before sending." when shapeCategories is empty); else survivedNotice.hideNotice(). FLAG-15-01 permits collapsing to clean->RISK (drop the Phase 13 WARN advisory branch) — prefer the simpler clean/RISK two-state. At lines 91-103: set val affirmative = if (scan.matched) "Send anyway" else "Send" (relabel ONLY on match, Delta 2), build options = arrayOf(affirmative, "Cancel"), and keep options[1] (Cancel) as the showOptionDialog initialValue (NEVER options[0] — G5/Pitfall 5). Return stays choice == 0; keep confirm()'s Boolean signature unchanged (G3 — its single caller ChatPanel.kt:299 routes false to "cancelled by user"). Add the pure gate helper that the test consumes: SecretTripwire.gateDecision(scan): GateDecision { bannerRisk, affirmativeLabel, cancelIsDefault=true } (put GateDecision + gateDecision() on SecretTripwire in the redact package so it stays AWT-free and testable; the dialog calls it to choose level + label). Update imports: add com.six2dez.burp.aiagent.redact.SecretTripwire; drop the now-unused SecretShapes import if the WARN advisory branch is removed. NEVER interpolate the matched token into the banner (names/categories only — UI-SPEC; G4). English-only copy.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.redact.SecretTripwireGateTest" &amp;&amp; grep -q "SecretTripwire.scan" src/main/kotlin/com/six2dez/burp/aiagent/ui/components/ContextPreviewDialog.kt &amp;&amp; grep -q "Send anyway" src/main/kotlin/com/six2dez/burp/aiagent/ui/components/ContextPreviewDialog.kt</automated>
  </verify>
  <done>The gate-decision portion of SecretTripwireGateTest is GREEN; ContextPreviewDialog uses SecretTripwire.scan, escalates to Level.RISK + "Send anyway" on match, keeps Cancel as default (options[1] initialValue) and the Boolean return. Clean path unchanged ("Send"/"Cancel"). Commit: feat(15-02): tripwire RISK gate in ContextPreviewDialog (Boolean-preserving).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ChatPanel allowlist audit — emit secret_tripwire_allow after createSession (real session id)</name>
  <files>src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt</files>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt (call site lines 296-316: confirm() at 299 with contextJson = capture.contextJson at 304; createSession returns `session` at 313 — emit AFTER this for a real id, G3/Open Q1 Option b)
    - src/main/kotlin/com/six2dez/burp/aiagent/audit/AuditLogger.kt (emitGlobal static channel line 26; logEvent appends ts/type/payload/payloadSha256 and respects isEnabled() line 54)
    - .planning/phases/15-pre-send-secret-tripwire/15-PATTERNS.md (ChatPanel MODIFY section — the post-createSession emit; "pick ONE home for the chat allow-event")
    - .planning/phases/15-pre-send-secret-tripwire/15-RESEARCH.md (§Code Examples audit event shape; §Open Questions Q1 recommendation b)
  </read_first>
  <action>
    In ChatPanel.startSessionFromContext, AFTER val session = createSession(title) at line 313 (the point where a real session.id exists), add: re-scan val scan = SecretTripwire.scan(capture.contextJson) (cheap, same final payload — confirm() already returned true to reach here) and, if scan.matched, call AuditLogger.emitGlobal("secret_tripwire_allow", payload) where payload is built via the shared SecretTripwire.buildAllowAuditPayload(scan, session.id) introduced in Task 2's helper set: a Map with "path" to "chat", "sessionId" to session.id, "shapeCategories" to scan.shapeCategories.toList().sorted(), and "entropyScore" to Entropy.truncatedScore(scan.maxEntropyBitsPerChar). NEVER put the matched token in the map (G4 — categories + score only). This is the SINGLE home for the chat allow-event (do not also emit inside confirm() — avoid double-logging; confirm() stays UI-only and Boolean). Add imports: com.six2dez.burp.aiagent.audit.AuditLogger, com.six2dez.burp.aiagent.redact.SecretTripwire, com.six2dez.burp.aiagent.redact.Entropy. English-only. Extend SecretTripwireGateTest (or it already covers buildAllowAuditPayload from Task 1) to assert the "chat"-path payload keys + no-leak; run it GREEN.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.redact.SecretTripwireGateTest" &amp;&amp; grep -q "secret_tripwire_allow" src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt</automated>
  </verify>
  <done>SecretTripwireGateTest GREEN (allow-payload shape + no-leak for path "chat"). ChatPanel emits secret_tripwire_allow once, after createSession, with session.id + truncated entropy score + categories, never the raw value (SC3). confirm() remains Boolean-only (no double-emit). Commit: feat(15-02): audit-log tripwire allowlist with real session id (SC3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user decision (dialog) -> AI backend wire | The human confirms or cancels sending a payload that may contain a surviving secret; the gate makes the choice explicit and non-default-affirmative. |
| allowlist decision -> audit log (audit.jsonl) | The "Send anyway" choice is recorded for attributability; the record must carry no secret. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Information Disclosure | secret_tripwire_allow audit payload in ChatPanel | mitigate | Payload built from `shapeCategories` (names) + `truncatedScore` (number) + `sessionId` only; the matched token is never interpolated. SecretTripwireGateTest asserts no input-token substring in the payload string form (SC3; CLAUDE.md audit-defaults). |
| T-15-05 | Information Disclosure | RISK banner copy in ContextPreviewDialog | mitigate | Banner names shape categories only (UI-SPEC Delta 1; G4); the high-entropy variant carries no token at all. SecretShapes.findSurviving already returns names only. |
| T-15-06 | Denial of Service (of the pentest workflow) | confirm() gate | mitigate | Never hard-blocks (SC2): Cancel is the default focus but "Send anyway" is always one click; the function returns Boolean and a legitimate base64 fuzz payload is dismissible to proceed. No code path drops the send because of a match. |
| T-15-07 | Repudiation | user reflexively bypasses the gate (alert fatigue) | accept | Conservative thresholds (from 15-01) limit false positives; Cancel is default focus; every "Send anyway" is audit-logged so bypasses are attributable. Same accepted disposition as Phase 13 T-13-11. |
| T-15-08 | Tampering | breaking confirm()'s Boolean contract (single caller) | mitigate | The signature is preserved; the scan happens in-dialog (mirroring the existing SecretShapes self-scan at line 60); the gate decision is a pure helper. ChatPanel.kt:299 caller is unchanged (G3 / FLAG-15-03). |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | None — zero external packages added (UI uses the existing SubtleNotice/JOptionPane; detector is in-repo from 15-01). No install task → no legitimacy checkpoint applies. |
</threat_model>

<verification>
- `./gradlew test --tests "*SecretTripwireGateTest"` green (SC5 gate decision: RISK + "Send anyway" + Cancel default when matched; SC3 allow-payload shape + no-leak).
- `./gradlew test` full suite green (no regression in the 399+ existing tests — confirm() Boolean contract intact, ContextPreviewConsistencyTest still passes).
- `ContextPreviewDialog.kt` contains `SecretTripwire.scan` and `Level.RISK` and `"Send anyway"`; `options[1]` remains the `showOptionDialog` initialValue (Cancel default).
- `ChatPanel.kt` emits `secret_tripwire_allow` exactly once, after `createSession`.
- Build/test via `./gradlew test`, NOT `ktlintCheck`.
- HUMAN-UAT (per 15-VALIDATION.md, not automatable): in Burp, send a chat whose post-redaction payload contains a surviving `AKIA…`/high-entropy token; confirm the RISK red banner + "Send anyway"/Cancel gate appears, Cancel is default focus, Cancel dismisses, "Send anyway" proceeds and is audit-logged.
</verification>

<success_criteria>
- Matched payload -> RISK banner + "Send anyway" affirmative + Cancel default focus; clean payload -> unchanged "Send"/"Cancel" (SC5).
- `confirm()` still returns Boolean; single caller unchanged (G3).
- "Send anyway" writes a `secret_tripwire_allow` audit event with sessionId + truncated entropy score + categories, never the raw value (SC3).
- Never hard-blocks any send (SC2).
</success_criteria>

<output>
Create `.planning/phases/15-pre-send-secret-tripwire/15-02-SUMMARY.md` when done.
</output>

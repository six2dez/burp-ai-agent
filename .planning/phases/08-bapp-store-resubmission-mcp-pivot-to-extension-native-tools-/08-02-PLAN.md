---
phase: 08-bapp-store-resubmission-mcp-pivot-to-extension-native-tools
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolCatalog.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolContext.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpRuntimeContextFactory.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHandlers.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpTools.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/AiTools.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/mcp/AiGateMcpToolTest.kt
autonomous: true
requirements:
  - MCP-08-NATIVE
  - MCP-08-GATE
  - MCP-08-UI

must_haves:
  truths:
    - "MCP tools/list on a store build returns only the 8 native tools (status, issue_create, ai_analyze, ai_passive_scan, ai_findings_recent, redact_preview, ai_audit_query, ai_backends_list)"
    - "Calling ai_analyze via MCP when api.ai().isEnabled()=false returns a message containing 'unavailable'"
    - "Calling redact_preview returns the redacted text without requiring ai.isEnabled()=true"
    - "McpToolParityTest passes (all new native IDs appear in both all() and allIds())"
    - "AiGateMcpToolTest both tests pass green"
    - "SettingsPanel MCP tool list shows only available() tools (filtered by store build flag)"
  artifacts:
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/AiTools.kt"
      provides: "Registration + input schemas for 6 new native tools"
      contains: "registerAiTools"
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolContext.kt"
      provides: "Optional supervisor, passiveScanner, backendRegistry fields"
      contains: "supervisor"
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpRuntimeContextFactory.kt"
      provides: "Populates supervisor/passiveScanner/backendRegistry into context"
      contains: "supervisor"
  key_links:
    - from: "McpTools.kt registerTools()"
      to: "McpToolCatalog.available()"
      via: "registerAiTools() iterates McpToolRegistrations.native filtered by available()"
      pattern: "available()"
    - from: "executeToolResult() ai_analyze branch"
      to: "context.supervisor.isAiEnabled()"
      via: "if (!supervisor.isAiEnabled()) return@runTool"
      pattern: "isAiEnabled"
    - from: "SettingsPanel MCP tools section"
      to: "McpToolCatalog.available()"
      via: "McpToolCatalog.available().groupBy { it.category }"
      pattern: "available()"
---

<objective>
Implement the six new extension-native MCP tools (ai_analyze, ai_passive_scan, ai_findings_recent, redact_preview, ai_audit_query, ai_backends_list), wire their context dependencies through McpToolContext/McpRuntimeContextFactory, route registration and the SettingsPanel UI through McpToolCatalog.available() so the store build neither registers nor displays generic tools, and make the AiGateMcpToolTest Wave-0 stubs pass green.

Purpose: This is the core of the MCP pivot — the six new tools give the extension its "hooks into your extension" surface that PortSwigger allows, while the available() routing ensures the store JAR only exposes them.

Output: AiTools.kt (new), modified McpToolContext.kt/McpRuntimeContextFactory.kt/McpToolHandlers.kt/McpTools.kt/SettingsPanel.kt/McpToolCatalog.kt.
</objective>

<execution_context>
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/08-bapp-store-resubmission-mcp-pivot-to-extension-native-tools-/08-CONTEXT.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/08-bapp-store-resubmission-mcp-pivot-to-extension-native-tools-/08-RESEARCH.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/08-bapp-store-resubmission-mcp-pivot-to-extension-native-tools-/08-PATTERNS.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/08-bapp-store-resubmission-mcp-pivot-to-extension-native-tools-/08-01-SUMMARY.md
</execution_context>

<context>
@/Users/six2dez/Tools/burp-ai-agent/.planning/PROJECT.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/ROADMAP.md
@/Users/six2dez/Tools/burp-ai-agent/src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolCatalog.kt
@/Users/six2dez/Tools/burp-ai-agent/src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolContext.kt
@/Users/six2dez/Tools/burp-ai-agent/src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpRuntimeContextFactory.kt
@/Users/six2dez/Tools/burp-ai-agent/src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHandlers.kt
@/Users/six2dez/Tools/burp-ai-agent/src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpTools.kt
@/Users/six2dez/Tools/burp-ai-agent/src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/IssueTools.kt
@/Users/six2dez/Tools/burp-ai-agent/src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/UtilityTools.kt
@/Users/six2dez/Tools/burp-ai-agent/src/main/kotlin/com/six2dez/burp/aiagent/supervisor/AgentSupervisor.kt
@/Users/six2dez/Tools/burp-ai-agent/src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScanner.kt
@/Users/six2dez/Tools/burp-ai-agent/src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt
@/Users/six2dez/Tools/burp-ai-agent/src/test/kotlin/com/six2dez/burp/aiagent/mcp/AiGateMcpToolTest.kt

<interfaces>
<!-- From McpToolContext.kt (after Plan 01 runs — current fields to keep): -->
data class McpToolContext(
    val api: MontoyaApi,
    val privacyMode: PrivacyMode,
    val determinismMode: Boolean,
    val hostSalt: String,
    val toolToggles: Map<String, Boolean>,
    val unsafeEnabled: Boolean,
    val unsafeTools: Set<String>,
    val enabledUnsafeTools: Set<String>,
    val limiter: McpRequestLimiter,
    val edition: BurpSuiteEdition,
    val maxBodyBytes: Int,
    val scopeOnly: Boolean,
    val aiRequestLogger: AiRequestLogger? = null,
    // New fields to add (this plan):
    // val supervisor: AgentSupervisor? = null,
    // val passiveScanner: PassiveAiScanner? = null,
    // val backendRegistry: BackendRegistry? = null,
)

<!-- From AgentSupervisor.kt (key methods for AI tools): -->
// Line 119-124: isAiEnabled(): Boolean — wraps api.ai().isEnabled() in try/catch
// Line 309: send(text, history, contextJson, privacyMode, determinismMode, onChunk, onComplete, jsonMode, maxOutputTokens)
// Line 578: status(): AgentStatus — returns current backend state

<!-- From PassiveAiScanner.kt (key methods for ai_passive_scan/ai_findings_recent): -->
// Line 500-504: getLastFindings(n: Int): List<AiFinding> — synchronous, thread-safe
// Line 543-586: manualScan(requests: List<HttpRequestResponse>)
// (enqueueForScanCheck() does not exist yet — added in Plan 03)

<!-- From PassiveAiScanner.kt blocking AI call pattern (lines 891-928): -->
// CountDownLatch(1) + supervisor.send(..., onComplete = { completionLatch.countDown() })
// completionLatch.await(120_000L, TimeUnit.MILLISECONDS)

<!-- From McpTools.kt (critical patterns): -->
// Line 51-64: Server.registerTools() — dispatcher function calling all register* helpers
// Line 1325-2068: McpToolExecutor.executeToolResult() — when block inside runTool { ... }
// Line 2067: context.redactIfNeeded(output) applied automatically to every tool's return
// Line 2111-2122: decode<T>(raw: String?) helper — use for all tools with input
// Line 2176-2248: inputSchema() — when block per tool ID

<!-- From McpToolHandlers.kt (registration structure): -->
// Line 10-101: McpToolRegistrations object with group lists
// Line 100: fun allIds(): Set<String> = (utility + history + ... + issue).toSet()
// Line 103-164: registerToolHandler(toolId, context) — audit/limiter wrapper; use as-is

<!-- From IssueTools.kt (registration file pattern): -->
// internal fun Server.registerIssueTools(context: McpToolContext) {
//     McpToolRegistrations.issue.forEach { registerToolHandler(it, context) }
// }

<!-- From SettingsPanel.kt (tool list call sites): -->
// Line 2194: McpToolCatalog.all().groupBy { it.category }.forEach { ... }  ← change to available()
// Line 2234-2235: McpToolCatalog.all().filter { it.unsafeOnly }  ← change to available()
// Line 2548: for (tool in McpToolCatalog.all())  ← change to available()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 6 native tool descriptors to McpToolCatalog and extend McpToolContext/McpRuntimeContextFactory with AI tool dependencies</name>
  <files>
    src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolCatalog.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolContext.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpRuntimeContextFactory.kt
  </files>
  <read_first>
    McpToolCatalog.kt lines 3-11 (McpToolDescriptor data class, now with nativeTool field from Plan 01), lines 405-430 (end of tools list + companion functions).
    McpToolContext.kt full file (small — ~35 lines) — all fields.
    McpRuntimeContextFactory.kt full file — constructor fields (api, aiRequestLogger at line 12), create() function.
    PATTERNS.md "mcp/McpToolContext.kt" and "mcp/McpRuntimeContextFactory.kt" sections.
    KtorMcpServerManager.kt lines 31-45 — shows contextFactory.aiRequestLogger = logger pattern (line 44) which we extend for supervisor/passiveScanner/backendRegistry.
    App.kt lines 35-77 — shows supervisor, passiveAiScanner, backendRegistry are all lateinit var on App object.
  </read_first>
  <behavior>
    - Test: McpToolCatalog.all().count { it.nativeTool } == 8 after this task (status + issue_create + 6 new)
    - Test: McpToolParityTest.registeredToolIds_matchCatalog still passes — new IDs must also be in McpToolRegistrations.native (added in Task 2)
    - Test: McpToolContext can be constructed with supervisor=mockSupervisor, passiveScanner=mockScanner, backendRegistry=mockRegistry without breaking existing callers (all have default = null)
  </behavior>
  <action>
    McpToolCatalog.kt — add 6 new McpToolDescriptor entries with nativeTool = true. Place them before the closing parenthesis of the tools listOf(), after the existing issue_create entry. Use the existing McpToolDescriptor constructor pattern. The 6 entries:

    McpToolDescriptor(id = "ai_analyze", title = "AI Analyze", description = "Sends text to the active AI backend and returns the analysis result.", category = "AI", defaultEnabled = true, nativeTool = true)

    McpToolDescriptor(id = "ai_passive_scan", title = "AI Passive Scan", description = "Queues requests for AI passive security analysis and returns the count enqueued.", category = "AI", defaultEnabled = true, nativeTool = true)

    McpToolDescriptor(id = "ai_findings_recent", title = "AI Recent Findings", description = "Returns the most recent AI passive scan findings (up to n).", category = "AI", defaultEnabled = true, nativeTool = true)

    McpToolDescriptor(id = "redact_preview", title = "Redact Preview", description = "Applies the extension's privacy redaction engine to arbitrary text and returns the redacted result.", category = "AI", defaultEnabled = true, nativeTool = true)

    McpToolDescriptor(id = "ai_audit_query", title = "AI Audit Query", description = "Returns recent AI request audit log entries (hashes only unless verbose mode is enabled).", category = "AI", defaultEnabled = true, nativeTool = true)

    McpToolDescriptor(id = "ai_backends_list", title = "AI Backends List", description = "Lists available AI backends and reports the current active backend and connection state.", category = "AI", defaultEnabled = true, nativeTool = true)

    McpToolContext.kt — add three nullable fields after aiRequestLogger. Use the EXACT same pattern as aiRequestLogger: named parameters with default null. Import AgentSupervisor (com.six2dez.burp.aiagent.supervisor.AgentSupervisor), PassiveAiScanner (com.six2dez.burp.aiagent.scanner.PassiveAiScanner), BackendRegistry (com.six2dez.burp.aiagent.backends.BackendRegistry). All existing call sites use named-argument construction so they compile without change.

    McpRuntimeContextFactory.kt — add three mutable var fields alongside aiRequestLogger (lines 11-12): var supervisor: AgentSupervisor? = null, var passiveScanner: PassiveAiScanner? = null, var backendRegistry: BackendRegistry? = null. In the create() function, pass supervisor = supervisor, passiveScanner = passiveScanner, backendRegistry = backendRegistry to the McpToolContext constructor call.

    KtorMcpServerManager.kt — add three parallel setter-style assignments in setAiRequestLogger() or add a new setAiToolDependencies(supervisor, passiveScanner, backendRegistry) method alongside setAiRequestLogger() at line 43. The simplest approach matching the existing pattern: add a fun setAiToolDependencies(supervisor, passiveScanner, backendRegistry) that sets the three fields on contextFactory. This is called from App.kt in Plan 02 Task 3.

    Do NOT change KtorMcpServerManager's constructor signature.
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/burp-ai-agent && ./gradlew compileKotlin --quiet 2>&1 | tail -5</automated>
    <automated>cd /Users/six2dez/Tools/burp-ai-agent && ./gradlew test --tests "*.McpToolCatalogStoreBuildTest" -PexcludeHeavyTests=true --quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - McpToolCatalog.all().count { it.nativeTool } == 8.
    - McpToolContext compiles with 3 new nullable fields.
    - McpRuntimeContextFactory compiles with new fields and populates them in create().
    - KtorMcpServerManager has setAiToolDependencies() method.
    - McpToolCatalogStoreBuildTest.available_returnsOnlyNativeToolsWhenStoreBuildTrue passes (8 native tools returned).
    - ./gradlew compileKotlin succeeds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create AiTools.kt, extend McpToolHandlers.kt native list and McpTools.kt (register + handlers + inputSchema)</name>
  <files>
    src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/AiTools.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHandlers.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpTools.kt
  </files>
  <read_first>
    McpToolHandlers.kt lines 10-101 (McpToolRegistrations groups and allIds()) and lines 103-164 (registerToolHandler wrapper).
    McpTools.kt lines 51-64 (registerTools dispatcher), lines 1325-2068 (executeToolResult when block), lines 2111-2122 (decode helper), lines 2176-2248 (inputSchema when block).
    IssueTools.kt full file — registration file pattern for AiTools.kt.
    PATTERNS.md "mcp/tools/AiTools.kt" section — all 6 handler patterns and input schema data classes.
    PATTERNS.md "Blocking AI Send" shared pattern — CountDownLatch usage.
    PATTERNS.md "Authentication / AI Gate" shared pattern — isAiEnabled() check.
    AgentSupervisor.kt lines 119-124 (isAiEnabled()), lines 291-350 (send() signature).
    PassiveAiScanner.kt lines 500-508 (getLastFindings), lines 543-595 (manualScan).
  </read_first>
  <behavior>
    - Test: McpToolParityTest.registeredToolIds_matchCatalog passes — new 6 IDs in McpToolRegistrations.native + McpToolCatalog (from Task 1)
    - Test: AiGateMcpToolTest.aiAnalyze_returnsErrorWhenIsEnabledFalse passes (ai_analyze returns "unavailable" message)
    - Test: AiGateMcpToolTest.aiAnalyze_doesNotGateNonAiTool passes (redact_preview works when isEnabled()=false)
    - Test: executeToolResult("ai_findings_recent", "{}", context) does not throw when passiveScanner is null (returns "Passive scanner not available.")
  </behavior>
  <action>
    McpToolHandlers.kt — in McpToolRegistrations object (lines 10-101): add val native = listOf("ai_analyze", "ai_passive_scan", "ai_findings_recent", "redact_preview", "ai_audit_query", "ai_backends_list") after the existing val issue list (around line 95). Extend fun allIds() to include native: change the return to (utility + history + siteMap + request + scanner + config + editor + collaborator + issue + native).toSet().

    AiTools.kt (NEW file) — create in package com.six2dez.burp.aiagent.mcp.tools. This file contains:
    1. The registration function: internal fun Server.registerAiTools(context: McpToolContext) { McpToolRegistrations.native.forEach { registerToolHandler(it, context) } }
    2. Six @Serializable data classes for input schemas. Place them as internal data classes in this file:
       - AiAnalyzeInput(val text: String, val jsonMode: Boolean = false, val maxOutputTokens: Int? = null)
       - AiPassiveScanInput(val proxyHistoryIndices: List<Int> = emptyList(), val siteMapUrl: String? = null, val maxRequests: Int = 10)
       - AiFindingsRecentInput(val n: Int = 10)
       - RedactPreviewInput(val text: String, val mode: String = "STRICT")
       - AiAuditQueryInput(val n: Int = 20)
       (ai_backends_list and ai_active_scan take no args — use Tool.Input() in inputSchema)
    Imports needed: kotlinx.serialization.Serializable, McpToolContext, McpToolRegistrations, Server (MCP SDK), and the registerToolHandler import.

    McpTools.kt — make three additions:
    1. In Server.registerTools() (lines 51-64): add registerAiTools(context) call after registerIssueTools(context).
    2. In McpToolExecutor.inputSchema() (lines 2176-2248): add when branches for all 6 new tool IDs. For ai_analyze, ai_passive_scan, ai_findings_recent, redact_preview, ai_audit_query map to the corresponding Input class via class.asInputSchema(). For ai_backends_list map to Tool.Input().
    3. In McpToolExecutor.executeToolResult() when block (lines 1325-2068): add 6 new branches inside the runTool { ... } lambda. Each branch is inside the existing when block, so they receive the toolId, normalizedArgs, and context. Implementations:

    "ai_analyze" branch: decode AiAnalyzeInput from normalizedArgs; get supervisor from context.supervisor, return "AI tools not initialized." if null; call supervisor.isAiEnabled(), return the "unavailable" message if false (exact text: "AI features unavailable: check that your Burp edition supports AI and the 'Use AI' toggle is enabled. Non-AI backends remain usable via the chat panel."); then perform blocking send using CountDownLatch(1) pattern from PATTERNS.md Blocking AI Send — accumulate chunks in responseBuffer: StringBuilder, await 120_000L ms, return timeout error if not completed, return "AI error: $error" if onComplete receives error, return responseBuffer.toString().trim() on success. Import CountDownLatch, AtomicReference, TimeUnit.

    "ai_passive_scan" branch: decode AiPassiveScanInput; get scanner from context.passiveScanner, return "Passive scanner not available." if null; resolve requests from proxy history: call context.api.proxy().history() then take up to input.maxRequests items (or filter by siteMapUrl if provided); call scanner.manualScan(requests) if the list is non-empty; return "Queued ${count} requests for AI passive scan." or "No matching requests found." if empty.

    "ai_findings_recent" branch: decode AiFindingsRecentInput; get scanner from context.passiveScanner, return "Passive scanner not available." if null; call scanner.getLastFindings(input.n); return "No findings recorded yet." if empty; else format each finding as "[timestamp] title (severity) - url: detail.take(500)" joined by \n\n.

    "redact_preview" branch: decode RedactPreviewInput; parse the mode string to PrivacyMode enum via PrivacyMode.valueOf(input.mode.uppercase()); create RedactionPolicy via RedactionPolicy.fromMode(mode); call Redaction.apply(input.text, policy, stableHostSalt = context.hostSalt). Return the redacted text directly. Do NOT call isAiEnabled() — no AI involved.

    "ai_audit_query" branch: decode AiAuditQueryInput; get logger from context.aiRequestLogger, return "Audit logging not configured." if null; call logger.getEntries(input.n) or equivalent — check the AiRequestLogger API for a method that returns recent entries; format as newline-delimited JSON or key:value lines. If no method exists to get recent entries, return logger summary/count info.

    "ai_backends_list" branch: get registry from context.backendRegistry and supervisor from context.supervisor; return "Registry not available." if either is null; call registry.listAllBackendIds() or registry.listBackendIds(settings) (check which method exists without settings param); call supervisor.status() to get AgentStatus; format as multi-line string: "Available backends: ids.joinToString(", ")\nCurrent: status.backendId ?: "none"\nState: status.state".

    IMPORTANT: context.redactIfNeeded() is applied automatically to every branch's return value at McpTools.kt:2067 — do NOT call it manually inside any branch.

    IMPORTANT: Every branch is inside runTool { ... } — use return@runTool "error message" for early exits, not throw.
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/burp-ai-agent && ./gradlew test --tests "*.McpToolParityTest" -PexcludeHeavyTests=true --quiet 2>&1 | tail -5</automated>
    <automated>cd /Users/six2dez/Tools/burp-ai-agent && ./gradlew test --tests "*.AiGateMcpToolTest" -PexcludeHeavyTests=true --quiet 2>&1 | tail -8</automated>
  </verify>
  <done>
    - AiTools.kt exists with 6 input schema data classes and registerAiTools() function.
    - McpToolHandlers.kt has native list with 6 IDs; allIds() includes native.
    - McpTools.kt registerTools() calls registerAiTools().
    - McpTools.kt inputSchema() has 6 new branches.
    - McpTools.kt executeToolResult() has 6 new handler branches with AI gate on ai_analyze and ai_passive_scan.
    - McpToolParityTest passes (all IDs in both all() and allIds()).
    - AiGateMcpToolTest both test methods pass green.
    - ./gradlew compileKotlin succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Route SettingsPanel MCP tool lists through available() and wire AI context deps in App.kt</name>
  <files>
    src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/App.kt
  </files>
  <read_first>
    SettingsPanel.kt lines 2189-2252 (MCP tool list rendering loop at line 2194 using McpToolCatalog.all()), lines 2234-2240 (unsafe tool filter), lines 2545-2555 (collect toggles loop at line 2548).
    App.kt lines 64-77 (initialization: supervisor at 67, mcpSupervisor at 71, passiveAiScanner at 74, backendRegistry at 35).
    KtorMcpServerManager.kt line 43-45 (setAiRequestLogger — model for setAiToolDependencies).
    McpSupervisor.kt lines 63-66 (setAiRequestLogger on McpSupervisor that delegates to serverManager) — add parallel method.
    PATTERNS.md "ui/SettingsPanel.kt" section — exact call site locations.
    CONTEXT.md Decision "MCP strategy (LOCKED)" — available() gates both registration and UI.
  </read_first>
  <action>
    SettingsPanel.kt — make three substitutions:
    1. Line 2194: Change McpToolCatalog.all().groupBy { it.category } to McpToolCatalog.available().groupBy { it.category }. This makes the MCP tools display list in the store build show only the 8 native tools.
    2. Lines 2234-2235: Change McpToolCatalog.all().filter { it.unsafeOnly } to McpToolCatalog.available().filter { it.unsafeOnly }. The unsafe allowlist UI should also reflect the available set.
    3. Line 2548: Change for (tool in McpToolCatalog.all()) to for (tool in McpToolCatalog.available()). This ensures the toggle-collection loop only saves toggles for available tools.

    App.kt — after the line mcpSupervisor.setAiRequestLogger(aiRequestLogger) (line 72), add a call to the new setAiToolDependencies method on mcpSupervisor (or directly on the serverManager if accessible). Since McpSupervisor's serverManager field is private, the cleanest approach is: add a fun setAiToolDependencies(supervisor: AgentSupervisor, passiveScanner: PassiveAiScanner, backendRegistry: BackendRegistry) method to McpSupervisor that delegates to serverManager.setAiToolDependencies(supervisor, passiveScanner, backendRegistry). Then in App.kt call: mcpSupervisor.setAiToolDependencies(supervisor, passiveAiScanner, backendRegistry).

    McpSupervisor.kt — add the delegating method (file modified slightly): fun setAiToolDependencies(supervisor: AgentSupervisor, passiveScanner: PassiveAiScanner, backendRegistry: BackendRegistry) { serverManager.setAiToolDependencies(supervisor, passiveScanner, backendRegistry) }. Add imports for AgentSupervisor, PassiveAiScanner, BackendRegistry at the top of McpSupervisor.kt.

    Note: The files_modified in this plan's frontmatter lists App.kt but not McpSupervisor.kt. Since McpSupervisor.kt changes are minimal (one method addition), include it in this task. Add McpSupervisor.kt to the task scope.

    Do NOT change any MCP infrastructure (auth, TLS, CORS, scope filter, audit). Only the tool-list call sites and the context wiring.
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/burp-ai-agent && ./gradlew compileKotlin --quiet 2>&1 | tail -5</automated>
    <automated>cd /Users/six2dez/Tools/burp-ai-agent && ./gradlew test -PexcludeHeavyTests=true --quiet 2>&1 | tail -8</automated>
  </verify>
  <done>
    - SettingsPanel.kt uses available() at lines 2194, 2234, 2548.
    - App.kt calls mcpSupervisor.setAiToolDependencies(supervisor, passiveAiScanner, backendRegistry) after initialization.
    - McpSupervisor.kt has setAiToolDependencies() delegating to KtorMcpServerManager.
    - KtorMcpServerManager.setAiToolDependencies() sets contextFactory.supervisor/passiveScanner/backendRegistry.
    - ./gradlew compileKotlin succeeds.
    - ./gradlew test -PexcludeHeavyTests=true succeeds (all new tests green, existing tests unbroken).
    - Store build: McpToolCatalog.available(storeBuild=true) returns 8 tools; UI list and registration both show only those 8.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client→ai_analyze | Untrusted text from MCP client flows into ai_analyze.text; must be redacted before AI send |
| ai_analyze response→MCP client | AI response may contain reflected sensitive data; context.redactIfNeeded() applied automatically |
| isEnabled() gate | MCP AI tools must not invoke AI backends when api.ai().isEnabled()=false |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | ai_analyze / ai_passive_scan args | mitigate | context.redactIfNeeded(output) applied automatically at McpTools.kt:2067 to every tool's return value. Input text never logged raw — only SHA in audit log via registerToolHandler wrapper. |
| T-08-01b | Tampering | Prompt injection via ai_analyze text | mitigate | Text passes through existing redaction pipeline before reaching supervisor.send(). Redaction strips patterns matching the active PrivacyMode before send. |
| T-08-03 | Tampering | Generic tools re-exposed in store build | mitigate | McpToolCatalog.available() + McpToolHandlers.native list routes registration; store build has STORE_BUILD=true const; unregistered tools are uncallable. SettingsPanel also uses available() so UI does not show them. Verified by McpToolCatalogStoreBuildTest. |
| T-08-02 | Information Disclosure | ai_findings_recent returns unsanitized data | mitigate | Findings pass through context.redactIfNeeded() automatically. No raw request bodies exposed — only pre-formatted finding summaries. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan. |
</threat_model>

<verification>
After all three tasks:

1. ./gradlew test --tests "*.McpToolParityTest" passes (8 native IDs + 51 generic IDs all in both all() and allIds()).
2. ./gradlew test --tests "*.AiGateMcpToolTest" passes — both aiAnalyze_returnsErrorWhenIsEnabledFalse and aiAnalyze_doesNotGateNonAiTool green.
3. ./gradlew test --tests "*.McpToolCatalogStoreBuildTest" passes — available(true) returns 8 tools.
4. ./gradlew test -PexcludeHeavyTests=true passes (no regressions in existing suite).
5. ./gradlew compileKotlin succeeds for both store and non-store builds.
6. Manual spot check: McpToolCatalog.available(storeBuild = true) returns exactly ai_analyze, ai_passive_scan, ai_findings_recent, redact_preview, ai_audit_query, ai_backends_list, status, issue_create.
</verification>

<success_criteria>
- 6 new native tool descriptors in McpToolCatalog with nativeTool=true.
- AiTools.kt exists with registerAiTools() and 5 @Serializable input classes.
- executeToolResult() handles all 6 new tool IDs.
- ai_analyze and ai_passive_scan return "unavailable" message when supervisor.isAiEnabled()=false.
- redact_preview, ai_findings_recent, ai_audit_query, ai_backends_list work without AI gate.
- SettingsPanel MCP tool lists go through available() at all 3 call sites.
- App.kt wires supervisor/passiveScanner/backendRegistry into McpSupervisor context chain.
- McpToolParityTest, AiGateMcpToolTest, McpToolCatalogStoreBuildTest all pass.
- Existing test suite has no regressions.
</success_criteria>

<output>
Create .planning/phases/08-bapp-store-resubmission-mcp-pivot-to-extension-native-tools-/08-02-SUMMARY.md when done.
</output>

---
phase: 08-bapp-store-resubmission-mcp-pivot-to-extension-native-tools
plan: 04
type: execute
wave: 4
depends_on:
  - 08-02
  - 08-03
files_modified:
  - .planning/phases/08-bapp-store-resubmission-mcp-pivot-to-extension-native-tools-/08-REOPEN-REPLY.md
autonomous: false
requirements:
  - VERIFY-08-ARTIFACTS
  - VERIFY-08-COMMUNITY
  - COMMS-08-REOPEN

must_haves:
  truths:
    - "./gradlew shadowJar -PstoreBuild=true produces Custom-AI-Agent-0.7.0.jar and its MCP tools/list shows only the 8 native tools"
    - "./gradlew shadowJar produces Custom-AI-Agent-full-0.7.0.jar and its MCP tools/list shows all 59 tools"
    - "On Burp Community: non-AI backends (Ollama, Claude CLI) start normally; registerPassiveScanCheck silently fails (logged, not thrown)"
    - "On Burp Pro with Use AI off: ai_analyze returns the 'unavailable' error message; non-AI backends still work"
    - "The /reopen reply document exists at 08-REOPEN-REPLY.md with all four review points addressed"
    - "Community verification result is documented — the AI gate decision is recorded, not assumed"
  artifacts:
    - path: ".planning/phases/08-bapp-store-resubmission-mcp-pivot-to-extension-native-tools-/08-REOPEN-REPLY.md"
      provides: "The /reopen comment text ready to post on issue #231"
      contains: "Custom AI Agent"
  key_links:
    - from: "store JAR MCP tools/list"
      to: "McpToolCatalog.available(storeBuild=true)"
      via: "BuildFlags.STORE_BUILD=true → only nativeTool=true tools registered"
      pattern: "STORE_BUILD"
    - from: "Community verification result"
      to: "AI gate design decision"
      via: "Manual test documented in COMMUNITY-VERIFICATION.md"
      pattern: "isEnabled"
---

<objective>
Verify both build artifacts (store and full) are correct, perform the manual Burp Community and Burp Pro AI-gate verification that was identified as the TOP RISK, record the verification results, and produce the /reopen reply document ready to post on GitHub issue #231.

Purpose: The AI gate Community question must be answered with a real Burp session — not assumed. The /reopen reply must accurately describe what was shipped.

Output: Both JAR artifacts verified, Community verification notes, 08-REOPEN-REPLY.md.
</objective>

<execution_context>
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/08-bapp-store-resubmission-mcp-pivot-to-extension-native-tools-/08-CONTEXT.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/08-bapp-store-resubmission-mcp-pivot-to-extension-native-tools-/08-RESEARCH.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/08-bapp-store-resubmission-mcp-pivot-to-extension-native-tools-/08-02-SUMMARY.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/08-bapp-store-resubmission-mcp-pivot-to-extension-native-tools-/08-03-SUMMARY.md
</execution_context>

<context>
@/Users/six2dez/Tools/burp-ai-agent/.planning/PROJECT.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and inspect both artifacts; run full test suite</name>
  <files>build/libs/ (artifact inspection only — no source files modified)</files>
  <read_first>
    08-CONTEXT.md Decision "Build gating (LOCKED)" — store artifact name = Custom-AI-Agent-{version}.jar, full artifact name = Custom-AI-Agent-full-{version}.jar.
    08-01-SUMMARY.md and 08-02-SUMMARY.md — confirm Plans 01 and 02 are complete.
    build.gradle.kts (current state) — confirm generateBuildFlags task and conditional archiveBaseName are present from Plan 01.
  </read_first>
  <action>
    Run the following commands in sequence, capture and verify all outputs:

    1. Clean and build the store artifact: ./gradlew clean shadowJar -PstoreBuild=true. Verify build/libs/ contains Custom-AI-Agent-0.7.0.jar and does NOT contain Custom-AI-Agent-full-0.7.0.jar.

    2. Clean and build the full artifact: ./gradlew clean shadowJar. Verify build/libs/ contains Custom-AI-Agent-full-0.7.0.jar and does NOT contain Custom-AI-Agent-0.7.0.jar.

    3. Run the full test suite: ./gradlew test ktlintCheck. Verify all tests pass and ktlint reports no errors (note: ktlint may have ignoreFailures=true by default per build.gradle.kts; check ktlint output for actual violations).

    4. Inspect the store JAR for the BuildFlags constant: use jar tf or unzip -p to read BuildFlags.class, or use ./gradlew shadowJar -PstoreBuild=true and then verify the generated BuildFlags.kt in build/generated/buildflags/ contains STORE_BUILD = true. Confirm by running: find build/generated/buildflags -name "BuildFlags.kt" and cat/read the file.

    5. Confirm McpToolCatalog.available(storeBuild=true) returns exactly 8 tools by running the unit test: ./gradlew test --tests "*.McpToolCatalogStoreBuildTest".

    If any step fails, investigate the root cause and fix before proceeding to the manual verification checkpoints.
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/burp-ai-agent && ./gradlew clean shadowJar -PstoreBuild=true --quiet 2>&1 | tail -3 && ls build/libs/</automated>
    <automated>cd /Users/six2dez/Tools/burp-ai-agent && ./gradlew clean shadowJar --quiet 2>&1 | tail -3 && ls build/libs/</automated>
    <automated>cd /Users/six2dez/Tools/burp-ai-agent && ./gradlew test --quiet 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Store build produces Custom-AI-Agent-0.7.0.jar (no "full" suffix).
    - Full build produces Custom-AI-Agent-full-0.7.0.jar.
    - Full test suite passes.
    - ktlintCheck reports no errors (or only pre-existing ignoreFailures suppressions).
    - McpToolCatalogStoreBuildTest passes.
    - AiGateMcpToolTest passes.
    - AiPassiveScanCheckTest passes.
    - McpToolParityTest passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual Burp Community + Burp Pro AI-gate verification</name>
  <what-built>
    Wave 1-3 implemented: -PstoreBuild gate, 6 new native MCP tools, AiPassiveScanCheck, global isEnabled() gate in ai_analyze/ai_passive_scan MCP tool handlers. Now we need to know the answer to the OPEN RISK question: does the isEnabled() gate behave correctly on both editions?
  </what-built>
  <how-to-verify>
    Perform ALL steps and record the result of each. Record answers in the resume signal below.

    BURP COMMUNITY VERIFICATION (use Custom-AI-Agent-full-0.7.0.jar — the non-store build):
    1. Load Custom-AI-Agent-full-0.7.0.jar in Burp Community (Extensions tab → Add → select JAR).
    2. Confirm the extension loads without errors in the Output tab.
    3. Go to Settings (or the AI tab) and look for a "Use AI" checkbox or toggle. Does it appear? (YES/NO)
    4. If YES: can you enable it? Does enabling it make api.ai().isEnabled() return true? (Test by: start any non-AI backend like Ollama or Claude CLI, then call ai_analyze via MCP — does it return the "unavailable" message or the AI result?)
    5. Start Ollama or another non-AI backend from the AI chat panel. Does it start normally WITHOUT being blocked by the isEnabled() gate? (YES/NO — expected YES)
    6. Confirm that registerPassiveScanCheck failure is logged silently, not thrown (check Output tab for "AI PassiveScanCheck not registered (Burp Pro required): ..." message). (YES/NO)

    BURP PRO AI-GATE VERIFICATION (use Custom-AI-Agent-full-0.7.0.jar):
    7. Load in Burp Pro. Enable Use AI (if not already enabled).
    8. Call ai_analyze via MCP (e.g., with Claude Desktop): does it return an AI result? (YES/NO)
    9. Disable the "Use AI" toggle/checkbox in Burp settings.
    10. Call ai_analyze again via MCP: does it return the "unavailable" message? (YES/NO — expected YES)
    11. Start Ollama or another non-AI backend from the AI chat panel: does it still start normally even with Use AI off? (YES/NO — expected YES, since non-burp-ai backends bypass the isEnabled gate in startOrAttach)

    STORE JAR VERIFICATION (use Custom-AI-Agent-0.7.0.jar):
    12. Load Custom-AI-Agent-0.7.0.jar in Burp.
    13. Start MCP server and call tools/list via MCP client (or check MCP panel in UI). Confirm ONLY these tools appear: status, issue_create, ai_analyze, ai_passive_scan, ai_findings_recent, redact_preview, ai_audit_query, ai_backends_list. (YES/NO)
    14. Confirm NO generic tools (proxy_http_history, site_map, http1_request, etc.) appear in tools/list. (YES/NO — expected YES)
  </how-to-verify>
  <resume-signal>
    Type "verified" followed by a brief summary answering these questions:
    - Community: Does "Use AI" checkbox appear? [YES/NO]
    - Community: Do Ollama/CLI backends start normally? [YES/NO]
    - Community: PassiveScanCheck silent fail logged? [YES/NO]
    - Pro: ai_analyze works with Use AI ON? [YES/NO]
    - Pro: ai_analyze returns "unavailable" with Use AI OFF? [YES/NO]
    - Pro: Non-AI backends unblocked with Use AI OFF? [YES/NO]
    - Store JAR: Only 8 native tools in tools/list? [YES/NO]
    - Any unexpected behavior to document?

    If any YES/NO is unexpected, describe what happened and we will adjust before proceeding.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 08-REOPEN-REPLY.md with all four review points addressed</name>
  <files>.planning/phases/08-bapp-store-resubmission-mcp-pivot-to-extension-native-tools-/08-REOPEN-REPLY.md</files>
  <read_first>
    08-CONTEXT.md Decision "Name (LOCKED)" — confirm "Custom AI Agent" in reply.
    drifting-hatching-sphinx.md Part 4 — the draft /reopen reply text (use as base, update with actual verified results from Task 2).
    The verification results from Task 2's resume signal — update the reply to accurately reflect what was verified.
    08-RESEARCH.md Q5 Best Practices table — reference what is confirmed done vs what is still pending.
  </read_first>
  <action>
    Create .planning/phases/08-bapp-store-resubmission-mcp-pivot-to-extension-native-tools-/08-REOPEN-REPLY.md.

    This file is the EXACT text to post as a comment on GitHub issue #231 (https://github.com/PortSwigger/extension-portal/issues/231) followed by /reopen.

    The reply must address all four review feedback points. Use the draft from drifting-hatching-sphinx.md Part 4 as the base and update it with:
    1. Any corrections based on the actual verification results from Task 2.
    2. The clarification about Community edition behavior (documented from Task 2).
    3. The clarification about passive scanning: the migration is to PassiveScanCheck.doCheck() (the modern interface), not the deprecated ScanCheck.passiveAudit().

    The reply should include:
    - Confirmation that the name is "Custom AI Agent" (already correct, no code change needed).
    - Explanation that the store build MCP now exposes only extension-native AI tools (list the 8 tools by ID).
    - Confirmation that all AI-calling MCP tools check ai.isEnabled() before dispatching. Note the Community edition behavior: isEnabled() returns false on Community, so ai_analyze etc. return an "unavailable" error; non-AI backends (Ollama, Claude CLI, etc.) remain fully functional on Community as they bypass the isEnabled gate per the extension's deliberate design.
    - Confirmation that passive scanning is now implemented via PassiveScanCheck.doCheck() (burp.api.montoya.scanner.scancheck package, Montoya 2026.2), registered via registerPassiveScanCheck(check, ScanCheckType.PER_REQUEST). The deprecated ScanCheck.passiveAudit() is no longer used for this purpose.
    - A note that the generic Montoya-API tools are still present in the GitHub "full" build for users who prefer the all-in-one setup, but the BApp Store build exposes only the extension-native AI tools.
    - Close with /reopen.

    The document should be written in first-person voice matching the tone of the original plan draft. Keep it concise (under 300 words for the comment body).

    After the reply text, add a section "--- Developer Notes (not for posting)" with:
    - The Community verification result summary from Task 2.
    - A note on the AI gate design decision: whether the gate was applied narrowly (MCP AI tools only) or broadly (all backends), and the rationale.
    - Link to the RESEARCH.md Q4 Pattern 4 for future reference.
  </action>
  <verify>
    <automated>ls -la /Users/six2dez/Tools/burp-ai-agent/.planning/phases/08-bapp-store-resubmission-mcp-pivot-to-extension-native-tools-/08-REOPEN-REPLY.md && grep -c "Custom AI Agent" /Users/six2dez/Tools/burp-ai-agent/.planning/phases/08-bapp-store-resubmission-mcp-pivot-to-extension-native-tools-/08-REOPEN-REPLY.md</automated>
    <human-check>Read 08-REOPEN-REPLY.md and confirm: all 4 review points addressed, tone is correct, /reopen appears at the end of the comment section.</human-check>
  </verify>
  <done>
    - 08-REOPEN-REPLY.md exists and contains the exact text to post.
    - All four review points (name, MCP, ai.isEnabled(), passive scan) are addressed.
    - The 8 native tool IDs are listed.
    - Community edition behavior is documented accurately.
    - /reopen appears at the end of the comment body.
    - Developer notes section is present after a separator.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build output→store submission | The store JAR must not expose generic tools; artifact inspection confirms compliance |
| Community runtime→isEnabled() gate | Community must not be broken by the AI gate; manual verification confirms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | Store JAR exposing generic tools | mitigate | Verified in Task 1 and 2: store JAR MCP tools/list shows only 8 native tools. BuildFlags.STORE_BUILD=true baked at compile time. |
| T-08-01 | Tampering | Prompt injection via ai_analyze on Community | accept | On Community isEnabled()=false → ai_analyze returns error, no AI call made. No prompt reaches the AI. |
| T-08-02 | Information Disclosure | Manual verification artifacts | accept | REOPEN-REPLY.md and Community verification notes are local planning documents, not committed to the extension codebase. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan. |
</threat_model>

<verification>
Phase complete when all of the following are true:

AUTOMATED (from Task 1):
1. ./gradlew shadowJar -PstoreBuild=true produces Custom-AI-Agent-0.7.0.jar (not Custom-AI-Agent-full-...).
2. ./gradlew shadowJar produces Custom-AI-Agent-full-0.7.0.jar (not Custom-AI-Agent-0.7.0.jar).
3. ./gradlew test passes (all tests green including AiPassiveScanCheckTest, AiGateMcpToolTest, McpToolCatalogStoreBuildTest, McpToolParityTest).
4. ./gradlew ktlintCheck (ignoreFailures mode — no new ktlint errors introduced by Phase 8 code).

MANUAL (from Task 2):
5. Community: non-AI backends start normally, PassiveScanCheck silent-fail logged.
6. Pro: ai_analyze gated by Use AI toggle, non-AI backends unblocked.
7. Store JAR MCP tools/list = only the 8 native tools.

ARTIFACT (from Task 3):
8. 08-REOPEN-REPLY.md exists with all four review points addressed and /reopen at end.
</verification>

<success_criteria>
- Both JAR artifacts verified correct (store = 8 tools only; full = all tools).
- Community AI gate behavior documented (verified in real Burp session, not assumed).
- Pro AI gate works as designed (ai_analyze blocked when Use AI off; non-AI backends unblocked).
- /reopen reply document ready for posting on issue #231.
- Full test suite green.
- Phase 8 complete — extension ready for BApp Store resubmission.
</success_criteria>

<output>
Create .planning/phases/08-bapp-store-resubmission-mcp-pivot-to-extension-native-tools-/08-04-SUMMARY.md when done.
</output>

---
phase: 10-mcp-tools-tab-redesign
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/design/Components.kt
autonomous: true
requirements:
  - UI-03
  - UI-04
  - UI-05
  - UI-07

must_haves:
  truths:
    - "buildMcpToolsPanel() returns a JScrollPane (via buildTabPanel) containing two sectionPanel wrappers: 'AI Tools (extension-native)' and 'Montoya Tools (generic)' (UI-03, SC-1)"
    - "Every tool row shows a toolBadge — NATIVE style for nativeTool=true tools, FULL style for nativeTool=false tools (UI-04, SC-2)"
    - "Search JTextField with DocumentListener narrows both sections in real time via show/hide (Option B); result count label updates on each keystroke (UI-05, SC-3)"
    - "Per-group 'Enable all' / 'Disable all' secondaryButton bars exist above each tool list; bulk toggle mutates only currently-visible enabled checkboxes in that group (UI-05, SC-4)"
    - "mcpToolCheckboxes map registration, collectMcpToolToggles(), applyMcpToolToggles(), enabledUnsafeTools, unsafeEnabled, and updateUnsafeToolStates() are unchanged (UI-07, SC-5)"
    - "Old global selectAll/deselectAll JButton pair from the previous buildMcpToolsPanel() is removed; no other SettingsPanel code is modified"
    - "Unsafe allowlist sub-panel is wrapped in AccordionPanel(initiallyExpanded=false) at the bottom of the Montoya section"
    - "No Color(), Font(), or raw spacing literal appears in the new buildMcpToolsPanel() — all styling from DesignTokens/Components"
    - "./gradlew test exits 0 after the change"
  artifacts:
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt"
      provides: "Redesigned buildMcpToolsPanel() returning JScrollPane"
      contains: "buildMcpToolsPanel"
  key_links:
    - from: "buildMcpToolsPanel()"
      to: "McpToolTabModel.groupTools()"
      via: "direct call with McpToolCatalog.available() result"
      pattern: "McpToolTabModel\\.groupTools"
    - from: "buildMcpToolsPanel()"
      to: "Components.kt builders"
      via: "sectionPanel, toolBadge, helpLabel, secondaryButton, buildTabPanel, applyFieldStyle"
      pattern: "sectionPanel|toolBadge|helpLabel|secondaryButton|buildTabPanel|applyFieldStyle"
    - from: "Tool row checkboxes"
      to: "mcpToolCheckboxes[tool.id]"
      via: "registration unchanged — same pattern as before"
      pattern: "mcpToolCheckboxes\\[tool\\.id\\]"
    - from: "'Enable all' button"
      to: "McpToolTabModel.bulkToggleTargets()"
      via: "captures current query + disabledIds set at click time"
      pattern: "bulkToggleTargets"
---

<objective>
Rebuild `buildMcpToolsPanel()` in `SettingsPanel.kt` as a thin Swing shell over the helpers
extracted in Plan 01. Replace the flat category-grid layout with two labelled `sectionPanel`
wrappers (AI Tools / Montoya Tools), per-row `toolBadge`, a live-search `JTextField`, and
per-group bulk-toggle button bars — all using Phase 9 design system components exclusively.

Purpose: Deliver the Phase 10 success criteria (UI-03 grouping, UI-04 badges, UI-05 search +
bulk toggle) while preserving the full persistence/behavior contract (UI-07). The UI-SPEC.md
contract is the primary source of truth for all layout decisions.

Output: Updated `SettingsPanel.kt` with redesigned `buildMcpToolsPanel()`. All other ~2600 lines
of SettingsPanel.kt are untouched. Full test suite remains green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-mcp-tools-tab-redesign/10-UI-SPEC.md
@.planning/phases/10-mcp-tools-tab-redesign/10-VALIDATION.md
@src/main/kotlin/com/six2dez/burp/aiagent/ui/McpToolTabModel.kt
@src/main/kotlin/com/six2dez/burp/aiagent/ui/design/DesignTokens.kt
@src/main/kotlin/com/six2dez/burp/aiagent/ui/design/Components.kt
@src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolCatalog.kt
@src/main/kotlin/com/six2dez/burp/aiagent/config/McpSettings.kt
@src/main/kotlin/com/six2dez/burp/aiagent/ui/components/AccordionPanel.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read current buildMcpToolsPanel() and replace with redesigned implementation</name>
  <files>src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt, src/main/kotlin/com/six2dez/burp/aiagent/ui/design/Components.kt</files>

  <read_first>
    Read lines 2166–2300 of SettingsPanel.kt (the full `buildMcpToolsPanel()` body plus its closing
    brace) before editing. Also read lines 638 and 644–652 (caller site and tab assembly) to confirm
    the return type change (JPanel → JScrollPane via buildTabPanel) is compatible with the
    BorderLayout.CENTER slot at line 638. JScrollPane extends JComponent so it is drop-in compatible.

    Confirm that `updateUnsafeToolStates()` at lines 2302–2344 uses `mcpToolCheckboxes` and
    `mcpUnsafeApprovalCheckboxes` maps only — this function is NOT modified; the redesigned
    `buildMcpToolsPanel()` must continue to populate both maps with the same keys.

    Existing imports needed by the new implementation: `javax.swing.BoxLayout`, `javax.swing.JPanel`,
    `javax.swing.JCheckBox`, `javax.swing.JLabel`, `javax.swing.JTextField`, `javax.swing.JScrollPane`,
    `javax.swing.Box`, `javax.swing.border.EmptyBorder`, `java.awt.BorderLayout`,
    `burple.portswigger.burp.extender.montoya.core.BurpSuiteEdition` (already imported),
    and the design-system imports: `com.six2dez.burp.aiagent.ui.design.*`,
    `com.six2dez.burp.aiagent.ui.McpToolTabModel`,
    `com.six2dez.burp.aiagent.ui.components.AccordionPanel`.
    Add any missing imports at the top of the file.
  </read_first>

  <action>
    Replace the entire body of `buildMcpToolsPanel()` (lines 2166–2300 in the original, which
    returns a `JPanel`) with a new implementation that returns a `JScrollPane` via `buildTabPanel`.
    Also update the return type declaration from `JPanel` to `JScrollPane`.

    Structure of the new `buildMcpToolsPanel(): JScrollPane`:

    STEP 1 — Shared state setup (same as before, per UI-07):
    Assign `val effectiveToggles = McpToolCatalog.mergeWithDefaults(settings.mcpSettings.toolToggles)`.
    Assign `val edition = api.burpSuite().version().edition()`.
    Assign `val unsafeEnabled = mcpUnsafe.isSelected`.
    Assign `val unsafeAllowlist = settings.mcpSettings.enabledUnsafeTools`.
    Assign `val grouping = McpToolTabModel.groupTools(McpToolCatalog.available())`.

    STEP 2 — Search bar panel:
    Create `val searchField = JTextField()` and call `applyFieldStyle(searchField)`.
    Set `searchField.toolTipText = "Search tools by name or description…"`.
    Set `searchField.columns = 0` (expands horizontally with BoxLayout).
    Create `val resultCountLabel = helpLabel("${McpToolCatalog.available().size} tools")`.
    Create `val searchBarPanel = JPanel()` with `BoxLayout(X_AXIS)`.
    Set `searchBarPanel.background = DesignTokens.Colors.surface`.
    Set `searchBarPanel.border = EmptyBorder(0, 0, DesignTokens.Spacing.lg, 0)`.
    Add: `searchField` (add with horizontal glue wrapping OR let it expand), then
    `Box.createRigidArea(Dimension(Spacing.sm, 0))`, then `resultCountLabel`.
    The searchField should receive `maximumSize = Dimension(Int.MAX_VALUE, searchField.preferredSize.height)`
    so BoxLayout gives it all available horizontal space.

    STEP 3 — Tool-row builder (private lambda or local fun):
    Build a local function `fun buildToolRow(tool: McpToolDescriptor): JPanel` inside the method body.
    The row is a `JPanel(BorderLayout())` with `isOpaque = false`.
    Row border: `EmptyBorder(Spacing.xs, Spacing.md, Spacing.xs, Spacing.md)`.

    NORTH sub-row (title bar): `JPanel()` with `BoxLayout(X_AXIS)`, `isOpaque = false`.
    - `JCheckBox(tool.title, effectiveToggles[tool.id] ?: false)` — title only, no "(unsafe)" suffix (per UI-SPEC).
      Apply the same `toolTipText` logic as the existing code (preserved verbatim, per UI-07):
        when not unsafeOnly: `tool.description`
        when unsafeOnly and unsafeEnabled: `"${tool.description} Allowed by global unsafe mode."`
        when unsafeOnly and allowlisted: `"${tool.description} Allowed by per-tool unsafe approval."`
        else: `"${tool.description} Blocked until unsafe mode is enabled globally or approved in allowlist."`
      Apply the same `isEnabled` logic (preserved verbatim, per UI-07 — BEHAVIOR UNCHANGED):
        if proOnly and edition != PROFESSIONAL: `isEnabled = false`; `putClientProperty("proDisabled", true)`;
          toolTipText = "${tool.description} (Pro only)"
        else: `putClientProperty("proDisabled", false)`.
      IMPORTANT — DO NOT introduce the UI-SPEC "Checkbox Enable/Disable Rules" gating
      (unsafeOnly=true AND unsafe OFF AND NOT allowlisted → isEnabled=false). That rule is
      intentionally DEFERRED to Phase 11 or a follow-up because it changes observable behavior
      (currently those checkboxes stay ENABLED; disabling them is a behavior change, not a
      redesign). For this phase, the per-tool checkbox isEnabled state follows the existing
      `updateUnsafeToolStates()` semantics: only proOnly tools are disabled at construction time;
      everything else starts enabled. The unsafe indicator label still renders as a visual-only
      label (see indicator step below) and does NOT affect isEnabled. Record as deferred: "unsafe
      checkbox gating (UI-SPEC Checkbox Enable/Disable Rules — Phase 11)".
      Apply `putClientProperty("unsafeOnly", tool.unsafeOnly)` and `putClientProperty("description", tool.description)`.
      Register: `mcpToolCheckboxes[tool.id] = checkbox`.
    - `Box.createRigidArea(Dimension(Spacing.sm, 0))`
    - `toolBadge(if (tool.nativeTool) "Store + Full" else "Full only", McpToolTabModel.badgeStyle(tool))`
    - `Box.createHorizontalGlue()`
    - Optional indicator JLabel (right-aligned): build per UI-SPEC Unsafe/Pro-Only Indicator table:
        if unsafeOnly=true AND !unsafeEnabled AND allowlisted: `JLabel("allowlisted")` with `Typography.caption` + `Colors.statusWarning`
        if unsafeOnly=true AND !unsafeEnabled AND NOT allowlisted: `JLabel("unsafe")` with `Typography.caption` + `Colors.statusError`
        if proOnly=true AND edition != PROFESSIONAL: `JLabel("Pro only")` with `Typography.caption` + `Colors.onSurfaceVariant`
        (both proOnly+unsafeOnly: show "Pro only" only, per UI-SPEC)
        else: no indicator label
      When an indicator label is created, add it to the north sub-row after the glue.
      These indicator labels are VISUAL ONLY — they do not affect checkbox isEnabled state.

    SOUTH sub-row: `helpLabel(tool.description)`.
    Set south label border: `EmptyBorder(0, Spacing.md, 0, 0)`.
    Add north sub-row to `BorderLayout.NORTH` and south label to `BorderLayout.SOUTH`.
    Return the row JPanel. Store a reference so the search filter can show/hide it.

    STEP 4 — AI Tools section list panel:
    Create `val aiToolRows = mutableListOf<Pair<McpToolDescriptor, JPanel>>()`.
    Create `val aiListPanel = JPanel()` with `BoxLayout(Y_AXIS)`, `isOpaque = false`.
    Build bulk-toggle bar: `val aiEnableAll = secondaryButton("Enable all")` and
    `val aiDisableAll = secondaryButton("Disable all")`.
    Build `val aiBulkBar = JPanel()` with `BoxLayout(X_AXIS)` and `isOpaque=false`.
    `aiBulkBar.border = EmptyBorder(0, 0, Spacing.sm, 0)`.
    Add `aiEnableAll`, `Box.createRigidArea(Dimension(Spacing.sm,0))`, `aiDisableAll`, `Box.createHorizontalGlue()`.
    Create empty-state label: `val aiEmptyLabel = helpLabel("No tools match your search.")`.
    `aiEmptyLabel.isVisible = false`.
    Add `aiBulkBar` to `aiListPanel`, then for each tool in `grouping.native`:
      build the tool row, add to `aiToolRows`, add to `aiListPanel`.
    Add `aiEmptyLabel` to `aiListPanel` (always present, visibility toggled by filter).

    Wire `aiEnableAll` action listener: compute `val targets = McpToolTabModel.bulkToggleTargets(grouping.native, searchField.text, disabledCheckboxIds())` where `disabledCheckboxIds()` is a private helper lambda returning `mcpToolCheckboxes.entries.filter { !it.value.isEnabled }.map { it.key }.toSet()`. For each target, set `mcpToolCheckboxes[target.id]?.isSelected = true`.

    Wire `aiDisableAll` action listener: same but sets `isSelected = false`.

    Wrap in `sectionPanel("AI Tools (extension-native)", "Extension-native tools — available in both the BApp Store and the full build.", aiListPanel)`.

    STEP 5 — Montoya Tools section list panel:
    Create `val montoyaToolRows = mutableListOf<Pair<McpToolDescriptor, JPanel>>()`.
    Create `val montoyaCategoryHeaders = mutableListOf<Pair<String, JLabel>>()`.
    Create `val montoyaListPanel = JPanel()` with `BoxLayout(Y_AXIS)` and `isOpaque=false`.
    Build bulk-toggle bar `val montoyaBulkBar` same structure as aiBulkBar.
    Create `val montoyaEnableAll = secondaryButton("Enable all")` and `val montoyaDisableAll = secondaryButton("Disable all")`.
    Create `val montoyaEmptyLabel = helpLabel("No tools match your search.")`.
    `montoyaEmptyLabel.isVisible = false`.

    Get `val categoryMap = McpToolTabModel.categoryGroups(grouping.generic)`.
    If `grouping.generic.isEmpty()`: add `helpLabel("No Montoya tools available in this build.")` to `montoyaListPanel`; hide `montoyaBulkBar`.
    Else: add `montoyaBulkBar` to `montoyaListPanel`, then for each `(category, tools)` in `categoryMap`:
      build a category sub-header `JLabel(category)` with `Typography.label` + `Colors.onSurfaceVariant`
      + `EmptyBorder(Spacing.sm, 0, Spacing.xs, 0)`. Store in `montoyaCategoryHeaders`.
      Add the header to `montoyaListPanel`.
      For each tool in `tools`: build row, add to `montoyaToolRows`, add to `montoyaListPanel`.
    Add `montoyaEmptyLabel` to `montoyaListPanel`.

    Wire `montoyaEnableAll` and `montoyaDisableAll` using `bulkToggleTargets(grouping.generic, ...)`.

    STEP 6 — Unsafe Allowlist AccordionPanel (per UI-SPEC):
    Build `val allowlistContentPanel = JPanel()` with `BoxLayout(Y_AXIS)`, `isOpaque=false`.
    Get `val unsafeTools = McpToolCatalog.available().filter { it.unsafeOnly }.sortedBy { it.title }`.
    For each unsafe tool: build a one-row JPanel (no badge, no description sub-row — just checkbox)
    following the same registration pattern as before (`mcpUnsafeApprovalCheckboxes[tool.id] = approval`).
    Preserve existing enable-disable logic verbatim: `approval.isEnabled = !unsafeEnabled` unless proDisabled.
    Preserve existing `approval.addActionListener { updateUnsafeToolStates() }`.
    Preserve existing `toolTipText` logic from the original allowlist block.
    Wrap in `AccordionPanel("Unsafe tool allowlist", "Approve individual unsafe tools without enabling global unsafe mode.", allowlistContentPanel, initiallyExpanded = false)`.
    Add the AccordionPanel to `montoyaListPanel` (below all tool rows, per UI-SPEC).

    Wrap montoya list in `sectionPanel("Montoya Tools (generic)", "Generic Montoya API wrappers — available in the full build only.", montoyaListPanel)`.

    STEP 7 — Section separator:
    Create a 1 px `JSeparator` or `Box.createRigidArea(Dimension(0, Spacing.xl))` between AI and Montoya sections. Per UI-SPEC, a `Spacing.xl` (24 px) rigid area divider is acceptable.

    STEP 8 — DocumentListener for live filter (Option B — show/hide):
    Add a `DocumentListener` to `searchField.document`. On every insertUpdate/removeUpdate/changedUpdate,
    call a local `fun applyFilter(query: String)` that:
      For each `(tool, row)` in `aiToolRows`: sets `row.isVisible = McpToolTabModel.filterPredicate(query, tool)`.
      For each `(tool, row)` in `montoyaToolRows`: sets `row.isVisible = McpToolTabModel.filterPredicate(query, tool)`.
      For each `(category, header)` in `montoyaCategoryHeaders`: sets `header.isVisible` to true only
        if at least one tool in `categoryMap[category]!!` has `filterPredicate(query, tool) == true`.
      Count visible AI tools and visible Montoya tools.
      Update `aiEmptyLabel.isVisible` = (visibleAiCount == 0 AND query.isNotBlank()).
      Update `aiBulkBar.isVisible` = (visibleAiCount > 0).
      Update `montoyaEmptyLabel.isVisible` = (visibleMontoyaCount == 0 AND query.isNotBlank() AND grouping.generic.isNotEmpty()).
      Update `montoyaBulkBar.isVisible` = (visibleMontoyaCount > 0 AND grouping.generic.isNotEmpty()).
      val totalVisible = visibleAiCount + visibleMontoyaCount
      val totalAll = McpToolCatalog.available().size
      Update `resultCountLabel.text` = if `query.isBlank()` then "$totalAll tools" else "$totalVisible of $totalAll tools".
      Call `aiListPanel.revalidate(); aiListPanel.repaint(); montoyaListPanel.revalidate(); montoyaListPanel.repaint()`.

    STEP 9 — Return via buildTabPanel:
    Return `buildTabPanel(listOf(searchBarPanel, aiSection, montoyaSection))`.
    The old `JPanel(BorderLayout)` wrapper with the scroll and the global selectAll/deselectAll buttons
    is completely removed (not returned; not added anywhere).

    FLAG-10-01 mitigation: Override `updateUI()` on each badge JLabel returned by `toolBadge()` to
    reapply `bgColor`/`fgColor` from current `DesignTokens.Colors` values. The `toolBadge()` builder
    in Components.kt already captures colors at creation time — to support theme switch, extend the
    badge object so `updateUI()` calls `super.updateUI()` then reassigns `foreground` from the
    appropriate `Colors` token (NATIVE → `Colors.statusSuccess`, FULL → `Colors.onSurfaceVariant`).
    Background repaint via the overridden `paintComponent` already re-calls `g2d.color = bgColor`
    so only foreground needs a refresh. Implement this by wrapping the badge creation:
    `toolBadge(label, style).also { badge -> badge.addPropertyChangeListener("UI") { ... } }` OR
    by subclassing inline. Since `toolBadge()` already returns an anonymous JLabel subclass, the
    cleanest approach is to add an `updateUI()` override in Components.kt's `toolBadge()` builder that
    reassigns `foreground` and re-triggers the background color via a stored-style reference.
    Update `toolBadge()` in Components.kt to add an `updateUI()` override to the anonymous JLabel
    class: call `super.updateUI()` then reassign `foreground` to `DesignTokens.Colors.statusSuccess`
    when `style == BadgeStyle.NATIVE`, else `DesignTokens.Colors.onSurfaceVariant`. The `style`
    parameter is already in scope in the anonymous class closure — reference it directly, no field
    promotion needed.
    NOTE: Do NOT place fenced code blocks in action text — the above is a prose description of the
    pattern; implement using the captured `style` variable already available in the anonymous class closure.
    Add `updateUI()` override to the anonymous JLabel class in `toolBadge()` in Components.kt.
    No new file; just amend the anonymous class body in `Components.kt`.
    This satisfies FLAG-10-01.

    DEFERRED (out of scope for this redesign phase — do NOT implement):
    The UI-SPEC "Checkbox Enable/Disable Rules" states that when unsafeOnly=true AND unsafe mode
    is OFF AND the tool is NOT allowlisted, the per-tool checkbox should be isEnabled=false. This
    rule is intentionally NOT applied here because it would change observable behavior (users
    currently see those checkboxes as enabled). Implementing it belongs in Phase 11 or a dedicated
    follow-up once the redesign is validated. The "unsafe" indicator label (visual only) MUST still
    render as specified above; it must NOT disable the checkbox.
  </action>

  <verify>
    <automated>./gradlew test 2>&1 | tail -30</automated>
  </verify>

  <done>
    - SettingsPanel.kt compiles with no errors.
    - No Color(), Font() literals in new buildMcpToolsPanel() code.
    - Old selectAll/deselectAll buttons removed.
    - mcpToolCheckboxes and mcpUnsafeApprovalCheckboxes maps populated identically to before.
    - buildTabPanel returns JScrollPane; caller at line 638 receives JScrollPane (compatible with BorderLayout.CENTER).
    - ./gradlew test exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual smoke-check of redesigned MCP tools tab</name>

  <what-built>
    Rebuilt MCP tools tab in SettingsPanel: two sectionPanel sections (AI Tools / Montoya Tools),
    per-row toolBadge pills, live search field, per-group Enable all/Disable all buttons,
    category sub-headers in Montoya section, AccordionPanel for unsafe allowlist.
  </what-built>

  <how-to-verify>
    1. Build the JAR: `./gradlew shadowJar`
    2. Load `build/libs/Custom-AI-Agent-*.jar` in Burp Suite.
    3. Open Settings → Burp Integration tab → scroll to the MCP Tools area.
    4. Confirm two sections: "AI Tools (extension-native)" and "Montoya Tools (generic)".
    5. Confirm each tool row has a badge pill: native tools show "Store + Full" (green tint), generic show "Full only" (neutral).
    6. Type "proxy" in the search field — confirm only proxy-related tools remain visible in both sections; result count label updates.
    7. Clear the search field — confirm all tools reappear.
    8. Click "Enable all" in AI Tools section — confirm all enabled AI tool checkboxes become checked.
    9. Click "Disable all" in AI Tools section — confirm all enabled AI tool checkboxes become unchecked.
    10. Expand the "Unsafe tool allowlist" accordion at the bottom of Montoya Tools — confirm unsafe tools are listed with checkboxes.
    11. Save settings and reload Burp — confirm per-tool toggle states persist correctly.
    12. Toggle Burp to dark theme (if available) — confirm badge colors remain readable (no stale light-theme colors).
    13. Confirm the global "Select all" / "Deselect all" buttons from the OLD implementation are gone.
  </how-to-verify>

  <resume-signal>Type "approved" if visual review passes, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swing UI → McpSettings persistence | Checkbox states written to in-memory SettingsPanel fields; only committed to AgentSettings on "Save Settings" click (pre-existing flow, unchanged) |
| Unsafe allowlist gating | AccordionPanel wraps existing allowlist checkboxes; mcpUnsafeApprovalCheckboxes registration and updateUnsafeToolStates() logic preserved verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering | Unsafe allowlist gating | mitigate | AccordionPanel is display-only; mcpUnsafeApprovalCheckboxes registration + updateUnsafeToolStates() semantics unchanged — gating logic not touched |
| T-10-02-02 | Information Disclosure | Search field input | accept | Search is a local in-memory filter; query string is never persisted, logged, or sent over network |
| T-10-02-SC | Tampering | No new npm/pip/cargo installs | accept | Kotlin + Swing only; no new dependencies introduced |

Risk level: LOW. The redesign is display-layer only. No new persistence paths, no new network I/O,
no new privilege escalation. Unsafe-tool gating remains behind the existing boolean flags.
HIGH threats: none identified.
</threat_model>

<verification>
1. `./gradlew test` exits 0 — full regression suite green (covers McpSettingsTest, McpToolTabModelTest, and all existing tests).
2. `grep -n "selectAll\|deselectAll\|\"Select all\"\|\"Deselect all\"" src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt | grep -v "//"` returns 0 matches in the redesigned function.
3. `grep -n "Color(" src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt` shows no new Color literals added in or after line 2166.
4. `grep -n "buildMcpToolsPanel" src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt` still shows the function definition and its single call site at line ~638.
5. Human smoke-check at Task 2 returns "approved".
</verification>

<success_criteria>
- Two section headers "AI Tools (extension-native)" and "Montoya Tools (generic)" visible in Burp (SC-1).
- Per-row badge shows "Store + Full" (NATIVE style) or "Full only" (FULL style) correctly (SC-2).
- Typing in search narrows both sections; clearing restores all; result count label accurate (SC-3).
- Per-group bulk toggles operate on visible-enabled checkboxes only; state persists through Save (SC-4).
- All existing persistence tests pass; collect/apply round-trip unchanged (SC-5 / UI-07).
- ./gradlew test exits 0.
- Human smoke-check approved.
</success_criteria>

<output>
Create `.planning/phases/10-mcp-tools-tab-redesign/10-02-SUMMARY.md` when done.
</output>

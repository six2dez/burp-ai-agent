---
phase: 17-reliability-concurrency-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/backends/cli/CliBackend.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/config/AgentSettings.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/config/Defaults.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/KtorMcpServerManager.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/backends/cli/CliBackendTempFileTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/backends/cli/CliTimeoutMessageTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/mcp/McpShutdownBoundTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionHostMapBoundTest.kt
autonomous: true
requirements: [REL-02, REL-04]

must_haves:
  truths:
    - "CLI temp files (uv prompt :121, codex output :109) call deleteOnExit() at creation AND are deleted in the existing finally (:274-288) — reliable even on a crash"
    - "A new defaulted AgentSettings field cliTimeoutSeconds (default = Defaults.CLI_PROCESS_TIMEOUT_SECONDS) is user-configurable; the CLI waitFor reads it instead of the hardcoded constant; the shared constant is NOT mutated (it doubles as the HTTP-backend default)"
    - "On CLI timeout, an extracted buildTimeoutMessage(tail, timeoutSeconds) names the configured limit AND suggests remediation (increase the timeout / pre-install the CLI) — the actionable fix for issue #71"
    - "McpServerManager.stop() is bounded (force-stop + Stopped/Failed callback after the bound) so it never hangs — mirroring the already-bounded shutdown()"
    - "The inner per-salt host-anonymization maps are bounded LRU (LinkedHashMap accessOrder + removeEldestEntry, synchronized); the outer ConcurrentHashMap + computeIfAbsent/remove and clearMappings are untouched; the host-<12hex>.local format and round-trip are preserved"
  artifacts:
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/config/AgentSettings.kt"
      provides: "Defaulted cliTimeoutSeconds field + load(coerceIn 30..3600) + baseline + persist + KEY_CLI_TIMEOUT"
      contains: "cliTimeoutSeconds"
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/backends/cli/CliBackend.kt"
      provides: "deleteOnExit() at both temp-file sites + configurable timeout read + extracted internal buildTimeoutMessage"
      contains: "buildTimeoutMessage"
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/mcp/KtorMcpServerManager.kt"
      provides: "Bounded stop() with force-stop + callback on timeout"
      contains: "awaitTermination"
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt"
      provides: "boundedLru inner host maps (LinkedHashMap accessOrder + removeEldestEntry)"
      contains: "removeEldestEntry"
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/backends/cli/CliBackendTempFileTest.kt"
      provides: "Temp-file finally + deleteOnExit-on-failure test (SC2)"
      contains: "CliBackendTempFileTest"
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/backends/cli/CliTimeoutMessageTest.kt"
      provides: "buildTimeoutMessage actionable-message unit test (SC4 / #71)"
      contains: "CliTimeoutMessageTest"
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/mcp/McpShutdownBoundTest.kt"
      provides: "Bounded stop() integration test (SC5a)"
      contains: "McpShutdownBoundTest"
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionHostMapBoundTest.kt"
      provides: "LRU-bound + round-trip + format-preserved test (SC5b)"
      contains: "RedactionHostMapBoundTest"
  key_links:
    - from: "CliBackend.kt timeout waitFor (:225)"
      to: "settings.cliTimeoutSeconds + buildTimeoutMessage"
      via: "read configurable timeout; build actionable message"
      pattern: "buildTimeoutMessage"
    - from: "KtorMcpServerManager.stop() submitted task (:232)"
      to: "bounded await + shutdownNow/force-stop + callback"
      via: "future.get(bound) or awaitTermination mirroring shutdown() :245-252"
      pattern: "awaitTermination|future.get"
    - from: "Redaction.anonymizeHost computeIfAbsent (:291-292)"
      to: "boundedLru inner map"
      via: "computeIfAbsent(salt) { boundedLru(HOST_MAP_CAP) }"
      pattern: "boundedLru"
---

<objective>
Close REL-02 (resource hardening) and REL-04 (issue #71) — four independent fixes on existing code paths, no new features:

- **REL-02 / CLI temp files:** add `deleteOnExit()` at both `CliBackend.createTempFile` sites (`:109` codex output, `:121` uv prompt) so a hard crash still cleans up prompt/context content; keep the existing `finally` deletes (`:274-288`) as the primary path.
- **REL-04 / issue #71:** the CLI process timeout is the hardcoded `Defaults.CLI_PROCESS_TIMEOUT_SECONDS = 120`, not user-overridable; `npx @google/gemini-cli` first-run download exceeds it and yields a bare "CLI command timed out". Add a defaulted, user-configurable `cliTimeoutSeconds` setting and an extracted `buildTimeoutMessage(tail, timeoutSeconds)` that names the limit and suggests remediation. ADD a new setting; do NOT mutate the shared constant (it doubles as the HTTP-backend default).
- **REL-02 / SC5a — bounded MCP shutdown:** `KtorMcpServerManager.stop()` submits work to an executor with no bound, so a hung `server.stop(...)` leaves `stop()` effectively unbounded. Give it the same bounded-await + force-stop semantics as the already-bounded `shutdown()` (`:245-252`), and still fire the lifecycle `callback`.
- **REL-02 / SC5b — bounded host maps:** the nested host-anonymization maps (`Redaction.kt:145-146`) are unbounded — a memory-growth/DoS vector over a long pentest. LRU-cap the INNER per-salt map; keep the outer `ConcurrentHashMap` + `computeIfAbsent`/`remove` and `clearMappings` untouched; preserve the `host-<12hex>.local` format and the round-trip.

Purpose: Strengthen privacy (crash-safe temp-file cleanup), close a real hang (bounded MCP stop), bound memory (host-map LRU), and give issue #71 an actionable, configurable fix.
Output: edits in `CliBackend.kt`, `AgentSettings.kt`, `Defaults.kt`, `KtorMcpServerManager.kt`, `Redaction.kt`, plus 4 new tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-reliability-concurrency-hardening/17-RESEARCH.md
@.planning/phases/17-reliability-concurrency-hardening/17-PATTERNS.md
@.planning/phases/17-reliability-concurrency-hardening/17-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: REL-04 — configurable cliTimeoutSeconds setting + extracted buildTimeoutMessage + CliTimeoutMessageTest (Wave 0)</name>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/config/Defaults.kt (CLI_PROCESS_TIMEOUT_SECONDS = 120 at :41 — do NOT mutate; it doubles as the HTTP-backend default)
    - src/main/kotlin/com/six2dez/burp/aiagent/config/AgentSettings.kt (sibling field perplexityTimeoutSeconds at :61 default 60; load+coerceIn(30,3600) at :289-291; baseline at :454; persist at :560; KEY const block at ~:805; the five touch-point pattern)
    - src/main/kotlin/com/six2dez/burp/aiagent/backends/cli/CliBackend.kt (standard timeout waitFor at :225 reading Defaults.CLI_PROCESS_TIMEOUT_SECONDS; existing timeout message at :233-239; opencode wall-clock at :222; embedded CliConnection loop at :736)
    - src/test/kotlin/com/six2dez/burp/aiagent/backends/cli/CopilotCommandBuilderTest.kt (extracted-internal-fun precedent: buildCopilotCommand tested directly, OS-independent)
    - .planning/phases/17-reliability-concurrency-hardening/17-RESEARCH.md (§Code Examples "REL-04 test"; §Pitfall 6 shared constant)
    - .planning/phases/17-reliability-concurrency-hardening/17-PATTERNS.md (§config touch points 1-5; §CliBackend REL-04 GOTCHAS — three waitFor sites)
  </read_first>
  <acceptance_criteria>
    - cliTimeoutSeconds is a DEFAULTED AgentSettings field (`val cliTimeoutSeconds: Int = Defaults.CLI_PROCESS_TIMEOUT_SECONDS`) so positional/named baselineSettings keep compiling
    - The five touch points exist: field decl, load (`(prefs.getInteger(KEY_CLI_TIMEOUT) ?: Defaults.CLI_PROCESS_TIMEOUT_SECONDS).coerceIn(30, 3600)`), baseline factory, persist (`prefs.setInteger(KEY_CLI_TIMEOUT, settings.cliTimeoutSeconds.coerceIn(30, 3600))`), and `private const val KEY_CLI_TIMEOUT = "cli.timeoutSeconds"`
    - Defaults.CLI_PROCESS_TIMEOUT_SECONDS is NOT changed (assertion: still `= 120`); the value is a plaintext pref (getInteger/setInteger), NOT SecretCipher
    - An `internal fun buildTimeoutMessage(tail: String, timeoutSeconds: Int): String` exists in CliBackend and: contains("timed out"), surfaces the configured limit (the number), and suggests remediation (contains "increase" OR "pre-install", case-insensitive); never interpolates more than the existing bounded tail
    - The CLI standard-path waitFor (:225) reads the configured cliTimeoutSeconds and uses buildTimeoutMessage for the error
    - CliTimeoutMessageTest asserts the extracted builder directly (OS-independent) and passes
  </acceptance_criteria>
  <action>
    First (Wave 0): create `src/test/kotlin/com/six2dez/burp/aiagent/backends/cli/CliTimeoutMessageTest.kt` modeled on CopilotCommandBuilderTest — unit-test the (not-yet-existing) `buildTimeoutMessage(tail, timeoutSeconds)` directly: assert the result `contains("timed out")`, contains the timeout number (e.g. "1" or "120" depending on the case), and `contains("increase", ignoreCase = true) || contains("pre-install", ignoreCase = true)`. Cover a blank-tail case and a non-blank-tail case (the message must still bound the tail). Run it to confirm RED.

    Add the `cliTimeoutSeconds` field to AgentSettings copying ALL FIVE perplexity/nvidia touch points: (1) field decl `val cliTimeoutSeconds: Int = Defaults.CLI_PROCESS_TIMEOUT_SECONDS` (defaulted — keeps positional baselineSettings compiling); (2) load `cliTimeoutSeconds = (prefs.getInteger(KEY_CLI_TIMEOUT) ?: Defaults.CLI_PROCESS_TIMEOUT_SECONDS).coerceIn(30, 3600)` (V5 input validation — same bounds as every sibling timeout; rejects negative/zero/absurd); (3) baseline `cliTimeoutSeconds = Defaults.CLI_PROCESS_TIMEOUT_SECONDS`; (4) persist `prefs.setInteger(KEY_CLI_TIMEOUT, settings.cliTimeoutSeconds.coerceIn(30, 3600))`; (5) `private const val KEY_CLI_TIMEOUT = "cli.timeoutSeconds"` in the companion. Plaintext pref (getInteger/setInteger), NOT SecretCipher — it is a timeout integer, matching the customRedactionPatterns config-not-secret precedent. Defaults.kt needs NO change (reuse the existing 120 constant as the default) — do NOT repurpose or rename CLI_PROCESS_TIMEOUT_SECONDS (Pitfall 6: it is also the HTTP-backend default at AgentSettings :943-961 and an AgentSupervisor fallback; mutating it would silently change HTTP defaults).

    In CliBackend, extract `internal fun buildTimeoutMessage(tail: String, timeoutSeconds: Int): String` that produces an actionable message: states it timed out, names the configured limit (timeoutSeconds), and suggests increasing the `cliTimeoutSeconds` setting / pre-installing the CLI (the #71 remediation, esp. for npx first-run downloads). Keep the existing `take(2000)` bounded-tail discipline — the message must name the LIMIT + remediation, never leak prompt content (Information Disclosure control). Wire the standard-path waitFor (:225) to read the user-configured `cliTimeoutSeconds` (from the connection's settings/config) instead of the hardcoded `Defaults.CLI_PROCESS_TIMEOUT_SECONDS`, and call `buildTimeoutMessage(tail, configuredTimeout)` for the IllegalStateException. The :225 standard path is the minimum required site; if the opencode wall-clock (:222) and embedded CliConnection loop (:736) trivially share the same configured value, update them too for consistency, but :225 is the gate. English-only comments.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.backends.cli.CliTimeoutMessageTest" 2>&1 | tail -30</automated>
  </verify>
  <done>cliTimeoutSeconds is a defaulted AgentSettings field with all five touch points + KEY_CLI_TIMEOUT; Defaults.CLI_PROCESS_TIMEOUT_SECONDS unchanged (=120); buildTimeoutMessage is extracted and names the limit + remediation; the :225 waitFor reads the configured value; CliTimeoutMessageTest passes. Spot-check `./gradlew test --tests "com.six2dez.burp.aiagent.config.*"` stays green (positional baselineSettings still compiles).</done>
</task>

<task type="auto">
  <name>Task 2: REL-02 — deleteOnExit() at both CLI temp-file sites + CliBackendTempFileTest (Wave 0)</name>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/backends/cli/CliBackend.kt (createTempFile codex output at :109; createTempFile uv prompt at :121; owner-only POSIX perms at ~:124-135; inline delete at :138; finally cleanup promptFile/outputFile.delete() at :274-288)
    - .planning/phases/17-reliability-concurrency-hardening/17-RESEARCH.md (§Code Examples "REL-02 temp-file test"; §Runtime State Inventory OS-registered state; §Anti-Pattern deleteOnExit accumulation)
    - .planning/phases/17-reliability-concurrency-hardening/17-PATTERNS.md (§CliBackend REL-02 — keep finally AND add deleteOnExit; both)
  </read_first>
  <acceptance_criteria>
    - Both createTempFile sites (:109 codex output, :121 uv prompt) call `deleteOnExit()` at creation
    - The existing `finally` deletes (:274-288, primary path) and inline delete (:138) and owner-only POSIX perms are preserved (no removals)
    - CliBackendTempFileTest: after a send path that takes a temp-file branch and then fails (exception mid-execution), no leftover `burp_uv_prompt_*` / `burp-ai-agent-codex*` file remains in $TMPDIR — proving finally cleanup; and deleteOnExit is registered as the crash-safety net
    - `./gradlew test --tests "*CliBackendTempFileTest"` passes
  </acceptance_criteria>
  <action>
    First (Wave 0): create `src/test/kotlin/com/six2dez/burp/aiagent/backends/cli/CliBackendTempFileTest.kt`. Implement the forced-failure behavioral test from RESEARCH §Code Examples: snapshot temp files matching the prefixes before, drive a CLI send that takes the temp-file branch (e.g. a >LARGE_PROMPT_THRESHOLD prompt for the uv-prompt path) with a command that fails fast / throws, then assert no leftover `burp_uv_prompt_` (and `burp-ai-agent-codex`) file remains — i.e. the count of matching temp files is unchanged. Use Assumptions to guard any OS-specific bits. Run to confirm the current behavior (finally already deletes on the normal failure path, but the test also asserts the deleteOnExit safety-net is registered — author the assertion to fail if deleteOnExit is missing, e.g. by asserting the create-site path goes through a helper that registers it, or by a focused unit on a small extracted temp-file factory if cleaner).

    Add `deleteOnExit()` at BOTH createTempFile sites: at :109 (`java.io.File.createTempFile("burp-ai-agent-codex", ".txt").apply { deleteOnExit() }` or a following `.deleteOnExit()` call) and at :121 (`tFile.deleteOnExit()` right after creation, before the owner-only POSIX perms block). Keep the existing `finally` deletes (:274-288) as the PRIMARY cleanup and the inline delete (:138) and the owner-only perms exactly as-is — deleteOnExit is the belt-and-suspenders crash net (it registers a JVM shutdown hook; the hook almost always no-ops because finally already deleted — acceptable per the Anti-Pattern note since temp files are per-send). Per the Architectural Responsibility Map, file lifecycle is the CLI backend's responsibility (primary) with the OS filesystem as the secondary tier; this strengthens the Information-Disclosure mitigation (prompt/context content cannot survive a crash). English-only comments.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.backends.cli.CliBackendTempFileTest" 2>&1 | tail -30</automated>
  </verify>
  <done>Both temp-file creation sites call deleteOnExit(); the finally deletes + inline delete + POSIX perms are preserved; CliBackendTempFileTest proves no temp file leaks on a mid-execution failure. Spot-check `./gradlew test --tests "com.six2dez.burp.aiagent.backends.cli.*"` stays green.</done>
</task>

<task type="auto">
  <name>Task 3: REL-02/SC5 — bounded MCP stop() + LRU-capped host maps + McpShutdownBoundTest + RedactionHostMapBoundTest (Wave 0)</name>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/KtorMcpServerManager.kt (UNBOUNDED stop() at :230-243 — executor.submit{ server?.stop(1000,5000); callback(...) } with no bound; the BOUNDED shutdown() analog at :245-252 — executor.shutdown() + awaitTermination(10,SECONDS) + shutdownNow())
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpServerManager.kt (interface: fun stop(callback) at :28, fun shutdown() at :30 — interface signatures must NOT change)
    - src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt (nested ConcurrentHashMap host maps at :145-146; create+write inside anonymizeHost computeIfAbsent at :291-292; deAnonymizeHost at :297; clearMappings at :302; host-<12hex>.local format note at :150,:289)
    - src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionTest.kt (round-trip + format regex `^host-[0-9a-f]{12}\.local$` + clearMappings assertions to copy and keep green)
    - .planning/phases/17-reliability-concurrency-hardening/17-RESEARCH.md (§Pattern 4 boundedLru; §Pitfall 5 clearMappings/round-trip; §Code Examples "bounded MCP stop()" + "RedactionHostMapBound")
    - .planning/phases/17-reliability-concurrency-hardening/17-PATTERNS.md (§KtorMcpServerManager bound stop(); §Redaction LRU GOTCHAS — bound INNER map only; §test naming note: McpShutdownBoundTest is NOT excluded by the heavy globs)
  </read_first>
  <acceptance_criteria>
    - KtorMcpServerManager.stop() is bounded: the submitted server-stop work cannot make stop() hang — on timeout it force-stops AND still fires callback(Stopped) or callback(Failed(...)) so the UI never waits forever; mirrors shutdown()'s awaitTermination+shutdownNow
    - The McpServerManager INTERFACE signatures are unchanged (only the KtorMcpServerManager impl changes)
    - The inner per-salt host maps are bounded LRU: `Collections.synchronizedMap(object : LinkedHashMap<K,V>(16, 0.75f, accessOrder=true) { override removeEldestEntry = size > cap })`, created via `computeIfAbsent(salt) { boundedLru(HOST_MAP_CAP) }`; the OUTER ConcurrentHashMap + computeIfAbsent/remove and clearMappings are UNCHANGED
    - HOST_MAP_CAP is a few thousand (4096); the host-<12hex>.local format and the anonymizeHost→deAnonymizeHost round-trip are preserved (RedactionTest stays green)
    - McpShutdownBoundTest: stop(callback) reaches Stopped/Failed within (bound + margin) via CountDownLatch and does NOT block indefinitely
    - RedactionHostMapBoundTest: anonymizing > CAP distinct hosts under one salt keeps the inner map size ≤ CAP AND a recently-used round-trip + format still hold (reset Redaction via clearMappings() in setup/teardown — Redaction is an object/singleton)
    - `./gradlew test --tests "*McpShutdownBoundTest" --tests "*RedactionHostMapBoundTest" --tests "*RedactionTest"` passes
  </acceptance_criteria>
  <action>
    First (Wave 0): create `src/test/kotlin/com/six2dez/burp/aiagent/mcp/McpShutdownBoundTest.kt` (analogs McpServerIntegrationTest / McpSupervisorRestartPolicyTest): start a manager, call stop(callback), assert via CountDownLatch.await(bound + margin) that the callback reaches Stopped/Failed and that it does NOT block indefinitely. NOTE the test-naming gotcha: `*IntegrationTest`/`*ConcurrencyTest`/`*RestartPolicyTest` are excluded under `-PexcludeHeavyTests=true`; `McpShutdownBoundTest` is NOT matched by those globs, so it runs in the standard suite (the SC5a gate) — keep that name. Create `src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionHostMapBoundTest.kt` (analog RedactionTest): in setup/teardown call `Redaction.clearMappings()` (it is a singleton object — avoid cross-test bleed); anonymize > CAP distinct hosts under one salt and assert the inner map stays ≤ CAP, then assert a recently-anonymized host still round-trips via deAnonymizeHost and the output matches `^host-[0-9a-f]{12}\.local$`. Run both to confirm RED (stop() unbounded test may pass-by-luck, so make the bound assertion strict; the host-bound test fails because the map is currently unbounded).

    Bound KtorMcpServerManager.stop() (:230-243): capture the executor.submit(...) as a Future and bound it (e.g. `future.get(BOUND, TimeUnit.SECONDS)`), OR mirror shutdown()'s `awaitTermination(BOUND, SECONDS)` + `shutdownNow()` pattern — choose the form that fits the single-thread executor. On timeout, force-stop (shutdownNow / destroy the server) AND drive `callback(McpServerState.Stopped)` or `callback(McpServerState.Failed(...))` so the lifecycle channel is never dropped (the UI must not wait forever). Keep `server.stop(1000, 5000)` (Ktor-level grace/timeout) as-is — the NEW bound is at the executor/manager level so stop() itself cannot hang. Do NOT change the McpServerManager interface signatures (:28-30) — only the KtorMcpServerManager impl. Per the Architectural Responsibility Map, the bound belongs at the manager's stop()/shutdown() (the Netty/executor own shutdown). This is the Denial-of-Service mitigation (no hung stop leaving a port bound).

    LRU-cap the inner host maps in Redaction. Add a private helper `private fun <K, V> boundedLru(maxEntries: Int): MutableMap<K, V> = java.util.Collections.synchronizedMap(object : LinkedHashMap<K, V>(16, 0.75f, true) { override fun removeEldestEntry(eldest: Map.Entry<K, V>): Boolean = size > maxEntries })` and a `private const val HOST_MAP_CAP = 4096` (a few thousand per CONTEXT discretion). At the create+write site (:291-292) change ONLY the inner-map factory: `hostForwardMap.computeIfAbsent(salt) { boundedLru(HOST_MAP_CAP) }[host] = anon` and the reverse the same way. The map is NESTED (salt → host → anon): bound the INNER per-salt map, keep the OUTER `ConcurrentHashMap<salt, ...>` and its `computeIfAbsent(salt){...}` / `remove(salt)` EXACTLY as-is so `clearMappings` keeps working (Pitfall 5). Do NOT touch the HKDF crypto or the `host-<12hex>.local` output format (V6 — privacy guarantee). Forward/reverse eviction skew is benign (reverse is the lookup path; re-anonymizing is deterministic because HKDF is pure) — add a one-line English comment documenting it; cap both maps at the same size. This is the memory-exhaustion DoS mitigation. English-only comments throughout.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.mcp.McpShutdownBoundTest" --tests "com.six2dez.burp.aiagent.redact.RedactionHostMapBoundTest" --tests "com.six2dez.burp.aiagent.redact.RedactionTest" 2>&1 | tail -30</automated>
  </verify>
  <done>stop() is bounded (force-stop + Stopped/Failed callback on timeout) and the interface is unchanged; the inner host maps are bounded LRU (cap 4096) with the outer map + clearMappings + format + round-trip preserved; McpShutdownBoundTest and RedactionHostMapBoundTest pass; RedactionTest stays green. Spot-check `./gradlew test --tests "com.six2dez.burp.aiagent.mcp.*" --tests "com.six2dez.burp.aiagent.redact.*"` stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI backend → OS filesystem ($TMPDIR) | Prompt/context content is written to system temp files for CLIs that need file input. The boundary risk is residue on a shared host if the JVM crashes before cleanup. |
| MCP server lifecycle → Ktor/Netty executor | stop()/shutdown() drive an executor + Netty engine; a hung server.stop must not hang the manager and leave resources/port held. |
| Redaction host-cache → process memory | The in-memory host-anonymization maps grow with distinct hosts seen over a session; unbounded growth is a memory-exhaustion vector. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Information Disclosure | CLI temp files containing prompt/context content after a crash | mitigate | `deleteOnExit()` at both createTempFile sites (crash-safe JVM shutdown hook) PLUS the existing `finally` deletes (primary) PLUS existing owner-only POSIX perms. CliBackendTempFileTest proves no residue on mid-execution failure. Strengthens the existing guarantee. |
| T-17-03-02 | Information Disclosure | REL-04 timeout error message | mitigate | buildTimeoutMessage names the LIMIT + remediation only; reuses the existing `take(2000)` bounded-tail discipline — never interpolates raw prompt content. No outbound telemetry (local message text only). |
| T-17-03-03 | Denial of Service | MCP `stop()` hanging, leaving a port bound / resources held | mitigate | Bound the submitted server-stop with `future.get(bound)` / `awaitTermination` + force-stop (shutdownNow), mirroring shutdown(); still fire the lifecycle callback on timeout so the UI never waits forever. McpShutdownBoundTest is the SC5a gate. |
| T-17-03-04 | Denial of Service | Unbounded host-anonymization map growth over a long pentest (memory exhaustion) | mitigate | LRU-cap the inner per-salt map (LinkedHashMap accessOrder + removeEldestEntry, synchronized, cap 4096); outer map + clearMappings preserved. RedactionHostMapBoundTest asserts the bound. This is itself a hardening control. |
| T-17-03-05 | Tampering (privacy regression) | LRU change altering the anonymization output or breaking de-anon | accept | The cap touches ONLY the inner-map container, never the HKDF crypto or the `host-<12hex>.local` format; RedactionTest round-trip + format regex stay green. Forward/reverse eviction skew is benign (reverse is the lookup path; HKDF is deterministic). Residual risk: a session touching >4096 distinct hosts re-anonymizes evicted entries (cheap, deterministic, no correctness loss). |
| T-17-03-06 | Tampering (config) | CLI timeout integer pref tampering / absurd values | mitigate | cliTimeoutSeconds is `coerceIn(30, 3600)` on both load and persist (V5 input validation), rejecting negative/zero/absurd values — same bounds as every sibling timeout. Plaintext pref (not a secret). |
| T-17-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (zero new deps — only JDK LinkedHashMap LRU + a new defaulted settings field + deleteOnExit). RESEARCH §Package Legitimacy Audit: not applicable. No legitimacy checkpoint required. |
</threat_model>

<verification>
- Per-task SC gates: `*CliTimeoutMessageTest` (SC4), `*CliBackendTempFileTest` (SC2), `*McpShutdownBoundTest` (SC5a), `*RedactionHostMapBoundTest` (SC5b) — all pass.
- Regression watch stays green: `./gradlew test --tests "com.six2dez.burp.aiagent.redact.*" --tests "com.six2dez.burp.aiagent.mcp.*" --tests "com.six2dez.burp.aiagent.config.*" --tests "com.six2dez.burp.aiagent.backends.cli.*"`.
- Shared-constant guard: `grep -n "CLI_PROCESS_TIMEOUT_SECONDS = 120" src/main/kotlin/com/six2dez/burp/aiagent/config/Defaults.kt` still returns the unchanged constant (Pitfall 6 — HTTP defaults not perturbed).
- Nested-map guard: `grep -n "computeIfAbsent(salt)" src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt` still present (outer map untouched); `grep -n "removeEldestEntry" Redaction.kt` present (inner map bounded).
- Build/test with `./gradlew test`, NOT `ktlintCheck` (generateBuildFlags defect).
- Manual UAT (out of automated scope, recorded for the maintainer): on a fresh machine, `npx @google/gemini-cli ...` via the CLI backend shows the actionable timeout message naming the configurable limit, and raising `cliTimeoutSeconds` lets it complete.
</verification>

<success_criteria>
- SC2: CLI temp files are deleted in `finally` AND via `deleteOnExit()`; confirmed by a forced-failure test.
- SC4: Issue #71 is given a configurable `cliTimeoutSeconds` + an actionable timeout message naming the limit; regression test (extracted builder) prevents recurrence.
- SC5a: `McpServerManager.stop()` completes within a bounded timeout — no hang.
- SC5b: Host-anonymization maps are bounded (LRU cap) to prevent memory growth; format + round-trip preserved.
</success_criteria>

<output>
Create `.planning/phases/17-reliability-concurrency-hardening/17-03-SUMMARY.md` when done.
</output>

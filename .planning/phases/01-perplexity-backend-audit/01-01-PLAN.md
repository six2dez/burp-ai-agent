---
phase: 01-perplexity-backend-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle.kts
  - src/test/kotlin/com/six2dez/burp/aiagent/backends/perplexity/PerplexityBackendFactoryTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/backends/openai/OpenAiCompatibleBackendDefaultsTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/config/AgentSettingsMigrationTest.kt
  - .planning/phases/01-perplexity-backend-audit/01-VERIFICATION.md
  - .planning/phases/01-perplexity-backend-audit/01-VALIDATION.md
autonomous: true
requirements: [PPLX-01, PPLX-02, PPLX-03, PPLX-04, PPLX-05]
tags: [audit, http-backend, perplexity, wire-level-test, mockwebserver, settings-migration]

must_haves:
  truths:
    - "D-01: perplexityModel default stays as empty string (free-form, no hard-coded 'sonar') — locked at AgentSettings.kt:58 and asserted by T3 (`assertEquals(\"\", loaded.perplexityModel)`)"
    - "D-02: perplexityUrl default stays as bare host 'https://api.perplexity.ai' (no path) — locked at AgentSettings.kt:57 and asserted by T3 (`assertEquals(\"https://api.perplexity.ai\", loaded.perplexityUrl)`); resolved runtime URL exercised wire-level by T1"
    - "D-03: CURRENT_SETTINGS_SCHEMA_VERSION stays at 3; no migrateIfNeeded bump — asserted by T3 (`assertEquals(3, prefs.integers[\"settings.schema.version\"])`)"
    - "D-04: All Perplexity assertions are wire-level via OkHttp MockWebServer (no reflection on private fields) — encoded in T1 and T2 test architecture"
    - "D-05: All new tests live in the fast suite (`./gradlew test -PexcludeHeavyTests=true` runs them); no `*IntegrationTest`/`*ConcurrencyTest`/`*BackpressureTest`/`*RestartPolicyTest` suffix on new class names"
    - "D-07: Test placement honoured exactly — new file PerplexityBackendFactoryTest.kt under backends/perplexity/, new file OpenAiCompatibleBackendDefaultsTest.kt under backends/openai/, extension to existing AgentSettingsMigrationTest.kt under config/"
    - "MockWebServer 4.12.0 is in build.gradle.kts testImplementation"
    - "Perplexity wire-level test class exists at src/test/kotlin/com/six2dez/burp/aiagent/backends/perplexity/PerplexityBackendFactoryTest.kt"
    - "Backwards-compat wire-level test class exists at src/test/kotlin/com/six2dez/burp/aiagent/backends/openai/OpenAiCompatibleBackendDefaultsTest.kt"
    - "AgentSettingsMigrationTest contains a @Test method asserting v0.6.x prefs load with safe perplexity defaults and CURRENT_SETTINGS_SCHEMA_VERSION = 3"
    - "./gradlew test -PexcludeHeavyTests=true exits 0"
    - "./gradlew ktlintCheck exits 0"
    - "01-VERIFICATION.md records the D-06 manual smoke and the two D-08 KNOWN-WORDING-GAP handoffs to Phase 5"
  artifacts:
    - path: build.gradle.kts
      provides: "MockWebServer 4.12.0 declared as testImplementation"
      contains: 'mockwebserver:4.12.0'
    - path: src/test/kotlin/com/six2dez/burp/aiagent/backends/perplexity/PerplexityBackendFactoryTest.kt
      provides: "Wire-level lock for PPLX-02 (URL form, no /v1) and PPLX-03 (no response_format)"
      min_lines: 70
      contains: "class PerplexityBackendFactoryTest"
    - path: src/test/kotlin/com/six2dez/burp/aiagent/backends/openai/OpenAiCompatibleBackendDefaultsTest.kt
      provides: "Wire-level lock for PPLX-04 (default constructor stays /v1/chat/completions + response_format)"
      min_lines: 70
      contains: "class OpenAiCompatibleBackendDefaultsTest"
    - path: src/test/kotlin/com/six2dez/burp/aiagent/config/AgentSettingsMigrationTest.kt
      provides: "Settings-load lock for PPLX-05 (v0.6.x prefs load with safe defaults; schema stays at 3)"
      contains: "load_v06xPreferencesYieldSafePerplexityDefaultsAndSchemaStaysV3"
    - path: .planning/phases/01-perplexity-backend-audit/01-VERIFICATION.md
      provides: "Records the one-time D-06 manual smoke fields and the two D-08 KNOWN-WORDING-GAP handoffs"
      contains: "KNOWN-WORDING-GAP"
  key_links:
    - from: "PerplexityBackendFactoryTest.kt"
      to: "MockWebServer (com.squareup.okhttp3:mockwebserver:4.12.0)"
      via: "testImplementation classpath via build.gradle.kts"
      pattern: "import okhttp3.mockwebserver"
    - from: "PerplexityBackendFactoryTest.kt"
      to: "src/main/.../backends/openai/OpenAiCompatibleBackend.kt lines 185-187 (response_format gate) and 408-418 (URL builder)"
      via: "RecordedRequest body + path assertions"
      pattern: 'assertEquals\\("/chat/completions", .*recorded.*path\\)'
    - from: "OpenAiCompatibleBackendDefaultsTest.kt"
      to: "src/main/.../backends/openai/OpenAiCompatibleBackend.kt lines 42-47 (constructor defaults)"
      via: "default-constructor backend instance + wire assertions"
      pattern: 'assertEquals\\("/v1/chat/completions", .*recorded.*path\\)'
    - from: "AgentSettingsMigrationTest.kt (new @Test)"
      to: "src/main/.../config/AgentSettings.kt lines 57-61 (defaults), 257-266 (load path), 780 (CURRENT_SETTINGS_SCHEMA_VERSION)"
      via: "InMemoryPrefs + AgentSettingsRepository.load() + assertions on returned AgentSettings"
      pattern: 'assertEquals\\(3, prefs\\.integers\\["settings\\.schema\\.version"\\]\\)'
---

<objective>
Lock the Perplexity backend behaviour that already shipped in `[Unreleased]` with three wire-level / settings tests so PPLX-01..05 cannot regress in v0.7.0.

Purpose: Phase 1 is a behaviour-locking audit — zero production code changes, only tests added. Test coverage gap for the Perplexity HTTP path is called out explicitly in `.planning/codebase/TESTING.md` "Known Coverage Gaps".

Output:
- One new test dependency (`com.squareup.okhttp3:mockwebserver:4.12.0`)
- Two new test classes (PerplexityBackendFactoryTest, OpenAiCompatibleBackendDefaultsTest) using MockWebServer
- One new `@Test` method extending the existing `AgentSettingsMigrationTest`
- A `01-VERIFICATION.md` audit-trail file recording the manual smoke (D-06) and two wording-gap handoffs to Phase 5 (D-08)
</objective>

<threat_model>
**ASVS Level:** 1
**Scope analysis:** Phase 1 is a behaviour-locking audit of an HTTP backend that already shipped. NO production code is modified — only test code is added/extended. The Perplexity backend RECEIVES only redacted prompts (per ADR-5; redaction runs pre-flight in `Redaction.apply` and `ContextCollector`, not in any backend code path under audit).

**Threats considered:**
- T-01: Test secrets in CI — MITIGATED. D-06 forbids real API keys; tests use `"pplx-test"` placeholder.
- T-02: Token leakage in test logs — MITIGATED. `BackendDiagnostics.log(...)` only logs URLs, not headers. Tests do not assert against log output.
- T-03: Cross-test pollution via `HttpBackendSupport.sharedClient` static cache — MITIGATED. Each MockWebServer gets a unique port → unique cache key. Do NOT call `shutdownSharedClients()` in `@AfterEach` (would break parallel Gradle test runs).
- T-04: MockWebServer left running across tests (slow tests, fd exhaustion) — MITIGATED by `@AfterEach { server.shutdown() }`.

**ASVS categories applicable:**
- V2 Authentication: no — audit does not touch auth.
- V5 Input Validation: partial — wire-level tests indirectly verify URL parsing is robust (no path injection via crafted strings).
- V6 Cryptography: no — Bearer token is opaque.

**Severity verdict:** No high-severity threats. All identified threats have mitigations encoded in the test design itself.
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-perplexity-backend-audit/01-CONTEXT.md
@.planning/phases/01-perplexity-backend-audit/01-RESEARCH.md
@.planning/phases/01-perplexity-backend-audit/01-PATTERNS.md
@.planning/phases/01-perplexity-backend-audit/01-VALIDATION.md
@AGENTS.md
@CLAUDE.md
@DECISIONS.md
@SPEC.md
</context>

<requirements_addressed>
| Requirement | Task(s) | Coverage type |
|-------------|---------|---------------|
| PPLX-01 (Perplexity card pre-populated to sane defaults) | T3 (indirect) | Indirect: the five `perplexity*` fields' defaults flow through `AgentSettings.kt:57-61` → `BackendConfigPanel.buildPerplexityPanel()`. The existing `SettingsDefaultsPersistenceTest` already exercises the Settings → Backend round-trip. T3 re-asserts the same defaults via the load path (when v0.6.x prefs have no `perplexity.*` keys). No additional UI test required (CONTEXT.md `<code_context>` confirms; D-08 records SPEC §4.4 wording gap as a Phase 5 handoff). |
| PPLX-02 (URL = `/chat/completions`, no `/v1`) | T1 | Direct: `PerplexityBackendFactoryTest` asserts `recorded.path == "/chat/completions"`. |
| PPLX-03 (no `response_format` even when `jsonMode = true`) | T1 | Direct: `PerplexityBackendFactoryTest` asserts `mapper.readTree(recorded.body.readUtf8()).has("response_format") == false`. |
| PPLX-04 (defaults stay `/v1/chat/completions` + `response_format`) | T2 | Direct: `OpenAiCompatibleBackendDefaultsTest` constructs `OpenAiCompatibleBackend()` with NO `chatCompletionsBasePath` / `supportsJsonObjectResponseFormat` overrides and asserts `recorded.path == "/v1/chat/completions"` and `body.get("response_format").get("type").asText() == "json_object"`. NVIDIA NIM factory (`NvidiaNimBackendFactory.kt:17-39`) does not override these knobs, so the default-constructor path IS NVIDIA's path. |
| PPLX-05 (v0.6.x prefs load with safe defaults; schema stays at 3) | T3 | Direct: new `@Test` extends `AgentSettingsMigrationTest`; pre-populates `InMemoryPrefs` with `settings.schema.version = 3` and zero `perplexity.*` keys; calls `repo.load()`; asserts five `perplexity*` fields default safely and post-load schema marker is still `3`. |

**PPLX-01 indirect-coverage rationale (per CONTEXT.md and VALIDATION.md):** The Perplexity card defaults are not a new code path — they are the five additive `AgentSettings` fields plus a `BackendConfigPanel` card already plumbed and exercised by `SettingsDefaultsPersistenceTest`. T3 locks the defaults at the deserialisation boundary, which is the only place PPLX-01 could regress without also breaking T3. No dedicated UI test is added (would duplicate existing coverage and grow the audit scope past CONTEXT.md's locked decisions).
</requirements_addressed>

<tasks>

<task type="auto">
  <name>Task 0: Add MockWebServer test dependency to build.gradle.kts</name>
  <wave>0</wave>
  <depends_on>[]</depends_on>
  <files>build.gradle.kts</files>
  <read_first>
    - build.gradle.kts (entire file — current state; the dependency goes in the existing `dependencies { }` block alongside lines 49-53)
    - .planning/phases/01-perplexity-backend-audit/01-RESEARCH.md §"Standard Stack → Supporting (NEW — must be added)" (lines 84-98) and §"Risks & Pitfalls" item 1 (line 869) — version pin rationale (4.12.0 must match the production OkHttp version already pinned at build.gradle.kts:31 so the two share transitive okio deps without classpath drift)
    - .planning/phases/01-perplexity-backend-audit/01-PATTERNS.md §"1. build.gradle.kts" (lines 24-54) — exact convention to respect: no version catalogs, inline literals, place adjacent to existing `testImplementation` block, do not reformat surrounding lines (ktlint catches spurious reorderings)
    - .planning/phases/01-perplexity-backend-audit/01-VALIDATION.md §"Wave 0 Requirements" — wire-level tests in T1 and T2 cannot compile without this dependency
    - .planning/phases/01-perplexity-backend-audit/01-CONTEXT.md §"Implementation Decisions → D-04 (wire-level capture via MockWebServer)"
  </read_first>
  <action>
    Add the single line `testImplementation("com.squareup.okhttp3:mockwebserver:4.12.0")` to the `dependencies { }` block of `build.gradle.kts`. Place it among the existing `testImplementation` declarations at lines 49-53 (no specific ordering required — ktlint does not enforce sort order within the dependencies block). Do NOT add it to `implementation` (this is a test-only dependency per ADR-4 hierarchy). Do NOT introduce a new test source set (`testIntegration` etc.) — the project uses a single `src/test/kotlin` source set. Do NOT introduce a version catalog or `libs.versions.toml` (this project inlines versions as literals; respect that convention). The version `4.12.0` is non-negotiable: it must match the OkHttp version at build.gradle.kts:31; mismatched versions silently clash on shared transitive okio classes (per D-04 and RESEARCH.md Risks #1).
  </action>
  <verify>
    <automated>grep -c 'com.squareup.okhttp3:mockwebserver:4.12.0' build.gradle.kts | grep -q '^1$' &amp;&amp; ./gradlew dependencies --configuration testRuntimeClasspath | grep -i mockwebserver</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'mockwebserver:4.12.0' build.gradle.kts` exits 0.
    - `./gradlew dependencies --configuration testRuntimeClasspath | grep -i mockwebserver` prints a line containing `com.squareup.okhttp3:mockwebserver:4.12.0`.
    - The new line lives inside the `dependencies { }` block (not at file root, not in a `plugins` or `repositories` block).
    - The line uses the literal version `4.12.0`, not a Gradle version-property reference or a version-catalog reference.
    - `./gradlew ktlintCheck` exits 0 (no formatting regression introduced by the edit).
    - No other lines in `build.gradle.kts` are modified (compare a diff: exactly one additive line, no reorderings).
  </acceptance_criteria>
  <done>
    MockWebServer 4.12.0 is on the test classpath. Subsequent tasks T1 and T2 (which `import okhttp3.mockwebserver.MockWebServer` / `MockResponse`) will compile.
  </done>
</task>

<task type="auto">
  <name>Task 1: Write PerplexityBackendFactoryTest.kt (PPLX-02, PPLX-03)</name>
  <wave>1</wave>
  <depends_on>[Task 0]</depends_on>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/backends/perplexity/PerplexityBackendFactoryTest.kt</files>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/backends/perplexity/PerplexityBackendFactory.kt (entire file — under-test factory; understand the constructor knobs it sets: `chatCompletionsBasePath = "/chat/completions"`, `supportsJsonObjectResponseFormat = false`, `streaming = true`)
    - src/main/kotlin/com/six2dez/burp/aiagent/backends/openai/OpenAiCompatibleBackend.kt lines 42-47 (constructor knobs), 183-187 (`response_format` gate the test must observe absent), 200-274 (transport-branch selection; the test relies on the default `transport = null` → OkHttp branch), 341-381 (SSE response handling; this is why MockResponse body MUST end with `data: [DONE]\n\n`), 408-418 (`buildChatCompletionsUrl`; the URL edge cases)
    - src/main/kotlin/com/six2dez/burp/aiagent/backends/BackendTypes.kt lines 5-20 (`BackendLaunchConfig` data class — the constructor-arg shape the test calls into)
    - src/test/kotlin/com/six2dez/burp/aiagent/backends/http/CircuitBreakerTest.kt (entire file — structural analog: CamelCase test method names, JUnit Jupiter imports, `Assertions.*` static imports — this is the style the new file MUST follow)
    - .planning/phases/01-perplexity-backend-audit/01-CONTEXT.md (locked decisions D-04 wire-level, D-05 fast-suite, D-07 file placement)
    - .planning/phases/01-perplexity-backend-audit/01-RESEARCH.md §"Pattern 1: MockWebServer wire-capture for one chat call" (lines 181-280) — canonical class scaffold, §"Q4 buildChatCompletionsUrl edge cases" (lines 418-470) — URL parametrisation table, §"Common Pitfalls → Pitfall 1 (SSE not terminated → hang)" and §"Pitfall 2 (sharedClient cache)" and §"Pitfall 5 (TestSettings has STRICT privacy mode)"
    - .planning/phases/01-perplexity-backend-audit/01-PATTERNS.md §"2. PerplexityBackendFactoryTest.kt" (lines 57-249) — canonical scaffold, URL edge-case table, "why no TestSettings here" rationale
    - .planning/codebase/CONVENTIONS.md §"HTTP Backend (subclass of OpenAiCompatibleBackend)"
    - .planning/codebase/TESTING.md §"Known Coverage Gaps → PerplexityBackend HTTP path" — the gap this task closes
  </read_first>
  <action>
    Create a NEW directory `src/test/kotlin/com/six2dez/burp/aiagent/backends/perplexity/` and inside it a NEW Kotlin file `PerplexityBackendFactoryTest.kt` in package `com.six2dez.burp.aiagent.backends.perplexity`.

    Use JUnit Jupiter (`org.junit.jupiter.api.Test`, `Assertions.assertEquals`, `Assertions.assertFalse`, `Assertions.assertTrue`, `BeforeEach`, `AfterEach`). Use CamelCase test method names (NOT backticked) per CONVENTIONS.md ktlint preference. Do NOT use `kotlin.test.*` imports.

    The class holds a `private lateinit var server: MockWebServer` plus a `private val mapper = ObjectMapper().registerKotlinModule()`. `@BeforeEach` starts a fresh `MockWebServer()`; `@AfterEach` calls `server.shutdown()`. Do NOT call `HttpBackendSupport.shutdownSharedClients()` anywhere — Risks #4.

    Provide a private helper `streamedResponse(): MockResponse` returning the canonical Perplexity-shaped SSE body — `Content-Type: text/event-stream`, body literal exactly `"data: {\"choices\":[{\"delta\":{\"content\":\"ok\"}}]}\n\ndata: [DONE]\n\n"` — terminated with `data: [DONE]\n\n` (mandatory per the SSE reader at `OpenAiCompatibleBackend.kt:357-362`; missing the sentinel causes a ~30 s OkHttp timeout hang).

    Implement FIVE `@Test` methods covering PPLX-02 URL edge cases (including the already-resolved /chat/completions path) and PPLX-03 absence of `response_format`:
    1. `targetsChatCompletionsWithoutV1PrefixOnBareHost` — baseUrl = `server.url("/").toString().trimEnd('/')`; assert `recorded.path == "/chat/completions"` and `recorded.method == "POST"`.
    2. `handlesTrailingSlashInUserConfiguredUrl` — baseUrl = `server.url("/").toString()` (NOT trimmed); same expected `recorded.path == "/chat/completions"`.
    3. `respectsExplicitV1UserUrl` — baseUrl = `server.url("/v1").toString().trimEnd('/')`; expected `recorded.path == "/v1/chat/completions"` (user-typed `/v1` is honoured per RESEARCH.md Q4).
    4. `omitsResponseFormatEvenWhenJsonModeRequested` — baseUrl = bare host, `jsonMode = true` passed to `connection.send(...)`; parse `recorded.body.readUtf8()` via the mapper, assert `body.has("response_format") == false`, `body.has("model") == true`, `body.has("messages") == true`.
    5. `doesNotDoubleAppendWhenUrlAlreadyHasChatCompletions` — baseUrl = `server.url("/chat/completions").toString().trimEnd('/')` (simulates a user who already typed the full chat-completions path); enqueue ONE `streamedResponse()`; send with `jsonMode = false`; assert `recorded.path == "/chat/completions"` (NOT `"/chat/completions/chat/completions"`). This exercises the `if (lower.endsWith("/chat/completions")) return trimmed` branch at `OpenAiCompatibleBackend.kt:411` — the precise regression PPLX-02 must lock against per CONTEXT.md `<specifics>` line 118 and PATTERNS.md §2 URL edge-case table row 4.

    For EACH test method: enqueue ONE `streamedResponse()` per `@Test`; obtain the backend via `PerplexityBackendFactory().create()`; call `backend.launch(BackendLaunchConfig(backendId = "perplexity", displayName = "Perplexity", baseUrl = <url>, model = "sonar", headers = mapOf("Authorization" to "Bearer pplx-test"), requestTimeoutSeconds = 30L))` — `transport` defaults to `null` (OkHttp branch per RESEARCH.md Risks #6); `connection.send(text = "hello", onChunk = {}, onComplete = { done.countDown() }, jsonMode = <true|false depending on test>)`. Use `java.util.concurrent.CountDownLatch(1)` and `done.await(5, TimeUnit.SECONDS)` (assert it returned true). Then `server.takeRequest(1, TimeUnit.SECONDS) ?: error("no request")` to capture and assert.

    Do NOT use `TestSettings.baselineSettings()` in this file — the test fires `backend.launch(BackendLaunchConfig(...))` directly and never instantiates an `AgentSettings`. Pulling in `TestSettings` would be dead code (PATTERNS.md §2 "Why no TestSettings here").

    Do NOT use reflection on private fields (`chatCompletionsBasePath`, `supportsJsonObjectResponseFormat`) — D-04 locks wire-level assertions only.

    Do NOT add 401/429/error-path tests — those are out of scope (already covered by `CircuitBreakerTest`, `BackendHealthCheckTest`).

    Do NOT add temperature/top_p/max_tokens assertions — these are optional payload fields and would make the test brittle (anti-pattern #3 in RESEARCH.md "Common Pitfalls").
  </action>
  <verify>
    <automated>./gradlew test -PexcludeHeavyTests=true --tests "*PerplexityBackendFactoryTest"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exactly `src/test/kotlin/com/six2dez/burp/aiagent/backends/perplexity/PerplexityBackendFactoryTest.kt`.
    - File declares `package com.six2dez.burp.aiagent.backends.perplexity`.
    - File contains a class `PerplexityBackendFactoryTest` with at least five `@Test`-annotated methods named: `targetsChatCompletionsWithoutV1PrefixOnBareHost`, `handlesTrailingSlashInUserConfiguredUrl`, `respectsExplicitV1UserUrl`, `omitsResponseFormatEvenWhenJsonModeRequested`, `doesNotDoubleAppendWhenUrlAlreadyHasChatCompletions`.
    - Test method `doesNotDoubleAppendWhenUrlAlreadyHasChatCompletions` exists in `PerplexityBackendFactoryTest.kt` and asserts `recorded.path == "/chat/completions"` when the input `baseUrl` already ends in `/chat/completions` (no double-append). Verify: `grep -c 'doesNotDoubleAppendWhenUrlAlreadyHasChatCompletions' src/test/kotlin/com/six2dez/burp/aiagent/backends/perplexity/PerplexityBackendFactoryTest.kt` returns 1.
    - At least one `@Test` body contains a literal `assertEquals("/chat/completions", recorded.path)` (or equivalent expression where the left side is the literal `"/chat/completions"` and the right side reads `path` off a `RecordedRequest`).
    - The `omitsResponseFormatEvenWhenJsonModeRequested` test body parses the request body via `ObjectMapper.readTree` and asserts via `assertFalse(<json>.has("response_format"))` (or equivalent boolean assertion that fails when the key is present).
    - The test class uses JUnit Jupiter imports only (`org.junit.jupiter.api.*`); no `kotlin.test.*` imports.
    - The class uses CamelCase method names (no backticks). Verify: `grep -E '^[[:space:]]*fun \`' src/test/kotlin/com/six2dez/burp/aiagent/backends/perplexity/PerplexityBackendFactoryTest.kt` returns no matches.
    - The MockResponse SSE body literal contains both `data: {\"choices\":[{\"delta\":{\"content\":\"ok\"}}]}\n\n` AND `data: [DONE]\n\n`.
    - `@BeforeEach` calls `server.start()`; `@AfterEach` calls `server.shutdown()`.
    - The file does NOT call `HttpBackendSupport.shutdownSharedClients()` anywhere. Verify: `grep -c 'shutdownSharedClients' <file>` returns 0.
    - The file does NOT use `TestSettings.baselineSettings()`. Verify: `grep -c 'TestSettings' <file>` returns 0.
    - The file does NOT use Java reflection (`getDeclaredField`, `isAccessible`). Verify: `grep -cE 'getDeclaredField|isAccessible' <file>` returns 0.
    - `./gradlew test -PexcludeHeavyTests=true --tests "*PerplexityBackendFactoryTest"` exits 0 and reports five tests passed.
    - Each test completes within 5 seconds (the CountDownLatch timeout) — no test hangs on missing SSE termination.
    - `./gradlew ktlintCheck` exits 0 on this file.
  </acceptance_criteria>
  <done>
    PPLX-02 (URL `/chat/completions` with no `/v1`) and PPLX-03 (no `response_format` when `jsonMode = true`) are locked by passing wire-level assertions on a captured `RecordedRequest`. The four URL edge cases (bare host, trailing slash, user-typed `/v1`, already-resolved `/chat/completions`) plus the JSON-mode assertion — five tests total — all green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write OpenAiCompatibleBackendDefaultsTest.kt (PPLX-04)</name>
  <wave>1</wave>
  <depends_on>[Task 0]</depends_on>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/backends/openai/OpenAiCompatibleBackendDefaultsTest.kt</files>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/backends/openai/OpenAiCompatibleBackend.kt lines 29-48 (constructor defaults under audit: `chatCompletionsBasePath = "/v1/chat/completions"`, `supportsJsonObjectResponseFormat = true`, `streaming = false`), 185-187 (`response_format` gate — the test must observe it PRESENT), 200-274 (transport branch — default `transport = null` → OkHttp branch), 311-339 (`handleNonStreamingResponse` — this is why MockResponse for this test is `application/json`, NOT SSE), 408-418 (`buildChatCompletionsUrl`)
    - src/main/kotlin/com/six2dez/burp/aiagent/backends/nvidia/NvidiaNimBackendFactory.kt lines 17-39 (proof that NVIDIA NIM does NOT override `chatCompletionsBasePath` or `supportsJsonObjectResponseFormat` — the default-constructor path IS NVIDIA's path; this is why one default test covers both NIM and Generic OpenAI-compatible per PPLX-04)
    - src/main/kotlin/com/six2dez/burp/aiagent/backends/openai/OpenAiCompatibleBackendFactory.kt (9 lines — Generic OpenAI-compatible factory; same defaults, no overrides)
    - src/main/kotlin/com/six2dez/burp/aiagent/backends/BackendTypes.kt lines 5-20 (`BackendLaunchConfig`)
    - src/test/kotlin/com/six2dez/burp/aiagent/backends/http/CircuitBreakerTest.kt (entire file — structural analog identical to T1)
    - .planning/phases/01-perplexity-backend-audit/01-CONTEXT.md (D-04, D-05, D-07)
    - .planning/phases/01-perplexity-backend-audit/01-RESEARCH.md §"Pattern 3: Backwards-compat assertion for OpenAiCompatibleBackend" (lines 314-347) — exact two-test scaffold
    - .planning/phases/01-perplexity-backend-audit/01-PATTERNS.md §"3. OpenAiCompatibleBackendDefaultsTest.kt" (lines 251-346) — "CAUTION — streaming flag differs from file #2" callout: the default constructor leaves `streaming = false`; MockResponse for this test must be a plain JSON body, NOT SSE
  </read_first>
  <action>
    Create a NEW directory `src/test/kotlin/com/six2dez/burp/aiagent/backends/openai/` and inside it a NEW Kotlin file `OpenAiCompatibleBackendDefaultsTest.kt` in package `com.six2dez.burp.aiagent.backends.openai`.

    Mirror the harness of T1 (MockWebServer per-test, JUnit Jupiter imports, CamelCase test names, `private val mapper = ObjectMapper().registerKotlinModule()`, `@BeforeEach { server = MockWebServer(); server.start() }`, `@AfterEach { server.shutdown() }`) — BUT swap the response body for a non-streaming JSON body. Reason: the default constructor of `OpenAiCompatibleBackend` leaves `streaming = false` at `OpenAiCompatibleBackend.kt:38`, so the connection takes the non-streaming branch at `OpenAiCompatibleBackend.kt:311-339` which parses `choices[0].message.content` from a plain JSON body. If you serve SSE here, the JSON parser sees no `choices` field and the connection completes with `IllegalStateException("response content was empty")` — the latch still trips, so the test silently goes red in a confusing way.

    Provide a private helper `nonStreamingJsonResponse(): MockResponse` returning `Content-Type: application/json` with body literal exactly `{"choices":[{"message":{"role":"assistant","content":"ok"}}]}` (one line, no SSE prefixes, no trailing `[DONE]`).

    Implement TWO `@Test` methods:
    1. `defaultsKeepV1PrefixOnBareHost` — construct `OpenAiCompatibleBackend(id = "test-default", displayName = "Default")` with NO `chatCompletionsBasePath` or `supportsJsonObjectResponseFormat` arguments (rely on data-class defaults at lines 44 and 47); baseUrl = `server.url("/").toString().trimEnd('/')`; model = `"gpt-4o"` (free-form placeholder); send with `jsonMode = false`; assert `recorded.path == "/v1/chat/completions"`.
    2. `defaultsEmitResponseFormatWhenJsonModeRequested` — same backend construction, baseUrl bare host, send with `jsonMode = true`; parse `recorded.body.readUtf8()` via mapper; assert `body.get("response_format")` is non-null and `body.get("response_format").get("type").asText() == "json_object"`.

    Both tests use `BackendLaunchConfig(backendId = "test-default", displayName = "Default", baseUrl = <url>, model = "gpt-4o", headers = emptyMap(), requestTimeoutSeconds = 30L)` and the `CountDownLatch + onComplete` pattern with a 5-second await.

    Do NOT use `TestSettings.baselineSettings()` (same rationale as T1).

    Do NOT use reflection on `chatCompletionsBasePath` / `supportsJsonObjectResponseFormat` (D-04).

    Do NOT add NVIDIA-NIM-factory-specific tests — the default-constructor `OpenAiCompatibleBackend` IS NVIDIA's path per `NvidiaNimBackendFactory.kt:17-39`.

    Do NOT add an Authorization header — the Generic OpenAI-compatible default does not require one for the test to compile/run; `headers = emptyMap()` is sufficient.
  </action>
  <verify>
    <automated>./gradlew test -PexcludeHeavyTests=true --tests "*OpenAiCompatibleBackendDefaultsTest"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exactly `src/test/kotlin/com/six2dez/burp/aiagent/backends/openai/OpenAiCompatibleBackendDefaultsTest.kt`.
    - File declares `package com.six2dez.burp.aiagent.backends.openai`.
    - File contains a class `OpenAiCompatibleBackendDefaultsTest` with at least two `@Test`-annotated methods named: `defaultsKeepV1PrefixOnBareHost`, `defaultsEmitResponseFormatWhenJsonModeRequested`.
    - The `defaultsKeepV1PrefixOnBareHost` test body contains a literal `assertEquals("/v1/chat/completions", recorded.path)` (or equivalent expression where the left side is the literal `"/v1/chat/completions"` and the right side reads `path` off a `RecordedRequest`).
    - The `defaultsEmitResponseFormatWhenJsonModeRequested` test body parses the request body via `ObjectMapper.readTree` and asserts that `<json>.get("response_format").get("type").asText() == "json_object"` (or equivalent path-extract assertion that fails when the key is absent or has a non-`"json_object"` value).
    - Backend is constructed with `OpenAiCompatibleBackend(id = ..., displayName = ...)` and NO `chatCompletionsBasePath` / `supportsJsonObjectResponseFormat` keyword arguments. Verify: `grep -c 'chatCompletionsBasePath' <file>` returns 0; `grep -c 'supportsJsonObjectResponseFormat' <file>` returns 0.
    - MockResponse body literal is `{"choices":[{"message":{"role":"assistant","content":"ok"}}]}` (NOT an SSE body). Verify: `grep -c 'text/event-stream' <file>` returns 0; `grep -c 'application/json' <file>` is at least 1.
    - File uses JUnit Jupiter imports only; CamelCase method names (no backticks).
    - File does NOT call `HttpBackendSupport.shutdownSharedClients()`; does NOT use `TestSettings`; does NOT use Java reflection.
    - `./gradlew test -PexcludeHeavyTests=true --tests "*OpenAiCompatibleBackendDefaultsTest"` exits 0 and reports two tests passed.
    - `./gradlew ktlintCheck` exits 0 on this file.
  </acceptance_criteria>
  <done>
    PPLX-04 is locked: the default-constructor `OpenAiCompatibleBackend` (the path NVIDIA NIM and Generic OpenAI-compatible take) still targets `/v1/chat/completions` and still emits `response_format: {"type":"json_object"}` when `jsonMode = true`. Backwards compatibility is durable against future internal refactors because the assertion is on the captured `RecordedRequest`, not on private fields.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend AgentSettingsMigrationTest with PPLX-05 @Test</name>
  <wave>1</wave>
  <depends_on>[]</depends_on>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/config/AgentSettingsMigrationTest.kt</files>
  <read_first>
    - src/test/kotlin/com/six2dez/burp/aiagent/config/AgentSettingsMigrationTest.kt (ENTIRE file — current state including the existing `load_v2InstallLoadsEmptyCustomPromptLibraryAndStampsV3` at lines 42-52, the `apiWith` helper at lines 54-58, the `InMemoryPrefs` private nested class at lines 60-88, and all imports). The new `@Test` MUST reuse these in-file helpers; do NOT extract `InMemoryPrefs` into a shared fixture in this phase.
    - src/main/kotlin/com/six2dez/burp/aiagent/config/AgentSettings.kt lines 57-61 (the five additive `perplexity*` fields with defaults), 257-266 (the load path — note the `.coerceIn(30, 3600)` clamp on `perplexityTimeoutSeconds`), 691-695 (preference key constants `KEY_PERPLEXITY_*`), 780 (`CURRENT_SETTINGS_SCHEMA_VERSION = 3`), 822-824 (`defaultPerplexityUrl()` and `defaultPerplexityTimeoutSeconds()` helpers)
    - src/main/kotlin/com/six2dez/burp/aiagent/config/Defaults.kt line 41 (`CLI_PROCESS_TIMEOUT_SECONDS = 120` — this is the value `defaultPerplexityTimeoutSeconds()` returns; the assertion `assertEquals(120, loaded.perplexityTimeoutSeconds)` is therefore correct, and 120 is within the `coerceIn(30, 3600)` range)
    - src/test/kotlin/com/six2dez/burp/aiagent/TestSettings.kt (lines 1-30 — `baselineSettings()`; the new test does NOT need this, but understanding the AgentSettings construction shape is useful)
    - .planning/phases/01-perplexity-backend-audit/01-CONTEXT.md (D-03 no schema bump, D-07 placement)
    - .planning/phases/01-perplexity-backend-audit/01-RESEARCH.md §"Q6 AgentSettings persistence path" (lines 491-545) — exact assertion targets, §"Pattern 2: Extend AgentSettingsMigrationTest for PPLX-05" (lines 281-313) — canonical new-test scaffold
    - .planning/phases/01-perplexity-backend-audit/01-PATTERNS.md §"4. AgentSettingsMigrationTest.kt (EXTEND)" (lines 348-479) — three excerpts (the existing analog test, the `InMemoryPrefs` class, the `apiWith` helper) plus the new-method body and naming-convention notes
  </read_first>
  <action>
    EXTEND `src/test/kotlin/com/six2dez/burp/aiagent/config/AgentSettingsMigrationTest.kt` by adding ONE new `@Test`-annotated method named `load_v06xPreferencesYieldSafePerplexityDefaultsAndSchemaStaysV3`. Insert the new method after the existing `load_v2InstallLoadsEmptyCustomPromptLibraryAndStampsV3` (currently at lines 42-52) and BEFORE the `private fun apiWith` (currently at lines 54-58). Match the file's existing in-file naming style — `load_…` snake_case prefix + CamelCase suffix — even though new files in this phase prefer pure CamelCase; in-file consistency wins.

    The method body must:
    1. Construct a `val prefs = InMemoryPrefs()` (the existing private nested class — DO NOT extract).
    2. Set `prefs.integers["settings.schema.version"] = 3` (simulates a v0.6.x install that ran the previous schema-3 migration but predates the additive Perplexity fields).
    3. Set NO `perplexity.*` string/integer keys (test scenario is "v0.6.x prefs file has zero perplexity entries; defaults must apply"). Do NOT populate any of `perplexity.url`, `perplexity.model`, `perplexity.apiKey`, `perplexity.headers`, `perplexity.timeoutSeconds`.
    4. Build the repo via `val repo = AgentSettingsRepository(apiWith(prefs.mock))` (reuse the existing `apiWith` helper).
    5. Call `val loaded = repo.load()`.
    6. Assert FIVE field values and the schema marker:
       - `assertEquals("https://api.perplexity.ai", loaded.perplexityUrl)`
       - `assertEquals("", loaded.perplexityModel)`
       - `assertEquals("", loaded.perplexityApiKey)`
       - `assertEquals("", loaded.perplexityHeaders)`
       - `assertEquals(120, loaded.perplexityTimeoutSeconds)` (literal 120 — that is `Defaults.CLI_PROCESS_TIMEOUT_SECONDS` returned by `defaultPerplexityTimeoutSeconds()`)
       - `assertEquals(3, prefs.integers["settings.schema.version"])` (post-load — proves NO bump; D-03 contract)

    Use `org.junit.jupiter.api.Test` and `org.junit.jupiter.api.Assertions.assertEquals` — both ALREADY imported at lines 5 and 7 of the existing file. Do NOT add `kotlin.test.Test` or `kotlin.test.assertEquals` imports (the existing file uses Jupiter exclusively).

    Do NOT touch any other test in the file; do NOT modify the `InMemoryPrefs` class, the `apiWith` helper, or the existing `@Test` methods. Do NOT add a new `@BeforeEach` (the existing file has none; each test constructs its own `InMemoryPrefs` per the in-file convention).

    Do NOT alter `CURRENT_SETTINGS_SCHEMA_VERSION` in production code; the assertion `assertEquals(3, ...)` proves the contract is upheld without code change.

    The total added LOC should be ~12-15 (one `@Test` method body plus the existing imports already cover everything else).
  </action>
  <verify>
    <automated>./gradlew test -PexcludeHeavyTests=true --tests "*AgentSettingsMigrationTest"</automated>
  </verify>
  <acceptance_criteria>
    - File still exists at `src/test/kotlin/com/six2dez/burp/aiagent/config/AgentSettingsMigrationTest.kt` (no rename).
    - File contains a NEW `@Test`-annotated method named exactly `load_v06xPreferencesYieldSafePerplexityDefaultsAndSchemaStaysV3` (count is +1 vs. the pre-task baseline). Verify: `grep -c 'fun load_v06xPreferencesYieldSafePerplexityDefaultsAndSchemaStaysV3' <file>` returns 1.
    - The new method body contains literal expressions: `assertEquals("https://api.perplexity.ai", loaded.perplexityUrl)`, `assertEquals("", loaded.perplexityModel)`, `assertEquals("", loaded.perplexityApiKey)`, `assertEquals("", loaded.perplexityHeaders)`, `assertEquals(120, loaded.perplexityTimeoutSeconds)`, `assertEquals(3, prefs.integers["settings.schema.version"])`.
    - The new method body sets `prefs.integers["settings.schema.version"] = 3` BEFORE calling `repo.load()`.
    - The new method body does NOT pre-populate any `perplexity.*` key. Verify: `grep -E 'prefs\\.(strings|integers)\\["perplexity' <file>` returns no matches inside the new method.
    - File imports remain Jupiter-only; no `kotlin.test.*` imports introduced. Verify: `grep -cE 'import kotlin\\.test' <file>` returns 0.
    - The existing `load_v2InstallLoadsEmptyCustomPromptLibraryAndStampsV3` test, the `InMemoryPrefs` private class, and the `apiWith` helper are unchanged byte-for-byte (the new test is purely additive). Verify by diff: the only changes are the insertion of the new `@Test` method between the existing test and the `apiWith` helper, plus possibly one blank line for ktlint.
    - `./gradlew test -PexcludeHeavyTests=true --tests "*AgentSettingsMigrationTest"` exits 0 and reports the new test plus all pre-existing tests in the class passed (total test count for the class is `<pre-task count> + 1`).
    - `./gradlew ktlintCheck` exits 0 on this file.
  </acceptance_criteria>
  <done>
    PPLX-05 is locked: a v0.6.x preferences file (schema marker 3, zero `perplexity.*` keys) deserialises into an `AgentSettings` with the five new Perplexity fields at their safe defaults, and the post-load schema marker is still `3` (no `migrateIfNeeded` bump, per D-03). PPLX-01's defaults are indirectly re-asserted at the load boundary (the Perplexity card reads from these same defaults).
  </done>
</task>

<task type="auto">
  <name>Task 4: Full-phase verification + sign-off artefacts</name>
  <wave>2</wave>
  <depends_on>[Task 1, Task 2, Task 3]</depends_on>
  <files>
    .planning/phases/01-perplexity-backend-audit/01-VERIFICATION.md,
    .planning/phases/01-perplexity-backend-audit/01-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/01-perplexity-backend-audit/01-CONTEXT.md §"Implementation Decisions → D-06 (one-time manual smoke)" and §"D-08 (two KNOWN-WORDING-GAP handoffs to Phase 5)" and §"<specifics> → Manual smoke (D-06)" (defines the six fields to record: API key source, date, model, request count, observed streamed completion, SHA-256 of response body)
    - .planning/phases/01-perplexity-backend-audit/01-VALIDATION.md (entire file — note the `nyquist_compliant: false` flag at line 4 and the §"Validation Sign-Off" checklist at lines 77-86 that this task flips to `true`)
    - .planning/phases/01-perplexity-backend-audit/01-RESEARCH.md §"Validation Architecture" (lines 624-662) and §"Recommended Plan Structure → Wave 2" (lines 844-848) — the Gradle invocations and the sign-off artefact set
    - .planning/ROADMAP.md §"Phase 1 → Success Criteria" — five success criteria; #1 (Sonar-family pre-population wording) and #5 (manual end-to-end) are the two D-08/D-06 anchors recorded in `01-VERIFICATION.md`
    - SPEC.md §4.4 (Pluggable AI backends — the section that omits Perplexity, recorded as the second KNOWN-WORDING-GAP for Phase 5 handoff)
  </read_first>
  <action>
    Two-step verification.

    STEP A — automated verification of T0+T1+T2+T3 output:
    1. Run `./gradlew test -PexcludeHeavyTests=true` (the full fast suite) — must exit 0. This is the cross-task Nyquist gate.
    2. Run `./gradlew ktlintCheck` — must exit 0.
    3. Run `./gradlew compileKotlin compileTestKotlin` — must exit 0 (proves zero production code drift; sanity check given Phase 1 is test-only).
    Do NOT run `./gradlew test` without `-PexcludeHeavyTests=true` (the heavy suite is out of scope for this fast-suite-only phase per D-05).

    STEP B — write `.planning/phases/01-perplexity-backend-audit/01-VERIFICATION.md` (NEW file) with three sections:

    Section 1: `## D-06 — Manual end-to-end Perplexity smoke (PPLX-02 + ROADMAP §Phase 1 SC#5)`. Record SIX fields (one row each, key: value):
    - `api_key_source:` maintainer's own paid Perplexity key (NOT in CI, NOT committed; literal text `maintainer-personal-pplx-key` or similar non-secret placeholder — the actual key is never written here)
    - `date:` the ISO-8601 date the maintainer ran the smoke
    - `model:` the Sonar-family model used (e.g. `sonar`, `sonar-pro`)
    - `request_count:` integer (e.g. `1`)
    - `observed_streamed_completion:` short one-line description (e.g. `streamed delta chunks rendered in chat panel, final assistant message rendered without truncation`)
    - `response_body_sha256:` 64-char hex SHA-256 of the captured response body — the maintainer computes this from the audit log when verbose mode is enabled (per ADR-7)
    If the maintainer has not yet run the smoke at the moment T4 executes, the executor stamps `pending — recorded on first maintainer pass` next to each field; the task still passes as long as the section exists with all six field keys and the `[ ] Maintainer to fill` checkbox is present at the top.

    Section 2: `## D-08 — Known wording gaps handed off to Phase 5 (Documentation Refresh)`. Record TWO entries, each one starting with the literal token `KNOWN-WORDING-GAP:` on its own line:
    - `KNOWN-WORDING-GAP: ROADMAP §Phase 1 SC#1 — tooltip suggests Sonar-family names; field is not pre-filled (Phase 5 to clarify the wording)`
    - `KNOWN-WORDING-GAP: SPEC §4.4 — Perplexity not yet listed in the pluggable HTTP backend table (Phase 5 to add Perplexity)`

    Section 3: `## Automated verification summary` — record the three Gradle invocations and their exit codes from STEP A (the literal command, the timestamp of execution, and `exit 0`).

    STEP C — update `01-VALIDATION.md` frontmatter (use the Edit tool, do NOT rewrite the file):
    - Change `nyquist_compliant: false` to `nyquist_compliant: true` (line 4)
    - Change `wave_0_complete: false` to `wave_0_complete: true` (line 5; Wave 0 = T0 dep, completed when T1/T2 compile)
    - Change `status: draft` to `status: green` (line 3)
    - Update the §"Validation Sign-Off → Approval:" line from `pending` to `green` (line 86 area)
    - Update the §"Per-Task Verification Map" Status column entries for 01-01-00 through 01-01-04 from `⬜ pending` to `✅ green` (lines 43-47)

    Do NOT edit any other field in the frontmatter; do NOT regenerate the file; do NOT touch the `## Manual-Only Verifications` table (the table is descriptive, the live record is in `01-VERIFICATION.md`).

    Do NOT commit any real API key, prompt body, or response body to `01-VERIFICATION.md`. Only metadata (hash, date, model name) lives there per ADR-7.

    Do NOT close out PPLX-01..05 in REQUIREMENTS.md or mark the phase complete in ROADMAP.md — those are downstream operations owned by `/gsd-verify-work` / `/gsd-complete-phase`, not by this plan.
  </action>
  <verify>
    <automated>./gradlew test -PexcludeHeavyTests=true &amp;&amp; ./gradlew ktlintCheck &amp;&amp; test -f .planning/phases/01-perplexity-backend-audit/01-VERIFICATION.md &amp;&amp; grep -c 'KNOWN-WORDING-GAP' .planning/phases/01-perplexity-backend-audit/01-VERIFICATION.md | grep -q '^[2-9]' &amp;&amp; grep -v '^#' .planning/phases/01-perplexity-backend-audit/01-VALIDATION.md | grep -q 'nyquist_compliant: true'</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test -PexcludeHeavyTests=true` exits 0; the test report lists ALL of: `PerplexityBackendFactoryTest` (5 tests), `OpenAiCompatibleBackendDefaultsTest` (2 tests), `AgentSettingsMigrationTest` (existing count + 1 new). No skipped/aborted tests in the Perplexity classes.
    - `./gradlew ktlintCheck` exits 0 across the whole project (no formatting regressions introduced by any task).
    - `./gradlew compileKotlin compileTestKotlin` exits 0 (production code compiles unchanged; tests compile against new MockWebServer dep).
    - File exists at `.planning/phases/01-perplexity-backend-audit/01-VERIFICATION.md`.
    - VERIFICATION.md contains a section heading exactly `## D-06 — Manual end-to-end Perplexity smoke` (or equivalent; the literal substring `D-06` must appear). Verify: `grep -c 'D-06' .planning/phases/01-perplexity-backend-audit/01-VERIFICATION.md` returns at least 1.
    - VERIFICATION.md contains all six D-06 field keys (case-insensitive): `api_key_source`, `date`, `model`, `request_count`, `observed_streamed_completion`, `response_body_sha256`. Verify: `grep -ciE 'api_key_source|date|model|request_count|observed_streamed_completion|response_body_sha256' .planning/phases/01-perplexity-backend-audit/01-VERIFICATION.md` returns at least 6.
    - VERIFICATION.md contains at least two lines starting with `KNOWN-WORDING-GAP:`. Verify: `grep -cE '^[[:space:]]*-?[[:space:]]*KNOWN-WORDING-GAP:' .planning/phases/01-perplexity-backend-audit/01-VERIFICATION.md` returns at least 2.
    - One KNOWN-WORDING-GAP entry references `ROADMAP §Phase 1 SC#1` (or `ROADMAP` + `SC#1` + `Sonar`); the other references `SPEC §4.4` (or `SPEC` + `4.4` + `Perplexity not`).
    - VERIFICATION.md contains the section heading `## Automated verification summary` with the three Gradle invocations recorded and their exit codes.
    - VERIFICATION.md contains NO 64-character SHA-256 hex string unless it is preceded by the literal `response_body_sha256:` (defensive check: real API responses must only be hashed, not embedded; the live SHA may be a placeholder until the maintainer runs the smoke).
    - VERIFICATION.md contains NO substring matching the Perplexity API key pattern `pplx-[A-Za-z0-9]{40,}` (defensive: never commit a real key). Verify: `grep -cE 'pplx-[A-Za-z0-9]{40}' .planning/phases/01-perplexity-backend-audit/01-VERIFICATION.md` returns 0.
    - `.planning/phases/01-perplexity-backend-audit/01-VALIDATION.md` frontmatter line `nyquist_compliant:` reads `true` (was `false`). Verify: `grep -E '^nyquist_compliant:' .planning/phases/01-perplexity-backend-audit/01-VALIDATION.md` reads `nyquist_compliant: true`.
    - `.planning/phases/01-perplexity-backend-audit/01-VALIDATION.md` frontmatter line `wave_0_complete:` reads `true`.
    - `.planning/phases/01-perplexity-backend-audit/01-VALIDATION.md` frontmatter line `status:` reads `green` (was `draft`).
    - The Per-Task Verification Map in `01-VALIDATION.md` shows ✅ green for all five task rows (01-01-00 through 01-01-04). Verify: `grep -c '✅ green' .planning/phases/01-perplexity-backend-audit/01-VALIDATION.md` returns at least 5.
  </acceptance_criteria>
  <done>
    Phase 1 is green and ready for `/gsd-verify-work` → `/gsd-complete-phase`. The seven must-have truths in this plan's frontmatter are all observable: MockWebServer is on the test classpath, three test artefacts exist, full fast suite + ktlint exit 0, and `01-VERIFICATION.md` records the D-06 manual smoke fields plus the two D-08 KNOWN-WORDING-GAP handoffs to Phase 5.
  </done>
</task>

</tasks>

<verification>
Phase-level verification (executed by T4):

1. **Test suite green:** `./gradlew test -PexcludeHeavyTests=true` exits 0. The three target test classes (`PerplexityBackendFactoryTest`, `OpenAiCompatibleBackendDefaultsTest`, `AgentSettingsMigrationTest`) all report passing tests with no skips/aborts.

2. **Lint green:** `./gradlew ktlintCheck` exits 0 across the whole project.

3. **Compile clean:** `./gradlew compileKotlin compileTestKotlin` exits 0 — proves no accidental production code drift.

4. **MockWebServer on classpath:** `./gradlew dependencies --configuration testRuntimeClasspath | grep mockwebserver` shows `4.12.0`.

5. **Goal-backward truths verified:**
   - Truth 1 (MockWebServer dep) ← acceptance for T0
   - Truth 2 (PerplexityBackendFactoryTest file) ← acceptance for T1
   - Truth 3 (OpenAiCompatibleBackendDefaultsTest file) ← acceptance for T2
   - Truth 4 (AgentSettingsMigrationTest extension) ← acceptance for T3
   - Truth 5 (full fast suite green) ← acceptance for T4
   - Truth 6 (ktlintCheck green) ← acceptance for T4
   - Truth 7 (VERIFICATION.md records D-06 + D-08 handoffs) ← acceptance for T4

6. **Threat model dispositions verified:**
   - T-01 (CI secrets): No real key in any test or VERIFICATION.md (grep gate in T4 acceptance).
   - T-02 (token log leakage): No assertion on log output; no `BackendDiagnostics` mocking.
   - T-03 (sharedClient cache pollution): No `shutdownSharedClients()` calls in any new test (grep gates in T1 and T2 acceptance).
   - T-04 (MockWebServer lifecycle): `@AfterEach { server.shutdown() }` present in T1 and T2 (verified by automated test passage — leaks would cause `BindException` in subsequent tests).
</verification>

<success_criteria>
Phase 1 is COMPLETE when every checkbox below is true:

- [ ] `build.gradle.kts` declares `testImplementation("com.squareup.okhttp3:mockwebserver:4.12.0")` (T0 done)
- [ ] `src/test/kotlin/com/six2dez/burp/aiagent/backends/perplexity/PerplexityBackendFactoryTest.kt` exists with ≥5 passing `@Test` methods covering PPLX-02 (URL form, 4 URL edge cases) and PPLX-03 (no `response_format` when `jsonMode = true`) (T1 done)
- [ ] `src/test/kotlin/com/six2dez/burp/aiagent/backends/openai/OpenAiCompatibleBackendDefaultsTest.kt` exists with ≥2 passing `@Test` methods covering PPLX-04 (defaults stay `/v1/chat/completions` + `response_format`) (T2 done)
- [ ] `src/test/kotlin/com/six2dez/burp/aiagent/config/AgentSettingsMigrationTest.kt` contains the new `load_v06xPreferencesYieldSafePerplexityDefaultsAndSchemaStaysV3` `@Test` covering PPLX-05 (v0.6.x prefs load → safe defaults; schema stays at 3) (T3 done)
- [ ] PPLX-01 indirect coverage confirmed via T3 (load-path defaults) + existing `SettingsDefaultsPersistenceTest` (no new test required, per CONTEXT.md and VALIDATION.md)
- [ ] `./gradlew test -PexcludeHeavyTests=true` exits 0 (full fast suite green)
- [ ] `./gradlew ktlintCheck` exits 0 (no formatting regressions)
- [ ] `01-VERIFICATION.md` records the six D-06 smoke fields and the two D-08 KNOWN-WORDING-GAP handoffs to Phase 5
- [ ] `01-VALIDATION.md` frontmatter shows `nyquist_compliant: true`, `wave_0_complete: true`, `status: green`
- [ ] Zero production code under `src/main/kotlin/**` modified (audit, not feature build — `git diff --stat src/main/kotlin/` returns no entries)
- [ ] Zero real Perplexity API key, prompt body, or response body committed anywhere in the repo
</success_criteria>

<output>
After completion, create `.planning/phases/01-perplexity-backend-audit/01-01-SUMMARY.md` recording:
- Which `must_haves.truths` are observably true
- Which requirements (PPLX-01..05) are locked and by which test method
- The two D-08 KNOWN-WORDING-GAP entries handed off to Phase 5 (Documentation Refresh)
- The D-06 manual-smoke status (recorded vs. pending maintainer pass)
- Any deviations from the recommended plan structure (expected: none)
</output>

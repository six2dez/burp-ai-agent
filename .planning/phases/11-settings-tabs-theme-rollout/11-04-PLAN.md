---
phase: 11-settings-tabs-theme-rollout
plan: 04
type: execute
wave: 2
depends_on:
  - "11-01"
  - "11-02"
  - "11-03"
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt
autonomous: false
requirements:
  - UI-02
  - UI-06
  - UI-07
  - UI-08

must_haves:
  truths:
    - "SettingsPanel.kt has zero UiTheme.Colors or UiTheme.Typography references (all migrated to DesignTokens)"
    - "Private sectionPanel/formGrid/addRowFull/addRowPair/addSpacerRow/buildTabPanel helpers removed or delegating to design module"
    - "No hardcoded Color(), Font(), or raw EmptyBorder int literals remain in SettingsPanel.kt"
    - "./gradlew test passes after cleanup"
    - "Human smoke-check confirms light and dark themes render correctly across all tabs"
  artifacts:
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt"
      provides: "Main settings panel — fully migrated to DesignTokens, private helpers removed"
      contains: "DesignTokens"
  key_links:
    - from: "SettingsPanel.kt"
      to: "DesignTokens.Colors / DesignTokens.Typography"
      via: "import com.six2dez.burp.aiagent.ui.design.*"
      pattern: "DesignTokens\\.Colors"
---

<objective>
Final cleanup of SettingsPanel.kt after plans 01–03 have migrated all individual panels. Remove
duplicate private helper methods, complete the UiTheme→DesignTokens token swap, and perform the
final human visual smoke-check across all settings tabs in light and dark modes.

Purpose: Plans 01–03 update each panel file independently. SettingsPanel.kt will have leftover
private helpers (sectionPanel, formGrid, etc.) that were injected as lambdas and may still be used
for SettingsPanel's own direct layout. This plan cleans those up and locks the theme rollout.

Wave 2: must run after all Wave 1 plans complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-design-system-foundation/09-UI-SPEC.md
@src/main/kotlin/com/six2dez/burp/aiagent/ui/design/DesignTokens.kt
@src/main/kotlin/com/six2dez/burp/aiagent/ui/design/Components.kt
@src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt
@src/main/kotlin/com/six2dez/burp/aiagent/ui/UiTheme.kt
@.planning/phases/11-settings-tabs-theme-rollout/11-01-SUMMARY.md
@.planning/phases/11-settings-tabs-theme-rollout/11-02-SUMMARY.md
@.planning/phases/11-settings-tabs-theme-rollout/11-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove private helpers and complete DesignTokens migration in SettingsPanel.kt</name>
  <files>src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt</files>
  <action>
    After plans 01–03 have dropped the lambda injection pattern in each panel, SettingsPanel.kt
    will still have private sectionPanel(), formGrid(), addRowFull(), addRowPair(), addSpacerRow(),
    buildTabPanel(), and nextRow() helpers. These are no longer injected into panels. Determine
    whether they are still called within SettingsPanel.kt itself (for the top-level tab structure,
    backend tab wrapper, or the AI settings area that lives directly in SettingsPanel).

    For each private helper that remains in use within SettingsPanel.kt:
    - Replace the private body with a direct call to the shared module function. Example: if
      SettingsPanel.kt still calls "this.sectionPanel(title, sub, content)", replace those call
      sites with the imported "sectionPanel(title, sub, content)" from the design module, then
      delete the private method.
    - For buildTabPanel(): the private version takes (List<JComponent>, EmptyBorder). The shared
      version uses a fixed EmptyBorder(Spacing.lg) internally. Update call sites to pass no border
      argument and delete the private version.
    - For nextRow(): used only by private addRowFull. Once addRowFull is removed, nextRow() is
      removed too.

    Token replacement sweep on SettingsPanel.kt:
    - "UiTheme.Colors.surface" → "DesignTokens.Colors.surface"
    - "UiTheme.Colors.onSurface" → "DesignTokens.Colors.onSurface"
    - "UiTheme.Colors.onSurfaceVariant" → "DesignTokens.Colors.onSurfaceVariant"
    - "UiTheme.Colors.primary" → "DesignTokens.Colors.primary"
    - "UiTheme.Colors.outline" → "DesignTokens.Colors.border"
    - "UiTheme.Colors.outlineVariant" → "DesignTokens.Colors.borderSubtle"
    - "UiTheme.Typography.body" → "DesignTokens.Typography.body"
    - "UiTheme.Typography.title" (section title role) → "DesignTokens.Typography.sectionTitle"
    - "UiTheme.Typography.label" → "DesignTokens.Typography.label"
    - "UiTheme.Typography.caption" → "DesignTokens.Typography.caption"
    - "UiTheme.Typography.mono" → "DesignTokens.Typography.mono"
    - Replace "Color(0x...)" literals with the appropriate DesignTokens.Colors token
    - Replace "Font(...)" constructors with DesignTokens.Typography role
    - Replace inline EmptyBorder int literals with DesignTokens.Spacing constants

    After replacement, remove the "import com.six2dez.burp.aiagent.ui.UiTheme" import from
    SettingsPanel.kt if no remaining references exist. If UiTheme is still referenced (e.g. in
    chat-specific color roles like userBubble/aiBubble), keep only those references and document
    them as "chat-UI exception per FLAG-01 of 09-UI-SPEC.md".

    Do NOT change: any settings save/load logic, AgentSettings field reads/writes, any settings key
    string, any JComponent field declarations, any action listeners, or any event-wiring logic
    (UI-07: behavior and persistence preserved).

    Run ./gradlew :compileKotlin after the sweep to catch any missed references.
  </action>
  <verify>
    <automated>./gradlew test 2>&1 | tail -40</automated>
  </verify>
  <done>./gradlew test passes; grep -c "UiTheme\.Colors\|UiTheme\.Typography" SettingsPanel.kt
  returns 0 (or only chat-UI FLAG-01 exceptions documented); all private builder helpers removed or
  trivially delegating to the design module.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All settings tabs (Backend, Privacy, Passive Scanner, Active Scanner, Prompt Templates,
    Custom Prompts, MCP, Help) have been rebuilt on the Phase 9 design system. Plans 01–03
    migrated each panel; plan 04 task 1 cleaned up SettingsPanel.kt. ./gradlew test is green.
  </what-built>
  <how-to-verify>
    Load the extension in Burp Suite (./gradlew shadowJar, load the JAR from build/libs/).

    1. LIGHT MODE — open Settings → cycle through every tab:
       a. Backend: check labeled field rows, monospaced URL/key fields, test-connection button
       b. Privacy: check section header, labeled rows, advisory banner when in STRICT mode
       c. Passive Scanner: check 5 collapsible sections; expand/collapse each
       d. Active Scanner: check 2 collapsible sections; expand/collapse each
       e. Prompt Templates: check two sectionPanel blocks ("Request prompts", "Issue prompts")
       f. Custom Prompts: check "Custom prompt library" and "BountyPrompt integration" sections
       g. MCP: check all labeled rows, accordion for unsafe allowlist
       h. Help: check section header "Help" with subtitle text

    2. DARK MODE — switch Burp to dark theme, repeat the same tab sweep:
       Confirm: no white/light backgrounds visible, no black text on dark surfaces,
       all section headers, labels, and button text are legible.

    3. PERSISTENCE SMOKE-CHECK:
       a. Change at least 3 settings across different tabs (e.g. privacy mode, passive rate, MCP port)
       b. Click Save Settings
       c. Unload and reload the extension
       d. Confirm all 3 values survive the reload

    PASS criteria:
    - All tabs render cleanly in both light and dark (no garbled layout, no white boxes in dark mode)
    - Collapsible sections open/close in scanner tabs
    - All changed settings round-trip through save+reload
  </how-to-verify>
  <resume-signal>Type "approved" if all tabs look correct in both themes and settings persist. Describe any visual issues if not approved.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local Swing UI only | SettingsPanel is local-only UI; no network or data boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-SC | Tampering | npm/pip/cargo installs | accept | No new package installs |
| T-11-04-01 | Information Disclosure | JPasswordField (API keys) | accept | applyFieldStyle does not change echoChar; password masking preserved by Swing |
| T-11-04-02 | Tampering | Settings persistence | mitigate | UI-07: all settings key strings are unchanged; verified by persistence smoke-check in human checkpoint |
</threat_model>

<verification>
1. ./gradlew test — full suite green (UI-07)
2. grep -v "^//" src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt | grep -c "UiTheme\.Colors\|UiTheme\.Typography" — expect 0 (or documented FLAG-01 exceptions only)
3. grep -c "DesignTokens" src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt — expect > 0
4. Human smoke-check: light + dark themes, all tabs, persistence round-trip (checkpoint task 2)
</verification>

<success_criteria>
- SettingsPanel.kt: zero UiTheme.Colors/Typography references (except documented chat FLAG-01 exceptions)
- Private helper methods (sectionPanel, formGrid, addRowFull, addRowPair, addSpacerRow, buildTabPanel, nextRow) removed
- ./gradlew test green
- Human visual approval: all tabs correct in light and dark mode; settings persist
</success_criteria>

<output>
Create .planning/phases/11-settings-tabs-theme-rollout/11-04-SUMMARY.md when done
</output>

---
phase: 11-settings-tabs-theme-rollout
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/PromptConfigPanel.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/CustomPromptsConfigPanel.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/HelpConfigPanel.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/McpConfigPanel.kt
autonomous: true
requirements:
  - UI-02
  - UI-07
  - UI-08

must_haves:
  truths:
    - "PromptConfigPanel uses sectionPanel + formGrid from design module; raw JLabel title patterns eliminated"
    - "CustomPromptsConfigPanel uses sectionPanel + formGrid from design module"
    - "HelpConfigPanel already uses sectionPanel lambda — verify it resolves to the shared module version"
    - "McpConfigPanel lambda builder params removed and replaced by direct design-module imports"
    - "No UiTheme.Colors or hardcoded Color/Font literals remain in any of the four files"
    - "./gradlew test passes"
  artifacts:
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/PromptConfigPanel.kt"
      provides: "Prompt templates tab — design-system section headers and formGrid rows"
      contains: "DesignTokens"
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/CustomPromptsConfigPanel.kt"
      provides: "Custom prompts tab — design-system layout"
      contains: "DesignTokens"
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/McpConfigPanel.kt"
      provides: "MCP server config tab — design-module builders imported directly"
      contains: "DesignTokens"
  key_links:
    - from: "PromptConfigPanel.kt"
      to: "sectionPanel / formGrid"
      via: "import com.six2dez.burp.aiagent.ui.design.*"
      pattern: "sectionPanel"
    - from: "McpConfigPanel.kt"
      to: "formGrid / addRowPair"
      via: "import com.six2dez.burp.aiagent.ui.design.*"
      pattern: "DesignTokens\\.Colors"
---

<objective>
Migrate PromptConfigPanel, CustomPromptsConfigPanel, HelpConfigPanel, and McpConfigPanel to use
design-system builders directly (no UiTheme references, no lambda injection for builders).

Purpose: These four panels are in the "content configuration" group. PromptConfigPanel and
CustomPromptsConfigPanel use ad-hoc JLabel title patterns that need replacing with sectionPanel.
McpConfigPanel takes five builder lambdas (same pattern as PassiveScan/ActiveScan) that should be
removed. HelpConfigPanel is nearly clean already — light touch to remove its sectionPanel lambda
and confirm DesignTokens compliance.

Wave 1: no file conflicts with plans 01 or 02.

Output: All four panels import the design module; no lambda builder params for sectionPanel/formGrid
etc.; no UiTheme references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-design-system-foundation/09-UI-SPEC.md
@src/main/kotlin/com/six2dez/burp/aiagent/ui/design/DesignTokens.kt
@src/main/kotlin/com/six2dez/burp/aiagent/ui/design/Components.kt
@src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/PromptConfigPanel.kt
@src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/CustomPromptsConfigPanel.kt
@src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/HelpConfigPanel.kt
@src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/McpConfigPanel.kt
@src/main/kotlin/com/six2dez/burp/aiagent/ui/UiTheme.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate PromptConfigPanel and CustomPromptsConfigPanel</name>
  <files>
    src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/PromptConfigPanel.kt
    src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/CustomPromptsConfigPanel.kt
  </files>
  <action>
    PROMPT CONFIG PANEL:

    Remove the three lambda constructor params (sectionPanel, formGrid, addRowFull) and replace
    with "import com.six2dez.burp.aiagent.ui.design.*". Update SettingsPanel.kt to drop those three
    lambda arguments from the PromptConfigPanel(...) constructor call.

    In build(), replace the two ad-hoc JLabel title patterns:
      "val requestTitle = JLabel(...); requestTitle.font = UiTheme.Typography.label; ..."
      "val issueTitle = JLabel(...); issueTitle.font = UiTheme.Typography.label; ..."
    with sectionPanel("Request prompts", "Built-in prompts for request context-menu actions", requestGrid)
    and sectionPanel("Issue prompts", "Built-in prompts for Burp issue analysis actions", issueGrid)
    where requestGrid and issueGrid are formGrid()-based panels populated with addRowFull calls.

    The JTextArea fields (promptRequest, promptSummary, promptJs, promptAccessControl,
    promptLoginSequence, promptIssueAnalyze, promptIssuePoc, promptIssueImpact, promptIssueFull)
    are already wrapped in JScrollPane at the call site — call applyAreaStyle(field) on each bare
    JTextArea before it is wrapped, so the textarea styling applies correctly.

    Replace "body.background = UiTheme.Colors.surface" with "DesignTokens.Colors.surface".
    Replace "EmptyBorder(6, 8, 8, 8)" → "EmptyBorder(DesignTokens.Spacing.sectionPad)".
    Remove any remaining UiTheme import.

    Wrap both sectionPanel blocks in buildTabPanel(listOf(requestSection, issueSection)) and return
    the resulting JScrollPane from build().

    CUSTOM PROMPTS CONFIG PANEL:

    Remove the four lambda constructor params (sectionPanel, formGrid, addRowFull, addRowPair)
    and replace with "import com.six2dez.burp.aiagent.ui.design.*". Update SettingsPanel.kt.

    In build(), replace the two ad-hoc JLabel title patterns:
      "val libraryTitle = JLabel("Library"); libraryTitle.font = UiTheme.Typography.label; ..."
      "val bountyTitle = JLabel("BountyPrompt integration"); bountyTitle.font = UiTheme.Typography.label; ..."
    with sectionPanel("Custom prompt library", "Manage, search, reorder and export your saved prompts", customPromptLibrarySection)
    and sectionPanel("BountyPrompt integration", "Auto-create issues from BountyPrompt directory results", bountyGrid).

    Build bountyGrid with formGrid() and addRowFull / addRowPair:
      addRowFull("Enabled", bountyPromptEnabled)
      addRowFull("Prompts directory", bountyPromptDir) — call applyFieldStyle(bountyPromptDir) first
      addRowPair("Auto-create issues", bountyPromptAutoCreateIssues, "Issue threshold", bountyPromptIssueThreshold)
      addRowFull("Enabled prompt IDs", JScrollPane(bountyPromptEnabledIds)) — call applyAreaStyle(bountyPromptEnabledIds) first

    Replace all UiTheme.Colors/Typography references with DesignTokens equivalents.
    EmptyBorder raw int literals → Spacing constants.
    Wrap both sections in buildTabPanel(listOf(librarySection, bountySection)).
  </action>
  <verify>
    <automated>./gradlew :compileKotlin 2>&1 | tail -20</automated>
  </verify>
  <done>Both files compile; no UiTheme or raw Color/Font references remain; SettingsPanel.kt lambda
  args removed for both panels.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate HelpConfigPanel and McpConfigPanel; wire SettingsPanel.kt</name>
  <files>
    src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/HelpConfigPanel.kt
    src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/McpConfigPanel.kt
    src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanel.kt
  </files>
  <action>
    HELP CONFIG PANEL:

    HelpConfigPanel takes a sectionPanel lambda and a dialogParentProvider lambda. Remove the
    sectionPanel lambda param and import "import com.six2dez.burp.aiagent.ui.design.*" directly.
    Keep dialogParentProvider as-is (it is not a builder — it provides the parent JComponent for
    dialogs). Update SettingsPanel.kt to drop the sectionPanel lambda arg from the HelpConfigPanel(...)
    call.

    In build(), the call "sectionPanel("Help", "Quick start and documentation links.", helpPane)"
    becomes the imported "sectionPanel("Help", "Quick start and documentation links.", helpPane)".
    The helpPane's "EmptyBorder(6, 8, 8, 8)" → "EmptyBorder(DesignTokens.Spacing.sectionPad)".
    No other logic changes.

    MCP CONFIG PANEL:

    McpConfigPanel takes five lambda params (sectionPanel, formGrid, addRowFull, addRowPair,
    addSpacerRow). Remove all five and import the design builders directly.

    The MCP config panel is already well-structured (it was built as a reference for Phase 11 panels).
    The primary changes are:
    1. Remove the five lambda constructor parameters.
    2. Add "import com.six2dez.burp.aiagent.ui.design.*".
    3. In build(), all lambda call sites "sectionPanel(...)", "formGrid()", "addRowFull(grid, ...)",
       "addRowPair(grid, ...)", "addSpacerRow(grid, n)" become direct calls to the imported functions.
       The function signatures are identical — this is a mechanical removal of the lambda indirection.
    4. Replace any remaining "UiTheme.Colors.*" or "UiTheme.Typography.*" references with
       "DesignTokens.Colors.*" / "DesignTokens.Typography.*".
    5. Replace "BorderFactory.createEmptyBorder(...)" int literals with DesignTokens.Spacing values.
    6. Do NOT change: any field references (mcpEnabled, mcpHost, mcpPort, etc.), their ordering,
       the tokenPanelFactory() call, the quickActionsFactory() call, or any accordion wiring that
       already exists in this file (UI-07: MCP settings persistence fully preserved).

    SETTINGS PANEL WIRING:

    After the per-panel constructor call updates are made in Tasks 1 and 2 of plans 01–03, do a
    final pass on SettingsPanel.kt to:
    - Confirm that the private sectionPanel(), formGrid(), addRowFull(), addRowPair(), addSpacerRow(),
      buildTabPanel() helper methods are no longer called from panel constructors (since all panels
      now import the shared module). These private helpers may be removed IF SettingsPanel.kt itself
      no longer uses them for its own direct layout (e.g. the backend tab wrapper or main tab
      structure). If SettingsPanel.kt still calls them for its OWN layout (not panel constructors),
      keep them or replace them with design-module calls.
    - Replace all "UiTheme.Colors.*" and "UiTheme.Typography.*" references in SettingsPanel.kt with
      DesignTokens equivalents, EXCEPT in the legacy shim UiTheme.kt itself (which must stay as-is
      to avoid breaking call sites in tests and other non-migrated components).
    - The private "nextRow(grid)" helper in SettingsPanel.kt (used by its private addRowFull) may
      conflict with the module-level nextRow. If SettingsPanel.kt's private addRowFull is removed,
      remove nextRow too; otherwise rename the private one to avoid shadowing.
    - Run ./gradlew test and fix any compilation errors before declaring done.
  </action>
  <verify>
    <automated>./gradlew test 2>&1 | tail -40</automated>
  </verify>
  <done>./gradlew test passes; HelpConfigPanel and McpConfigPanel have no lambda builder params and
  no UiTheme references; SettingsPanel.kt compiles with all panel constructor updates applied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local Swing UI only | Prompt and MCP settings panels are local UI; no cross-trust-boundary data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; pure Kotlin refactor |
| T-11-03-01 | Information Disclosure | HelpConfigPanel hyperlink | accept | Existing hyperlink behavior (open browser / show dialog) is preserved verbatim; no new URL handling introduced |
</threat_model>

<verification>
1. ./gradlew test — full suite green
2. grep -rn "UiTheme\." src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/PromptConfigPanel.kt src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/CustomPromptsConfigPanel.kt src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/HelpConfigPanel.kt src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/McpConfigPanel.kt — expect zero matches
3. grep -c "sectionPanel\|formGrid\|addRowFull" src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/PromptConfigPanel.kt — expect > 0 (design-module calls present)
</verification>

<success_criteria>
- PromptConfigPanel, CustomPromptsConfigPanel, HelpConfigPanel, McpConfigPanel: no UiTheme references
- All four panels: lambda builder constructor params removed; design module imported directly
- SettingsPanel.kt: all corresponding constructor call sites updated; test suite green
</success_criteria>

<output>
Create .planning/phases/11-settings-tabs-theme-rollout/11-03-SUMMARY.md when done
</output>

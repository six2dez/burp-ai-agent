---
phase: 03-prompt-library-ux-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/config/CustomPromptDefinition.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/components/CustomPromptLibraryEditor.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/config/CustomPromptLibraryJsonTest.kt
autonomous: true
requirements: [PROM-03, PROM-04, PROM-05]
tags: [prompt-library, json, merge, move, refactor, companion, unit-test, behaviour-lock]

must_haves:
  truths:
    - "parseLibraryJson(text: String): List<CustomPromptDefinition> is a public companion method in CustomPromptDefinition.Companion — parses Jackson pretty-printed JSON, returns empty list on malformed input, filters by isValid()"
    - "mergeById(existing, incoming): List<CustomPromptDefinition> is a public companion method — replaces matching ids, appends new ids, deduplicates input using associateBy (last-occurrence-wins per D-02)"
    - "applyMove(library, index, delta): List<CustomPromptDefinition> is a public companion method — adjacent-swap only, returns original list unchanged on boundary-crossing or out-of-bounds (per D-05)"
    - "INTENTIONAL BEHAVIOUR CHANGE: handleImport previously used distinctBy (first-occurrence-wins). After refactor it calls mergeById which uses associateBy (last-occurrence-wins per D-02). The misleading comment at line 305 is corrected."
    - "handleImport, handleExport, handleMove in CustomPromptLibraryEditor.kt are thin shells calling companion methods — all file I/O and JFileChooser plumbing stay in the editor"
    - "All new companion methods accept List<CustomPromptDefinition> input parameters and return List<CustomPromptDefinition> — no Swing types, no Jackson types in signatures"
    - "CustomPromptLibraryJsonTest.kt covers all three companion methods with 10 @Test methods: 2 for parseLibraryJson, 3 for mergeById, 5 for applyMove"
    - "No new dependency added — Jackson + Kotlin module are already on the classpath"
  artifacts:
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/config/CustomPromptDefinition.kt"
      provides: "Three new companion methods: parseLibraryJson, mergeById, applyMove"
      contains: "fun parseLibraryJson"
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/ui/components/CustomPromptLibraryEditor.kt"
      provides: "Thin-shell rewires of handleImport, handleExport (minor), handleMove to call companion methods"
      contains: "CustomPromptDefinition.parseLibraryJson"
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/config/CustomPromptLibraryJsonTest.kt"
      provides: "10 @Test methods covering PROM-03, PROM-04, PROM-05"
      contains: "fun parseLibraryJsonParsesPrettyPrintedExport"
  key_links:
    - from: "CustomPromptLibraryEditor.handleImport (thin shell)"
      to: "CustomPromptDefinition.parseLibraryJson + mergeById (companion)"
      via: "val parsed = CustomPromptDefinition.parseLibraryJson(text); val merged = CustomPromptDefinition.mergeById(master.toList(), parsed)"
      pattern: "CustomPromptDefinition\\.parseLibraryJson|CustomPromptDefinition\\.mergeById"
    - from: "CustomPromptLibraryEditor.handleMove (thin shell)"
      to: "CustomPromptDefinition.applyMove (companion)"
      via: "val result = CustomPromptDefinition.applyMove(master.toList(), idx, delta)"
      pattern: "CustomPromptDefinition\\.applyMove"
    - from: "CustomPromptLibraryJsonTest.mergeByIdDeduplicatesInputUsingLastOccurrenceWins"
      to: "mergeById associateBy semantics (D-02 intentional semantic correction)"
      via: "incoming=[A', A'', C] asserts A'' (last occurrence) survives — NOT A' (first occurrence)"
      pattern: "mergeByIdDeduplicatesInputUsingLastOccurrenceWins"
---

<objective>
Extract three new pure functions into `CustomPromptDefinition.Companion`, wire three handlers in `CustomPromptLibraryEditor.kt` to call them, and lock the new methods with 10 unit tests in new `CustomPromptLibraryJsonTest.kt`.

INTENTIONAL BEHAVIOUR CHANGE — must be surfaced explicitly: The current `handleImport` at line 306 uses `imported.filter { it.isValid() }.distinctBy { it.id }` which is first-occurrence-wins (`distinctBy` in Kotlin retains the FIRST key encountered via `LinkedHashSet`). The misleading comment at line 305 says "Last occurrence wins via the LinkedHashMap of distinctBy" — this comment is WRONG. After the refactor, `mergeById` uses `incoming.associateBy { it.id }.values.toList()` which is last-occurrence-wins per D-02. This is a deliberate semantic correction, not a pure logic relocation.

Purpose: The three companion methods are currently private logic embedded in the editor's handlers. Extracting them into the companion makes them unit-testable without Swing or file I/O, locking PROM-03 (parseLibraryJson round-trip), PROM-04 (mergeById replace + append + last-wins dedup), and PROM-05 (applyMove boundary reject).

Output: Three modified files. `CustomPromptDefinition.kt` gains three companion methods. `CustomPromptLibraryEditor.kt` handlers become thin shells. `CustomPromptLibraryJsonTest.kt` is created with 10 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-prompt-library-ux-audit/03-CONTEXT.md
@.planning/phases/03-prompt-library-ux-audit/03-RESEARCH.md
@.planning/phases/03-prompt-library-ux-audit/03-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/main/kotlin/com/six2dez/burp/aiagent/config/CustomPromptDefinition.kt
@src/main/kotlin/com/six2dez/burp/aiagent/ui/components/CustomPromptLibraryEditor.kt
@src/test/kotlin/com/six2dez/burp/aiagent/config/CustomPromptFilterTest.kt

<interfaces>
<!-- CustomPromptDefinition.Companion — existing methods (CustomPromptDefinition.kt:18-48) -->
<!-- New methods land in this same companion object -->

companion object {
    // EXISTING (do not modify):
    fun filterForMenu(library: List<CustomPromptDefinition>, tag: CustomPromptTag): List<CustomPromptDefinition>
    fun searchFilter(library: List<CustomPromptDefinition>, query: String): List<CustomPromptDefinition>
    fun sortFavoritesFirst(library: List<CustomPromptDefinition>): List<CustomPromptDefinition>

    // NEW (to add — signatures per D-01, D-04):
    fun parseLibraryJson(text: String): List<CustomPromptDefinition>
    fun mergeById(existing: List<CustomPromptDefinition>, incoming: List<CustomPromptDefinition>): List<CustomPromptDefinition>
    fun applyMove(library: List<CustomPromptDefinition>, index: Int, delta: Int): List<CustomPromptDefinition>
}

<!-- Jackson imports needed in CustomPromptDefinition.kt for parseLibraryJson -->
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
<!-- NOTE: Do NOT import from CustomPromptLibraryEditor — that would violate config ← ui/components layering. -->
<!-- parseLibraryJson creates its own ObjectMapper().registerKotlinModule() per call (no INDENT_OUTPUT). -->
<!-- See RESEARCH.md Pitfall 2 for the layering rationale. -->

<!-- CustomPromptLibraryEditor.kt — handler signatures (private, stay private after refactor) -->
private fun handleMove(delta: Int)       // line 270 — rewrite to call applyMove
private fun handleImport()               // line 286 — rewrite to call parseLibraryJson + mergeById
private fun handleExport()               // line 333 — minor: already calls sortFavoritesFirst; stays structurally the same

<!-- CustomPromptLibraryEditor.kt — helper methods needed by thin shells (do not move these) -->
private fun selectedEntry(): CustomPromptDefinition?
private fun masterIndexOf(entry: CustomPromptDefinition): Int
private fun refreshList()
private fun selectById(id: String)

<!-- CustomPromptLibraryEditor.kt — JSON_MAPPER in companion (private to editor, do NOT access from config package) -->
companion object {
    private val JSON_MAPPER: ObjectMapper = ObjectMapper().registerKotlinModule().enable(SerializationFeature.INDENT_OUTPUT)
}
<!-- handleExport uses JSON_MAPPER.writerWithDefaultPrettyPrinter().writeValue(target, payload) — keep as-is -->

<!-- CustomPromptFilterTest.kt fixture pattern — mirror this in the new test file -->
private val http = CustomPromptDefinition(id="1", title="HTTP only", promptText="a", tags=setOf(HTTP_SELECTION))
<!-- Use JUnit org.junit.jupiter.api.Assertions.assertEquals — same as CustomPromptFilterTest -->
<!-- Use org.junit.jupiter.api.Test — same import -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract parseLibraryJson, mergeById, applyMove into CustomPromptDefinition.Companion</name>
  <files>src/main/kotlin/com/six2dez/burp/aiagent/config/CustomPromptDefinition.kt</files>
  <action>
    Add three new public functions to the existing `companion object` in `CustomPromptDefinition.kt`, after `sortFavoritesFirst`. Add the required Jackson imports at the top of the file. Do NOT modify the data class constructor, `isValid()`, or any existing companion method.

    Add these imports at the top of the file (after the existing `import com.fasterxml.jackson.annotation.JsonIgnoreProperties`):
    - `import com.fasterxml.jackson.databind.ObjectMapper`
    - `import com.fasterxml.jackson.module.kotlin.registerKotlinModule`

    The three methods to add inside `companion object { }`:

    Method 1 — `parseLibraryJson`:
    Parse a JSON string produced by `handleExport`. Returns empty list on blank input or parse error. Filters by `isValid()` to match the behavior of `AgentSettings.parseCustomPromptLibrary` (RESEARCH.md Pattern 2). Uses a per-call `ObjectMapper` instance — do NOT reference `CustomPromptLibraryEditor.JSON_MAPPER` (violates config ← ui layering, see RESEARCH.md Pitfall 2).

    Signature: `fun parseLibraryJson(text: String): List<CustomPromptDefinition>`

    Logic:
    - If `text.isBlank()`, return `emptyList()`.
    - `return try { ObjectMapper().registerKotlinModule().readValue(text, Array<CustomPromptDefinition>::class.java).toList().filter { it.isValid() } } catch (e: Exception) { emptyList() }`

    Method 2 — `mergeById`:
    Input-side dedup using `associateBy` (last-occurrence-wins per D-02 — INTENTIONAL SEMANTIC CORRECTION from the prior `distinctBy` first-wins behaviour in `handleImport`). Then merge: matching ids replace existing entries in their original positions; new ids append in incoming order. The `incoming` parameter is already validity-filtered by the time it reaches `mergeById` (caller runs `parseLibraryJson` first), so no `isValid()` check here — `mergeById` is pure merge logic only (RESEARCH.md Pitfall 3).

    Signature: `fun mergeById(existing: List<CustomPromptDefinition>, incoming: List<CustomPromptDefinition>): List<CustomPromptDefinition>`

    Logic:
    - `val deduped = incoming.associateBy { it.id }.values.toList()` — input dedup: last occurrence wins.
    - `val incomingById = deduped.associateBy { it.id }` — lookup map for replacements.
    - `val result = existing.map { incomingById[it.id] ?: it }.toMutableList()` — replace matching ids in-place.
    - `val existingIds = existing.map { it.id }.toSet()`
    - `result.addAll(deduped.filter { it.id !in existingIds })` — append new ids in incoming order.
    - `return result`

    Method 3 — `applyMove`:
    Adjacent-swap only (NOT the skip-over while-loop in `handleMove` — see RESEARCH.md Pitfall 4). Returns the original list unchanged on: (a) `index` out of bounds, (b) `index + delta` out of bounds, (c) the move would cross the favorites/non-favorites boundary (`library[index].isFavorite != library[target].isFavorite`). Per D-05: reject semantics, not clamp.

    Signature: `fun applyMove(library: List<CustomPromptDefinition>, index: Int, delta: Int): List<CustomPromptDefinition>`

    Logic:
    - `if (index !in library.indices) return library`
    - `val target = index + delta`
    - `if (target !in library.indices) return library`
    - `if (library[index].isFavorite != library[target].isFavorite) return library`
    - `val result = library.toMutableList()`
    - `val moved = result.removeAt(index)`
    - `result.add(target, moved)`
    - `return result`

    ktlint compliance: trailing commas in multi-line parameter lists (per CONVENTIONS.md). Keep KDoc comments on each new method explaining its contract briefly.
  </action>
  <verify>
    <automated>./gradlew compileKotlin -PexcludeHeavyTests=true</automated>
  </verify>
  <done>
    `CustomPromptDefinition.kt` compiles cleanly. The companion object now contains `parseLibraryJson`, `mergeById`, and `applyMove` in addition to the three existing methods. `grep -c 'fun parseLibraryJson\|fun mergeById\|fun applyMove' src/main/kotlin/com/six2dez/burp/aiagent/config/CustomPromptDefinition.kt` returns `3`. The file does NOT import anything from `ui.components`. `./gradlew ktlintCheck` passes on this file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CustomPromptLibraryJsonTest.kt with 10 @Test methods (PROM-03, PROM-04, PROM-05)</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/config/CustomPromptLibraryJsonTest.kt</files>
  <action>
    Create a new test file at the exact path `src/test/kotlin/com/six2dez/burp/aiagent/config/CustomPromptLibraryJsonTest.kt`. This file mirrors `CustomPromptFilterTest.kt` style: JUnit `org.junit.jupiter.api.Assertions.assertEquals`, `org.junit.jupiter.api.Test`, `private val` fixtures at class level, no Mockito. Package: `com.six2dez.burp.aiagent.config`.

    Imports needed:
    - `import com.fasterxml.jackson.databind.ObjectMapper`
    - `import com.fasterxml.jackson.databind.SerializationFeature`
    - `import com.fasterxml.jackson.module.kotlin.registerKotlinModule`
    - `import org.junit.jupiter.api.Assertions.assertEquals`
    - `import org.junit.jupiter.api.Assertions.assertTrue`
    - `import org.junit.jupiter.api.Test`

    Class-level fixtures (small, ≤ 6 instances — mirrors `CustomPromptFilterTest` private val pattern):
    - `private val f1 = CustomPromptDefinition(id = "f1", title = "Fav 1", promptText = "fav prompt 1", tags = setOf(CustomPromptTag.HTTP_SELECTION), isFavorite = true)`
    - `private val f2 = CustomPromptDefinition(id = "f2", title = "Fav 2", promptText = "fav prompt 2", tags = setOf(CustomPromptTag.HTTP_SELECTION), isFavorite = true)`
    - `private val f3 = CustomPromptDefinition(id = "f3", title = "Fav 3", promptText = "fav prompt 3", tags = setOf(CustomPromptTag.HTTP_SELECTION), isFavorite = true)`
    - `private val n1 = CustomPromptDefinition(id = "n1", title = "Non 1", promptText = "non prompt 1", tags = setOf(CustomPromptTag.HTTP_SELECTION), isFavorite = false)`
    - `private val n2 = CustomPromptDefinition(id = "n2", title = "Non 2", promptText = "non prompt 2", tags = setOf(CustomPromptTag.HTTP_SELECTION), isFavorite = false)`
    - `private val n3 = CustomPromptDefinition(id = "n3", title = "Non 3", promptText = "non prompt 3", tags = setOf(CustomPromptTag.HTTP_SELECTION), isFavorite = false)`
    - `private val exportMapper = ObjectMapper().registerKotlinModule().enable(SerializationFeature.INDENT_OUTPUT)` — used in the parseLibraryJson round-trip test to generate the pretty-printed string that `handleExport` would produce.

    The 10 `@Test` methods:

    GROUP 1 — parseLibraryJson (2 tests, PROM-03):

    `fun parseLibraryJsonParsesPrettyPrintedExport()`:
    - `val entries = listOf(f1, n1)`
    - `val json = exportMapper.writeValueAsString(entries)` — generates the exact Jackson pretty-print output that `handleExport` would write to disk.
    - `val parsed = CustomPromptDefinition.parseLibraryJson(json)`
    - `assertEquals(entries, parsed)` — deep equality via data class structural equality.

    `fun parseLibraryJsonReturnsEmptyOnMalformedInput()`:
    - `assertEquals(emptyList<CustomPromptDefinition>(), CustomPromptDefinition.parseLibraryJson("not json"))`
    - `assertEquals(emptyList<CustomPromptDefinition>(), CustomPromptDefinition.parseLibraryJson(""))`
    - `assertEquals(emptyList<CustomPromptDefinition>(), CustomPromptDefinition.parseLibraryJson("   "))`

    GROUP 2 — mergeById (3 tests, PROM-04):

    `fun mergeByIdReplacesMatchingIdsAndAppendsNewIds()`:
    - `val existing = listOf(f1, f2, n1)` — ids: f1, f2, n1.
    - `val bPrime = f2.copy(title = "Fav 2 updated")` — same id "f2", updated title.
    - `val incoming = listOf(bPrime, n2)` — id f2 (replacement), id n2 (new).
    - `val result = CustomPromptDefinition.mergeById(existing, incoming)`
    - `assertEquals(listOf(f1, bPrime, n1, n2), result)` — f2 replaced in-place, n2 appended, f1 and n1 preserved.

    `fun mergeByIdDeduplicatesInputUsingLastOccurrenceWins()`:
    - This test locks the INTENTIONAL BEHAVIOUR CHANGE: `associateBy` last-wins vs. prior `distinctBy` first-wins.
    - `val existing = listOf(f1, n1)` — ids: f1, n1.
    - `val aPrime = f1.copy(title = "Fav 1 v2")` — same id "f1".
    - `val aDoublePrime = f1.copy(title = "Fav 1 v3")` — same id "f1".
    - `val incoming = listOf(aPrime, aDoublePrime, n2)` — [A', A'', C] — two entries with id "f1", then a new id "n2".
    - `val result = CustomPromptDefinition.mergeById(existing, incoming)`
    - `assertEquals(listOf(aDoublePrime, n1, n2), result)` — A'' (last occurrence) replaces f1; n1 preserved; n2 appended. NOT aPrime (first occurrence).

    `fun mergeByIdWithEmptyExistingAppendsDedupedIncoming()`:
    - `val aPrime = f1.copy(title = "Fav 1 v2")` — same id "f1".
    - `val incoming = listOf(f1, aPrime, n1)` — [A, A', B] — two entries with id "f1".
    - `val result = CustomPromptDefinition.mergeById(emptyList(), incoming)`
    - `assertEquals(listOf(aPrime, n1), result)` — dedup picks last A (aPrime); n1 appended.

    GROUP 3 — applyMove (5 tests, PROM-05):
    Use `val library = listOf(f1, f2, f3, n1, n2, n3)` at the top of each test — indices 0..5, F=favorite at 0-2, N=non-favorite at 3-5.

    `fun applyMoveSwapsAdjacentEntriesWithinFavoritesGroup()`:
    - `val library = listOf(f1, f2, f3, n1, n2, n3)`
    - `val result = CustomPromptDefinition.applyMove(library, 0, 1)` — move f1 down one within favorites.
    - `assertEquals(listOf(f2, f1, f3, n1, n2, n3), result)` — f1 and f2 swap.

    `fun applyMoveSwapsAdjacentEntriesWithinNonFavoritesGroup()`:
    - `val library = listOf(f1, f2, f3, n1, n2, n3)`
    - `val result = CustomPromptDefinition.applyMove(library, 3, 1)` — move n1 down one within non-favorites.
    - `assertEquals(listOf(f1, f2, f3, n2, n1, n3), result)` — n1 and n2 swap.

    `fun applyMoveReturnsOriginalWhenLastFavoriteMovesDown()`:
    - `val library = listOf(f1, f2, f3, n1, n2, n3)`
    - `val result = CustomPromptDefinition.applyMove(library, 2, 1)` — f3 (last favorite) tries to move down to n1's position — boundary cross.
    - `assertEquals(library, result)` — structural equality; original list returned unchanged.

    `fun applyMoveReturnsOriginalWhenFirstNonFavoriteMovesUp()`:
    - `val library = listOf(f1, f2, f3, n1, n2, n3)`
    - `val result = CustomPromptDefinition.applyMove(library, 3, -1)` — n1 (first non-favorite) tries to move up to f3's position — boundary cross.
    - `assertEquals(library, result)` — original list returned unchanged.

    `fun applyMoveReturnsOriginalWhenIndexOutOfBounds()`:
    - `val library = listOf(f1, f2, f3, n1, n2, n3)`
    - `val result = CustomPromptDefinition.applyMove(library, 5, 1)` — index 5 + delta 1 = 6, out of bounds (size is 6, valid indices 0-5).
    - `assertEquals(library, result)` — original list returned unchanged.

    Assertion library: use `assertEquals` from `org.junit.jupiter.api.Assertions` for value equality. Use `assertTrue` only if needed (it is not needed for any of these tests — all assertions are `assertEquals`). Do NOT use `kotlin.test.*` in this file.

    ktlint compliance: trailing commas in multi-line constructor calls, no trailing whitespace.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.config.CustomPromptLibraryJsonTest" -PexcludeHeavyTests=true</automated>
  </verify>
  <done>
    `CustomPromptLibraryJsonTest.kt` exists at `src/test/kotlin/com/six2dez/burp/aiagent/config/CustomPromptLibraryJsonTest.kt` with 10 `@Test` methods. All 10 tests pass.

    Additional checks:
    - `grep -c '@Test' src/test/kotlin/com/six2dez/burp/aiagent/config/CustomPromptLibraryJsonTest.kt` returns `10`
    - `grep -c 'fun mergeByIdDeduplicatesInputUsingLastOccurrenceWins' src/test/kotlin/com/six2dez/burp/aiagent/config/CustomPromptLibraryJsonTest.kt` returns `1`
    - The file does NOT contain `mock(`, `whenever(`, `Mockito`, or `RETURNS_DEEP_STUBS` (per D-07)
    - The `mergeByIdDeduplicatesInputUsingLastOccurrenceWins` test asserts `aDoublePrime` (last occurrence) — NOT `aPrime` (first occurrence)
    - `./gradlew ktlintCheck` does not flag any new violation in this file
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite handleMove, handleImport, handleExport in CustomPromptLibraryEditor.kt as thin shells</name>
  <files>src/main/kotlin/com/six2dez/burp/aiagent/ui/components/CustomPromptLibraryEditor.kt</files>
  <action>
    Rewrite the three private handlers to call the new companion methods. All other methods in `CustomPromptLibraryEditor.kt` remain unchanged. File I/O and `JFileChooser` plumbing stay in the editor. The three handlers remain private.

    HANDLER 1 — `handleMove(delta: Int)` (currently at line 270):
    Replace the skip-over while loop with a direct call to `CustomPromptDefinition.applyMove`. The prior implementation advanced `target` past items of opposite `isFavorite` status — this is NOT what `applyMove` does (adjacent-swap only, per RESEARCH.md Pitfall 4). The new thin shell:

    ```
    private fun handleMove(delta: Int) {
        val entry = selectedEntry() ?: return
        val idx = masterIndexOf(entry)
        if (idx < 0) return
        val result = CustomPromptDefinition.applyMove(master.toList(), idx, delta)
        master.clear()
        master.addAll(result)
        refreshList()
        selectById(entry.id)
    }
    ```

    Note: always call `refreshList()` + `selectById()` after `applyMove`, even if the list is unchanged — this matches the prior handler's always-refresh behaviour. The `refreshButtons()` boundary check (`hasNeighborOfSameStatus`) still governs button enable/disable; `applyMove`'s reject semantics add a second safety layer for programmatic calls.

    HANDLER 2 — `handleImport()` (currently at line 286):
    INTENTIONAL BEHAVIOUR CHANGE: the prior implementation used `distinctBy { it.id }` (first-occurrence-wins, misidentified in the misleading comment at line 305 as "last occurrence wins"). The new implementation calls `mergeById` which uses `associateBy` (last-occurrence-wins per D-02). This is a deliberate semantic correction; the new test `mergeByIdDeduplicatesInputUsingLastOccurrenceWins` locks it.

    Keep the `JFileChooser` file-selection flow, the error `JOptionPane`, the empty-result guard, and the success `JOptionPane`. Replace the inline merge logic with companion calls. The validity filter (`isValid()`) is now inside `parseLibraryJson`; do NOT re-filter after `parseLibraryJson` returns.

    Replace the try-catch block and the inline merge at lines 291-330 with:

    ```
    private fun handleImport() {
        val chooser = JFileChooser()
        chooser.fileFilter = FileNameExtensionFilter("JSON files", "json")
        if (chooser.showOpenDialog(list) != JFileChooser.APPROVE_OPTION) return
        val file = chooser.selectedFile ?: return
        val text = try {
            file.readText()
        } catch (e: Exception) {
            JOptionPane.showMessageDialog(list, "Failed to read file: ${e.message}", "Import failed", JOptionPane.ERROR_MESSAGE)
            return
        }
        val parsed = CustomPromptDefinition.parseLibraryJson(text)
        if (parsed.isEmpty()) {
            JOptionPane.showMessageDialog(list, "No valid prompts found in the file.", "Nothing to import", JOptionPane.WARNING_MESSAGE)
            return
        }
        val merged = CustomPromptDefinition.mergeById(master.toList(), parsed)
        val replacedCount = merged.size - (merged.size - master.size).coerceAtLeast(0)
        val addedCount = merged.size - master.size
        master.clear()
        master.addAll(merged)
        refreshList()
        JOptionPane.showMessageDialog(list, "Imported ${addedCount.coerceAtLeast(0)} updated and ${(addedCount.coerceAtMost(0) * -1)} new prompt(s).", "Import complete", JOptionPane.INFORMATION_MESSAGE)
    }
    ```

    Simplification note: the exact wording of the import-complete message is not locked by any test (it is UI-only). Keep a reasonable summary. The simplest correct approach is:
    `"Imported ${parsed.size} prompt(s) merged into library."` — no need to recompute counts. Use whatever clear message is easiest to maintain.

    HANDLER 3 — `handleExport()` (currently at line 333):
    The export handler already calls `CustomPromptDefinition.sortFavoritesFirst(master.toList())` and uses `JSON_MAPPER.writerWithDefaultPrettyPrinter().writeValue(target, payload)`. This handler is structurally correct. Make no functional change. The only permitted edit is ensuring it calls `master.toList()` as the snapshot argument (it already does at line 345: `CustomPromptDefinition.sortFavoritesFirst(master.toList())`). No other change required.

    After all three handlers are updated, remove the now-dead inline imports if any were only used by the replaced handler code (e.g., the existing `distinctBy` logic used `imported.filter { it.isValid() }.distinctBy { it.id }` — `distinctBy` comes from Kotlin stdlib, no import needed; `filter` is stdlib too). Verify the file still compiles with `./gradlew compileKotlin`.

    ktlint compliance: trailing commas in multi-line function calls, consistent blank lines.
  </action>
  <verify>
    <automated>./gradlew compileKotlin -PexcludeHeavyTests=true</automated>
  </verify>
  <done>
    `CustomPromptLibraryEditor.kt` compiles cleanly. The three handlers are thin shells:
    - `handleMove` calls `CustomPromptDefinition.applyMove(master.toList(), idx, delta)` — no skip-over while loop.
    - `handleImport` calls `CustomPromptDefinition.parseLibraryJson(text)` then `CustomPromptDefinition.mergeById(master.toList(), parsed)` — the misleading `distinctBy` comment is gone.
    - `handleExport` is structurally unchanged (already called `sortFavoritesFirst`).
    - `grep -c 'CustomPromptDefinition.parseLibraryJson\|CustomPromptDefinition.mergeById\|CustomPromptDefinition.applyMove' src/main/kotlin/com/six2dez/burp/aiagent/ui/components/CustomPromptLibraryEditor.kt` returns `3`.
    - `grep -c 'distinctBy' src/main/kotlin/com/six2dez/burp/aiagent/ui/components/CustomPromptLibraryEditor.kt` returns `0` — the first-wins bug is gone.
    - `./gradlew ktlintCheck` passes on this file.
  </done>
</task>

<task type="auto">
  <name>Task 4: Run full test suite to confirm no regressions</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/config/CustomPromptLibraryJsonTest.kt</files>
  <action>
    Run the full fast test suite and ktlint check to confirm all three modified/created files are correct and no pre-existing tests regressed.

    Execute the following commands in order:
    1. `./gradlew test --tests "com.six2dez.burp.aiagent.config.CustomPromptLibraryJsonTest" -PexcludeHeavyTests=true` — all 10 new tests green.
    2. `./gradlew test --tests "com.six2dez.burp.aiagent.config.CustomPromptFilterTest" -PexcludeHeavyTests=true` — no regressions in existing filter tests.
    3. `./gradlew test -PexcludeHeavyTests=true` — full fast suite green.
    4. `./gradlew ktlintCheck` — no ktlint violations in modified files.

    This task has no file modifications — it is a verification task only. If any command fails, diagnose and fix in the files modified by Tasks 1-3.
  </action>
  <verify>
    <automated>./gradlew test -PexcludeHeavyTests=true && ./gradlew ktlintCheck</automated>
  </verify>
  <done>
    All four commands exit 0. The full fast suite is green. No ktlint violations. The three-file change set (CustomPromptDefinition.kt + CustomPromptLibraryEditor.kt + CustomPromptLibraryJsonTest.kt) is complete and verified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User's filesystem ↔ handleImport / handleExport | The user selects a local `.json` file via `JFileChooser`. The file content crosses into the application trust boundary at `file.readText()` in `handleImport`. `parseLibraryJson` applies Jackson deserialization with `@JsonIgnoreProperties(ignoreUnknown = true)` on `CustomPromptDefinition`, and filters by `isValid()`, providing a defence-in-depth validation layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-PROM03-01 | T (Tampering) | `parseLibraryJson` — user-supplied JSON file content | mitigate | `@JsonIgnoreProperties(ignoreUnknown = true)` on the data class protects against unknown fields (forward compat). `filter { it.isValid() }` inside `parseLibraryJson` drops entries with blank `id`, `title`, `promptText`, or empty `tags` — malformed entries cannot enter `master`. Jackson parse exceptions are caught and return empty list. |
| T-3-PROM04-01 | T (Tampering) | `mergeById` — duplicate id injection in import file | mitigate | `associateBy { it.id }.values.toList()` (last-wins) collapses duplicate ids before merge, preventing id-collision non-determinism. The `mergeByIdDeduplicatesInputUsingLastOccurrenceWins` test locks this. |
| T-3-PROM05-01 | I (Information Disclosure) | `applyMove` — cross-boundary reorder of favorites/non-favorites | accept | `applyMove` returns the original list unchanged when the move would cross the favorites/non-favorites boundary. The `refreshButtons()` UI-level gate also prevents the user reaching `handleMove` at a boundary from the editor UI. Both controls are independent; the companion-level control protects programmatic callers. |
</threat_model>

<verification>
After all tasks complete, run:

```
./gradlew test --tests "com.six2dez.burp.aiagent.config.CustomPromptLibraryJsonTest" -PexcludeHeavyTests=true
./gradlew test --tests "com.six2dez.burp.aiagent.config.CustomPromptFilterTest" -PexcludeHeavyTests=true
./gradlew test -PexcludeHeavyTests=true
./gradlew ktlintCheck
```

All must exit 0.

Confirm structural changes:
- `grep -c 'fun parseLibraryJson\|fun mergeById\|fun applyMove' src/main/kotlin/com/six2dez/burp/aiagent/config/CustomPromptDefinition.kt` returns `3`
- `grep -c 'distinctBy' src/main/kotlin/com/six2dez/burp/aiagent/ui/components/CustomPromptLibraryEditor.kt` returns `0` (the first-wins bug is fully removed)
- `grep -c 'CustomPromptDefinition\.parseLibraryJson\|CustomPromptDefinition\.mergeById\|CustomPromptDefinition\.applyMove' src/main/kotlin/com/six2dez/burp/aiagent/ui/components/CustomPromptLibraryEditor.kt` returns `3`
- `grep -c '@Test' src/test/kotlin/com/six2dez/burp/aiagent/config/CustomPromptLibraryJsonTest.kt` returns `10`
</verification>

<success_criteria>
- Three new companion methods (`parseLibraryJson`, `mergeById`, `applyMove`) exist in `CustomPromptDefinition.Companion` and compile cleanly.
- `handleImport`, `handleExport`, `handleMove` in `CustomPromptLibraryEditor.kt` are thin shells. `distinctBy` is gone. The misleading comment is removed.
- INTENTIONAL BEHAVIOUR CHANGE is in effect: `mergeById` uses `associateBy` (last-occurrence-wins). The `mergeByIdDeduplicatesInputUsingLastOccurrenceWins` test locks this semantic correction.
- `CustomPromptLibraryJsonTest.kt` exists with 10 passing `@Test` methods covering PROM-03 (2 tests), PROM-04 (3 tests), PROM-05 (5 tests).
- No new dependency added. No Mockito in `CustomPromptLibraryJsonTest.kt`. No reflection.
- Full fast suite green. ktlint clean on all three modified/created files.
- `CustomPromptDefinition.kt` does NOT import anything from `com.six2dez.burp.aiagent.ui` (config ← ui layering intact).
</success_criteria>

<output>
After completion, create `.planning/phases/03-prompt-library-ux-audit/03-02-SUMMARY.md` documenting the three companion methods added, the three handlers rewired, the 10 tests created, and the test run output.
</output>

---
phase: 03-prompt-library-ux-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/kotlin/com/six2dez/burp/aiagent/config/CustomPromptFilterTest.kt
autonomous: true
requirements: [PROM-06]
tags: [prompt-library, filter, favorites, unit-test, behaviour-lock]

must_haves:
  truths:
    - "filterForMenu preserves input order (favorites-first) when the caller pre-sorts the library — the function does NOT re-sort internally"
    - "A test explicitly feeds a favorites-first-sorted library to filterForMenu and asserts the returned list preserves favorites before non-favorites, locking PROM-06"
    - "searchFilter whitespace-only query already locked by the existing test at line 92-95 (asserts both empty string AND three-space string) — no duplicate test added for PROM-01"
    - "All new test methods use CamelCase and JUnit org.junit.jupiter.api.Assertions.assertEquals — matching the existing file convention"
    - "sortFavoritesFirst tests are NOT added — the sort half of PROM-02 is already locked by three tests at lines 127-147"
  artifacts:
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/config/CustomPromptFilterTest.kt"
      provides: "1 new @Test method locking PROM-06 favorites-first ordering invariant of filterForMenu"
      contains: "fun filterForMenuPreservesExternalFavoritesFirstOrder"
  key_links:
    - from: "CustomPromptFilterTest.filterForMenuPreservesExternalFavoritesFirstOrder"
      to: "CustomPromptDefinition.filterForMenu (CustomPromptDefinition.kt:19-22)"
      via: "direct companion method call on a pre-sorted favorites-first library"
      pattern: "CustomPromptDefinition\\.filterForMenu"
    - from: "filterForMenuPreservesExternalFavoritesFirstOrder test assertion"
      to: "PROM-06 right-click submenu ordering contract"
      via: "assertEquals(listOf(favHttp, favDual, http, dual), result) — favorites first in result, not re-sorted"
      pattern: "filterForMenuPreservesExternalFavoritesFirstOrder"
---

<objective>
Add one `@Test` method to `CustomPromptFilterTest.kt` to lock PROM-06: the `filterForMenu` pure function preserves input order (favorites first) when the caller pre-sorts the library, and does NOT re-sort internally.

Purpose: PROM-06 requires that right-click submenus iterate entries in editor order (favorites first) without re-sorting at menu-build time. The five existing `filterForMenu` tests (`httpTagReturnsHttpAndDualOrdered`, `issueTagReturnsIssueAndDual`, `hiddenEntriesExcluded`, `emptyLibraryReturnsEmpty`, `preservesLibraryOrder`) do not use any `isFavorite = true` entries. The new test feeds a favorites-first-sorted library and asserts the filter preserves that ordering.

WHITESPACE DECISION: `searchFilterEmptyQueryReturnsLibraryUnchanged` (line 92) already asserts BOTH `""` and `"   "` in one test method (line 95: `CustomPromptDefinition.searchFilter(library, "   ")`). The whitespace path is already locked. No new PROM-01 test is needed.

SORT DECISION: All three `sortFavoritesFirst` variants are locked at lines 127-147. No new PROM-02 sort tests are added.

Output: One new `@Test fun filterForMenuPreservesExternalFavoritesFirstOrder()` method inside `class CustomPromptFilterTest`. No new file, no new helpers, no new imports, no production change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-prompt-library-ux-audit/03-CONTEXT.md
@.planning/phases/03-prompt-library-ux-audit/03-RESEARCH.md
@.planning/phases/03-prompt-library-ux-audit/03-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/test/kotlin/com/six2dez/burp/aiagent/config/CustomPromptFilterTest.kt
@src/main/kotlin/com/six2dez/burp/aiagent/config/CustomPromptDefinition.kt

<interfaces>
<!-- filterForMenu signature — verified from CustomPromptDefinition.kt:19-22 -->
<!-- This function is a pure filter. It does NOT sort. It preserves input order. -->
fun filterForMenu(
    library: List<CustomPromptDefinition>,
    tag: CustomPromptTag,
): List<CustomPromptDefinition> = library.filter { tag in it.tags && it.showInContextMenu }

<!-- CustomPromptDefinition data class — verified from CustomPromptDefinition.kt:8-15 -->
data class CustomPromptDefinition(
    val id: String,
    val title: String,
    val promptText: String,
    val tags: Set<CustomPromptTag>,
    val showInContextMenu: Boolean = true,
    val isFavorite: Boolean = false,
)
fun isValid(): Boolean = id.isNotBlank() && title.isNotBlank() && promptText.isNotBlank() && tags.isNotEmpty()

enum class CustomPromptTag { HTTP_SELECTION, SCANNER_ISSUE }

<!-- Existing fixtures in CustomPromptFilterTest.kt — lines 7-35. REUSE via .copy() — do not redefine. -->
private val http = CustomPromptDefinition(id="1", title="HTTP only", promptText="a", tags=setOf(HTTP_SELECTION))
private val issue = CustomPromptDefinition(id="2", title="Issue only", promptText="b", tags=setOf(SCANNER_ISSUE))
private val dual = CustomPromptDefinition(id="3", title="Both", promptText="c", tags=setOf(HTTP_SELECTION, SCANNER_ISSUE))
private val hiddenHttp = CustomPromptDefinition(id="4", title="Hidden", promptText="d", tags=setOf(HTTP_SELECTION), showInContextMenu=false)

<!-- Existing imports (lines 1-5) — no new imports required for the new test -->
import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Test
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add filterForMenuPreservesExternalFavoritesFirstOrder @Test method (PROM-06)</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/config/CustomPromptFilterTest.kt</files>
  <action>
    Add a new `@Test fun filterForMenuPreservesExternalFavoritesFirstOrder()` method to the existing `class CustomPromptFilterTest` body. This is the only change to this file.

    Implementation: Use `.copy()` on the existing `http` and `dual` fixtures to create two favorite variants, then build a pre-sorted library and assert the filter preserves favorites-first order.

    Method body:
    - Create `val favHttp = http.copy(id = "1f", isFavorite = true)` — an HTTP_SELECTION favorite derived from `http`.
    - Create `val favDual = dual.copy(id = "3f", isFavorite = true)` — an HTTP_SELECTION + SCANNER_ISSUE favorite derived from `dual`.
    - Build `val library = listOf(favHttp, favDual, http, dual)` — this list is already favorites-first (as `snapshot()` produces it via `sortFavoritesFirst`). Both `http` and `dual` have `showInContextMenu = true` (default). `favHttp` and `favDual` inherit `showInContextMenu = true` from their source fixtures.
    - Call `val result = CustomPromptDefinition.filterForMenu(library, CustomPromptTag.HTTP_SELECTION)`.
    - Assert `assertEquals(listOf(favHttp, favDual, http, dual), result)`.

    What this locks: `filterForMenu` is `library.filter { tag in it.tags && it.showInContextMenu }`. It is a pure filter — it preserves input order. If anyone accidentally inserts a sort inside `filterForMenu`, this test fails. All four entries have `HTTP_SELECTION` in `tags` and `showInContextMenu = true`, so all four pass the filter. The assertion proves the output order matches the input order (favorites first, then non-favorites), which is the PROM-06 invariant.

    Style rules:
    - Use `org.junit.jupiter.api.Assertions.assertEquals` — already imported at line 3. Do NOT add `kotlin.test.*` imports to this file (the file uses JUnit Assertions throughout).
    - Use `@Test` from `org.junit.jupiter.api.Test` — already imported at line 4.
    - CamelCase method name (per Phase 1 D-07 and CONVENTIONS.md) — do not adopt the snake_case style seen in some existing tests.
    - Do NOT add a `@DisplayName` annotation — none of the existing tests use it.
    - Do NOT add any new `private val` fixtures at class level — derive all needed instances from existing fixtures via `.copy()`.
    - Place the method at the end of the class body, after `sortFavoritesFirstAllFavoritesReturnsLibraryUnchanged` (line 146). This clusters the new PROM-06 test after the existing filterForMenu tests and sort tests.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.config.CustomPromptFilterTest.filterForMenuPreservesExternalFavoritesFirstOrder" -PexcludeHeavyTests=true</automated>
  </verify>
  <done>
    `filterForMenuPreservesExternalFavoritesFirstOrder` exists in `CustomPromptFilterTest.kt`, builds a pre-sorted favorites-first library, calls `filterForMenu` with `HTTP_SELECTION`, and asserts the returned list is `[favHttp, favDual, http, dual]` (favorites first, non-favorites second, input order preserved within each group). The test passes.

    Additional checks:
    - `grep -v '^[[:space:]]*//' src/test/kotlin/com/six2dez/burp/aiagent/config/CustomPromptFilterTest.kt | grep -c 'fun filterForMenuPreservesExternalFavoritesFirstOrder'` returns `1`
    - `./gradlew test --tests "com.six2dez.burp.aiagent.config.CustomPromptFilterTest" -PexcludeHeavyTests=true` exits 0 (all pre-existing + new test pass, no regressions)
    - `./gradlew ktlintCheck` does not flag any new violation in this file
    - No new import added, no new class-level fixture added, no production file changed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none for this plan) | Plan 01 adds a single test-only change to an existing test file. No production code is touched; no new attack surface is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-PROM06-01 | I (Information Disclosure) | `filterForMenu` ordering in right-click submenus | accept | Plan 01 locks the ordering invariant with a unit test. The function is a pure filter with no I/O, no network, and no state mutation. No security-relevant control is introduced or modified by this test-only plan. |
</threat_model>

<verification>
After Task 1 completes, run the full filter test class to confirm no regressions among the pre-existing tests:

```
./gradlew test --tests "com.six2dez.burp.aiagent.config.CustomPromptFilterTest" -PexcludeHeavyTests=true
```

Then run the full fast suite to confirm Plan 01 did not break anything elsewhere:

```
./gradlew test -PexcludeHeavyTests=true
```

Both must exit 0. ktlintCheck must remain clean on `CustomPromptFilterTest.kt`.

Confirm that no production file was modified and no new test file was created.
</verification>

<success_criteria>
- 1 new `@Test` method exists in `src/test/kotlin/com/six2dez/burp/aiagent/config/CustomPromptFilterTest.kt`: `filterForMenuPreservesExternalFavoritesFirstOrder`.
- The method uses `http.copy(id = "1f", isFavorite = true)` and `dual.copy(id = "3f", isFavorite = true)` to create favorite variants — no new class-level fixtures.
- The method asserts `assertEquals(listOf(favHttp, favDual, http, dual), result)` — favorites-first ordering preserved by `filterForMenu`.
- All pre-existing tests in `CustomPromptFilterTest.kt` still pass (no regressions).
- No new imports added. No production file changed. No new test file created.
- PROM-06 success criterion 5 ("Right-click submenu order matches editor order, without re-sorting at menu-build time") is locked by this test.
- ktlint check on the modified file remains clean.
</success_criteria>

<output>
After completion, create `.planning/phases/03-prompt-library-ux-audit/03-01-SUMMARY.md` documenting the method added and the test run output.
</output>

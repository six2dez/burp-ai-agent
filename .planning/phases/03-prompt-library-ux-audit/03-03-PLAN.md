---
phase: 03-prompt-library-ux-audit
plan: 03
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - .planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md
autonomous: true
requirements: [PROM-01, PROM-02, PROM-03, PROM-04, PROM-05]
tags: [prompt-library, manual-smoke, audit-artefact, human-uat]

must_haves:
  truths:
    - "A maintainer-fillable manual-smoke audit artefact exists at `.planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md` with the exact YAML frontmatter + section structure used by Phase 2's `02-HUMAN-UAT.md`"
    - "Per D-09: scenario 1 locks the search live-filter behaviour (PROM-01)"
    - "Per D-09: scenario 2 locks the favorite toggle + star renderer (PROM-02)"
    - "Per D-09: scenario 3 locks the Move Up/Down button enable/disable at the favorites/non-favorites boundary (PROM-05)"
    - "Per D-09: scenario 4 locks the Export + Import JFileChooser round-trip including duplicate-id injection (PROM-03, PROM-04)"
    - "Per D-10: frontmatter uses status: partial, phase: 03-prompt-library-ux-audit, source: [03-VERIFICATION.md], ISO-8601 started/updated, counters total:4 / pending:4 / all others 0"
  artifacts:
    - path: ".planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md"
      provides: "Four-scenario manual-smoke audit trail for PROM-01/02/03/04/05 UI behaviours. YAML frontmatter mirrors 02-HUMAN-UAT.md."
      contains: "phase: 03-prompt-library-ux-audit"
  key_links:
    - from: "03-HUMAN-UAT.md scenario 1"
      to: "CustomPromptLibraryEditor.kt DocumentListener wiring (lines 81-89) + CustomPromptDefinition.searchFilter companion"
      via: "manual observation: type substring in search field, watch row set update; unit-level logic locked by CustomPromptFilterTest"
      pattern: "Search field live-filter"
    - from: "03-HUMAN-UAT.md scenario 3"
      to: "CustomPromptLibraryEditor.refreshButtons() + hasNeighborOfSameStatus() + applyMove companion"
      via: "manual observation: select last favorite, Move Down disabled; select first non-favorite, Move Up disabled"
      pattern: "Move Up/Down button"
    - from: "03-HUMAN-UAT.md scenario 4"
      to: "handleImport (thin shell) + parseLibraryJson + mergeById + associateBy last-wins semantics (D-02)"
      via: "manual: hand-edit JSON to inject duplicate id, re-import, observe library deduplicates with last-wins behaviour"
      pattern: "duplicate id"
---

<objective>
Generate the maintainer-fillable manual-smoke audit artefact `.planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md`. This is the proof-of-behaviour vehicle for the four Swing-layer invariants that pure-function unit tests cannot reach: (1) search field live-filter DocumentListener wiring, (2) favorite toggle + `ListCellRenderer` star rendering, (3) Move button enable/disable at the favorites/non-favorites boundary, (4) Export/Import JFileChooser round-trip including the duplicate-id deduplication path.

Purpose: lock the UI-layer contract for PROM-01/02/03/04/05 by recording four scenarios with the exact expected outcomes. The planner builds the scaffolding; the maintainer fills `result:` fields during execution in real Burp (mirrors Phase 2 pattern in `02-HUMAN-UAT.md`).

Wave dependency: This plan depends on `03-02` because the maintainer's smoke runs against the wired-up editor (handlers calling companion methods). Scenario 4's duplicate-id deduplication step specifically validates the intentional `distinctBy` → `associateBy` semantic correction from Plan 02.

Output: one new file at `.planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md`. No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-prompt-library-ux-audit/03-CONTEXT.md
@.planning/phases/03-prompt-library-ux-audit/03-RESEARCH.md
@.planning/phases/03-prompt-library-ux-audit/03-VALIDATION.md
@.planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md

<interfaces>
<!-- Phase 2 frontmatter (verbatim from 02-HUMAN-UAT.md — Phase 3 lifts this shape 1:1 except keyed values) -->
---
status: partial
phase: 02-insertion-point-scan-audit
source: [02-VERIFICATION.md]
started: 2026-05-13T11:35:00Z
updated: 2026-05-13T11:35:00Z
---

<!-- Phase 2 section structure (from 02-HUMAN-UAT.md) — Phase 3 mirrors exactly, scaled to 4 scenarios -->
## Current Test
[awaiting human testing]

## Tests
### N. <name> (<req-ids>)
expected: <literal>
result: [pending]

## Summary
total: N
passed: 0
issues: 0
pending: N
skipped: 0
blocked: 0

## Gaps

<!-- Phase 3 has 4 scenarios (not 6 like Phase 2) — frontmatter counters: total:4 / pending:4 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 03-HUMAN-UAT.md with YAML frontmatter and four-scenario manual-smoke scaffolding</name>
  <files>.planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md</files>
  <action>
    Create the new file `.planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md` using the Write tool (not heredoc/cat). Structure mirrors `02-HUMAN-UAT.md` exactly, scaled to 4 scenarios.

    File content layout:

    1. YAML frontmatter (5 keys in this exact order — mirrors `02-HUMAN-UAT.md`):
       - `status: partial`
       - `phase: 03-prompt-library-ux-audit`
       - `source: [03-VERIFICATION.md]`
       - `started: 2026-05-13T12:00:00Z` (ISO-8601 UTC — use this fixed value matching today's date)
       - `updated: 2026-05-13T12:00:00Z`

    2. Section `## Current Test` with the literal placeholder `[awaiting human testing]` on its own line.

    3. Section `## Tests` containing four scenario blocks. Each block has a `### N. <title> (<req-ids>)` heading line, an `expected: <literal>` line, and a `result: [pending]` line.

       The four scenarios:

       Scenario 1 — `### 1. Search field live-filter (PROM-01)`
       expected: `Open Settings -> Prompt Templates with 3 or more entries. Type a substring into the search field. The visible row set updates on every keystroke without lag. Clear the field — all rows return.`
       result: `[pending]`

       Scenario 2 — `### 2. Favorite toggle and star renderer (PROM-02)`
       expected: `Select a non-favorite entry. Click the favorite toggle button. The entry jumps to the top of the table with a visible star prefix in the list cell. Toggle off — the entry returns to its position within the non-favorites group.`
       result: `[pending]`

       Scenario 3 — `### 3. Move Up/Down disabled at favorites/non-favorites boundary (PROM-05)`
       expected: `Select the last favorite entry. Move Down button is disabled. Select the first non-favorite entry. Move Up button is disabled. Entries cannot be reordered across the boundary via the UI.`
       result: `[pending]`

       Scenario 4 — `### 4. Export and Import JFileChooser round-trip with duplicate-id injection (PROM-03, PROM-04)`
       expected: `Click Export, pick a target .json file. Open the file in a text editor — favorites appear first, JSON is pretty-printed. Click Import on the same file — library unchanged. Hand-edit the JSON to inject a second entry with a duplicate id (different title). Re-import — library deduplicates defensively: the last-occurring entry for the duplicate id replaces the existing one; no JOptionPane error dialog.`
       result: `[pending]`

    4. Section `## Summary` with six counter lines reflecting "all four pending":
       - `total: 4`
       - `passed: 0`
       - `issues: 0`
       - `pending: 4`
       - `skipped: 0`
       - `blocked: 0`

    5. Trailing section `## Gaps` with empty body (mirrors `02-HUMAN-UAT.md` structure — present so the maintainer can append observations without touching the file structure).

    Formatting rules:
    - LF line endings.
    - One blank line between top-level sections.
    - No trailing whitespace. Final newline present.
    - The `## Gaps` section has no body content (empty body — maintainer fills it in after smoke runs).
    - Do NOT include Kotlin code, test method references, or implementation details inside scenario blocks — the scenarios describe user-visible behaviour only.
    - Scenario 4's `expected:` description must explicitly mention "last-occurring entry" deduplication behaviour (validates the D-02 intentional semantic correction from Plan 02).
  </action>
  <verify>
    <automated>test -f .planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md && head -1 .planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md | grep -q '^---$' && grep -c '^### [1-4]\. ' .planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md | grep -q '^4$' && grep -q 'phase: 03-prompt-library-ux-audit' .planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md && grep -q 'total: 4' .planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md && grep -q 'pending: 4' .planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md && grep -c '^result: \[pending\]$' .planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md | grep -q '^4$' && grep -q 'last-occurring' .planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md && echo OK</automated>
  </verify>
  <done>
    `.planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md` exists with valid YAML frontmatter and 4 scenario blocks mirroring `02-HUMAN-UAT.md`.

    Acceptance checks:
    - File exists and first line is `---` (YAML frontmatter open).
    - Frontmatter contains all five keys: `status: partial`, `phase: 03-prompt-library-ux-audit`, `source: [03-VERIFICATION.md]`, `started: 2026-05-13T12:00:00Z`, `updated: 2026-05-13T12:00:00Z`.
    - File contains exactly 4 lines matching `^### [1-4]\. ` (four scenario headings).
    - File contains exactly 4 lines matching `^result: \[pending\]$` (one per scenario).
    - `## Summary` block contains `total: 4`, `passed: 0`, `issues: 0`, `pending: 4`, `skipped: 0`, `blocked: 0`.
    - Scenario 4's `expected:` line references "last-occurring" to describe the D-02 deduplication behaviour.
    - File ends with `## Gaps` followed by a final newline (no body content).
    - File does NOT contain Kotlin code, class names, or method names.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none for this plan) | Plan 03 creates a documentation artefact only. No code path is added or changed; no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-PROM01-01 | I (Information Disclosure) | Search field — DocumentListener wiring | accept | The search field filters the `listModel` view only — it does not affect `master`. No data is written outside the local JVM. The companion-level `searchFilter` pure function is already locked by `CustomPromptFilterTest`. This plan's scenario 1 verifies the Swing wiring (keystroke → `refreshList()` → `searchFilter`) in real Burp. |
| T-3-PROM0304-01 | T (Tampering) | Import JFileChooser — user-supplied JSON file | accept | Plan 02 mitigates this via `parseLibraryJson` (`isValid()` filter + Jackson exception catch) and `mergeById` (`associateBy` last-wins dedup). Scenario 4 of this plan verifies the end-to-end path in real Burp, including the duplicate-id injection case. No additional mitigation needed at the documentation-artefact layer. |
</threat_model>

<verification>
After Task 1 completes, run the structural sanity checks:

```
test -f .planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md
grep -c '^### [1-4]\. ' .planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md
grep -c '^result: \[pending\]$' .planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md
```

First command must succeed. Second and third must each return `4`.

No gradle build or test run is needed — Plan 03 touches only the `.planning/` directory.

The maintainer's smoke run (in real Burp with the JAR built from the Plan 02 refactored code) is a separate manual activity outside this plan's automated execution. Plan 03 is complete once the scaffolding file exists and is structurally valid.
</verification>

<success_criteria>
- New file `.planning/phases/03-prompt-library-ux-audit/03-HUMAN-UAT.md` exists with valid YAML frontmatter and four-scenario scaffolding mirroring `02-HUMAN-UAT.md`.
- All four scenarios have clear `expected:` descriptions for user-visible Swing behaviour (search live-filter, favorite toggle + star, Move button boundary, Export/Import round-trip with duplicate-id).
- Scenario 4 explicitly names "last-occurring entry" deduplication to validate the D-02 intentional semantic correction from Plan 02.
- Summary counters reflect "all four pending" (`total: 4`, `pending: 4`).
- No production code changed. No new test file created.
- Artefact is ready for the maintainer to record results during the Phase 3 execution smoke run.
</success_criteria>

<output>
After completion, create `.planning/phases/03-prompt-library-ux-audit/03-03-SUMMARY.md` documenting the artefact creation and the timestamp values chosen for the YAML frontmatter.
</output>

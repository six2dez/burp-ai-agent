---
phase: 13-privacy-redaction-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/redact/SafeRegex.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/redact/SafeRegexTest.kt
autonomous: true
requirements: [PRIV-01, PRIV-02]
must_haves:
  truths:
    - "Redaction.anonymizeHost derives the anonymized host via HMAC-SHA256 HKDF (extract-then-expand), not a SHA-256 message digest"
    - "Anonymized host output keeps the exact format host-<12hex>.local"
    - "Same (salt, host) yields the same anonymized host; different salt yields a different one (determinism + per-salt difference preserved)"
    - "SafeRegex bounds any single regex match to ~50 ms and returns the original input unchanged on timeout (never hangs, never throws into the pipeline)"
    - "SafeRegex.isPatternSafe rejects a catastrophic-backtracking pattern within the timeout budget and accepts a benign pattern"
    - "The full existing test suite stays green after the HKDF swap (no hardcoded host hashes exist to update)"
  artifacts:
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt"
      provides: "HKDF anonymizeHost (HMAC-SHA256 extract/expand) replacing the SHA-256 digest, signature preserved"
      contains: "HmacSHA256"
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/redact/SafeRegex.kt"
      provides: "Interruptible-CharSequence ReDoS-safe regex primitive (replaceAllSafe, isPatternSafe) reused by body redaction and custom-pattern validation"
      min_lines: 40
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/redact/SafeRegexTest.kt"
      provides: "ReDoS guard coverage (catastrophic pattern times out; benign passes)"
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionTest.kt"
      provides: "HKDF format + RFC 5869 vector assertions added to existing STRICT/BALANCED/OFF coverage"
  key_links:
    - from: "src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt"
      to: "javax.crypto.Mac (HmacSHA256)"
      via: "private hmacSha256/hkdfExtract/hkdfExpand helpers inside anonymizeHost"
      pattern: "Mac\\.getInstance\\(\"HmacSHA256\"\\)"
    - from: "src/main/kotlin/com/six2dez/burp/aiagent/redact/SafeRegex.kt"
      to: "java.util.regex.Matcher"
      via: "DeadlineCharSequence whose get() throws RegexTimeoutException past the deadline"
      pattern: "DeadlineCharSequence"
---

<objective>
Lay the redaction-core foundation for Phase 13: migrate host anonymization to real RFC 5869 HKDF (PRIV-01) and create the reusable ReDoS-safe regex primitive `SafeRegex` (PRIV-02 foundation). Both live in the AWT-free `redact` package so Phase 15's tripwire can reuse them. This plan owns `Redaction.kt`'s `anonymizeHost` body and the new `SafeRegex.kt`; it deliberately does NOT add body/JSON/custom-pattern matching (Plan 02 does that, building on `SafeRegex`).

Purpose: The SPEC documents "HKDF host anonymization" but the code uses a plain SHA-256 digest (PRIV-01 fixes the privacy-claim/implementation mismatch). The custom-pattern feature (Plan 02) and its save-time validation (Plan 02 UI) both need a single ReDoS-bounding primitive — building it here first keeps `Redaction.kt` edits in Plan 02 small and gives Plan 02/Plan 15 a tested dependency.
Output: HKDF `anonymizeHost` (signature unchanged), `redact/SafeRegex.kt`, `redact/SafeRegexTest.kt`, and HKDF format/vector assertions added to `RedactionTest.kt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-privacy-redaction-hardening/13-CONTEXT.md
@.planning/phases/13-privacy-redaction-hardening/13-RESEARCH.md
@.planning/phases/13-privacy-redaction-hardening/13-PATTERNS.md
@.planning/phases/13-privacy-redaction-hardening/13-VALIDATION.md

# Crypto idiom analog (JCA getInstance -> init(SecretKeySpec) -> doFinal; fail-soft logging)
@src/main/kotlin/com/six2dez/burp/aiagent/config/SecretCipher.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — add HKDF format/vector tests and create SafeRegexTest (RED)</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionTest.kt, src/test/kotlin/com/six2dez/burp/aiagent/redact/SafeRegexTest.kt</files>
  <read_first>
    - src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionTest.kt (existing JUnit Jupiter shape to extend — assertion style asserts the `host-` prefix substring + determinism, never a literal hash)
    - .planning/phases/13-privacy-redaction-hardening/13-VALIDATION.md (the per-requirement automated command map — these test names are the verification contract)
    - .planning/phases/13-privacy-redaction-hardening/13-RESEARCH.md (Pattern 1 lines 242-289 for the RFC 5869 PRK/OKM construction; Sources line 748 for the published Test Case 1 PRK `077709362...` / OKM `3cb25f25...` vectors; Pattern 2 lines 358-361 for the SafeRegex adversarial cases)
    - .planning/phases/13-privacy-redaction-hardening/13-PATTERNS.md (RedactionTest analog lines 379-433 — existing test shape and the prefix-only assertion rule)
  </read_first>
  <behavior>
    - Test (RedactionTest, NEW): `hostAnonymizationFormatIsStable` — `Redaction.anonymizeHost("example.com", "salt")` matches the regex `^host-[0-9a-f]{12}\.local$`.
    - Test (RedactionTest, NEW): `hkdfMatchesRfc5869Vector` — assert that the plan's HKDF construction reproduces RFC 5869 Test Case 1: given IKM = 22 bytes of 0x0b, salt = 0x000102...0c (13 bytes), info = 0xf0f1...f9 (10 bytes), L = 42, the OKM equals the published `3cb25f25faacd57a90434f64d0362f2a...34007208d5b887185865` (test against whatever public/internal HKDF entry the impl exposes; if HKDF helpers are private, expose a single internal test seam or assert via a temporary internal function — Claude's discretion, keep it minimal).
    - Test (SafeRegexTest, NEW file): `catastrophicPatternIsRejectedWithinBudget` — `SafeRegex.isPatternSafe("(a+)+$")` returns false and the call completes well under ~200 ms wall-clock.
    - Test (SafeRegexTest, NEW file): `benignPatternIsAccepted` — `SafeRegex.isPatternSafe("\\d+")` returns true.
    - Test (SafeRegexTest, NEW file): `catastrophicPatternTimesOutAndReturnsInput` — `SafeRegex.replaceAllSafe(<long "aaaa...a!" string>, Pattern.compile("(a+)+$"), "[REDACTED]")` returns the original input unchanged (fail-open) and does not hang.
    - Test (SafeRegexTest, NEW file): `benignReplaceAppliesReplacement` — `replaceAllSafe("id=12345", Pattern.compile("\\d+"), "[REDACTED]")` returns `"id=[REDACTED]"`.
    - These compile-and-fail (RED) because `SafeRegex` and the new `anonymizeHost` behavior do not exist yet — that is expected; Task 2/3 make them pass.
  </behavior>
  <action>
    Extend `RedactionTest.kt` with the two HKDF tests above and create the new `SafeRegexTest.kt` in the same package with the four SafeRegex tests, mirroring the existing JUnit Jupiter shape (`org.junit.jupiter.api.Assertions.*`, `@Test`, trimIndent fixtures). Do NOT hardcode any expected anonymized host hash value — assert the `host-[0-9a-f]{12}\.local` FORMAT and per-salt determinism only (per 13-PATTERNS.md anti-pattern note: keep the prefix+determinism style so the crypto can evolve). For `hkdfMatchesRfc5869Vector`, use the RFC 5869 Test Case 1 inputs/outputs from 13-RESEARCH.md Sources; this test pins the HKDF math itself (independent of the host-format truncation). Wave 0 RED is expected to fail to compile/run until Tasks 2-3 land — commit it anyway as the failing baseline.
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/burp-ai-agent &amp;&amp; ./gradlew test --tests "com.six2dez.burp.aiagent.redact.*" ; test $? -ne 0 &amp;&amp; echo "RED-as-expected"</automated>
  </verify>
  <acceptance_criteria>
    - `src/test/kotlin/com/six2dez/burp/aiagent/redact/SafeRegexTest.kt` exists with the four named tests.
    - `RedactionTest.kt` contains `hostAnonymizationFormatIsStable` and `hkdfMatchesRfc5869Vector`.
    - No test asserts a literal anonymized-host hash value (grep: `grep -n "host-[0-9a-f]\{12\}\.local" src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionTest.kt` shows only a FORMAT/regex assertion, not an equality against a fixed 12-hex string).
    - The suite fails to compile or fails the new tests at this point (RED) — `SafeRegex` and HKDF do not exist yet.
  </acceptance_criteria>
  <done>Wave 0 test scaffold committed; the four SafeRegex tests + two HKDF tests exist and are RED (referencing not-yet-created `SafeRegex` / not-yet-changed `anonymizeHost`).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create redact/SafeRegex.kt (interruptible-CharSequence ReDoS guard) — GREEN for SafeRegexTest</name>
  <files>src/main/kotlin/com/six2dez/burp/aiagent/redact/SafeRegex.kt</files>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt (the `object` + object-level const/Regex layout to mirror; same `redact` package)
    - src/main/kotlin/com/six2dez/burp/aiagent/config/SecretCipher.kt (fail-soft try/catch discipline lines 105-109: catch, log key name never value, return safe fallback; companion-const grouping lines 148-169)
    - .planning/phases/13-privacy-redaction-hardening/13-RESEARCH.md (Pattern 2 lines 305-357 — the [CITED] ocpsoft interruptible-CharSequence reference impl: DeadlineCharSequence, RegexTimeoutException, replaceAllSafe, isPatternSafe, ADVERSARIAL_PROBE)
    - .planning/phases/13-privacy-redaction-hardening/13-PATTERNS.md (SafeRegex.kt analog section lines 130-168 — object/const layout + fail-soft idiom + the abandon-no-executor anti-pattern)
  </read_first>
  <behavior>
    - `SafeRegex.isPatternSafe("(a+)+$")` == false (catastrophic backtracking exceeds the deadline); `SafeRegex.isPatternSafe("\\d+")` == true.
    - `SafeRegex.isPatternSafe("[")` == false (uncompilable regex — caught as PatternSyntaxException).
    - `SafeRegex.replaceAllSafe(catastrophicInput, Pattern.compile("(a+)+$"), "[REDACTED]")` returns the input unchanged within budget (fail-open).
    - `SafeRegex.replaceAllSafe("id=12345", Pattern.compile("\\d+"), "[REDACTED]")` == `"id=[REDACTED]"`.
  </behavior>
  <action>
    Create `src/main/kotlin/com/six2dez/burp/aiagent/redact/SafeRegex.kt` as a top-level `object SafeRegex` in package `com.six2dez.burp.aiagent.redact`, following 13-RESEARCH.md Pattern 2 verbatim in structure: a private `class DeadlineCharSequence(inner, deadlineNanos)` implementing `CharSequence` whose `get(index)` throws `RegexTimeoutException` once `System.nanoTime() > deadlineNanos` (the `Matcher` reads input via `charAt`/`get` during backtracking, so the deadline is observed promptly); a `class RegexTimeoutException : RuntimeException()`; `const val DEFAULT_TIMEOUT_MS = 50L`; `fun replaceAllSafe(input, pattern, replacement, timeoutMs = DEFAULT_TIMEOUT_MS): String` that wraps the input in `DeadlineCharSequence`, calls `pattern.matcher(...).replaceAll(replacement)`, and on `RegexTimeoutException` returns the ORIGINAL `input` unchanged (fail-open — never throw into the redaction pipeline, never hang); `fun isPatternSafe(regex, timeoutMs = DEFAULT_TIMEOUT_MS): Boolean` that `Pattern.compile(regex)` (syntax check), runs `.matcher(DeadlineCharSequence(ADVERSARIAL_PROBE, deadline)).find()`, returns true on completion, false on either `PatternSyntaxException` or `RegexTimeoutException`; a `private const val ADVERSARIAL_PROBE` of ~64 `a`s + trailing `!` (per Pitfall 2: keep the probe short so even exponential blowup is detected well under 50 ms). Take the `java.util.regex.Pattern`/`Matcher` import family (NOT `kotlin.text.Regex` — the interruptible-CharSequence trick needs the raw `Matcher`). Keep this file AWT-free (no Swing import) so Phase 15's scanner-side tripwire can depend on it. Do NOT use an `ExecutorService` you abandon on timeout (13-PATTERNS.md / 13-RESEARCH.md anti-pattern: the runaway regex keeps burning a pool thread). English-only comments. After writing, the SafeRegexTest cases from Task 1 turn GREEN; do NOT touch `Redaction.kt` in this task.
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/burp-ai-agent &amp;&amp; ./gradlew test --tests "com.six2dez.burp.aiagent.redact.SafeRegexTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "*SafeRegexTest"` passes (all four/five SafeRegex tests GREEN).
    - `SafeRegex.kt` contains `class DeadlineCharSequence` and `const val DEFAULT_TIMEOUT_MS = 50L`.
    - `SafeRegex.kt` contains NO `import java.awt`, NO `import javax.swing`, and NO `ExecutorService`/`newFixedThreadPool` usage (grep returns empty for `java.awt`, `javax.swing`, `ExecutorService`).
    - `replaceAllSafe` returns the original input on `RegexTimeoutException` (fail-open verified by `catastrophicPatternTimesOutAndReturnsInput`).
  </acceptance_criteria>
  <done>`SafeRegex` exists, is AWT-free, bounds matches to ~50 ms, fails open on timeout, and all SafeRegexTest cases pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Swap anonymizeHost SHA-256 -> RFC 5869 HKDF, signature preserved — GREEN for HKDF tests + full suite</name>
  <files>src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt</files>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt (the exact method at lines 121-137: preserve the `anonymizeHost(host, salt, recordMapping = true)` signature, the `host-<short>.local` format, and the in-memory `hostForwardMap`/`hostReverseMap` recording at lines 131-135; only replace the digest body at lines 126-130; import block lines 1-5)
    - src/main/kotlin/com/six2dez/burp/aiagent/config/SecretCipher.kt (JCA idiom lines 142-145: getInstance + init(SecretKeySpec) + doFinal; the repo already uses PBKDF2WithHmacSHA256 — same HMAC family)
    - .planning/phases/13-privacy-redaction-hardening/13-RESEARCH.md (Pattern 1 lines 242-289 — the [VERIFIED] RFC 5869 extract/expand: hmacSha256 with empty-key guard, hkdfExtract, hkdfExpand, HKDF_INFO = "burp-ai-agent:host", HKDF_OKM_LEN = 6; Pitfall 1 line 574 empty-salt guard; Pitfall 6 line 604 keep the signature)
    - .planning/phases/13-privacy-redaction-hardening/13-PATTERNS.md (Redaction.kt MODIFY section lines 39-126 — the exact lines being replaced and the JCA idiom to mirror)
  </read_first>
  <behavior>
    - `anonymizeHost("example.com", "salt")` returns a string matching `^host-[0-9a-f]{12}\.local$` (format preserved; `hostAnonymizationFormatIsStable` GREEN).
    - `anonymizeHost("example.com", "salt-a") == anonymizeHost("example.com", "salt-a")` and `!= anonymizeHost("example.com", "salt-b")` (HMAC determinism + per-salt difference; existing `hostAnonymizationIsStablePerSalt` stays GREEN).
    - The HKDF math reproduces RFC 5869 Test Case 1 PRK/OKM (`hkdfMatchesRfc5869Vector` GREEN).
    - `deAnonymizeHost` still resolves a previously recorded anon host (existing `clearMappings*` test stays GREEN).
    - STRICT mode still strips cookies/tokens/hosts; the full ~308-test suite stays GREEN.
  </behavior>
  <action>
    Per PRIV-01 and the CONTEXT locked decision (real HKDF, keep `host-<12hex>.local`, reuse `stableHostSalt`, no persistence migration): inside `Redaction` add three private helpers from 13-RESEARCH.md Pattern 1 — `hmacSha256(key, data)` using `Mac.getInstance("HmacSHA256")` + `SecretKeySpec` (substitute a single zero byte when the key is empty, per Pitfall 1, since `SecretKeySpec` rejects a 0-length key), `hkdfExtract(salt, ikm) = HMAC(salt, ikm)`, and `hkdfExpand(prk, info, length)` (T(i)=HMAC(PRK, T(i-1)|info|counterByte), concatenate to `length`). Add private constants `HKDF_INFO = "burp-ai-agent:host"` and `HKDF_OKM_LEN = 6` (6 bytes -> 12 hex -> exact format preservation; `L=6` keeps the 2^48 collision space identical to the old `digest.take(6)`). Replace `anonymizeHost`'s body lines 126-130 only: compute `prk = hkdfExtract(salt.toByteArray(UTF_8), host.toByteArray(UTF_8))`, `okm = hkdfExpand(prk, HKDF_INFO.toByteArray(UTF_8), HKDF_OKM_LEN)`, `short = okm.joinToString("") { "%02x".format(it) }`, then `"host-$short.local"`. Keep the `recordMapping` map-write block (lines 131-135) verbatim and keep the exact signature `anonymizeHost(host, salt, recordMapping = true)` (Pitfall 6: ~10 callers depend on it — do NOT add an `info`/`L` parameter; make them private constants). Update imports: add `javax.crypto.Mac`, `javax.crypto.spec.SecretKeySpec`, `java.io.ByteArrayOutputStream`; remove `import java.security.MessageDigest` once the digest is gone (confirm no other `MessageDigest` use remains in the file). English-only comments; reference the RFC 5869 source in a brief comment. Do NOT add body/JSON/custom-pattern redaction here — that is Plan 02.
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/burp-ai-agent &amp;&amp; ./gradlew test --tests "com.six2dez.burp.aiagent.redact.*" &amp;&amp; ./gradlew test</automated>
  </verify>
  <acceptance_criteria>
    - `Redaction.kt` `anonymizeHost` contains `Mac.getInstance("HmacSHA256")` and does NOT contain `MessageDigest.getInstance("SHA-256")` (grep: `grep -c 'MessageDigest' src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt` returns 0).
    - The `anonymizeHost` signature is unchanged: `grep -n "fun anonymizeHost(" Redaction.kt` shows the same `host`, `salt`, `recordMapping = true` parameters; no caller file outside `redact/` was edited (grep the 7 caller files compile unchanged).
    - `./gradlew test --tests "*RedactionTest"` passes including `hostAnonymizationFormatIsStable`, `hkdfMatchesRfc5869Vector`, `hostAnonymizationIsStablePerSalt`, `clearMappings*`, `strictMode*`.
    - Full `./gradlew test` is green (~308 tests baseline; no regression).
  </acceptance_criteria>
  <done>Host anonymization uses RFC 5869 HKDF (HMAC-SHA256) with the format/signature preserved; HKDF vector + format tests pass; the full suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP message string -> Redaction.apply | Untrusted captured traffic (headers + body) crosses into the redaction transform before any AI send |
| stableHostSalt -> anonymizeHost | Non-secret per-install salt used as HKDF salt/IKM input; output must not leak the real host |
| (Plan 02/UI) custom regex -> SafeRegex | Arbitrary user-supplied regex evaluated against captured traffic — the ReDoS surface this plan's primitive guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure / Repudiation | `Redaction.anonymizeHost` (SHA-256 mislabeled "HKDF") | mitigate | Replace with RFC 5869 HKDF (HMAC-SHA256 extract/expand) so the SPEC's privacy claim matches the code; verified against RFC 5869 Test Case 1 (Task 3, `hkdfMatchesRfc5869Vector`). ASVS V6. **HIGH — secret/privacy correctness.** |
| T-13-02 | Denial of Service | `SafeRegex` (regex engine on captured traffic / user patterns) | mitigate | Interruptible-`CharSequence` 50 ms deadline bounds catastrophic backtracking; `replaceAllSafe` fails open (returns input), `isPatternSafe` rejects slow/uncompilable patterns. Verified by `SafeRegexTest` (Tasks 1-2). ASVS V5. **HIGH — ReDoS.** |
| T-13-03 | Information Disclosure | `SafeRegex` / `anonymizeHost` error paths | mitigate | Fail-soft discipline mirrored from `SecretCipher`: never throw into the pipeline, never log the matched/secret content or the real host; `RegexTimeoutException` is swallowed and the input returned. ASVS V7. |
| T-13-04 | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — all primitives are JDK 21 built-ins (`javax.crypto.Mac`, `java.util.regex`). RESEARCH "Package Legitimacy Audit" = not applicable. |
| T-13-SC | Tampering | dependency supply chain | accept | Zero new dependencies (CLAUDE.md "prefer JDK built-ins"; REQUIREMENTS excludes Bouncy Castle/Tink). No slopcheck surface. |
</threat_model>

<verification>
- `./gradlew test --tests "com.six2dez.burp.aiagent.redact.*"` green (HKDF format + RFC vector + determinism + SafeRegex guard).
- `./gradlew test` full suite green (no regression from the HKDF swap; confirms no hardcoded host hashes existed).
- Source assertions: `Redaction.kt` contains `Mac.getInstance("HmacSHA256")`, contains no `MessageDigest`; `SafeRegex.kt` is AWT-free and uses no abandoned executor.
- Do NOT gate on `./gradlew ktlintCheck` (pre-existing `generateBuildFlags` wiring defect — MEMORY/VALIDATION note).
</verification>

<success_criteria>
- PRIV-01: `anonymizeHost` uses HKDF (HMAC-SHA256), format `host-<12hex>.local` and signature preserved, existing privacy-mode tests stay green (ROADMAP SC1).
- PRIV-02 foundation: `SafeRegex` exists, AWT-free, bounds any single match to ~50 ms, fails open, and its ReDoS guard is unit-tested (ROADMAP SC3/SC5 prerequisite consumed by Plan 02).
- No new runtime dependency added (CLAUDE.md / REQUIREMENTS out-of-scope row).
</success_criteria>

<output>
Create `.planning/phases/13-privacy-redaction-hardening/13-01-SUMMARY.md` when done.
</output>

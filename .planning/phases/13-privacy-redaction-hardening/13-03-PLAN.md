---
phase: 13-privacy-redaction-hardening
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/redact/SecretShapes.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/components/ContextPreviewDialog.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/redact/SecretShapesTest.kt
autonomous: true
requirements: [PRIV-04]
user_setup: []
must_haves:
  truths:
    - "A shared, AWT-free SecretShapes object exposes a curated set of high-confidence secret shapes (OpenAI key, AWS access key, GitHub token, JWT, etc.) and findSurviving(text) returns the set of matching category names"
    - "Each curated shape matches a positive sample and a benign string yields an empty set"
    - "The redaction preview dialog shows a non-blocking WARN banner naming the surviving shape CATEGORIES (never the raw value) when a known secret shape survives post-redaction; clean context shows no banner"
    - "The banner is informational only — the dialog's existing Send/Cancel options are unchanged (no hard stop, no confirmation step)"
    - "SecretShapes carries no Swing/AWT import so the Phase 15 tripwire can reuse it as a single source of truth"
  artifacts:
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/redact/SecretShapes.kt"
      provides: "object SecretShapes — ordered List<Shape(category, Regex)> + findSurviving(text): Set<String>; single source of truth for PRIV-04 and the Phase 15 tripwire"
      contains: "findSurviving"
      min_lines: 20
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/ui/components/ContextPreviewDialog.kt"
      provides: "SubtleNotice WARN banner in the header stack, driven by SecretShapes.findSurviving(contextJson)"
      contains: "SecretShapes.findSurviving"
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/redact/SecretShapesTest.kt"
      provides: "Positive (each shape) + negative (benign) coverage of findSurviving"
  key_links:
    - from: "src/main/kotlin/com/six2dez/burp/aiagent/ui/components/ContextPreviewDialog.kt"
      to: "SecretShapes.findSurviving"
      via: "scan the post-redaction contextJson; non-empty -> SubtleNotice.setMessage(Level.WARN, …), empty -> hideNotice()"
      pattern: "SecretShapes\\.findSurviving"
    - from: "src/main/kotlin/com/six2dez/burp/aiagent/ui/components/ContextPreviewDialog.kt"
      to: "SubtleNotice (Level.WARN)"
      via: "banner added to the header BoxLayout(Y_AXIS) stack before bodyScroll"
      pattern: "SubtleNotice"
---

<objective>
Implement PRIV-04: introduce the shared, AWT-free `redact/SecretShapes.kt` curated secret-shape set (the single source of truth that Phase 15's tripwire will also consume) and surface a non-blocking WARN banner in the existing `ContextPreviewDialog` when a known secret shape survived the redaction pipeline — so the user sees what the pipeline missed before clicking Send.

Purpose: Redaction is best-effort hand-curated regex; PRIV-04 closes the feedback loop by revealing residual high-confidence secrets in the post-redaction preview. Building `SecretShapes` here (not in Phase 15) establishes the single source of truth now, per the CONTEXT decision. This plan is independent of Plan 02 (no shared files) but depends on Plan 01's redact-package conventions.
Output: `redact/SecretShapes.kt`, `redact/SecretShapesTest.kt`, and the `SubtleNotice` banner wired into `ContextPreviewDialog.confirm`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-privacy-redaction-hardening/13-CONTEXT.md
@.planning/phases/13-privacy-redaction-hardening/13-RESEARCH.md
@.planning/phases/13-privacy-redaction-hardening/13-PATTERNS.md
@.planning/phases/13-privacy-redaction-hardening/13-UI-SPEC.md
@.planning/phases/13-privacy-redaction-hardening/13-VALIDATION.md

# The banner component to reuse (do NOT build a new banner)
@src/main/kotlin/com/six2dez/burp/aiagent/ui/components/SubtleNotice.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — create SecretShapesTest (positive + negative) (RED)</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/redact/SecretShapesTest.kt</files>
  <read_first>
    - src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionTest.kt (the JUnit Jupiter shape to copy: `org.junit.jupiter.api.Assertions.*`, `@Test`)
    - .planning/phases/13-privacy-redaction-hardening/13-VALIDATION.md (test names: SecretShapesTest, findSurvivingReturnsCategories — positive per shape + benign negative)
    - .planning/phases/13-privacy-redaction-hardening/13-RESEARCH.md (Pattern 4 lines 418-461 — the curated shape set + sample values for OpenAI `sk-…`/`sk-proj-…`, AWS `AKIA…`, GitHub `ghp_…`, Google `AIza…`, Slack `xox…`, JWT `eyJ….….…`, high-entropy hex; Open Question 3 lines 654-656 on the hex shape)
    - .planning/phases/13-privacy-redaction-hardening/13-PATTERNS.md (SecretShapesTest analog lines 431-433)
  </read_first>
  <behavior>
    - `findSurvivingReturnsCategories`: `SecretShapes.findSurviving("token is sk-abc123def456ghi789jkl012")` contains the OpenAI-key category; `findSurviving("AKIAIOSFODNN7EXAMPLE")` contains the AWS category; `findSurviving("ghp_0123456789012345678901234567890123456")` contains the GitHub category; a JWT sample `eyJhbGciOi.eyJzdWIi.SflKxwRJ` contains the JWT category.
    - `benignTextHasNoSurvivors`: `SecretShapes.findSurviving("hello world, just a normal sentence with name=alice")` returns an empty set.
    - The returned value is a `Set<String>` of human-readable category names (no raw matched values).
  </behavior>
  <action>
    Create `src/test/kotlin/com/six2dez/burp/aiagent/redact/SecretShapesTest.kt` in package `com.six2dez.burp.aiagent.redact`, mirroring the `RedactionTest` JUnit Jupiter shape. Add at least one positive assertion per curated shape from 13-RESEARCH.md Pattern 4 (use the sample values there) and a benign-negative test asserting an empty set. Assert on category-name membership (e.g. `assertTrue(result.any { it.contains("OpenAI", ignoreCase = true) })`) so the test is robust to the exact wording Claude picks for category names in Task 2. RED until Task 2 creates `SecretShapes`. Commit as the failing baseline.
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/burp-ai-agent &amp;&amp; ./gradlew test --tests "com.six2dez.burp.aiagent.redact.SecretShapesTest" ; test $? -ne 0 &amp;&amp; echo "RED-as-expected"</automated>
  </verify>
  <acceptance_criteria>
    - `src/test/kotlin/com/six2dez/burp/aiagent/redact/SecretShapesTest.kt` exists with `findSurvivingReturnsCategories` (≥1 positive per shape) and a benign-negative test.
    - Assertions check category-name membership, not raw matched values, and not a brittle exact-set equality.
    - The test is RED (`SecretShapes` does not exist yet).
  </acceptance_criteria>
  <done>SecretShapesTest committed and RED, covering each curated shape positively and a benign string negatively.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create redact/SecretShapes.kt (curated set + findSurviving) — GREEN for SecretShapesTest</name>
  <files>src/main/kotlin/com/six2dez/burp/aiagent/redact/SecretShapes.kt</files>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt (the `object` + curated-`Regex`-val idiom to mirror; the existing `jwtRegex` at line 71 is literally one of the shapes — closest possible analog, same package)
    - .planning/phases/13-privacy-redaction-hardening/13-RESEARCH.md (Pattern 4 lines 418-461 — the [VERIFIED] curated set: `data class Shape(category, regex)`, the ordered `shapes` list, `findSurviving(text): Set<String>`; the OpenAI legacy+modern `sk-(proj-|svcacct-|admin-)?…` form; the reuse contract line 456 keep it AWT-free; Open Question 3 lines 654-656 — order the high-entropy hex shape last or omit it)
    - .planning/phases/13-privacy-redaction-hardening/13-PATTERNS.md (SecretShapes.kt CREATE section lines 171-193 — mirror `Redaction`'s curated regex vals; AWT-free reuse contract)
  </read_first>
  <behavior>
    - `SecretShapes.findSurviving(text)` returns the set of category names whose shape's `containsMatchIn(text)` is true.
    - Positive: each curated shape matches its sample (OpenAI/AWS/GitHub/JWT at minimum); negative: a benign string returns the empty set.
    - The object holds an ordered `List<Shape>`; `Shape(category: String, regex: Regex)`.
  </behavior>
  <action>
    Create `src/main/kotlin/com/six2dez/burp/aiagent/redact/SecretShapes.kt` as `object SecretShapes` in package `com.six2dez.burp.aiagent.redact`, following 13-RESEARCH.md Pattern 4: a `data class Shape(val category: String, val regex: Regex)`, a `val shapes: List<Shape>` with the verified curated patterns (OpenAI key `\bsk-(?:proj-|svcacct-|admin-)?[A-Za-z0-9_-]{20,}\b` covering legacy + modern; AWS access key `\bAKIA[0-9A-Z]{16}\b`; GitHub token `\bgh[pousr]_[A-Za-z0-9]{36,}\b`; GitHub fine-grained PAT `\bgithub_pat_[A-Za-z0-9_]{22,}\b`; Google API key `\bAIza[0-9A-Za-z_-]{35}\b`; Slack token `\bxox[baprs]-[0-9A-Za-z-]{10,}\b`; JWT `\beyJ[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\b`), and `fun findSurviving(text: String): Set<String> = shapes.filter { it.regex.containsMatchIn(text) }.map { it.category }.toSet()`. Use friendly human-readable category names ("OpenAI key", "AWS access key", "GitHub token", "JWT", …) — these become the banner copy. Per Open Question 3, the broad `high-entropy hex key` `\b[0-9a-fA-F]{32,}\b` shape matches benign MD5/SHA hashes; include it LAST or omit it (Claude's discretion — the banner is non-blocking so a false positive is low-harm, but it can desensitize users; document the choice in a brief comment). Keep this file COMPLETELY AWT-free (no `java.awt`/`javax.swing` import) so the Phase 15 tripwire can depend on it (reuse contract). English-only comments; reference the secret-scanning-corpus provenance briefly. After this task the SecretShapesTest cases turn GREEN.
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/burp-ai-agent &amp;&amp; ./gradlew test --tests "com.six2dez.burp.aiagent.redact.SecretShapesTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "*SecretShapesTest"` passes (positive per shape + benign negative GREEN).
    - `SecretShapes.kt` contains `data class Shape` and `fun findSurviving`.
    - `SecretShapes.kt` has NO `import java.awt` and NO `import javax.swing` (grep returns empty) — verified headless-testable / Phase-15-reusable.
    - The curated set includes at minimum OpenAI, AWS, GitHub, and JWT shapes; the broad hex shape (if present) is ordered last.
  </acceptance_criteria>
  <done>`SecretShapes` exists, is AWT-free, exposes `findSurviving`, covers the verified curated shapes, and its tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Add the non-blocking survived-secret WARN banner to ContextPreviewDialog</name>
  <files>src/main/kotlin/com/six2dez/burp/aiagent/ui/components/ContextPreviewDialog.kt</files>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/components/ContextPreviewDialog.kt (the `confirm(...)` method at line 16; `contextJson` param at line 21; the `header` `BoxLayout(Y_AXIS)` stack lines 26-50; the "Context (as will be sent, after redaction):" label at line 50; `bodyScroll` added to `BorderLayout.CENTER` at line 65 — insert the banner between line 50 and the CENTER add)
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/components/SubtleNotice.kt (the component to reuse: `enum class Level { INFO, WARN, RISK }` line 25, `fun setMessage(level, html)` line 69, `fun hideNotice()` line 87, self-managing theme via `updateUI()` line 91 — starts hidden, already token-compliant)
    - src/main/kotlin/com/six2dez/burp/aiagent/redact/SecretShapes.kt (Task 2 — `findSurviving(contextJson): Set<String>` drives the banner)
    - .planning/phases/13-privacy-redaction-hardening/13-UI-SPEC.md (Touch Point 2 — placement, `Level.WARN` rationale (advisory, non-blocking — NOT RISK), message names the shape CATEGORY only never the raw value, scan target = post-redaction `contextJson`; Copywriting single/multiple banner strings; FLAG-13-02 do NOT migrate the surrounding dialog literals; FLAG-13-03 WARN palette note)
    - .planning/phases/13-privacy-redaction-hardening/13-PATTERNS.md (ContextPreviewDialog MODIFY lines 338-371 — insertion point + SubtleNotice API + FLAG-13-02)
  </read_first>
  <action>
    In `ContextPreviewDialog.confirm(...)`, after the "Context (as will be sent, after redaction):" label is added to the `header` stack (line 50) and BEFORE `bodyScroll` is added to `BorderLayout.CENTER` (line 65): create a `val survivedNotice = SubtleNotice()`; compute `val survivors = SecretShapes.findSurviving(contextJson)` (scan the POST-redaction `contextJson` only — the parameter the dialog already receives, per UI-SPEC §Touch Point 2); if `survivors.isNotEmpty()` call `survivedNotice.setMessage(SubtleNotice.Level.WARN, <html message>)` else `survivedNotice.hideNotice()`; add `survivedNotice` to the `header` stack (optionally with a `Box.createVerticalStrut` matching the existing 6 px struts already in this dialog — reusing the dialog's existing literal is acceptable since FLAG-13-02 keeps this dialog un-migrated; do NOT introduce NEW literals or migrate existing ones). Build the HTML message from the UI-SPEC §Copywriting strings: single -> "A value matching a known secret shape ({shape}) survived redaction. Review before sending."; multiple -> "{N} values matching known secret shapes ({shapes}) survived redaction. Review before sending." where `{shape}`/`{shapes}` are the comma-joined `survivors` CATEGORY names — NEVER echo the raw matched value (UI-SPEC §Message content; re-exposing the value in the UI is the threat). Use `Level.WARN` (amber), NOT `Level.RISK` — the banner is advisory and non-blocking (UI-SPEC §Color rationale). Keep the existing `Send`/`Cancel`/`confirm` return behavior UNCHANGED — the banner never disables Send and adds no confirmation step (warn-with-confirmation is Phase 15). English-only copy. Since the single caller is `ChatPanel.kt:290` and the `confirm` signature already receives `contextJson`, NO change to `ChatPanel` is required.
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/burp-ai-agent &amp;&amp; ./gradlew test</automated>
  </verify>
  <acceptance_criteria>
    - `ContextPreviewDialog.kt` contains `SecretShapes.findSurviving(contextJson)` and `SubtleNotice` with `Level.WARN`; it does NOT use `Level.RISK` for this banner.
    - The banner copy is built from category names only — grep confirms no interpolation of a raw matched secret value into the message (the message uses `survivors`/category strings, not the matched substring).
    - The `confirm(...)` signature and its `Send`/`Cancel` return semantics are unchanged (no new parameter; no caller edit — `ChatPanel.kt:290` still compiles); grep shows no new `Color(`/`Font(`/`Dimension(` literal was added (FLAG-13-02: surrounding dialog left un-migrated; banner uses `SubtleNotice`).
    - Full `./gradlew test` is green (~308 baseline; the dialog change is additive and does not break existing tests such as `ContextPreviewConsistencyTest`).
  </acceptance_criteria>
  <done>The pre-send preview shows a non-blocking WARN banner naming surviving secret-shape categories (never the raw value) when `SecretShapes.findSurviving(contextJson)` is non-empty; Send/Cancel are unchanged; the full suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Post-redaction contextJson -> SecretShapes.findSurviving | The about-to-be-sent (already redacted) context is scanned for residual high-confidence secrets |
| Survivor categories -> ContextPreviewDialog banner | What is rendered to the user must reveal the RISK without re-exposing the secret value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Information Disclosure | A high-confidence secret surviving redaction, sent silently to the AI backend | mitigate | `SecretShapes.findSurviving` scans the post-redaction context and the dialog surfaces a pre-send WARN banner so the user can cancel; verified by `SecretShapesTest` + manual preview smoke. ASVS V7 (operator-facing detection). **HIGH-adjacent — secret leakage visibility.** |
| T-13-10 | Information Disclosure | Re-exposing the suspected secret in the UI banner text | mitigate | The banner names the shape CATEGORY only (e.g. "OpenAI key"), never the matched value (UI-SPEC §Message content); acceptance criterion greps that no raw match is interpolated into the message. |
| T-13-11 | Denial of Service / Usability | False-positive WARN desensitizing the user (broad hex shape) | accept | The broad `high-entropy hex` shape is ordered last or omitted (Open Question 3); the banner is non-blocking so a false positive only adds an advisory — acceptable per the "reveal what survived" purpose. |
| T-13-12 | Tampering | Phase 15 tripwire forking the shape set | mitigate | `SecretShapes` is the single AWT-free source of truth (no Swing import) so Phase 15 reuses it rather than duplicating drift-prone patterns; acceptance criterion greps the file is AWT-free. |
| T-13-SC | Tampering | dependency supply chain | accept | Zero new dependencies — pure Kotlin/`kotlin.text.Regex` + the existing `SubtleNotice` component. RESEARCH Package Legitimacy Audit = not applicable; no slopcheck surface. |
</threat_model>

<verification>
- `./gradlew test --tests "com.six2dez.burp.aiagent.redact.SecretShapesTest"` green (positive per shape + benign negative).
- `./gradlew test` full suite green (the additive dialog banner does not break existing tests).
- Source assertions: `SecretShapes.kt` is AWT-free and exposes `findSurviving`; `ContextPreviewDialog.kt` calls `SecretShapes.findSurviving(contextJson)`, uses `Level.WARN` (not RISK), interpolates only category names, and adds no new style literal.
- Manual (Burp smoke, per VALIDATION §Manual-Only): trigger a Send with a payload containing a surviving `sk-proj-…` -> the preview dialog shows a non-blocking WARN banner naming the category; Send stays enabled. Swing rendering is not unit-tested; the `findSurviving` logic is.
- Do NOT gate on `./gradlew ktlintCheck` (pre-existing `generateBuildFlags` defect).
</verification>

<success_criteria>
- PRIV-04: the redaction preview dialog flags survived known-secret shapes using the same curated `SecretShapes` set the Phase 15 tripwire will reuse (ROADMAP SC4); the indicator is non-blocking and names categories only.
- `SecretShapes` is the AWT-free single source of truth, headless-unit-tested and reusable by Phase 15.
- No new runtime dependency; `ContextPreviewDialog`'s un-migrated literals are left untouched (FLAG-13-02), only the token-compliant `SubtleNotice` banner is added.
</success_criteria>

<output>
Create `.planning/phases/13-privacy-redaction-hardening/13-03-SUMMARY.md` when done.
</output>

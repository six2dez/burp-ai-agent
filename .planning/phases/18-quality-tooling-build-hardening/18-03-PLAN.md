---
phase: 18-quality-tooling-build-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 18-02
files_modified:
  - src/test/kotlin/com/six2dez/burp/aiagent/cache/PersistentPromptCacheTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerDedupTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/backends/cli/CliSupervisionTest.kt
autonomous: true
requirements:
  - QUAL-03
must_haves:
  truths:
    - "PersistentPromptCacheTest covers put/get round-trip, TTL eviction, and disk-size eviction"
    - "ActiveScannerDedupTest covers queueTarget dedup (same target within DEDUP_WINDOW_MS not re-queued)"
    - "CliSupervisionTest covers NonInteractiveCliConnection timeout path via the public AgentConnection interface"
    - "All three tests pass on the CI test suite"
    - "Tests use tmpDir (not ~/.burp-ai-agent/cache/) for any filesystem operations"
  artifacts:
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/cache/PersistentPromptCacheTest.kt"
      provides: "3 tests: putAndGetRoundTrip, getReturnsNullForExpiredEntry, evictsOldestWhenDiskLimitExceeded"
      min_lines: 50
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerDedupTest.kt"
      provides: "2 tests: queueTargetDedupPreventsRequeueWithinWindow, queueTargetAllowsRequeueAfterWindowExpires"
      min_lines: 60
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/backends/cli/CliSupervisionTest.kt"
      provides: "1 test: sendTimesOutAndReportsViaOnComplete (platform-guarded sleep command)"
      min_lines: 40
  key_links:
    - from: "PersistentPromptCacheTest"
      to: "PersistentPromptCache"
      via: "constructor injection: cacheDir = tmpDir"
      pattern: "PersistentPromptCache\\(cacheDir"
    - from: "ActiveScannerDedupTest"
      to: "ActiveAiScanner.queueTarget()"
      via: "processedTargets ConcurrentHashMap (lines 137-145 of ActiveAiScanner.kt)"
      pattern: "queueTarget"
    - from: "CliSupervisionTest"
      to: "NonInteractiveCliConnection.send()"
      via: "CliBackend.launch() returning AgentConnection"
      pattern: "backend.launch"
---

<objective>
Raise test coverage for the three zero/near-zero coverage modules: cache, scanner dedup, and CLI supervision (SC4/QUAL-03).

One meaningful test class per module exercising the critical path — no hard coverage-percentage gate, but every test must exercise real production code paths.

Purpose: PersistentPromptCache has zero test coverage; its put/get/evict paths are the diagnostic infrastructure for the scanner pipeline. Scanner dedup is the correctness gate that prevents duplicate scan work. CLI supervision timeout is the REL-04 root cause that spawned issue #71 — tests here prevent regression.

Output: Three new test files. All three must be committed after the ktlintFormat run (Plan 02 Wave 2) to avoid reformatting churn.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-quality-tooling-build-hardening/18-PATTERNS.md
@.planning/phases/18-quality-tooling-build-hardening/18-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PersistentPromptCacheTest — put/get round-trip, TTL eviction, disk-size eviction</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/cache/PersistentPromptCacheTest.kt</files>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/cache/PersistentPromptCache.kt (full file — constructor signature at lines 24-28; public API: get(), put(), clear(), diskSizeBytes(), entryCount(); CachedEntry and CachedIssue data classes; line 63-65 intentional silent swallow in put() catch block)
    - src/test/kotlin/com/six2dez/burp/aiagent/backends/cli/CliBackendTempFileTest.kt (temp-dir seam pattern — BeforeEach setUp, AfterEach tearDown with deleteRecursively)
    - .planning/phases/18-quality-tooling-build-hardening/18-PATTERNS.md (PersistentPromptCacheTest section — full constructor signature, data class definitions, imports list, three test methods with expected assertions)
    - .planning/phases/18-quality-tooling-build-hardening/18-RESEARCH.md (Pitfall 5 — put() catch is intentional silent swallow; Pitfall 6 — never use real home dir)
  </read_first>
  <behavior>
    - putAndGetRoundTrip: cache.put("abc123", CachedEntry(now, listOf(CachedIssue(title="SQLI", severity="HIGH")))) → cache.get("abc123") returns non-null entry; entry.issues.first().title == "SQLI"
    - getReturnsNullForExpiredEntry: PersistentPromptCache(cacheDir=tmpDir, ttlMs=1L) → put entry with createdAtMs = now-1000 → Thread.sleep(5) → get returns null
    - evictsOldestWhenDiskLimitExceeded: PersistentPromptCache(cacheDir=tmpDir, maxDiskBytes=200L) → put 20 entries → diskSizeBytes() <= 200L
  </behavior>
  <action>
    Create src/test/kotlin/com/six2dez/burp/aiagent/cache/PersistentPromptCacheTest.kt.

    Package: `com.six2dez.burp.aiagent.cache`

    The cache module has no existing test directory — create the directory as needed.

    Imports: use the exact list from PATTERNS.md PersistentPromptCacheTest section (org.junit.jupiter.api.AfterEach, BeforeEach, Test, Assertions.assertEquals, Assertions.assertNull, Assertions.assertNotNull, Assertions.assertTrue, java.io.File, java.nio.file.Files).

    Setup/teardown: @BeforeEach creates `tmpDir = Files.createTempDirectory("cache-test").toFile()`; @AfterEach calls `tmpDir.deleteRecursively()`. NEVER use `File(System.getProperty("user.home"), ".burp-ai-agent/cache")` in tests.

    Test 1 — putAndGetRoundTrip: construct `PersistentPromptCache(cacheDir = tmpDir)`; put a CachedEntry; get it back; assertNotNull and assertEquals on title.

    Test 2 — getReturnsNullForExpiredEntry: construct with `ttlMs = 1L`; put an entry with `createdAtMs = System.currentTimeMillis() - 1000L`; Thread.sleep(5); assertNull on get result.

    Test 3 — evictsOldestWhenDiskLimitExceeded: construct with `maxDiskBytes = 200L`; put 20 entries in a loop (each with a different hash key); assertTrue that `diskSizeBytes() <= 200L`.

    Note from PATTERNS.md/Pitfall 5: PersistentPromptCache.put() catch block is an INTENTIONAL silent swallow (disk write best-effort). Do NOT test the exception path — do NOT call put() with a read-only directory to trigger the catch block.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.cache.PersistentPromptCacheTest" --no-daemon 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.six2dez.burp.aiagent.cache.PersistentPromptCacheTest" --no-daemon` exits 0; all 3 tests pass.
    - Test file package is `com.six2dez.burp.aiagent.cache` (matches PersistentPromptCache.kt).
    - setUp() creates a temp directory using Files.createTempDirectory; tearDown() calls deleteRecursively().
    - No reference to `user.home` or `.burp-ai-agent` appears in the test file.
    - Test file passes `./gradlew ktlintCheck --no-daemon` (no style violations — file was written after ktlintFormat ran in Plan 02).
  </acceptance_criteria>
  <done>PersistentPromptCacheTest: 3 tests, all passing, no real-FS dependency, ktlint-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ActiveScannerDedupTest and CliSupervisionTest</name>
  <files>
    src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerDedupTest.kt,
    src/test/kotlin/com/six2dez/burp/aiagent/backends/cli/CliSupervisionTest.kt
  </files>
  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/ActiveAiScanner.kt (dedup logic at lines 137-145: processedTargets.putIfAbsent keyed on target.id; line 128 enabled check; line 133 api.scope().isInScope(); setEnabled() method signature)
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScanModels.kt (ActiveScanTarget constructor and id computed val at line 300: "${originalRequest.request().url()}_${injectionPoint.name}_${vulnHint.vulnClass}")
    - src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerQueueModelTest.kt (full file — newScannerForQueueTests() factory; requestResponse() helper with mock setup; imports block; TestSettings usage)
    - src/test/kotlin/com/six2dez/burp/aiagent/TestSettings.kt (baselineSettings() signature)
    - src/main/kotlin/com/six2dez/burp/aiagent/backends/cli/CliBackend.kt (NonInteractiveCliConnection.send() timeout logic at lines 241-252; BackendLaunchConfig field names; CliBackend class constructors; cliTimeoutSeconds floor coercion at line 38)
    - src/test/kotlin/com/six2dez/burp/aiagent/backends/cli/CliTimeoutMessageTest.kt (direct test pattern without reflection)
    - .planning/phases/18-quality-tooling-build-hardening/18-PATTERNS.md (ActiveScannerDedupTest section — full test methods; CliSupervisionTest section — sendTimesOutAndReportsViaOnComplete with platform guard)
    - .planning/phases/18-quality-tooling-build-hardening/18-RESEARCH.md (SC4 section; seam description for CliSupervisionTest)
  </read_first>
  <behavior>
    ActiveScannerDedupTest:
    - queueTargetDedupPreventsRequeueWithinWindow: scanner.setEnabled(true) → queueTarget(target) twice with same target → queue size after second enqueue equals queue size after first enqueue (dedup prevented re-queue within DEDUP_WINDOW_MS)
    - queueTargetAllowsRequeueAfterWindowExpires: scanner.setEnabled(true) → queueTarget(target) → scanner.resetStats() (clears processedTargets) → queueTarget(target) again → queue size >= 1 (re-queue succeeded after dedup cleared)

    CliSupervisionTest:
    - sendTimesOutAndReportsViaOnComplete: CliBackend.launch() with "sleep 60" command (1-second timeout) → connection.send() → CountDownLatch await(5s) → completionError is not null; completionError.message contains "timed out" (case-insensitive)
  </behavior>
  <action>
    Create ActiveScannerDedupTest.kt:
    Package: `com.six2dez.burp.aiagent.scanner`

    Use the exact imports list from PATTERNS.md ActiveScannerDedupTest section. The mock factory `newScanner()` uses `mock<MontoyaApi>(defaultAnswer = Answers.RETURNS_DEEP_STUBS)` (deep stubs required because queueTarget calls api.scope().isInScope() and api.logging().logToOutput()). Set `scopeOnly = false` so scope check does not gate the enqueue. Use `TestSettings.baselineSettings()` for the getSettings lambda (do NOT inline a copy of the settings block).

    For both dedup tests: construct an ActiveScanTarget using the public constructor (url, injectionPoint, vulnHint, priority). InjectionPoint and VulnHint constructor signatures must be confirmed from ActiveScanModels.kt before writing (do not guess field names). After the first queueTarget(), call `getQueueItems(limit = 10).size` to capture the queue size — asserting after the second call that the size is unchanged proves dedup worked.

    For the resetStats test: call `scanner.resetStats()` between the two enqueue operations. The PATTERNS.md excerpt shows that `resetStats()` clears `processedTargets` (verify this is true from the source — if the method name is different, use the correct name).

    Create CliSupervisionTest.kt:
    Package: `com.six2dez.burp.aiagent.backends.cli`

    The sleep command must be platform-guarded per PATTERNS.md:
    ```
    val sleepCmd = if (System.getProperty("os.name").lowercase().contains("win")) {
        listOf("cmd", "/c", "timeout", "/t", "60")
    } else {
        listOf("sleep", "60")
    }
    ```

    Construct `BackendLaunchConfig` with `command = sleepCmd`, `cliTimeoutSeconds = 1`, `embeddedMode = true`. Note: CliBackend.kt coerces cliTimeoutSeconds to floor 30 unless a specific minimum is set — check the coercion logic at line 38 before writing the test, and use the minimum valid timeout that will still produce a fast timeout for CI (if floor is 30 seconds, the test will take 30 seconds — document this in a comment). Use a CountDownLatch(1) with a 5-second await for the onComplete callback (if floor coercion makes the actual timeout > 5s, increase the latch timeout to 65 seconds and add a `@Timeout(70, unit = TimeUnit.SECONDS)` annotation).

    Assert: `completionError != null` and `completionError!!.message?.contains("timed out", ignoreCase = true) == true`. Call `connection.stop()` in a finally block after the assert.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.scanner.ActiveScannerDedupTest" --no-daemon 2>&1 | tail -15 && ./gradlew test --tests "com.six2dez.burp.aiagent.backends.cli.CliSupervisionTest" --no-daemon 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.six2dez.burp.aiagent.scanner.ActiveScannerDedupTest" --no-daemon` exits 0; 2 dedup tests pass.
    - `./gradlew test --tests "com.six2dez.burp.aiagent.backends.cli.CliSupervisionTest" --no-daemon` exits 0; timeout test passes.
    - ActiveScannerDedupTest uses `TestSettings.baselineSettings()` (no inline settings copy).
    - ActiveScannerDedupTest uses `mock<MontoyaApi>(defaultAnswer = Answers.RETURNS_DEEP_STUBS)`.
    - CliSupervisionTest has a platform guard for the sleep command (different argv on Windows vs Unix).
    - Both test files pass `./gradlew ktlintCheck --no-daemon`.
    - `./gradlew test -PexcludeHeavyTests=true --no-daemon` exits 0 (neither test is tagged as a heavy test).
  </acceptance_criteria>
  <done>ActiveScannerDedupTest and CliSupervisionTest: all tests passing; no real external dependencies; ktlint-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test filesystem operations | PersistentPromptCacheTest writes JSON to tmpDir — must not write to production cache dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Information Disclosure | PersistentPromptCacheTest writing real cache data | mitigate | Tests use `Files.createTempDirectory("cache-test")` injected via constructor; never reference user.home or .burp-ai-agent; tearDown() deletes tmpDir |
| T-18-05 | Denial of Service (CI) | CliSupervisionTest running "sleep 60" on CI | mitigate | cliTimeoutSeconds = 1 (or minimum floor) kills the process quickly; CountDownLatch + @Timeout annotation bounds CI impact to ~65 seconds maximum |

No HIGH-severity threat. These are test-only files with no runtime exposure.
</threat_model>

<verification>
Run after both tasks complete:

1. `./gradlew test --tests "com.six2dez.burp.aiagent.cache.PersistentPromptCacheTest" --no-daemon` — all 3 tests pass
2. `./gradlew test --tests "com.six2dez.burp.aiagent.scanner.ActiveScannerDedupTest" --no-daemon` — 2 tests pass
3. `./gradlew test --tests "com.six2dez.burp.aiagent.backends.cli.CliSupervisionTest" --no-daemon` — 1 test passes
4. `./gradlew test -PexcludeHeavyTests=true --no-daemon` — full fast suite still passes (no regressions)
5. `./gradlew ktlintCheck --no-daemon` — still passes (new test files are ktlint-clean)
6. `grep "user.home\|burp-ai-agent" src/test/kotlin/com/six2dez/burp/aiagent/cache/PersistentPromptCacheTest.kt` — returns no matches
</verification>

<success_criteria>
- All three new test files exist and contain meaningful tests (not empty stubs)
- All three test classes pass `./gradlew test` independently
- `./gradlew test -PexcludeHeavyTests=true --no-daemon` exits 0 (no regressions from new tests)
- No test writes to the production ~/.burp-ai-agent/cache/ directory
- Test coverage for cache, scanner dedup, and CLI supervision raised from 0% (SC4 minimum: one meaningful test class per module)
</success_criteria>

<output>
Create `.planning/phases/18-quality-tooling-build-hardening/18-03-SUMMARY.md` when done.
</output>

---
phase: 18-quality-tooling-build-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - build.gradle.kts
  - .github/workflows/build.yml
  - src/main/kotlin/**/*.kt  # mass-reformatted by ktlintFormat in Commit A
autonomous: true
requirements:
  - QUAL-03
must_haves:
  truths:
    - "ktlintFormat mass-format commit exists in git history BEFORE the gate-flip commit"
    - "ktlintCheck is strict by default (fails without -PktlintLenient=true)"
    - "build.yml lint job ktlintCheck step has no continue-on-error"
    - "git log shows: format commit precedes gate-flip commit"
  artifacts:
    - path: "build.gradle.kts"
      provides: "inverted ignoreFailures gate (strict by default, lenient via -PktlintLenient=true)"
      contains: "ktlintLenient"
    - path: ".github/workflows/build.yml"
      provides: "ktlintCheck step without continue-on-error"
  key_links:
    - from: "build.gradle.kts ktlint{} block"
      to: "ktlintCheck task"
      via: "ignoreFailures.set(ktlintLenient == true)"
      pattern: "ktlintLenient"
    - from: ".github/workflows/build.yml lint job"
      to: "ktlintCheck step"
      via: "continue-on-error removed"
      pattern: "ktlint check"
---

<objective>
Run ktlintFormat across the entire codebase (Commit A) and then flip the ktlint gate to strict-by-default (Commit B), completing SC3/QUAL-03-ktlint.

The MANDATORY two-commit ordering is the success criterion: Commit A (mass-format) MUST appear before Commit B (gate flip) in git log. This plan encodes the two-step sequence as two separate tasks that execute in order and produce two separate commits.

Purpose: After Plan 01 fixed generateBuildFlags so ktlintCheck runs standalone, this plan makes ktlintCheck a blocking gate by default. Auto-format first prevents the gate flip from immediately breaking CI on pre-existing style violations.

Output: Two git commits in the correct order; updated build.gradle.kts; updated build.yml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-quality-tooling-build-hardening/18-RESEARCH.md
@.planning/phases/18-quality-tooling-build-hardening/18-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Commit A — ktlintFormat mass-format (DO NOT flip the gate yet)</name>
  <files>src/main/kotlin/**/*.kt (reformatted), src/test/kotlin/**/*.kt (reformatted)</files>
  <read_first>
    - build.gradle.kts (verify current ignoreFailures.set(...) line at lines 171-173 still uses ktlintStrict; verify the filter block at lines 178-181 still has exclude("**/build/**") and exclude("**/generated/**"))
    - .planning/phases/18-quality-tooling-build-hardening/18-RESEARCH.md (SC3 section — Pitfall 3 about ktlintFormat touching generated sources)
  </read_first>
  <action>
    Before running ktlintFormat, confirm:
    - build.gradle.kts ktlint filter block still excludes `**/build/**` and `**/generated/**`. This ensures `build/generated/buildflags/com/six2dez/burp/aiagent/BuildFlags.kt` is NOT touched by the format run (Pitfall 3).
    - The ignoreFailures gate has NOT been flipped yet (it should still reference `ktlintStrict`). Do NOT change build.gradle.kts in this task.

    Run ktlintFormat:
    `./gradlew ktlintFormat --no-daemon`

    This auto-fixes all style violations across src/main/kotlin and src/test/kotlin. Expect many files to be modified (indentation, trailing commas, imports, etc.).

    After ktlintFormat completes, run ktlintCheck to confirm the reformatted codebase is clean:
    `./gradlew ktlintCheck --no-daemon`

    If ktlintCheck still reports violations after ktlintFormat, the violations require manual fixes (ktlintFormat cannot auto-fix all rules). Fix any remaining violations manually — do not proceed to Commit A until ktlintCheck exits 0.

    Commit A (mass-format only — no gate change):
    Stage all modified .kt files: `git add src/`
    Commit: `git commit -m "style(sc3): run ktlintFormat across entire codebase — pre-gate-flip mass format"`

    IMPORTANT: Do NOT modify build.gradle.kts or .github/workflows/build.yml in this commit. The gate flip happens in Task 2 (Commit B).
  </action>
  <verify>
    <automated>./gradlew ktlintCheck --no-daemon 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew ktlintCheck --no-daemon` exits 0 after the ktlintFormat run.
    - `git log --oneline -1` shows the mass-format commit (message contains "ktlintFormat" or "pre-gate-flip").
    - build.gradle.kts is NOT in the staged/committed changes of Commit A — the ignoreFailures line still references `ktlintStrict`.
    - `build/generated/buildflags/com/six2dez/burp/aiagent/BuildFlags.kt` was NOT modified by ktlintFormat (or if it was, that's a Pitfall 3 symptom — report it).
    - ktlintCheck exit code is 0.
  </acceptance_criteria>
  <done>Commit A is in git history; ktlintCheck passes; gate has NOT been flipped yet.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commit B — flip ktlint to strict-by-default and update CI</name>
  <files>build.gradle.kts, .github/workflows/build.yml</files>
  <read_first>
    - build.gradle.kts (lines 166-182 — the full ktlint{} block; specifically the ignoreFailures.set(...) call at lines 171-173)
    - .github/workflows/build.yml (lines 21-23 — the ktlintCheck step with continue-on-error: true)
    - .planning/phases/18-quality-tooling-build-hardening/18-PATTERNS.md (SC3 — exact before/after for ignoreFailures.set and build.yml CI change)
  </read_first>
  <action>
    Edit build.gradle.kts — flip the ignoreFailures gate (lines 171-173):

    REMOVE the current lenient-by-default logic:
    ```
    ignoreFailures.set(
        (project.findProperty("ktlintStrict") as? String)?.equals("true", ignoreCase = true) != true,
    )
    ```

    REPLACE with strict-by-default logic (escape hatch via -PktlintLenient=true):
    ```
    ignoreFailures.set(
        (project.findProperty("ktlintLenient") as? String)?.equals("true", ignoreCase = true) == true,
    )
    ```

    Also update the comment above the ignoreFailures block to reflect the new default (strict, escape hatch via -PktlintLenient=true).

    Edit .github/workflows/build.yml — remove continue-on-error from ktlintCheck step:
    REMOVE the line `continue-on-error: true` from the ktlintCheck step in the lint job. Also update the step name from "ktlint check (non-blocking until baseline is clean)" to "ktlint check".

    Verify the gate flip is sound:
    `./gradlew ktlintCheck --no-daemon` — must still exit 0 (Commit A's format run made the codebase clean).
    `./gradlew ktlintCheck -PktlintLenient=true --no-daemon` — must also exit 0 (escape hatch works).

    Commit B (gate flip + CI update):
    `git add build.gradle.kts .github/workflows/build.yml`
    `git commit -m "feat(sc3): flip ktlint to strict-by-default (escape hatch: -PktlintLenient=true)"`

    Verify commit ordering:
    `git log --oneline -3` must show Commit B (gate flip) AFTER Commit A (mass format) — i.e., Commit A is older (lower in the log).
  </action>
  <verify>
    <automated>./gradlew ktlintCheck --no-daemon 2>&1 | tail -5 && git log --oneline -3</automated>
  </verify>
  <acceptance_criteria>
    - build.gradle.kts ignoreFailures line contains `ktlintLenient` (not `ktlintStrict`).
    - `./gradlew ktlintCheck --no-daemon` exits 0 after the gate flip.
    - `./gradlew ktlintCheck -PktlintLenient=true --no-daemon` exits 0.
    - `grep -c 'ktlintStrict' build.gradle.kts` returns 0 (old property reference gone).
    - .github/workflows/build.yml lint job ktlintCheck step does NOT contain `continue-on-error`.
    - `git log --oneline -5` shows the mass-format commit (Commit A) appearing BEFORE the gate-flip commit (Commit B) — mass-format commit has an older timestamp / lower position in the log.
    - Both commits are distinct (different SHA, different message).
  </acceptance_criteria>
  <done>SC3 complete: two-commit sequence in correct git log order; ktlint is strict by default; CI gate is blocking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build.gradle.kts ktlint gate | Inverted default changes who bears the burden: developers must now pass ktlintCheck to merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Denial of Service (build) | ktlintFormat modifying generated sources | mitigate | Verify `exclude("**/build/**")` and `exclude("**/generated/**")` remain in the ktlint filter block before running ktlintFormat; BuildFlags.kt must not be touched |
| T-18-SC | Tampering | No new npm/pip/cargo packages in this plan | accept | No new build dependencies introduced — ktlint engine version unchanged (1.5.0); only the ignoreFailures default changes |

No HIGH-severity threat exists for this plan. The gate flip affects developer workflow but not runtime security. The ktlintFormat run modifies source files but introduces no secrets or sensitive data — all changes are style-only.
</threat_model>

<verification>
Run after both tasks complete:

1. `git log --oneline -5` — Commit A (format) appears OLDER than Commit B (gate flip); this is the SC3 git-log ordering criterion
2. `./gradlew ktlintCheck --no-daemon` exits 0 (strict gate active, codebase clean)
3. `grep "ktlintLenient" build.gradle.kts` returns a match
4. `grep -v '^#' .github/workflows/build.yml | grep "continue-on-error"` returns 0 matches in the ktlintCheck step
5. `./gradlew check --no-daemon` exits 0 (full gate: detekt + ktlintCheck + tests)
</verification>

<success_criteria>
- git log shows the ktlintFormat mass-format commit (Commit A) at an older position than the gate-flip commit (Commit B)
- `./gradlew ktlintCheck --no-daemon` exits 0 with no -PktlintLenient flag needed
- build.gradle.kts ignoreFailures logic references `ktlintLenient` not `ktlintStrict`
- .github/workflows/build.yml ktlintCheck step is blocking (no continue-on-error)
</success_criteria>

<output>
Create `.planning/phases/18-quality-tooling-build-hardening/18-02-SUMMARY.md` when done.
</output>

---
phase: 02-insertion-point-scan-audit
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - .planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md
autonomous: true
requirements: [INSP-01, INSP-02]
tags: [insertion-point, ui, manual-smoke, audit-artefact, human-uat]

must_haves:
  truths:
    - "A maintainer-fillable manual-smoke audit artefact exists at `.planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md` with the exact YAML front-matter + section structure used by Phase 1's `01-HUMAN-UAT.md` (per Pitfall #8) so downstream tooling can parse pass/fail consistently"
    - "Per D-09: scenarios 1-5 lock the menu-shows-on-overlap cases (URL param / body param / cookie / header / JSON value) — INSP-01"
    - "Per D-09: scenario 6 locks the menu-hidden-on-empty-or-whitespace-selection case — INSP-02"
    - "Per Pitfall #8: every `expected:` line uses the exact lowercase, space-separated label substring produced by `UiActions.kt:366` — `url param`, `body param`, `cookie`, `header`, `json field`"
    - "Per D-08: no Mockito-Kotlin unit test of `buildAiScanInsertionPointItem` is created — this manual-smoke artefact IS the proof for the UI builder branches (selectionContext, offsets, range, request presence)"
  artifacts:
    - path: ".planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md"
      provides: "Six-scenario manual-smoke audit trail for the right-click menu visibility contract (INSP-01, INSP-02). YAML frontmatter mirrors `01-HUMAN-UAT.md`."
      contains: "phase: 02-insertion-point-scan-audit"
  key_links:
    - from: "02-HUMAN-UAT.md scenarios 1-5"
      to: "UiActions.buildAiScanInsertionPointItem label format at UiActions.kt:366"
      via: "literal label substrings (`url param:`, `body param:`, `cookie:`, `header:`, `json field:`)"
      pattern: "url param:|body param:|cookie:|header:|json field:"
    - from: "02-HUMAN-UAT.md scenario 6"
      to: "buildAiScanInsertionPointItem null-return guard at UiActions.kt:354 (selectionEnd <= selectionStart) + resolver null at InjectionPointExtractor.kt:159+247"
      via: "menu hidden when no candidate (Pitfall #8 — both empty and whitespace-only converge to the same outcome from the user's perspective)"
      pattern: "Menu item does not appear"
---

<objective>
Generate the maintainer-fillable manual-smoke audit artefact `.planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md`. This is the proof-of-behaviour vehicle for INSP-01 (menu shows on selection) and INSP-02 (menu hidden on empty selection) — per D-08, Plan 03 deliberately does NOT add a Mockito-Kotlin unit test for `UiActions.buildAiScanInsertionPointItem`; the manual smoke run in real Burp by the maintainer is the audit's primary signal.

Purpose: lock the right-click menu visibility contract by recording six scenarios (5 menu-shows cases + 1 menu-hidden case) with the exact label substrings the production code at `UiActions.kt:366` emits. The planner builds the scaffolding here; the maintainer fills the `result:` fields during execution (mirrors the Phase 1 pattern in `01-HUMAN-UAT.md`).

Wave dependency: This plan is `depends_on: ["02-01"]` because the maintainer's Burp build during the smoke includes the new resolver tests from Plan 01 — those tests must compile so the maintainer's `./gradlew shadowJar` produces a JAR that matches the audited test coverage. (Plan 02's queue tests are not strictly required for the menu smoke, but Plan 01's resolver tests must compile.)

Output: one new file at `.planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md`. No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-insertion-point-scan-audit/02-CONTEXT.md
@.planning/phases/02-insertion-point-scan-audit/02-RESEARCH.md
@.planning/phases/02-insertion-point-scan-audit/02-PATTERNS.md
@.planning/phases/02-insertion-point-scan-audit/02-VALIDATION.md
@.planning/phases/01-perplexity-backend-audit/01-HUMAN-UAT.md
@src/main/kotlin/com/six2dez/burp/aiagent/ui/UiActions.kt

<interfaces>
<!-- The literal label-format string produced by the UI builder at UiActions.kt:366.
     Each scenario's `expected:` line must contain the lowercase, space-separated label form.
     For InjectionType `URL_PARAM` the substring is `url param`, etc. -->

UiActions.kt:366:
"AI Scan on Selected Insertion Point (${insertionPoint.type.name.lowercase().replace('_', ' ')}: ${insertionPoint.name})"

<!-- InjectionType → label substring mapping (derived from the .lowercase().replace('_', ' ') transform):
     URL_PARAM       → "url param"
     BODY_PARAM      → "body param"
     COOKIE          → "cookie"
     HEADER          → "header"
     JSON_FIELD      → "json field"
     XML_ELEMENT     → "xml element"          (not part of the smoke — XML body selection in Burp's editor is rare and not in D-09)
     PATH_SEGMENT    → "path segment"         (not part of the smoke — same reason)
-->

<!-- Phase 1 frontmatter (verbatim from .planning/phases/01-perplexity-backend-audit/01-HUMAN-UAT.md:1-7).
     Phase 2 must lift this 1:1 except for the keyed values. -->
---
status: partial
phase: 01-perplexity-backend-audit
source: [01-VERIFICATION.md]
started: 2026-05-13T11:35:00Z
updated: 2026-05-13T11:35:00Z
---

<!-- Phase 1 section structure (verbatim from .planning/phases/01-perplexity-backend-audit/01-HUMAN-UAT.md:9-28):
     ## Current Test
     [awaiting human testing]

     ## Tests
     ### N. <name> (<req-ids>)
     expected: <literal>
     result: [pending]

     ## Summary
     total: N
     passed: 0
     issues: 0
     pending: N
     skipped: 0
     blocked: 0

     ## Gaps
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 02-HUMAN-UAT.md with YAML frontmatter and six-scenario manual-smoke scaffolding</name>
  <files>.planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/01-perplexity-backend-audit/01-HUMAN-UAT.md (FULL FILE — Phase 1 template; lift 1:1 except for the keyed values per PATTERNS.md "Cross-phase template — lift YAML shape 1:1, scale scenarios from 1 to 6")
    - .planning/phases/02-insertion-point-scan-audit/02-PATTERNS.md section "Section structure" and the full "What to change for Phase 2 (six scenarios, exact label wording per RESEARCH.md Pitfall #8)" block — this contains the exact 6-scenario layout to copy
    - .planning/phases/02-insertion-point-scan-audit/02-RESEARCH.md Pitfall #8 (the literal label substring at UiActions.kt:366 — `url param: <name>`, `body param: <name>`, `cookie: <name>`, `header: <name>`, `json field: <name>`) and Open Question #3 (scenario 6 wording: "Empty or whitespace-only selection")
    - .planning/phases/02-insertion-point-scan-audit/02-CONTEXT.md D-09 (six scenarios) and D-08 (rationale for skipping a Mockito unit test — this file IS the proof)
  </read_first>
  <action>
    Create the new file `.planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md` with the structure below. Use the **Write tool** (not heredoc / cat). The structure mirrors `01-HUMAN-UAT.md` exactly except for the keyed values and scaled scenario count (1 → 6 per D-09).

    File content layout:

    1. YAML frontmatter (5 keys in this exact order — mirrors 01-HUMAN-UAT.md:1-7):
       - `status: partial`
       - `phase: 02-insertion-point-scan-audit`
       - `source: [02-VERIFICATION.md]` (the verifier may later create that file; the citation is forward-looking but consistent with Phase 1's pattern)
       - `started: <UTC ISO-8601 timestamp at the moment this file is written, format `YYYY-MM-DDTHH:MM:SSZ` — match Phase 1's shape exactly, e.g. `2026-05-13T11:35:00Z`>`. Use the current execution-time clock for the actual value.
       - `updated: <same timestamp as started>`

    2. Section `## Current Test` with the literal placeholder `[awaiting human testing]` on its own line (mirrors 01-HUMAN-UAT.md:11).

    3. Section `## Tests` containing six scenario blocks. Each block has a `### N. <title> (<req-ids>)` heading line, an `expected: <literal>` line, and a `result: [pending]` line — exactly the shape used by 01-HUMAN-UAT.md scenario 1.

       The six scenarios (use these exact titles, requirement IDs, and `expected:` literals — Pitfall #8 says every label substring must match `UiActions.kt:366` exactly):

       Scenario 1 — title `Selection inside a URL parameter value (INSP-01, INSP-04)`. expected: `Right-click menu shows "AI Scan on Selected Insertion Point (url param: <name>)".`

       Scenario 2 — title `Selection inside a body parameter value (INSP-01, INSP-04)`. expected: `Right-click menu shows "AI Scan on Selected Insertion Point (body param: <name>)".`

       Scenario 3 — title `Selection inside a cookie value (INSP-01, INSP-04)`. expected: `Right-click menu shows "AI Scan on Selected Insertion Point (cookie: <name>)".`

       Scenario 4 — title `Selection inside a header line (INSP-01, INSP-04)`. expected: `Right-click menu shows "AI Scan on Selected Insertion Point (header: <name>)".`

       Scenario 5 — title `Selection inside a JSON value (INSP-01, INSP-04)`. expected: `Right-click menu shows "AI Scan on Selected Insertion Point (json field: <name>)".`

       Scenario 6 — title `Empty or whitespace-only selection (INSP-02)`. expected: `Menu item does not appear in the context menu.`

       For every scenario, the result line is exactly `result: [pending]` (the maintainer overwrites this during execution).

    4. Section `## Summary` with six counter lines (mirrors 01-HUMAN-UAT.md:20-26). Initial counters reflect "all six pending":
       - `total: 6`
       - `passed: 0`
       - `issues: 0`
       - `pending: 6`
       - `skipped: 0`
       - `blocked: 0`

    5. Trailing section `## Gaps` (empty body — mirrors 01-HUMAN-UAT.md:28 — present so the maintainer can append observations without touching the file structure).

    Formatting rules:
    - Use `\n` (LF) line endings (matches `.editorconfig` `end_of_line = lf`).
    - One blank line between top-level sections (`---` block → blank → `## Current Test`, etc.) — mirrors 01-HUMAN-UAT.md spacing.
    - No trailing whitespace on any line. Final newline present (matches `.editorconfig` `insert_final_newline = true`).
    - Do NOT add a `## Gaps` placeholder body (Phase 1 leaves it empty; mirror that).
    - Do NOT include `xml element` or `path segment` scenarios — D-09 does not include them; the maintainer's editor selection rarely hits XML or numeric path segments in everyday Burp usage, and the resolver-side coverage in Plan 01 is the unit-level proof for those types.
    - Do NOT include any `## D-XX —` sub-section blocks (Phase 1 references its `01-VERIFICATION.md` via the `source:` frontmatter, not via inline sub-headers in this file).
  </action>
  <verify>
    <automated>test -f .planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md && head -1 .planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md | grep -q '^---$' && grep -c '^### [1-6]\. ' .planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md | grep -q '^6$' && grep -q 'phase: 02-insertion-point-scan-audit' .planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md && grep -q 'url param: <name>' .planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md && grep -q 'body param: <name>' .planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md && grep -q 'cookie: <name>' .planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md && grep -q 'header: <name>' .planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md && grep -q 'json field: <name>' .planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md && grep -q 'Menu item does not appear' .planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md && grep -q '^total: 6$' .planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md && grep -q '^pending: 6$' .planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md && grep -c '^result: \[pending\]$' .planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md | grep -q '^6$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md` exists.
    - First line of the file is `---` (start of YAML frontmatter), matching `01-HUMAN-UAT.md`.
    - Frontmatter contains all five keys: `status: partial`, `phase: 02-insertion-point-scan-audit`, `source: [02-VERIFICATION.md]`, `started: <ISO-8601 UTC>`, `updated: <ISO-8601 UTC>`.
    - File contains exactly 6 lines matching `^### [1-6]\. ` (six scenario headings).
    - File contains exactly 6 lines matching `^result: \[pending\]$` (one per scenario).
    - File contains the exact label substrings `url param: <name>`, `body param: <name>`, `cookie: <name>`, `header: <name>`, `json field: <name>` (Pitfall #8 — these must match the production label-format at UiActions.kt:366).
    - File contains `Menu item does not appear in the context menu.` (scenario 6 — INSP-02).
    - File's `## Summary` block contains `total: 6`, `passed: 0`, `issues: 0`, `pending: 6`, `skipped: 0`, `blocked: 0`.
    - File ends with `## Gaps` followed by a single newline (no body content) — mirrors `01-HUMAN-UAT.md` structure.
    - File does NOT contain `xml element` or `path segment` label substrings (not in D-09 scope).
    - File does NOT contain Kotlin code, test methods, or any reference to Mockito (per D-08 — no unit test of the UI builder).
  </acceptance_criteria>
  <done>
    `.planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md` exists with valid YAML frontmatter and 6 scenario blocks following the `01-HUMAN-UAT.md` template exactly. Each scenario's `expected:` line uses the literal label substring produced by `UiActions.kt:366`. The `## Summary` counters reflect "all six pending". The file is ready for the maintainer to record results during execution.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none for this plan) | Plan 03 creates a documentation artefact. No code path is added or changed; no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-INSP01-01 | I (Information Disclosure) | Right-click menu visibility | accept | The audit verifies that the menu reflects only valid candidates; out-of-scope leakage at the queueing layer is handled by T-2-01 in Plan 02. The UI builder cannot leak data on its own — it just decides whether to render a JMenuItem. |
| T-2-INSP02-01 | I (Information Disclosure) | Menu hidden on empty/whitespace selection | accept | Scenario 6 of the manual smoke is the audit proof that no menu (and therefore no scan dispatch) is offered when there is no candidate. The mitigation is the existing `null` return at `buildAiScanInsertionPointItem` UiActions.kt:354 + the resolver's `null` return — this plan locks the user-visible outcome. |

Plan 03 introduces zero production code. The two acceptances above record that the relevant security-relevant outcomes (no menu when there is no candidate; menu reflects only valid candidates) are observed by the maintainer in real Burp during the manual smoke.
</threat_model>

<verification>
After Task 1 completes, run the structural sanity checks:

```
test -f .planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md
diff <(grep -c '^### [1-6]\. ' .planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md) <(echo 6)
diff <(grep -c '^result: \[pending\]$' .planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md) <(echo 6)
```

All three must succeed. No gradle build or test run is needed — Plan 03 touches only the `.planning/` directory.

The maintainer's smoke run (in real Burp) is a separate manual activity outside this plan's automated execution. Plan 03 is complete once the scaffolding file exists and is structurally valid.
</verification>

<success_criteria>
- New file `.planning/phases/02-insertion-point-scan-audit/02-HUMAN-UAT.md` exists with valid YAML frontmatter and six-scenario scaffolding mirroring `01-HUMAN-UAT.md`.
- All five label substrings (`url param`, `body param`, `cookie`, `header`, `json field`) appear in the file, locking the UI label contract from `UiActions.kt:366` (Pitfall #8).
- Scenario 6 records the INSP-02 outcome ("Menu item does not appear in the context menu.").
- Summary counters reflect "all six pending" (`total: 6`, `pending: 6`).
- No production code changed. No new test file created. No Mockito-Kotlin unit test of `buildAiScanInsertionPointItem` introduced (D-08 honoured).
</success_criteria>

<output>
After completion, create `.planning/phases/02-insertion-point-scan-audit/02-03-SUMMARY.md` documenting the artefact creation and any timestamp values chosen for the YAML frontmatter.
</output>
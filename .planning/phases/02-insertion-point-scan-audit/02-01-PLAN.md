---
phase: 02-insertion-point-scan-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractorTest.kt
autonomous: true
requirements: [INSP-04, INSP-02]
tags: [scanner, insertion-point, resolver, unit-test, mockito-kotlin]

must_haves:
  truths:
    - "Resolver branch 1 (parsed params via valueOffsets) returns InjectionType.BODY_PARAM when selection lands inside a body-param value range"
    - "Resolver branch 1 returns InjectionType.COOKIE when selection lands inside a cookie value's valueOffsets() range"
    - "Resolver branch 3 (body substring) returns InjectionType.XML_ELEMENT with the matching element name when selection lands inside an XML value in a Content-Type: application/xml body"
    - "Resolver branch 4 (pathIdPattern) returns InjectionType.PATH_SEGMENT with originalValue set to the matched segment when selection lands inside a numeric path segment"
    - "Resolver returns null when selection lands on a header line whose name is not in a non-empty headerAllowlist (INSP-02 boundary)"
    - "All new tests reuse the existing byteArrayMock(text) and rangeMock(start, end) helpers verbatim (no new helpers, no Answers.RETURNS_DEEP_STUBS, no third assertion style)"
    - "Test method names use CamelCase (per Phase 1 D-07) and assertions come only from kotlin.test.* (matches file convention)"
    - "Per D-02: one @Test method per resolver sub-case; no @ParameterizedTest"
  artifacts:
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractorTest.kt"
      provides: "5 new @Test methods: matchInsertionPointPicksBodyParam, matchInsertionPointPicksCookie, matchInsertionPointPicksXmlElement, matchInsertionPointPicksPathSegment, matchInsertionPointRespectsNonEmptyHeaderAllowlist"
      contains: "fun matchInsertionPointPicksBodyParam"
  key_links:
    - from: "InjectionPointExtractorTest.kt new tests"
      to: "InjectionPointExtractor.matchInsertionPoint (src/main/kotlin/.../InjectionPointExtractor.kt:152-256)"
      via: "direct static call with mocked HttpRequest"
      pattern: "InjectionPointExtractor\\.matchInsertionPoint"
    - from: "matchInsertionPointRespectsNonEmptyHeaderAllowlist"
      to: "headerAllowlist filter (InjectionPointExtractor.kt:186)"
      via: "kwarg headerAllowlist = setOf(\"x-foo-only\")"
      pattern: "headerAllowlist\\s*="
---

<objective>
Lock resolver coverage for INSP-04 (URL + body + cookie + header + JSON/XML + path segment) and the INSP-02 boundary (non-empty headerAllowlist returns null) by adding 5 new @Test methods to `InjectionPointExtractorTest.kt`. Three resolver sub-cases (BODY_PARAM, COOKIE) hit parsed-params branch 1; XML_ELEMENT hits body-substring branch 3; PATH_SEGMENT hits pathIdPattern branch 4; the allowlist test hits the header-line filter at line 186.

Purpose: This phase is a behaviour-locking audit on already-shipped resolver code. The tests are the deliverable, not RED/GREEN/REFACTOR cycles. The resolver implementation does not change. Pass on first run is the success criterion.

Output: 5 new `@Test fun ...()` methods inside the existing `class InjectionPointExtractorTest` body. No new test file, no new helpers, no new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-insertion-point-scan-audit/02-CONTEXT.md
@.planning/phases/02-insertion-point-scan-audit/02-RESEARCH.md
@.planning/phases/02-insertion-point-scan-audit/02-PATTERNS.md
@.planning/phases/02-insertion-point-scan-audit/02-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/test/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractorTest.kt
@src/main/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractor.kt

<interfaces>
<!-- The resolver signature the new tests call. Verified from InjectionPointExtractor.kt:152-158 -->

fun InjectionPointExtractor.matchInsertionPoint(
    request: HttpRequest,
    selectionStart: Int,
    selectionEnd: Int,
    headerAllowlist: Set<String> = emptySet(),
    maxFields: Int = 50,
): InjectionPoint?

<!-- InjectionPoint data class (from ActiveScanModels.kt:279-284) — fields the assertions read -->

data class InjectionPoint(
    val type: InjectionType,
    val name: String,
    val originalValue: String,
    val position: Int? = null,
)

<!-- Helpers already present in the file at lines 189-203 — REUSE; do not redefine -->
private fun byteArrayMock(text: String): burp.api.montoya.core.ByteArray
private fun rangeMock(start: Int, end: Int): Range

<!-- Import set already present (lines 1-13) — no new imports required -->
package com.six2dez.burp.aiagent.scanner
import burp.api.montoya.core.Range
import burp.api.montoya.http.message.HttpHeader
import burp.api.montoya.http.message.params.HttpParameterType
import burp.api.montoya.http.message.params.ParsedHttpParameter
import burp.api.montoya.http.message.requests.HttpRequest
import org.mockito.kotlin.mock
import org.mockito.kotlin.whenever
import kotlin.test.Test
import kotlin.test.assertEquals
import kotlin.test.assertNull
import kotlin.test.assertTrue
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add matchInsertionPointPicksBodyParam @Test method (D-01.1)</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractorTest.kt</files>
  <read_first>
    - src/test/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractorTest.kt (FULL FILE — lines 86-130 are the analog `matchInsertionPointPicksOverlappingUrlParam`; lines 189-203 are the helpers to reuse)
    - .planning/phases/02-insertion-point-scan-audit/02-PATTERNS.md sections "Imports pattern", "Helper reuse", "Core pattern: parsed-param sub-case test", and the URL_PARAM → BODY_PARAM column of the per-sub-case table
    - .planning/phases/02-insertion-point-scan-audit/02-RESEARCH.md "Pattern 1: Resolver Sub-Case Test (D-01)" and Pitfall #5 (body-substring branch ordering)
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractor.kt:152-175 (branch 1 of the resolver — parsed parameters via valueOffsets, the BODY → InjectionType.BODY_PARAM mapping at line 168)
  </read_first>
  <action>
    Add a new `@Test fun matchInsertionPointPicksBodyParam()` method to the existing `class InjectionPointExtractorTest`. Mirror the structure of the in-file analog `matchInsertionPointPicksOverlappingUrlParam` (lines 86-130) exactly, with the BODY_PARAM substitutions from the PATTERNS.md per-sub-case table.

    Per D-01.1, set up a POST request with `application/x-www-form-urlencoded` body and one form-encoded body parameter:
    - `raw = "POST /api HTTP/1.1\r\nHost: example.com\r\nContent-Type: application/x-www-form-urlencoded\r\n\r\nusername=alice&role=user"` (use `\r\n` literals; mirror the existing test's string-concatenation style).
    - Compute `aliceStart = raw.indexOf("alice")` and `aliceEnd = aliceStart + "alice".length`. The selection range must be INSIDE (not equal to) these offsets — use `selectionStart = aliceStart + 1, selectionEnd = aliceEnd - 1` (mirrors the `qStart + 1, qStart + 4` shape at line 124 of the analog).
    - Build the `ParsedHttpParameter` mock with the four whenever calls in the analog's order: `type() -> HttpParameterType.BODY` (per D-01.1), `name() -> "username"`, `value() -> "alice"`, `valueOffsets() -> rangeMock(aliceStart, aliceEnd)`.
    - Build the `HttpRequest` mock with the eight whenever calls in the analog's order: `parameters() -> listOf(usernameParam)`, `headers() -> emptyList()`, `toByteArray() -> byteArrayMock(raw)`, `bodyOffset() -> raw.indexOf("username")` (the byte index where the body starts, immediately after the blank header-terminator line), `bodyToString() -> "username=alice&role=user"`, `headerValue("Content-Type") -> "application/x-www-form-urlencoded"`, `url() -> "http://example.com/api"`.
    - Call `InjectionPointExtractor.matchInsertionPoint(request = request, selectionStart = aliceStart + 1, selectionEnd = aliceEnd - 1)` and assign to `val match`.
    - Assert three things in order (matching the analog's assertion sequence): `assertTrue(match != null)`, `assertEquals(InjectionType.BODY_PARAM, match!!.type)`, `assertEquals("username", match.name)`.

    Do NOT add `Answers.RETURNS_DEEP_STUBS` here. Do NOT add new JUnit 5 imports — only kotlin.test.* is allowed in this file (PATTERNS.md "Assertion library policy"). Do NOT add a `parsedParamMock` helper — D-discretion forbids it unless 6 whenever calls repeat 3+ times. Reuse `byteArrayMock` and `rangeMock` verbatim from lines 189-203.

    Pitfall #5 guard: branch 1 of the resolver fires before branch 3, so once `valueOffsets()` overlaps the selection, the BODY_PARAM branch wins. No need to stub headers or other body content — the parsed-param mock is sufficient.

    Place the new method in the class body. Position is flexible; placing it adjacent to `matchInsertionPointPicksOverlappingUrlParam` (after line 130, before `matchInsertionPointFallsBackToHeaderWhenSelectionHitsHeaderLine` at line 132) keeps the parsed-param sub-cases together.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.scanner.InjectionPointExtractorTest.matchInsertionPointPicksBodyParam" -PexcludeHeavyTests=true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' src/test/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractorTest.kt | grep -c 'fun matchInsertionPointPicksBodyParam'` returns `1`
    - The new method contains `HttpParameterType.BODY` (resolver branch 1 BODY mapping)
    - The new method asserts `InjectionType.BODY_PARAM` (the resolver return value at InjectionPointExtractor.kt:172)
    - The new method asserts `"username"` as the parameter name
    - The new method calls `byteArrayMock(` and `rangeMock(` (reuses existing helpers, does NOT define new ones)
    - The new method does NOT contain `Answers.RETURNS_DEEP_STUBS` (shallow mocks only per PATTERNS.md)
    - The new method does NOT contain `org.junit.jupiter.api.Assertions` (kotlin.test.* only in this file)
    - `./gradlew test --tests "*InjectionPointExtractorTest.matchInsertionPointPicksBodyParam" -PexcludeHeavyTests=true` exits 0
    - `./gradlew ktlintCheck` does not flag any new violation in this file
  </acceptance_criteria>
  <done>
    `matchInsertionPointPicksBodyParam` exists in `InjectionPointExtractorTest.kt`, mirrors the URL_PARAM analog's mocking shape with BODY_PARAM substitutions, asserts the resolver returns `InjectionType.BODY_PARAM` with `name = "username"`, and the test passes via the gradle command above.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add matchInsertionPointPicksCookie @Test method (D-01.2)</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractorTest.kt</files>
  <read_first>
    - src/test/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractorTest.kt (the file as left by Task 1; lines 86-130 remain the canonical analog)
    - .planning/phases/02-insertion-point-scan-audit/02-PATTERNS.md "Core pattern: parsed-param sub-case test" and the COOKIE column of the per-sub-case table
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractor.kt:152-175 (branch 1 COOKIE mapping at line 169)
  </read_first>
  <action>
    Add a new `@Test fun matchInsertionPointPicksCookie()` method using the same parsed-param branch-1 template as Task 1, with COOKIE substitutions per D-01.2.

    Set up a GET request with a single `Cookie:` header carrying a `session=abc` pair:
    - `raw = "GET / HTTP/1.1\r\nHost: example.com\r\nCookie: session=abc; tracker=xyz\r\n\r\n"`.
    - Compute `abcStart = raw.indexOf("abc")` (this lands inside the `Cookie:` header line, not in the body — there is no body). Compute `abcEnd = abcStart + "abc".length`.
    - Build the `ParsedHttpParameter` mock for the session cookie: `type() -> HttpParameterType.COOKIE`, `name() -> "session"`, `value() -> "abc"`, `valueOffsets() -> rangeMock(abcStart, abcEnd)`. Per Pitfall #5 (PATTERNS.md and RESEARCH.md), only one cookie param is needed — branch 1 wins as soon as overlap is detected.
    - Build the `HttpRequest` mock with the same eight whenever calls: `parameters() -> listOf(sessionParam)`, `headers() -> emptyList()` (the cookie data is provided through the parsed-param mock, not the header list — this keeps branch 2 from firing on the Cookie header line), `toByteArray() -> byteArrayMock(raw)`, `bodyOffset() -> raw.length` (no body — same shape as the URL analog at line 115), `bodyToString() -> ""`, `headerValue("Content-Type") -> null`, `url() -> "http://example.com/"`.
    - Call `InjectionPointExtractor.matchInsertionPoint(request = request, selectionStart = abcStart + 1, selectionEnd = abcEnd - 1)` (inside-the-range selection per the URL analog's `+1`/`-1` convention).
    - Assert: `assertTrue(match != null)`, `assertEquals(InjectionType.COOKIE, match!!.type)`, `assertEquals("session", match.name)`.

    Same rules as Task 1: kotlin.test.* assertions only; reuse `byteArrayMock`/`rangeMock`; no new helpers; no `Answers.RETURNS_DEEP_STUBS`; CamelCase test name.

    Place the new method immediately after `matchInsertionPointPicksBodyParam` so the four parsed-param resolver sub-cases (URL, BODY, COOKIE, then later XML/PATH) cluster together.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.scanner.InjectionPointExtractorTest.matchInsertionPointPicksCookie" -PexcludeHeavyTests=true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fun matchInsertionPointPicksCookie' src/test/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractorTest.kt` returns `1`
    - The new method contains `HttpParameterType.COOKIE`
    - The new method asserts `InjectionType.COOKIE` and `"session"` as the param name
    - The new method does NOT stub `request.headers()` with the cookie header (only `emptyList()`) so the test exercises resolver branch 1, not branch 2 (Pitfall #5)
    - `./gradlew test --tests "*InjectionPointExtractorTest.matchInsertionPointPicksCookie" -PexcludeHeavyTests=true` exits 0
    - `./gradlew ktlintCheck` does not flag any new violation in this file
  </acceptance_criteria>
  <done>
    `matchInsertionPointPicksCookie` exists in `InjectionPointExtractorTest.kt`, asserts resolver returns `InjectionType.COOKIE` with `name = "session"` when selection lands inside the cookie's `valueOffsets()` range, and the test passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add matchInsertionPointPicksXmlElement @Test method (D-01.3)</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractorTest.kt</files>
  <read_first>
    - src/test/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractorTest.kt (lines 205-232 are the analog `matchInsertionPointPicksJsonFieldWhenSelectionInBody`)
    - .planning/phases/02-insertion-point-scan-audit/02-PATTERNS.md "Core pattern: body-substring sub-case test" and the XML_ELEMENT column of the per-sub-case table
    - .planning/phases/02-insertion-point-scan-audit/02-RESEARCH.md Pitfall #5 (body-substring branch ordering — parameters MUST be empty)
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractor.kt:196-223 (branch 3 body-substring matcher; XML route enters at line 212 when Content-Type contains "xml" or body trim-starts with "<")
  </read_first>
  <action>
    Add a new `@Test fun matchInsertionPointPicksXmlElement()` method using the body-substring branch-3 template (analog: `matchInsertionPointPicksJsonFieldWhenSelectionInBody` at lines 205-232), swapped to XML per D-01.3.

    Set up a POST with `Content-Type: application/xml` and a tiny XML body:
    - `headers = "POST /api HTTP/1.1\r\nHost: example.com\r\nContent-Type: application/xml\r\n\r\n"` (declared as a separate `val` to make `bodyOffset()` arithmetic clean — mirrors line 207 of the analog).
    - `body = "<order><id>42</id></order>"`.
    - `raw = headers + body`.
    - The resolver translates selection offsets to body-relative via `selectionStart - bodyOffset` (line 205 of InjectionPointExtractor.kt). The selection range is computed from absolute `raw` offsets: `val fortyTwoStart = raw.indexOf("42")` and `val fortyTwoEnd = fortyTwoStart + "42".length`.
    - Build the `HttpRequest` mock per analog. Pitfall #5 is critical: `parameters() -> emptyList()` AND `headers() -> emptyList()` so branches 1 and 2 are skipped and branch 3 fires. Stub: `parameters() -> emptyList()`, `headers() -> emptyList()`, `toByteArray() -> byteArrayMock(raw)`, `bodyOffset() -> headers.length` (the start of `body` inside `raw`), `bodyToString() -> body`, `headerValue("Content-Type") -> "application/xml"` (drives the resolver into the XML branch at line 212), `url() -> "http://example.com/api"`.
    - Call `InjectionPointExtractor.matchInsertionPoint(request = request, selectionStart = fortyTwoStart, selectionEnd = fortyTwoEnd)` (the analog uses non-`+1`/`-1` offsets on the body-substring path because the matcher uses `body.indexOf(needle)` then `rangesOverlap` — see line 220).
    - Assert four things per PATTERNS.md XML_ELEMENT row (one more than the JSON analog): `assertTrue(match != null)`, `assertEquals(InjectionType.XML_ELEMENT, match!!.type)`, `assertEquals("id", match.name)`, `assertEquals("42", match.originalValue)`. The fourth assertion (`originalValue`) is explicitly called out in CONTEXT.md D-01.3 and PATTERNS.md.

    Place the new method adjacent to `matchInsertionPointPicksJsonFieldWhenSelectionInBody` (right after line 232) so the two body-substring tests cluster.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.scanner.InjectionPointExtractorTest.matchInsertionPointPicksXmlElement" -PexcludeHeavyTests=true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fun matchInsertionPointPicksXmlElement' src/test/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractorTest.kt` returns `1`
    - The new method stubs `request.parameters()` to return `emptyList()` (Pitfall #5)
    - The new method stubs `request.headerValue("Content-Type")` to return `"application/xml"`
    - The new method asserts `InjectionType.XML_ELEMENT`, `"id"` (name), AND `"42"` (originalValue) — four assertions total
    - `./gradlew test --tests "*InjectionPointExtractorTest.matchInsertionPointPicksXmlElement" -PexcludeHeavyTests=true` exits 0
    - `./gradlew ktlintCheck` does not flag any new violation in this file
  </acceptance_criteria>
  <done>
    `matchInsertionPointPicksXmlElement` exists, drives the resolver into branch 3 (body substring + XML), and asserts `InjectionType.XML_ELEMENT` with `name = "id"` and `originalValue = "42"` when selection lands inside `42` in the XML body. The test passes.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add matchInsertionPointPicksPathSegment @Test method (D-01.4)</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractorTest.kt</files>
  <read_first>
    - src/test/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractorTest.kt (lines 86-130 — URL_PARAM analog for the `raw.indexOf(...)` absolute-offset pattern; lines 167-187 — null-match analog for the empty-params/empty-headers/empty-body shape)
    - .planning/phases/02-insertion-point-scan-audit/02-PATTERNS.md "Core pattern: path-segment sub-case test (NO direct analog — composite of body-substring + raw-byte arithmetic)" (full block with the recommended test body)
    - .planning/phases/02-insertion-point-scan-audit/02-RESEARCH.md Pitfall #1 (path-segment offset translation — selectionStart MUST be the ABSOLUTE byte offset, not path-relative)
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractor.kt:225-245 (branch 4 — pathIdPattern regex; line 235 `pathStart = raw.indexOf(path)` and line 238-239 `absStart = pathStart + match.range.first`, `absEnd = pathStart + match.range.last + 1`)
  </read_first>
  <action>
    Add a new `@Test fun matchInsertionPointPicksPathSegment()` method using the path-segment branch-4 template documented in PATTERNS.md. There is no direct analog in the file — compose from the URL_PARAM analog's raw-byte arithmetic plus the empty-params/empty-headers shape of `matchInsertionPointReturnsNullWhenSelectionMissesEverything`.

    Set up a GET request with a numeric path segment:
    - `raw = "GET /api/users/12345 HTTP/1.1\r\nHost: example.com\r\n\r\n"`.
    - Compute `idStart = raw.indexOf("12345")` and `idEnd = idStart + "12345".length`. These are ABSOLUTE byte offsets in the raw request (Pitfall #1) — do NOT compute `path.indexOf("12345")`.
    - Build the `HttpRequest` mock with all four upstream branches drained: `parameters() -> emptyList()` (skip branch 1), `headers() -> emptyList()` (skip branch 2), `toByteArray() -> byteArrayMock(raw)`, `bodyOffset() -> raw.length`, `bodyToString() -> ""` (skip branch 3 — `body.isNotBlank()` short-circuits at line 204), `headerValue("Content-Type") -> null`, `url() -> "http://example.com/api/users/12345"`.
    - Call `InjectionPointExtractor.matchInsertionPoint(request = request, selectionStart = idStart, selectionEnd = idEnd)` (absolute offsets, no `+1`/`-1` — branch 4 uses `rangesOverlap` on the absolute path range).
    - Assert: `assertTrue(match != null)`, `assertEquals(InjectionType.PATH_SEGMENT, match!!.type)`, `assertEquals("12345", match.originalValue)` — the per-segment identity is in `originalValue`; the `name` field is the constant `"path_id"` (set at InjectionPointExtractor.kt:241). Per CONTEXT.md D-01.4 Specifics, assert `originalValue` (the planner may optionally also assert `name = "path_id"` but D-01.4 only requires `originalValue`).

    Pitfall #1 guard: the resolver does `pathStart = raw.indexOf(path)` then `absStart = pathStart + match.range.first`. Passing path-relative offsets to `matchInsertionPoint` would land the selection outside the absolute path range and return null. The test MUST compute `idStart` from `raw`, not from `path`.

    Place this method adjacent to `matchInsertionPointPicksXmlElement` (the two body-skipping fallback tests cluster naturally) — or at the bottom of the test class, just before the private helpers at line 189. Either placement is acceptable.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.scanner.InjectionPointExtractorTest.matchInsertionPointPicksPathSegment" -PexcludeHeavyTests=true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fun matchInsertionPointPicksPathSegment' src/test/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractorTest.kt` returns `1`
    - The new method computes its selection offset from `raw.indexOf("12345")` (NOT from a path-relative `indexOf` on a substring) — Pitfall #1
    - The new method stubs `request.parameters()` and `request.headers()` both to `emptyList()` (drains branches 1 and 2)
    - The new method asserts `InjectionType.PATH_SEGMENT` AND `match.originalValue == "12345"`
    - `./gradlew test --tests "*InjectionPointExtractorTest.matchInsertionPointPicksPathSegment" -PexcludeHeavyTests=true` exits 0
    - `./gradlew ktlintCheck` does not flag any new violation in this file
  </acceptance_criteria>
  <done>
    `matchInsertionPointPicksPathSegment` exists, drains branches 1-3 via empty params/headers/body, and asserts the resolver returns `InjectionType.PATH_SEGMENT` with `originalValue = "12345"` when the selection lands inside the numeric segment of `/api/users/12345` (computed via absolute raw-byte offset per Pitfall #1). The test passes.
  </done>
</task>

<task type="auto">
  <name>Task 5: Add matchInsertionPointRespectsNonEmptyHeaderAllowlist @Test method (D-03)</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractorTest.kt</files>
  <read_first>
    - src/test/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractorTest.kt (lines 132-164 — analog `matchInsertionPointFallsBackToHeaderWhenSelectionHitsHeaderLine`; lines 167-187 — `assertNull(match)` pattern)
    - .planning/phases/02-insertion-point-scan-audit/02-PATTERNS.md "Header-allowlist non-empty test (D-03)" (full recommended block)
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractor.kt:152-194 (signature with `headerAllowlist: Set<String> = emptySet()` at line 156; the filter at line 186 — `if (headerAllowlist.isNotEmpty() && !headerAllowlist.contains(header.name().lowercase())) return@firstOrNull false`)
  </read_first>
  <action>
    Add a new `@Test fun matchInsertionPointRespectsNonEmptyHeaderAllowlist()` method. This locks the INSP-02 boundary: when the caller passes a non-empty `headerAllowlist` that does NOT contain the selected header's lowercased name, the resolver returns `null` (the line is filtered out by the predicate at InjectionPointExtractor.kt:186).

    Set up identically to `matchInsertionPointFallsBackToHeaderWhenSelectionHitsHeaderLine` (lines 132-164) — same `X-Forwarded-Host: attacker.com` request, same selection range hitting the header line. The only differences are the `headerAllowlist` kwarg and the assertion shape:
    - `raw = "GET / HTTP/1.1\r\nHost: example.com\r\nX-Forwarded-Host: attacker.com\r\n\r\n"`.
    - `xfhStart = raw.indexOf("X-Forwarded-Host:")` and `xfhEnd = xfhStart + "X-Forwarded-Host: attacker.com".length`.
    - Build the `HttpHeader` mock identical to the analog: `name() -> "X-Forwarded-Host"`, `value() -> "attacker.com"`.
    - Build the `HttpRequest` mock identical to the analog: `parameters() -> emptyList()`, `headers() -> listOf(header)`, `toByteArray() -> byteArrayMock(raw)`, `bodyOffset() -> raw.length`, `bodyToString() -> ""`, `headerValue("Content-Type") -> null`, `url() -> "http://example.com/"`.
    - Call `InjectionPointExtractor.matchInsertionPoint(request = request, selectionStart = xfhStart + 5, selectionEnd = xfhEnd, headerAllowlist = setOf("x-foo-only"))` — note the FOURTH kwarg `headerAllowlist`. The set contains a lowercased name that does NOT match `"x-forwarded-host"` (the header name is compared after `.lowercase()` at InjectionPointExtractor.kt:186), so the predicate returns false and the `firstOrNull` finds no match → `null` propagates to branch 3 → empty body → branch 4 → URL `"/"` has no numeric segment → `null` returned.
    - Assert exactly one thing: `assertNull(match)`. Do NOT add post-null `assertEquals` calls — `match` is null.

    The resolver signature already supports `headerAllowlist: Set<String>` (verified from InjectionPointExtractor.kt:156). No production change.

    Place the new method directly after `matchInsertionPointFallsBackToHeaderWhenSelectionHitsHeaderLine` (line 164) so the two header-line tests (empty allowlist passes, non-empty rejects) sit side-by-side.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.scanner.InjectionPointExtractorTest.matchInsertionPointRespectsNonEmptyHeaderAllowlist" -PexcludeHeavyTests=true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fun matchInsertionPointRespectsNonEmptyHeaderAllowlist' src/test/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractorTest.kt` returns `1`
    - The new method calls `matchInsertionPoint(...)` with `headerAllowlist = setOf("x-foo-only")` (a name NOT matching `"x-forwarded-host"`)
    - The new method uses `assertNull(match)` (NOT `assertTrue(match != null)`)
    - The new method does NOT assert any `InjectionType.*` after the null check (match is null; further calls would NPE)
    - `./gradlew test --tests "*InjectionPointExtractorTest.matchInsertionPointRespectsNonEmptyHeaderAllowlist" -PexcludeHeavyTests=true` exits 0
    - Running the full file `./gradlew test --tests "com.six2dez.burp.aiagent.scanner.InjectionPointExtractorTest" -PexcludeHeavyTests=true` exits 0 (all 5 new + 7 pre-existing tests pass — no regressions)
    - `./gradlew ktlintCheck` does not flag any new violation in this file
  </acceptance_criteria>
  <done>
    `matchInsertionPointRespectsNonEmptyHeaderAllowlist` exists, passes a non-empty `headerAllowlist = setOf("x-foo-only")` to a resolver call whose selection lands on `X-Forwarded-Host: attacker.com`, and asserts the resolver returns `null`. All 12 tests in `InjectionPointExtractorTest.kt` pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none for this plan) | Plan 01 adds test-only changes; no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-INSP04-01 | I (Information Disclosure) | `matchInsertionPoint` resolver | accept | Out of scope for Plan 01. Plan 02 owns the scope-predicate gate (T-2-01); the resolver is a pure function over a single `HttpRequest`, does not consult scope, and cannot leak out-of-scope targets on its own. |

No security-relevant control is introduced or modified by Plan 01. The resolver was already present in `[Unreleased]`; this plan locks behaviour with tests, no production-code edit.
</threat_model>

<verification>
After all 5 tasks complete, run the resolver test class in isolation to prove no regressions:

```
./gradlew test --tests "com.six2dez.burp.aiagent.scanner.InjectionPointExtractorTest" -PexcludeHeavyTests=true
```

Then run the full fast suite to confirm Plan 01 did not break anything elsewhere:

```
./gradlew test -PexcludeHeavyTests=true
```

Both must exit 0. ktlintCheck must remain clean on `InjectionPointExtractorTest.kt`.
</verification>

<success_criteria>
- 5 new `@Test` methods exist in `src/test/kotlin/com/six2dez/burp/aiagent/scanner/InjectionPointExtractorTest.kt`: `matchInsertionPointPicksBodyParam`, `matchInsertionPointPicksCookie`, `matchInsertionPointPicksXmlElement`, `matchInsertionPointPicksPathSegment`, `matchInsertionPointRespectsNonEmptyHeaderAllowlist`.
- All 12 `@Test` methods in `InjectionPointExtractorTest.kt` pass: `./gradlew test --tests "*InjectionPointExtractorTest" -PexcludeHeavyTests=true` exits 0.
- No new file is created. No new helper is defined. No new import is added. No new dependency is declared. `Answers.RETURNS_DEEP_STUBS` does NOT appear in any new method.
- ktlint check on the modified file remains clean.
- INSP-04 sub-case coverage matrix (URL ✓ pre-existing, BODY ✓ new, COOKIE ✓ new, HEADER ✓ pre-existing for empty allowlist + ✓ new for non-empty, JSON ✓ pre-existing, XML ✓ new, PATH ✓ new) is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/02-insertion-point-scan-audit/02-01-SUMMARY.md` documenting which 5 methods landed and the test run output.
</output>
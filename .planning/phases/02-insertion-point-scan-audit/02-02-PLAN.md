---
phase: 02-insertion-point-scan-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerQueueModelTest.kt
autonomous: true
requirements: [INSP-03]
tags: [scanner, active-scan, queue, mockito-kotlin, scope, dos, unit-test]

must_haves:
  truths:
    - "manualScanInsertionPoint queues one ActiveScanTarget per filtered vuln class — re-invoking on the same insertion point queues a fresh set (dedup bypass per D-12, deliberately skipping the processedTargets window that queueTarget uses)"
    - "Priority 60 is locked at ActiveAiScanner.kt:235 — per Pitfall #4, ActiveScanQueueItem does not surface priority; the test KDoc anchors the line reference and the assertion proves the per-class loop fired (D-05.2 amended)"
    - "manualScanInsertionPoint returns 0 and leaves the queue empty when scopeOnly is true and api.scope().isInScope(url) returns false (threat T-2-01 mitigation locked, D-10)"
    - "manualScanInsertionPoint filters out classes in ScanPolicy.PASSIVE_ONLY_VULN_CLASSES even when the caller requests them (D-11)"
    - "manualScanInsertionPoint returns a short count (≤ maxQueueSize) when the requested vuln classes exceed remaining queue capacity (threat T-2-02 mitigation locked, D-13)"
    - "All queue tests reuse newScannerForQueueTests() builder and requestResponse(...) factory — no new helpers, no MockWebServer, no reflection (D-04, D-06, D-07)"
    - "Per D-12: dedup-bypass is folded into the D-05 test as invariant 3; no separate test"
    - "Per D-04: assertions use only public scanner.getQueueItems(limit) snapshot — no reflection into the private ConcurrentLinkedQueue"
  artifacts:
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerQueueModelTest.kt"
      provides: "4 new @Test methods: manualScanInsertionPointQueuesOnePerClassAtPriority60WithoutDedup, manualScanInsertionPointReturnsZeroAndDoesNotQueueWhenOutOfScope, manualScanInsertionPointFiltersPassiveOnlyVulnClasses, manualScanInsertionPointReturnsShortCountWhenQueueFull"
      contains: "fun manualScanInsertionPointQueuesOnePerClassAtPriority60WithoutDedup"
  key_links:
    - from: "ActiveScannerQueueModelTest.kt new tests"
      to: "ActiveAiScanner.manualScanInsertionPoint (src/main/kotlin/.../ActiveAiScanner.kt:214-255)"
      via: "direct method call on the scanner built by newScannerForQueueTests"
      pattern: "scanner\\.manualScanInsertionPoint"
    - from: "manualScanInsertionPointReturnsZeroAndDoesNotQueueWhenOutOfScope"
      to: "scope gate at ActiveAiScanner.kt:225"
      via: "whenever(api.scope().isInScope(any<String>())).thenReturn(false)"
      pattern: "isInScope"
    - from: "manualScanInsertionPointFiltersPassiveOnlyVulnClasses"
      to: "ScanPolicy.PASSIVE_ONLY_VULN_CLASSES at ActiveScanModels.kt:110-126"
      via: "vulnClasses list mixes CORS_MISCONFIGURATION (passive-only canary) and SQLI (active-eligible canary)"
      pattern: "VulnClass\\.CORS_MISCONFIGURATION"
    - from: "manualScanInsertionPointReturnsShortCountWhenQueueFull"
      to: "offerIfQueueNotFull at ActiveAiScanner.kt:257-264"
      via: "scanner.maxQueueSize = 2 + 5 active-eligible vuln classes"
      pattern: "maxQueueSize\\s*=\\s*2"
---

<objective>
Lock the queueing contract for INSP-03 (priority 60, one per filtered class, dedup-bypass) and the three adjacent invariants that gate or shape the queue behaviour: out-of-scope short-circuit (D-10, threat T-2-01), passive-only filter (D-11), and queue-full short count (D-13, threat T-2-02). Add 4 new `@Test` methods to `ActiveScannerQueueModelTest.kt`, reusing the existing `newScannerForQueueTests()` builder and `requestResponse(...)` factory.

Purpose: behaviour-locking audit — no production change. The four tests sit in the FAST suite (no `*BackpressureTest` / `*ConcurrencyTest` / `*IntegrationTest` suffix) so the PR gate covers them via `-PexcludeHeavyTests=true`.

Pitfall #4 advisory: `ActiveScanQueueItem` does not surface `priority`. The CONTEXT.md D-05.2 phrasing "assert every snapshot item has priority == 60" is satisfied by a KDoc-anchored line reference to `ActiveAiScanner.kt:235` rather than a direct field assertion — adding `priority` to `ActiveScanQueueItem` is a production change out of audit scope, and reflection is forbidden by D-04. This is an amended interpretation of D-05.2 per RESEARCH.md Pitfall #4 + Open Question #1; the substantive contract (size, per-class identity, dedup-bypass) is still proven.

Output: 4 new `@Test fun ...()` methods inside the existing `class ActiveScannerQueueModelTest` body.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-insertion-point-scan-audit/02-CONTEXT.md
@.planning/phases/02-insertion-point-scan-audit/02-RESEARCH.md
@.planning/phases/02-insertion-point-scan-audit/02-PATTERNS.md
@.planning/phases/02-insertion-point-scan-audit/02-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerQueueModelTest.kt
@src/test/kotlin/com/six2dez/burp/aiagent/TestSettings.kt
@src/main/kotlin/com/six2dez/burp/aiagent/scanner/ActiveAiScanner.kt
@src/main/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScanModels.kt

<interfaces>
<!-- Scanner method under audit. Verified from ActiveAiScanner.kt:204-255 -->

fun ActiveAiScanner.manualScanInsertionPoint(
    request: HttpRequestResponse,
    insertionPoint: InjectionPoint,
    vulnClasses: List<VulnClass>,
): Int  // returns number of targets queued

<!-- Public mutable fields the new tests mutate after construction -->
var ActiveAiScanner.scopeOnly: Boolean        // production default true; newScannerForQueueTests sets false
var ActiveAiScanner.maxQueueSize: Int         // newScannerForQueueTests sets 64
var ActiveAiScanner.scanMode: ScanMode        // newScannerForQueueTests sets FULL

<!-- Queue snapshot API (verified ActiveAiScanner.kt:271-288). NOTE: no priority field. -->
fun ActiveAiScanner.getQueueItems(limit: Int = 500): List<ActiveScanQueueItem>

data class ActiveScanQueueItem(
    val id: String,
    val url: String,
    val vulnClass: String,           // e.g. "SQLI"
    val injectionPoint: String,      // "${type}:${name}" e.g. "URL_PARAM:id"
    val status: String,              // always "QUEUED" in snapshot
    val queuedAtEpochMs: Long,
)

<!-- InjectionPoint constructor (verified ActiveScanModels.kt:279-284) -->
data class InjectionPoint(
    val type: InjectionType,
    val name: String,
    val originalValue: String,
    val position: Int? = null,
)

<!-- Existing builders to REUSE (lines 61-95 of the test file). DO NOT redefine. -->
private fun newScannerForQueueTests(): ActiveAiScanner  // builds with scopeOnly=false, maxQueueSize=64, scanMode=FULL
private fun requestResponse(url: String, name: String, value: String): HttpRequestResponse

<!-- ScanPolicy.PASSIVE_ONLY_VULN_CLASSES (verified ActiveScanModels.kt:110-126). 14 members. -->
val PASSIVE_ONLY_VULN_CLASSES = setOf(
    VulnClass.CORS_MISCONFIGURATION,       // canary for D-11 passive-only side
    VulnClass.MISSING_SECURITY_HEADERS,
    VulnClass.VERSION_DISCLOSURE,
    VulnClass.INSECURE_COOKIE,
    VulnClass.REQUEST_SMUGGLING,
    VulnClass.CSRF,
    VulnClass.UNRESTRICTED_FILE_UPLOAD,
    VulnClass.DESERIALIZATION,
    VulnClass.SUBDOMAIN_TAKEOVER,
    VulnClass.S3_MISCONFIGURATION,
    VulnClass.SOURCEMAP_DISCLOSURE,
    VulnClass.GIT_EXPOSURE,
    VulnClass.BACKUP_DISCLOSURE,
    VulnClass.DEBUG_EXPOSURE,
)
// SQLI is active-eligible canary (per RESEARCH.md Pitfall #6) and included in bugBountyClasses + pentestClasses + FULL.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add manualScanInsertionPointQueuesOnePerClassAtPriority60WithoutDedup @Test method (D-05 + D-12)</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerQueueModelTest.kt</files>
  <read_first>
    - src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerQueueModelTest.kt (FULL FILE — lines 23-42 are the analog `manualScanPopulatesQueueSnapshotAndRespectsLimit`; lines 61-95 are the builders to reuse)
    - .planning/phases/02-insertion-point-scan-audit/02-PATTERNS.md "Core pattern: queue-contract test" and the D-05 row of the per-test table
    - .planning/phases/02-insertion-point-scan-audit/02-RESEARCH.md Pitfall #4 (ActiveScanQueueItem does not surface priority — KDoc anchor in lieu of priority assertion) and Open Question #1 (recommendation: option (a) document, don't assert)
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/ActiveAiScanner.kt:204-255 (full `manualScanInsertionPoint` body; line 235 `priority = 60` hardcoded; line 237 `offerIfQueueNotFull` direct call — bypasses queueTarget's processedTargets dedup)
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/ActiveAiScanner.kt:271-288 (`getQueueItems` snapshot — confirms the snapshot fields available for assertion)
  </read_first>
  <action>
    Add a new `@Test fun manualScanInsertionPointQueuesOnePerClassAtPriority60WithoutDedup()` method that exercises the three invariants of D-05 (queue size = 3, vuln-class set matches request, dedup-bypass on re-invoke) in one dense method per D-05.

    Open the method with a one-block KDoc that anchors the priority assertion per Pitfall #4: `/** Priority 60 is hardcoded at ActiveAiScanner.kt:235; ActiveScanQueueItem does not surface priority, so the queueing-success assertions below prove the per-class loop fired. Reflection into the private scanQueue is forbidden by D-04; adding a priority field to ActiveScanQueueItem is out of audit scope. */`. This is the audit's record that D-05.2 is satisfied via documentation rather than a runtime field check.

    Body:
    - `val scanner = newScannerForQueueTests()`. The builder already sets `scopeOnly = false` (so the scope gate doesn't fire), `maxQueueSize = 64` (room for 6 queue entries across the two invocations), `scanMode = ScanMode.FULL` (so SQLI, XSS_REFLECTED, CMDI pass `ScanPolicy.isAllowedForMode` — see Pitfall #7).
    - `val rr = requestResponse("http://example.com/?id=1", "id", "1")`.
    - `val point = InjectionPoint(InjectionType.URL_PARAM, "id", "1")` (three positional args; `position` defaults to `null`).
    - Choose three active-eligible vuln classes: `val vulnClasses = listOf(VulnClass.SQLI, VulnClass.XSS_REFLECTED, VulnClass.CMDI)`. All three are absent from `PASSIVE_ONLY_VULN_CLASSES` (per the inventory at ActiveScanModels.kt:110-126) and present in the FULL set (line 141 of ActiveScanModels.kt — FULL allows everything).

    Invariant 1 — queue size after first invocation:
    - `val firstCount = scanner.manualScanInsertionPoint(rr, point, vulnClasses)`.
    - `assertEquals(3, firstCount)` (kotlin.test.assertEquals — already imported at line 20 of the file).
    - `val firstItems = scanner.getQueueItems(limit = 10)`.
    - `assertEquals(3, firstItems.size)`.

    Invariant 2 — per-item vuln-class set + injectionPoint stringification (this proves the per-class loop at ActiveAiScanner.kt:229 fired and built `ActiveScanTarget` with `vulnHint.vulnClass` = each requested class):
    - `assertEquals(setOf("SQLI", "XSS_REFLECTED", "CMDI"), firstItems.map { it.vulnClass }.toSet())` — the snapshot maps `target.vulnHint.vulnClass.name` (line 283 of ActiveAiScanner.kt).
    - `assertTrue(firstItems.all { it.injectionPoint == "URL_PARAM:id" })` (JUnit `Assertions.assertTrue` — already imported at line 15 of the file; format is `${target.injectionPoint.type}:${target.injectionPoint.name}` per ActiveAiScanner.kt:284-286).
    - `assertTrue(firstItems.all { it.status == "QUEUED" })` (status is hardcoded to QUEUED in the snapshot).

    Invariant 3 — dedup-bypass on re-invoke (D-12 folded in per CONTEXT.md):
    - `val secondCount = scanner.manualScanInsertionPoint(rr, point, vulnClasses)` (same rr, same point, same vulnClasses).
    - `assertEquals(3, secondCount)`.
    - `val totalItems = scanner.getQueueItems(limit = 10)`.
    - `assertEquals(6, totalItems.size)` — this is the substantive proof of dedup-bypass: if `manualScanInsertionPoint` shared the `processedTargets` window with `queueTarget`, the second call would queue 0 fresh entries and total size would still be 3. The KDoc at ActiveAiScanner.kt:210-213 explicitly notes this bypass; the test locks it.

    Constraints:
    - Match file assertion-library policy: `kotlin.test.assertEquals` for value equality; `org.junit.jupiter.api.Assertions.assertTrue` / `assertFalse` for booleans. Both are already imported (lines 14-15, 20). Do NOT add `org.junit.jupiter.api.Assertions.assertEquals` (would shadow the kotlin.test one and break the existing convention) and do NOT add `kotlin.test.assertTrue`.
    - Do NOT import or use `any` from `org.mockito.kotlin` — this test does not need it.
    - Do NOT create new helpers. `newScannerForQueueTests` and `requestResponse` already exist at lines 61-73 and 75-95; reuse verbatim.
    - Place the new method directly after `cancelQueuedTargetRemovesOnlyMatchingId` (line 59) so all four `manualScanInsertionPoint*` tests cluster at the top of the class, ahead of the private helpers.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.scanner.ActiveScannerQueueModelTest.manualScanInsertionPointQueuesOnePerClassAtPriority60WithoutDedup" -PexcludeHeavyTests=true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fun manualScanInsertionPointQueuesOnePerClassAtPriority60WithoutDedup' src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerQueueModelTest.kt` returns `1`
    - The new method invokes `scanner.manualScanInsertionPoint(...)` EXACTLY TWICE (dedup-bypass requires the re-invoke)
    - The new method asserts the queue size is `3` after the first call and `6` after the second
    - The new method asserts `setOf("SQLI", "XSS_REFLECTED", "CMDI")` matches `items.map { it.vulnClass }.toSet()` (per-class loop proof)
    - The new method's KDoc contains a reference to `ActiveAiScanner.kt:235` (the priority-60 line) — Pitfall #4 documentation requirement
    - The new method does NOT use reflection (`javaClass.getDeclaredField`, `isAccessible`, etc.) — D-04
    - The new method does NOT reference `priority` as a field on `ActiveScanQueueItem` (compile error if it did — the field does not exist)
    - `./gradlew test --tests "*ActiveScannerQueueModelTest.manualScanInsertionPointQueuesOnePerClassAtPriority60WithoutDedup" -PexcludeHeavyTests=true` exits 0
    - `./gradlew ktlintCheck` does not flag any new violation in this file
  </acceptance_criteria>
  <done>
    `manualScanInsertionPointQueuesOnePerClassAtPriority60WithoutDedup` exists and is green. The KDoc anchors priority=60 to `ActiveAiScanner.kt:235`. The test proves: (1) one queue entry per filtered vuln class, (2) vuln-class set matches request, (3) dedup-bypass on re-invoke (size doubles to 6).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add manualScanInsertionPointReturnsZeroAndDoesNotQueueWhenOutOfScope @Test method (D-10, threat T-2-01)</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerQueueModelTest.kt</files>
  <read_first>
    - src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerQueueModelTest.kt (Task 1 leaves it; lines 61-73 are the inline builder template for Pitfall #2)
    - .planning/phases/02-insertion-point-scan-audit/02-RESEARCH.md Pitfall #2 (mocking `api.scope().isInScope(url)` — defensively explicit) and Pitfall #3 (`newScannerForQueueTests` sets `scopeOnly = false`; D-10 MUST flip it back to true)
    - .planning/phases/02-insertion-point-scan-audit/02-PATTERNS.md D-10 row of the per-test table; section "Imports pattern" — note `import org.mockito.kotlin.any` is NOT currently in the file and must be added for the explicit scope stub
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/ActiveAiScanner.kt:225 (the scope gate: `if (scopeOnly && !api.scope().isInScope(request.request().url())) { return 0 }`) — the gate is upstream of the per-class loop, so a scope miss leaves the queue untouched
    - src/test/kotlin/com/six2dez/burp/aiagent/TestSettings.kt (verify `TestSettings.baselineSettings()` exists — used by the inline builder if Task 2 chooses inline construction)
  </read_first>
  <action>
    Add a new `@Test fun manualScanInsertionPointReturnsZeroAndDoesNotQueueWhenOutOfScope()` method that locks the scope-predicate gate (threat T-2-01, INSP-03 boundary). Choose the **inline-construction** path per RESEARCH.md Pitfall #2 recommendation: it's more readable than calling `newScannerForQueueTests()` and then mutating `scopeOnly` back to true, and it makes the explicit `isInScope` stub local to the test that needs it.

    Add the import that the inline builder needs (top of file, after line 17 `import org.mockito.kotlin.mock`): `import org.mockito.kotlin.any`. Place it alphabetically (Mockito-Kotlin imports already sit between line 17 and line 19; `any` slots in just before `mock` lexically — exact placement is left to ktlint's reorderer, just make sure the import is present).

    Body of the new test:
    - `val api = mock<MontoyaApi>(defaultAnswer = Answers.RETURNS_DEEP_STUBS)` — same deep-stub pattern as `newScannerForQueueTests`.
    - `whenever(api.scope().isInScope(any<String>())).thenReturn(false)` — explicit defensive stub per Pitfall #2. The chain `api.scope()` returns a deep-stubbed `Scope`; `isInScope(String)` is the production overload called at ActiveAiScanner.kt:225.
    - `val scanner = ActiveAiScanner(api = api, supervisor = mock<AgentSupervisor>(), audit = mock<AuditLogger>(), getSettings = { baselineSettings() }).apply { scopeOnly = true; maxQueueSize = 64; scanMode = ScanMode.FULL }`. The `apply` block mirrors `newScannerForQueueTests` BUT flips `scopeOnly` to `true` (Pitfall #3 — the helper sets it to `false` which would bypass the gate). Use the same `baselineSettings()` reference (already defined as a private function in this file at lines 97-181 — do NOT re-import from TestSettings; the file has its own copy).
    - `val rr = requestResponse("http://out-of-scope.example.com/?id=1", "id", "1")` (any URL works because `isInScope` is mocked to false unconditionally; the out-of-scope hostname is purely cosmetic for test readability).
    - `val point = InjectionPoint(InjectionType.URL_PARAM, "id", "1")`.
    - `val count = scanner.manualScanInsertionPoint(rr, point, listOf(VulnClass.SQLI))`.
    - Assert exactly two things:
      - `assertEquals(0, count)` — the return value at ActiveAiScanner.kt:226 short-circuits the function before the per-class loop runs.
      - `assertTrue(scanner.getQueueItems(limit = 10).isEmpty())` — no `offerIfQueueNotFull` calls fired, so the queue stays empty.

    KDoc anchor: `/** Locks the out-of-scope short-circuit at ActiveAiScanner.kt:225. Threat model T-2-01 (out-of-scope target leakage / information disclosure) — mitigated by scopeOnly + api.scope().isInScope() gate. */`

    Constraints:
    - Do NOT call `newScannerForQueueTests()` here — Pitfall #3 says inline construction is preferred for the scope test. The shared builder forces a manual `scanner.scopeOnly = true` after construction which is functionally equivalent but less readable.
    - Match assertion-library policy (kotlin.test.assertEquals + JUnit Assertions.assertTrue).
    - Do NOT add any new helper. The inline construction is intentional and one-time.

    Place the new method immediately after `manualScanInsertionPointQueuesOnePerClassAtPriority60WithoutDedup` (Task 1's output).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.scanner.ActiveScannerQueueModelTest.manualScanInsertionPointReturnsZeroAndDoesNotQueueWhenOutOfScope" -PexcludeHeavyTests=true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fun manualScanInsertionPointReturnsZeroAndDoesNotQueueWhenOutOfScope' src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerQueueModelTest.kt` returns `1`
    - The new method contains `scopeOnly = true` (NOT relying on `newScannerForQueueTests`'s `false` default — Pitfall #3)
    - The new method contains `whenever(api.scope().isInScope(any<String>())).thenReturn(false)` — explicit defensive stub (Pitfall #2)
    - `grep -c 'import org.mockito.kotlin.any' src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerQueueModelTest.kt` returns `1` (the import was added)
    - The new method asserts `assertEquals(0, count)` AND `assertTrue(scanner.getQueueItems(limit = 10).isEmpty())` — both proofs of the short-circuit
    - The new method's KDoc references `ActiveAiScanner.kt:225` and threat ID `T-2-01`
    - `./gradlew test --tests "*ActiveScannerQueueModelTest.manualScanInsertionPointReturnsZeroAndDoesNotQueueWhenOutOfScope" -PexcludeHeavyTests=true` exits 0
    - `./gradlew ktlintCheck` does not flag any new violation in this file
  </acceptance_criteria>
  <done>
    `manualScanInsertionPointReturnsZeroAndDoesNotQueueWhenOutOfScope` exists, inline-constructs the scanner with `scopeOnly = true` and `isInScope` mocked to false, and asserts both `count == 0` and an empty queue. Threat T-2-01 (out-of-scope leakage) is locked by test.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add manualScanInsertionPointFiltersPassiveOnlyVulnClasses @Test method (D-11)</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerQueueModelTest.kt</files>
  <read_first>
    - src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerQueueModelTest.kt (after Task 2)
    - .planning/phases/02-insertion-point-scan-audit/02-RESEARCH.md Pitfall #6 (PASSIVE_ONLY_VULN_CLASSES membership — `CORS_MISCONFIGURATION` is the stable passive-only canary; `SQLI` is the stable active-eligible canary) and Pitfall #7 (ScanMode.FULL is required so SQLI is not filtered by `isAllowedForMode`)
    - .planning/phases/02-insertion-point-scan-audit/02-PATTERNS.md D-11 row of the per-test table
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/ActiveAiScanner.kt:220-223 (the filter pipeline: `vulnClasses.filterNot { it in ScanPolicy.PASSIVE_ONLY_VULN_CLASSES }.filter { ScanPolicy.isAllowedForMode(scanMode, it) }`)
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScanModels.kt:110-126 (the PASSIVE_ONLY set — `CORS_MISCONFIGURATION` listed first, stable canary) and line 141 (ScanMode.FULL allows all classes)
  </read_first>
  <action>
    Add a new `@Test fun manualScanInsertionPointFiltersPassiveOnlyVulnClasses()` method using the `newScannerForQueueTests()` builder unchanged (Pitfall #7: the builder sets `scanMode = ScanMode.FULL` which is required so SQLI passes the `isAllowedForMode` filter and the only filter that fires is the PASSIVE_ONLY one).

    Body:
    - `val scanner = newScannerForQueueTests()` (defaults: scopeOnly=false, maxQueueSize=64, scanMode=FULL — all correct for this test).
    - `val rr = requestResponse("http://example.com/?id=1", "id", "1")`.
    - `val point = InjectionPoint(InjectionType.URL_PARAM, "id", "1")`.
    - `val vulnClasses = listOf(VulnClass.CORS_MISCONFIGURATION, VulnClass.SQLI)`. Per Pitfall #6: `CORS_MISCONFIGURATION` is listed first in `PASSIVE_ONLY_VULN_CLASSES` at ActiveScanModels.kt:112 — it is the most-stable passive-only canary (least likely to be moved if the set is reordered later). `SQLI` is the universal active-eligible canary used by every other queue test.
    - `val count = scanner.manualScanInsertionPoint(rr, point, vulnClasses)`.
    - Assert: `assertEquals(1, count)` — the passive-only filter at ActiveAiScanner.kt:222 strips CORS_MISCONFIGURATION; SQLI survives both filters and is queued.
    - `val items = scanner.getQueueItems(limit = 10)`.
    - `assertEquals(1, items.size)`.
    - `assertEquals("SQLI", items.single().vulnClass)` — the surviving entry is SQLI. `items.single()` throws if the size is not 1 (defensive — equivalent to `items.first()` but with the size invariant baked in).

    KDoc anchor: `/** Locks the PASSIVE_ONLY_VULN_CLASSES filter on the manual-insertion-point path. Per RESEARCH.md Pitfall #6: CORS_MISCONFIGURATION is the stable passive-only canary (ActiveScanModels.kt:112) and SQLI is the stable active-eligible canary (used by every other queue test). Filter chain: ActiveAiScanner.kt:220-223. */`

    Constraints:
    - Do NOT pass any class from `PASSIVE_ONLY_VULN_CLASSES` other than `CORS_MISCONFIGURATION` for the passive-only side — picking an unstable member risks the test silently flipping if the set is reordered.
    - Do NOT change the scanner's scanMode from the default `FULL` — per Pitfall #7, BUG_BOUNTY and PENTEST modes also allow SQLI, but FULL is the simplest invariant to lock and matches the builder's default.
    - Match assertion-library policy.

    Place the new method directly after `manualScanInsertionPointReturnsZeroAndDoesNotQueueWhenOutOfScope` (Task 2's output).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.scanner.ActiveScannerQueueModelTest.manualScanInsertionPointFiltersPassiveOnlyVulnClasses" -PexcludeHeavyTests=true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fun manualScanInsertionPointFiltersPassiveOnlyVulnClasses' src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerQueueModelTest.kt` returns `1`
    - The new method's vulnClasses list contains both `VulnClass.CORS_MISCONFIGURATION` (passive-only canary) and `VulnClass.SQLI` (active-eligible canary)
    - The new method asserts `assertEquals(1, count)` AND `assertEquals("SQLI", items.single().vulnClass)` (proves CORS_MISCONFIGURATION was filtered, SQLI survived)
    - The new method's KDoc references `ActiveScanModels.kt:112` (CORS_MISCONFIGURATION position) and `ActiveAiScanner.kt:220-223` (the filter chain)
    - `./gradlew test --tests "*ActiveScannerQueueModelTest.manualScanInsertionPointFiltersPassiveOnlyVulnClasses" -PexcludeHeavyTests=true` exits 0
    - `./gradlew ktlintCheck` does not flag any new violation in this file
  </acceptance_criteria>
  <done>
    `manualScanInsertionPointFiltersPassiveOnlyVulnClasses` exists and is green. The PASSIVE_ONLY filter on the manual-insertion-point path is locked: a mix of one passive-only and one active-eligible class queues exactly one entry, and that entry is the active-eligible class.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add manualScanInsertionPointReturnsShortCountWhenQueueFull @Test method (D-13, threat T-2-02)</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerQueueModelTest.kt</files>
  <read_first>
    - src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerQueueModelTest.kt (after Task 3)
    - .planning/phases/02-insertion-point-scan-audit/02-RESEARCH.md Pitfall #6 (5 active-eligible classes for the saturate-the-queue setup), Pitfall #7 (ScanMode.FULL allows all), Open Question #2 (recommended `assertEquals(2, count)` strict — single-threaded, deterministic)
    - .planning/phases/02-insertion-point-scan-audit/02-PATTERNS.md "Fast-suite placement" callout — D-13 goes in `ActiveScannerQueueModelTest.kt` NOT `ScannerQueueBackpressureTest.kt` (the latter has the heavy `*BackpressureTest` suffix and is excluded by `-PexcludeHeavyTests=true`)
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/ActiveAiScanner.kt:229-240 (per-class loop) and lines 257-264 (`offerIfQueueNotFull` short-circuit: `if (scanQueue.size >= maxQueueSize.coerceAtLeast(1)) ... return false`)
  </read_first>
  <action>
    Add a new `@Test fun manualScanInsertionPointReturnsShortCountWhenQueueFull()` method that locks the queue-saturation short-count return (threat T-2-02, DoS mitigation). Per the "Fast-suite placement" rule in PATTERNS.md, this test goes in the FAST-suite file `ActiveScannerQueueModelTest.kt` despite the conceptual overlap with the heavy `ScannerQueueBackpressureTest.kt`. The audit's PR gate must cover the new contract.

    Body:
    - `val scanner = newScannerForQueueTests().apply { maxQueueSize = 2 }`. The `apply` block uses the public mutable `maxQueueSize` setter (the field is `var` on ActiveAiScanner) — same pattern as the builder's own `apply` at lines 68-72. After this mutation: `scopeOnly=false`, `maxQueueSize=2`, `scanMode=FULL`.
    - `val rr = requestResponse("http://example.com/?id=1", "id", "1")`.
    - `val point = InjectionPoint(InjectionType.URL_PARAM, "id", "1")`.
    - Pick 5 distinct active-eligible vuln classes (per Pitfall #6 + Pitfall #7): `val vulnClasses = listOf(VulnClass.SQLI, VulnClass.XSS_REFLECTED, VulnClass.CMDI, VulnClass.SSTI, VulnClass.XXE)`. All 5 are in `bugBountyClasses()` (ActiveScanModels.kt:149-175) and in `pentestClasses()` (line 177 — anything not in PASSIVE_ONLY), and ScanMode.FULL allows everything. So `filteredClasses` at ActiveAiScanner.kt:220-223 retains all 5; the per-class loop attempts 5 offers; `offerIfQueueNotFull` accepts the first 2 (queue size goes 0→1→2) then rejects the remaining 3 (line 258 `scanQueue.size >= maxQueueSize` short-circuits).
    - `val count = scanner.manualScanInsertionPoint(rr, point, vulnClasses)`.
    - Per Open Question #2, choose the strict assertion (single-threaded, deterministic): `assertEquals(2, count)`.
    - `assertEquals(2, scanner.getQueueItems(limit = 10).size)` — queue snapshot confirms exactly 2 items survived.

    KDoc anchor: `/** Locks the queue-saturation short-count return. Threat model T-2-02 (queue-saturation DoS / denial of service) — mitigated by maxQueueSize-bounded offerIfQueueNotFull at ActiveAiScanner.kt:257-264. Test lives in the fast suite (PR gate), NOT in ScannerQueueBackpressureTest.kt (heavy suite, excluded by -PexcludeHeavyTests=true) per PATTERNS.md "Fast-suite placement". */`

    Constraints:
    - Do NOT create a new test file with a `*BackpressureTest` or `*ConcurrencyTest` suffix — those are heavy-suite suffixes excluded from the PR gate.
    - Do NOT pick a vuln class from `PASSIVE_ONLY_VULN_CLASSES` for any of the 5 — the passive-only filter would drop it before the queue limit fires, making `count < 5` for the wrong reason (filter, not queue limit).
    - All 5 vuln classes must be present in `pentestClasses()` AND `bugBountyClasses()` for the test to be stable across mode changes (ScanMode.FULL allows everything, but defending against accidental mode drift in the test fixture is cheap). SQLI/XSS_REFLECTED/CMDI/SSTI/XXE all satisfy both.
    - Match assertion-library policy.

    Place the new method directly after `manualScanInsertionPointFiltersPassiveOnlyVulnClasses` (Task 3's output) so all four `manualScanInsertionPoint*` tests cluster.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.six2dez.burp.aiagent.scanner.ActiveScannerQueueModelTest.manualScanInsertionPointReturnsShortCountWhenQueueFull" -PexcludeHeavyTests=true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fun manualScanInsertionPointReturnsShortCountWhenQueueFull' src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerQueueModelTest.kt` returns `1`
    - The new method contains `maxQueueSize = 2` (the saturation trigger)
    - The new method's vulnClasses list has exactly 5 distinct active-eligible classes (size = 5)
    - The new method asserts `assertEquals(2, count)` AND `assertEquals(2, scanner.getQueueItems(limit = 10).size)`
    - The new method's KDoc references `ActiveAiScanner.kt:257-264` (offerIfQueueNotFull location) and threat ID `T-2-02`
    - No new test file was created with `*BackpressureTest`, `*ConcurrencyTest`, `*IntegrationTest`, or `*RestartPolicyTest` suffix — the entire change is to `ActiveScannerQueueModelTest.kt`
    - `./gradlew test --tests "*ActiveScannerQueueModelTest.manualScanInsertionPointReturnsShortCountWhenQueueFull" -PexcludeHeavyTests=true` exits 0
    - Running the full file `./gradlew test --tests "com.six2dez.burp.aiagent.scanner.ActiveScannerQueueModelTest" -PexcludeHeavyTests=true` exits 0 (all 4 new + 2 pre-existing tests pass)
    - `./gradlew ktlintCheck` does not flag any new violation in this file
  </acceptance_criteria>
  <done>
    `manualScanInsertionPointReturnsShortCountWhenQueueFull` exists and is green. With `maxQueueSize = 2` and 5 active-eligible vuln classes requested, the test asserts `count == 2` and the queue snapshot contains exactly 2 items. Threat T-2-02 (queue-saturation DoS) is locked. All 6 tests in `ActiveScannerQueueModelTest.kt` pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Burp scope ↔ scanner queue | The `api.scope().isInScope(url)` predicate is the contract between Burp's user-configured scope and the scanner's willingness to enqueue work on a URL. Plan 02 Task 2 locks the boundary control. |
| Caller ↔ scanner queue capacity | `maxQueueSize` is the upper bound that protects scanner resources from unbounded enqueueing. Plan 02 Task 4 locks the boundary control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | I (Information Disclosure) | `ActiveAiScanner.manualScanInsertionPoint` scope gate at `ActiveAiScanner.kt:225` | mitigate | Existing `scopeOnly && !api.scope().isInScope(url)` gate is locked by Task 2 (`manualScanInsertionPointReturnsZeroAndDoesNotQueueWhenOutOfScope`): inline-construct scanner with `scopeOnly = true`, mock `isInScope` to `false`, assert `count == 0` and queue empty. Threat: leaking out-of-scope targets to the AI active-scan executor would dispatch authored payloads against domains the user did not consent to scan. |
| T-2-02 | D (Denial of Service) | `ActiveAiScanner.manualScanInsertionPoint` + `offerIfQueueNotFull` at `ActiveAiScanner.kt:257-264` | mitigate | Existing `scanQueue.size >= maxQueueSize` short-circuit is locked by Task 4 (`manualScanInsertionPointReturnsShortCountWhenQueueFull`): set `maxQueueSize = 2`, request 5 classes, assert `count == 2`. Threat: an unbounded queue would let a malicious or buggy caller exhaust memory and starve other Burp tooling. |
</threat_model>

<verification>
After all 4 tasks complete, run the queue test class in isolation to prove no regressions:

```
./gradlew test --tests "com.six2dez.burp.aiagent.scanner.ActiveScannerQueueModelTest" -PexcludeHeavyTests=true
```

Then run the full fast suite to confirm Plan 02 did not break anything elsewhere:

```
./gradlew test -PexcludeHeavyTests=true
```

Both must exit 0. ktlintCheck must remain clean on `ActiveScannerQueueModelTest.kt`. Confirm no new file was created under `src/test/kotlin/.../scanner/` (the entire plan is single-file edits).
</verification>

<success_criteria>
- 4 new `@Test` methods exist in `src/test/kotlin/com/six2dez/burp/aiagent/scanner/ActiveScannerQueueModelTest.kt`: `manualScanInsertionPointQueuesOnePerClassAtPriority60WithoutDedup`, `manualScanInsertionPointReturnsZeroAndDoesNotQueueWhenOutOfScope`, `manualScanInsertionPointFiltersPassiveOnlyVulnClasses`, `manualScanInsertionPointReturnsShortCountWhenQueueFull`.
- All 6 `@Test` methods in `ActiveScannerQueueModelTest.kt` pass: `./gradlew test --tests "*ActiveScannerQueueModelTest" -PexcludeHeavyTests=true` exits 0.
- One new import (`org.mockito.kotlin.any`) added for Task 2's inline scope stub; no other dependency changes.
- No reflection (`javaClass.getDeclaredField`, `isAccessible`) anywhere — D-04.
- No new test file. No `*BackpressureTest`/`*ConcurrencyTest`/`*IntegrationTest`/`*RestartPolicyTest` suffix introduced — the new tests must run in the fast PR-gate suite.
- ktlint check on the modified file remains clean.
- Threat T-2-01 (out-of-scope leakage) and T-2-02 (queue-saturation DoS) are both locked by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-insertion-point-scan-audit/02-02-SUMMARY.md` documenting which 4 methods landed, the import added for Task 2, and the test run output.
</output>
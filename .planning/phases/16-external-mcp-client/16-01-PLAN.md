---
phase: 16-external-mcp-client
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle.kts
  - src/test/kotlin/com/six2dez/burp/aiagent/mcp/external/ExternalMcpClientManagerTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/config/ExternalMcpSettingsMigrationTest.kt
autonomous: true
requirements:
  - CAP-02

must_haves:
  truths:
    - "`./gradlew compileKotlin --no-daemon` exits 0 with the three new dep lines present"
    - "Wave 0 test scaffolds exist as compilable (but initially failing) test files"
    - "`build.gradle.kts` contains `io.ktor:ktor-client-core:3.1.3`, `io.ktor:ktor-client-cio:3.1.3`, and `io.github.oshai:kotlin-logging-jvm:7.0.7`"
    - "`io.modelcontextprotocol:kotlin-sdk` remains pinned at `0.5.0` — no version change"
  artifacts:
    - path: "build.gradle.kts"
      provides: "Pinned Ktor client + kotlin-logging deps"
      contains: "ktor-client-core:3.1.3"
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/mcp/external/ExternalMcpClientManagerTest.kt"
      provides: "Wave 0 test scaffold for lifecycle, trust-boundary, ext: routing"
      min_lines: 40
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/config/ExternalMcpSettingsMigrationTest.kt"
      provides: "Wave 0 test scaffold for schema-v5 migration round-trip"
      min_lines: 30
  key_links:
    - from: "build.gradle.kts"
      to: "io.ktor:ktor-client-cio:3.1.3"
      via: "implementation() declaration"
      pattern: "ktor-client-cio:3\\.1\\.3"
---

<objective>
Add the three new explicit dependency declarations required for the MCP client (per D-SSE-routing and Path A verdict), then create the two Wave 0 test scaffolds that subsequent plans depend on.

Purpose: The compile spike already proved these deps work; this plan performs the fast confirmation (`compileKotlin`) and establishes the Nyquist-required test stubs so Wave 2 plans can reference them. No SDK version bump — only explicit pinning of already-transitive packages.

Output:
- `build.gradle.kts` gains 3 lines under the existing MCP/Ktor block
- Two new skeleton test files with correct package declarations and test class shapes (tests will initially fail until implementation plans complete — that is expected)
</objective>

<execution_context>
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/16-external-mcp-client/16-RESEARCH.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/16-external-mcp-client/16-PATTERNS.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/16-external-mcp-client/16-VALIDATION.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/six2dez/Tools/burp-ai-agent/.planning/PROJECT.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/ROADMAP.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/STATE.md
@/Users/six2dez/Tools/burp-ai-agent/build.gradle.kts
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/16-external-mcp-client/16-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add three Ktor client + kotlin-logging dep declarations to build.gradle.kts</name>
  <files>build.gradle.kts</files>
  <read_first>
    - `build.gradle.kts` lines 34-44 (existing MCP/Ktor server block — insert immediately after line 44)
    - `16-PATTERNS.md` section "`build.gradle.kts` (config — MODIFY)" — exact three lines to add
    - `16-RESEARCH.md` "Exact dependency lines for Path A" section
    - `16-CONTEXT.md` `<specifics>` block — hard constraint: do NOT bump `kotlin-sdk`, do NOT bump Kotlin plugin
  </read_first>
  <action>
    In `build.gradle.kts`, immediately after the line `implementation("io.ktor:ktor-serialization-kotlinx-json:3.1.3")` (the last line of the existing Ktor server block), add the following three `implementation()` lines with the accompanying comment, in this exact order:

    1. A comment line: `// Phase 16: Ktor CLIENT modules (pin to 3.1.3 to match server-side Ktor family)`
    2. `implementation("io.ktor:ktor-client-core:3.1.3")`
    3. `implementation("io.ktor:ktor-client-cio:3.1.3")`
    4. A second comment line: `// kotlin-logging: transitive via kotlin-sdk:0.5.0 StdioClientTransport; declared explicitly to pin version`
    5. `implementation("io.github.oshai:kotlin-logging-jvm:7.0.7")`

    Do NOT touch `io.modelcontextprotocol:kotlin-sdk` — it stays at `0.5.0`. Do NOT add any new `resolutionStrategy.force()` calls. Do NOT bump any Ktor version already present. The goal is three new lines only.

    After editing, run `./gradlew compileKotlin --no-daemon` and confirm `BUILD SUCCESSFUL`. Also run `./gradlew dependencies --configuration runtimeClasspath --no-daemon | grep ktor-client-cio` and confirm `3.1.3` is selected (not `3.0.2`).
  </action>
  <verify>
    <automated>
      ./gradlew compileKotlin --no-daemon
      grep -c "ktor-client-cio:3.1.3" /Users/six2dez/Tools/burp-ai-agent/build.gradle.kts
      grep -c "ktor-client-core:3.1.3" /Users/six2dez/Tools/burp-ai-agent/build.gradle.kts
      grep -c "kotlin-logging-jvm:7.0.7" /Users/six2dez/Tools/burp-ai-agent/build.gradle.kts
    </automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew compileKotlin --no-daemon` exits 0 (BUILD SUCCESSFUL)
    - `build.gradle.kts` contains exactly the string `io.ktor:ktor-client-cio:3.1.3` (grep exits 0)
    - `build.gradle.kts` contains exactly the string `io.ktor:ktor-client-core:3.1.3`
    - `build.gradle.kts` contains exactly the string `io.github.oshai:kotlin-logging-jvm:7.0.7`
    - `build.gradle.kts` still contains `io.modelcontextprotocol:kotlin-sdk:0.5.0` (unchanged)
    - No Kotlin plugin version reference in `build.gradle.kts` is modified (still `2.1.21`)
  </acceptance_criteria>
  <done>Three new dep lines in build.gradle.kts; `compileKotlin` succeeds; SDK stays at 0.5.0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Wave 0 test scaffolds — ExternalMcpClientManagerTest + ExternalMcpSettingsMigrationTest</name>
  <files>
    src/test/kotlin/com/six2dez/burp/aiagent/mcp/external/ExternalMcpClientManagerTest.kt
    src/test/kotlin/com/six2dez/burp/aiagent/config/ExternalMcpSettingsMigrationTest.kt
  </files>
  <read_first>
    - `16-PATTERNS.md` section "`mcp/external/ExternalMcpClientManagerTest.kt` (test — CREATE)" — imports, mock setup, CountDownLatch async assertions pattern
    - `16-PATTERNS.md` section "`config/ExternalMcpSettingsMigrationTest.kt` (test — CREATE)" — InMemoryPrefs pattern, round-trip test shape, ENC1: prefix assertion, schema version assertion
    - `16-VALIDATION.md` "Wave 0 Requirements" section — lists exact behaviors each test must eventually cover (lifecycle, trust-boundary, ext: routing, encrypt round-trip, idempotency, schema-v5)
    - `src/test/kotlin/com/six2dez/burp/aiagent/mcp/McpSupervisorRestartPolicyTest.kt` — analog: mock setup, CountDownLatch pattern (read lines 1-40)
    - `src/test/kotlin/com/six2dez/burp/aiagent/config/AgentSettingsMigrationTest.kt` — analog: InMemoryPrefs class, apiWith() helper, round-trip test shape (read lines 1-50 and lines 205-240)
  </read_first>
  <action>
    Create two new test files. Both should compile cleanly but tests that reference not-yet-existing production classes may be annotated `@Disabled("Wave 0 stub — implementation in plan 02/03")` so the test suite does not fail compilation.

    File 1: `src/test/kotlin/com/six2dez/burp/aiagent/mcp/external/ExternalMcpClientManagerTest.kt`
    - Package: `com.six2dez.burp.aiagent.mcp.external`
    - Class: `ExternalMcpClientManagerTest`
    - Include skeleton `@Test` methods (with `@Disabled` until Plan 03 implementation) for:
      - `trustBoundaryWrap_addsCorrectMarkers` — asserts raw result is wrapped with `[EXTERNAL-TOOL-RESULT:serverName]...[/EXTERNAL-TOOL-RESULT]`
      - `extPrefixedToolName_routesToCorrectServer` — verifies `ext:<server>:<tool>` naming after `availableTools()` returns
      - `stop_destroysStdioProcess` — verifies process is destroyed on shutdown
      - `connectAndListTools_returnsExpectedCount` — integration-style; `@Disabled` with note "requires mock MCP server"
    - Imports from PATTERNS.md analog: `org.junit.jupiter.api.Assertions.*`, `org.junit.jupiter.api.Test`, `org.junit.jupiter.api.Disabled`, `org.mockito.Answers`, `org.mockito.kotlin.mock`

    File 2: `src/test/kotlin/com/six2dez/burp/aiagent/config/ExternalMcpSettingsMigrationTest.kt`
    - Package: `com.six2dez.burp.aiagent.config`
    - Class: `ExternalMcpSettingsMigrationTest`
    - Include a private `InMemoryPrefs` class mirroring the analog in `AgentSettingsMigrationTest.kt` (strings/booleans/integers maps + Mockito stubs), and a private `apiWith()` helper
    - Include skeleton `@Test` methods (with `@Disabled` until Plan 02 implementation) for:
      - `externalMcpServers_roundTripsThroughSaveLoad` — save a list with one SSE config, reload, assert name matches
      - `externalServerBlob_isStoredEncrypted` — assert stored pref string starts with `ENC1:`
      - `schemaVersion_bumpedToFive` — assert `settings.schema.version` pref = 5
      - `migrationIsIdempotent_doubleLoadDoesNotDoubleEncrypt` — load twice, blob prefix still `ENC1:` exactly once at start

    Both files must compile after Task 1 completes. References to not-yet-existing production classes (`ExternalMcpClientManager`, `ExternalMcpServerConfig`, `AgentSettingsRepository`) should be commented out or wrapped in `@Disabled` stubs. Run `./gradlew test -PexcludeHeavyTests=true --no-daemon` and confirm the test classes compile and the `@Disabled` tests show as skipped (not failed).

    Run `./gradlew ktlintFormat --no-daemon` on both new files immediately after creating them (Phase 18 STRICT gate).
  </action>
  <verify>
    <automated>./gradlew test -PexcludeHeavyTests=true --no-daemon 2>&1 | grep -E "BUILD|SKIPPED|ExternalMcp"</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist at the specified paths
    - `./gradlew test -PexcludeHeavyTests=true --no-daemon` exits 0 (BUILD SUCCESSFUL — all tests pass or are explicitly `@Disabled`)
    - No compilation errors referencing missing production classes
    - `ExternalMcpClientManagerTest` contains at least the 4 stub test methods listed above
    - `ExternalMcpSettingsMigrationTest` contains `InMemoryPrefs` inner class and at least the 4 stub test methods listed above
    - `./gradlew ktlintCheck --no-daemon` exits 0 for the new files (no style violations)
  </acceptance_criteria>
  <done>Two Wave 0 test scaffold files compile and pass as @Disabled stubs; full test suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build.gradle.kts → Maven Central | New dep declarations will pull artifacts from Maven Central at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-SC | Tampering | npm/pip/cargo/maven installs | mitigate | All 3 packages are JetBrains/oshai — major OSS projects; approved in RESEARCH.md Package Legitimacy Audit; pinned at exact versions (`3.1.3`, `7.0.7`); CycloneDX SBOM auto-captures on `./gradlew cyclonedxBom` |
| T-16-01-SDK | Tampering | kotlin-sdk version bump | mitigate | Hard constraint enforced: `io.modelcontextprotocol:kotlin-sdk` MUST stay at `0.5.0`; acceptance criterion verifies unchanged SDK pin; any bump triggers Kotlin 2.2+ compat failures caught by `compileKotlin` gate |

No HIGH severity threats in this plan — this plan only modifies `build.gradle.kts` (dep declarations) and creates test stubs. No production execution path changes.
</threat_model>

<verification>
- `./gradlew compileKotlin --no-daemon` → exits 0
- `./gradlew test -PexcludeHeavyTests=true --no-daemon` → exits 0 (stubs are @Disabled)
- `./gradlew ktlintCheck --no-daemon` → exits 0
- `grep "kotlin-sdk:0.5.0" build.gradle.kts` → matches (SDK unchanged)
- `grep "ktor-client-cio:3.1.3" build.gradle.kts` → matches
</verification>

<success_criteria>
1. `./gradlew compileKotlin --no-daemon` exits 0 with the 3 new dep lines
2. Both Wave 0 test scaffold files compile and appear as SKIPPED (not FAILED) in the test report
3. `kotlin-sdk` version in `build.gradle.kts` is still `0.5.0`
4. `./gradlew ktlintCheck --no-daemon` exits 0 for new test files
</success_criteria>

<output>
Create `.planning/phases/16-external-mcp-client/16-01-SUMMARY.md` when done.
</output>

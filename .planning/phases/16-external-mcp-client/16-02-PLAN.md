---
phase: 16-external-mcp-client
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/external/ExternalMcpServerConfig.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/config/McpSettings.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/config/AgentSettings.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/config/ExternalMcpSettingsMigrationTest.kt
autonomous: true
requirements:
  - CAP-02

must_haves:
  truths:
    - "ExternalMcpServerConfig data class exists with name/transport/url/command/envVars/encryptedToken/enabled fields"
    - "McpSettings gains `externalMcpServers: List<ExternalMcpServerConfig> = emptyList()` field without breaking existing serialization"
    - "`CURRENT_SETTINGS_SCHEMA_VERSION` is bumped to 5 in the same commit as the `if (effectiveVersion < 5)` migration branch"
    - "Bearer token blob is stored under key `mcp.external.servers.v1` encrypted with `ENC1:` prefix"
    - "ExternalMcpSettingsMigrationTest passes: round-trip, ENC1: prefix, schema-v5 version assertion, idempotency"
  artifacts:
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/mcp/external/ExternalMcpServerConfig.kt"
      provides: "Data model for external MCP server registration"
      exports: ["ExternalMcpServerConfig", "ExternalMcpTransport"]
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/config/McpSettings.kt"
      provides: "McpSettings data class with new externalMcpServers field"
      contains: "externalMcpServers"
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/config/AgentSettings.kt"
      provides: "Schema v5 migration + encrypted blob load/save"
      contains: "CURRENT_SETTINGS_SCHEMA_VERSION = 5"
  key_links:
    - from: "AgentSettings.kt"
      to: "SecretCipher"
      via: "cipher.encrypt / cipher.decrypt in loadExternalMcpServers / saveExternalMcpServers"
      pattern: "cipher\\.encrypt.*KEY_EXT_MCP_SERVERS"
    - from: "AgentSettings.kt"
      to: "McpSettings.externalMcpServers"
      via: "loadExternalMcpServers() called in mcpSettings load path"
      pattern: "externalMcpServers"
---

<objective>
Define the `ExternalMcpServerConfig` data model and `ExternalMcpTransport` enum; extend `McpSettings` with the `externalMcpServers` list; bump `AgentSettings` to schema v5 with encrypted blob load/save; enable the Wave 0 migration test stubs.

Purpose: Establishes the persistence layer so Plan 03 (client manager) can read configs without circular dependencies. Encrypted at-rest storage follows the Phase 12 `SecretCipher` pattern exactly — no new crypto code.

Output:
- New file: `ExternalMcpServerConfig.kt` (data class + enum)
- Modified: `McpSettings.kt` (new field with default)
- Modified: `AgentSettings.kt` (schema v5 bump + migration + load/save helpers)
- Enabled: `ExternalMcpSettingsMigrationTest.kt` stubs from Plan 01 → full passing tests
</objective>

<execution_context>
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/16-external-mcp-client/16-RESEARCH.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/16-external-mcp-client/16-PATTERNS.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/16-external-mcp-client/16-CONTEXT.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/six2dez/Tools/burp-ai-agent/.planning/PROJECT.md
@/Users/six2dez/Tools/burp-ai-agent/.planning/STATE.md
@/Users/six2dez/Tools/burp-ai-agent/src/main/kotlin/com/six2dez/burp/aiagent/config/McpSettings.kt
@/Users/six2dez/Tools/burp-ai-agent/src/main/kotlin/com/six2dez/burp/aiagent/config/AgentSettings.kt
@/Users/six2dez/Tools/burp-ai-agent/.planning/phases/16-external-mcp-client/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ExternalMcpServerConfig.kt and extend McpSettings.kt</name>
  <files>
    src/main/kotlin/com/six2dez/burp/aiagent/mcp/external/ExternalMcpServerConfig.kt
    src/main/kotlin/com/six2dez/burp/aiagent/config/McpSettings.kt
  </files>
  <read_first>
    - `16-PATTERNS.md` section "`mcp/external/ExternalMcpServerConfig.kt` (model, CRUD)" — exact data class shape with all fields and defaults
    - `16-PATTERNS.md` section "`config/McpSettings.kt` (model, CRUD — MODIFY)" — the `scopeOnly` tail pattern; exact new field to append
    - `src/main/kotlin/com/six2dez/burp/aiagent/config/McpSettings.kt` lines 1-35 — current class structure and companion object
    - `16-RESEARCH.md` "Bearer Token Storage (SC4)" section — serialization approach (entire list as JSON blob encrypted via SecretCipher, not per-field)
  </read_first>
  <behavior>
    - `ExternalMcpTransport.SSE` and `ExternalMcpTransport.STDIO` enum values exist
    - `ExternalMcpServerConfig(name="test", transport=SSE)` constructs with defaults: `url=""`, `command=emptyList()`, `extraArgs=emptyList()`, `envVars=emptyMap()`, `encryptedToken=""`, `enabled=true`
    - `McpSettings` with no `externalMcpServers` argument still constructs (field has default `emptyList()`)
    - Existing `McpSettings` constructions with all previous arguments compile unchanged (new field must be last with a default)
  </behavior>
  <action>
    Create `src/main/kotlin/com/six2dez/burp/aiagent/mcp/external/ExternalMcpServerConfig.kt`:
    - Package: `com.six2dez.burp.aiagent.mcp.external`
    - Enum `ExternalMcpTransport { SSE, STDIO }` — uppercase values
    - Data class `ExternalMcpServerConfig` with fields (all have defaults except `name` and `transport`):
      - `name: String` — display name; also used as namespace key in `ext:<name>:<tool>` prefix
      - `transport: ExternalMcpTransport` — SSE or STDIO
      - `url: String = ""` — SSE endpoint URL; blank for stdio servers
      - `command: List<String> = emptyList()` — parsed command list for STDIO transport
      - `extraArgs: List<String> = emptyList()` — additional arguments appended to command
      - `envVars: Map<String, String> = emptyMap()` — environment variables for the subprocess
      - `encryptedToken: String = ""` — SSE bearer token, stored encrypted via SecretCipher (raw value never stored here)
      - `enabled: Boolean = true` — user can disable a server without removing it
    - No companion object needed in this file (serialization is handled in AgentSettings.kt)
    - The class is annotated with nothing special — plain Kotlin data class (Jackson serializes it automatically via the existing mapper in AgentSettings.kt)

    Modify `src/main/kotlin/com/six2dez/burp/aiagent/config/McpSettings.kt`:
    - Add import: `import com.six2dez.burp.aiagent.mcp.external.ExternalMcpServerConfig` at top of file
    - Append `val externalMcpServers: List<ExternalMcpServerConfig> = emptyList()` as the last field in the data class, after `val scopeOnly: Boolean = false`, following the exact pattern shown in `16-PATTERNS.md` (comment + field)
    - No other changes to McpSettings.kt

    Run `./gradlew ktlintFormat --no-daemon` on both files immediately after creating/editing them.
  </action>
  <verify>
    <automated>./gradlew compileKotlin --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - `ExternalMcpServerConfig.kt` file exists at `src/main/kotlin/com/six2dez/burp/aiagent/mcp/external/ExternalMcpServerConfig.kt`
    - `ExternalMcpTransport` enum has exactly two values: `SSE` and `STDIO`
    - `ExternalMcpServerConfig` has all 8 fields with correct types
    - `McpSettings.kt` contains `externalMcpServers: List<ExternalMcpServerConfig> = emptyList()`
    - `McpSettings.kt` compiles without errors — existing constructions (no new field) still work due to default value
    - `./gradlew compileKotlin --no-daemon` exits 0
    - `./gradlew ktlintCheck --no-daemon` exits 0 for both files
  </acceptance_criteria>
  <done>Data model files created/extended; compilation succeeds; no ktlint violations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schema v5 migration + encrypted blob load/save in AgentSettings.kt; enable migration tests</name>
  <files>
    src/main/kotlin/com/six2dez/burp/aiagent/config/AgentSettings.kt
    src/test/kotlin/com/six2dez/burp/aiagent/config/ExternalMcpSettingsMigrationTest.kt
  </files>
  <read_first>
    - `16-PATTERNS.md` section "`config/AgentSettings.kt` (config/persistence, CRUD — MODIFY)" — ALL patterns: CURRENT_SETTINGS_SCHEMA_VERSION bump, migration ladder extension, KEY_EXT_MCP_SERVERS constant, loadExternalMcpServers/saveExternalMcpServers helpers, encrypt/decrypt pattern
    - `AgentSettings.kt` lines 700-790 — migration ladder `migrateIfNeeded()` and `migrateToSchemaV4()` patterns (read these sections carefully)
    - `AgentSettings.kt` lines 940-945 — `CURRENT_SETTINGS_SCHEMA_VERSION` constant location
    - `AgentSettings.kt` lines 240-280 — `ollamaApiKey` encrypt/decrypt pattern (template for the blob encryption)
    - `16-PATTERNS.md` "ObjectMapper / JSON Serialization (for server list blob)" — Jackson + Kotlin module serialization of the list
    - `16-PATTERNS.md` "SecretCipher Encrypt/Decrypt" shared pattern — `cipher.encrypt(plaintext, KEY_EXT_MCP_SERVERS)` and `cipher.decrypt(stored, KEY_EXT_MCP_SERVERS)`
    - `16-PATTERNS.md` "`config/ExternalMcpSettingsMigrationTest.kt` (test, CRUD — CREATE)" — all test method bodies for the 4 migration tests
    - `src/test/kotlin/com/six2dez/burp/aiagent/config/AgentSettingsMigrationTest.kt` lines 1-100 — InMemoryPrefs class structure and apiWith() pattern
  </read_first>
  <behavior>
    - Saving an `AgentSettings` with `mcpSettings.externalMcpServers = listOf(ExternalMcpServerConfig("test", SSE))` persists a string under key `mcp.external.servers.v1` that starts with `ENC1:`
    - Loading that same preference string back produces `externalMcpServers` with one entry, name `"test"`, transport `SSE`
    - After load/save cycle, schema version pref (`settings.schema.version`) equals 5
    - Running load twice on an already-v5 store does not double-encrypt (idempotency)
    - Settings with no `mcp.external.servers.v1` key load successfully, returning `externalMcpServers = emptyList()`
  </behavior>
  <action>
    In `AgentSettings.kt` make the following changes (all in a single commit per the PATTERNS.md constraint that schema bump and migration branch must be atomic):

    1. Add private constant after the existing `KEY_MCP_STDIO` or similar key block:
       `private const val KEY_EXT_MCP_SERVERS = "mcp.external.servers.v1"`

    2. Bump `CURRENT_SETTINGS_SCHEMA_VERSION` from `4` to `5` (line 942 per PATTERNS.md).

    3. In `migrateIfNeeded()`, add the v5 branch immediately after the existing v4 block:
       - `if (effectiveVersion < 5) { /* v5: externalMcpServers encrypted blob — new key, empty default, no existing data to migrate */ effectiveVersion = 5 }`
       The comment is mandatory to satisfy Phase 18 exception-annotation requirements.

    4. Add private helper method `loadExternalMcpServers(): List<ExternalMcpServerConfig>`:
       - Reads `prefs.getString(KEY_EXT_MCP_SERVERS)`
       - If null or blank → return `emptyList()`
       - Decrypts with `cipher.decrypt(stored, KEY_EXT_MCP_SERVERS)` (returns `""` on failure per SecretCipher fail-soft)
       - Parses the decrypted JSON string via the existing `mapper.readValue(json, Array<ExternalMcpServerConfig>::class.java).toList()`
       - On any parse exception → log only the key name (never the value), return `emptyList()`
       - Use the same `mapper` instance (Jackson `JsonMapper` with Kotlin module) already present in the class

    5. Add private helper method `saveExternalMcpServers(configs: List<ExternalMcpServerConfig>)`:
       - Serializes list to JSON string via `mapper.writeValueAsString(configs)`
       - Encrypts with `cipher.encrypt(json, KEY_EXT_MCP_SERVERS)`
       - Stores via `prefs.setString(KEY_EXT_MCP_SERVERS, encryptedBlob)`
       - On serialization exception → log only the key name, do not write partial data

    6. In the existing `load()` method, wire `externalMcpServers = loadExternalMcpServers()` into the `McpSettings(...)` construction at the appropriate position (after `scopeOnly`).

    7. In the existing `save()` method, call `saveExternalMcpServers(settings.mcpSettings.externalMcpServers)` immediately after the other MCP pref writes.

    Then update `ExternalMcpSettingsMigrationTest.kt`: remove `@Disabled` from all four test methods and fill in their bodies using the `InMemoryPrefs` + `apiWith()` pattern from `16-PATTERNS.md`. Each test creates an `AgentSettingsRepository(apiWith(prefs.mock))`, saves settings with an `ExternalMcpServerConfig`, then loads, asserting the expected outcomes listed in the behavior section above.

    Run `./gradlew ktlintFormat --no-daemon` on both files after editing.
  </action>
  <verify>
    <automated>./gradlew test --tests "*.ExternalMcpSettingsMigrationTest" --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "*.ExternalMcpSettingsMigrationTest" --no-daemon` exits 0 — all 4 tests pass (not skipped)
    - `AgentSettings.kt` contains `private const val CURRENT_SETTINGS_SCHEMA_VERSION = 5`
    - `AgentSettings.kt` contains `if (effectiveVersion < 5)` migration branch
    - `AgentSettings.kt` contains `private const val KEY_EXT_MCP_SERVERS = "mcp.external.servers.v1"`
    - Bearer token blob stored under `KEY_EXT_MCP_SERVERS` starts with `ENC1:` (asserted by test `externalServerBlob_isStoredEncrypted`)
    - Schema version pref is 5 after first save (asserted by test `schemaVersion_bumpedToFive`)
    - Full test suite still green: `./gradlew test --no-daemon` exits 0
    - `./gradlew ktlintCheck --no-daemon` exits 0
    - `./gradlew detekt --no-daemon` exits 0 (no new violations beyond baseline)
  </acceptance_criteria>
  <done>Schema v5 live; encrypted external-server blob persists and round-trips; 4 migration tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Burp Preferences → AgentSettings | Serialized external server list (including token) crosses from JVM preferences storage into application memory; token must be encrypted in storage |
| AgentSettings → SecretCipher | Plaintext token must only exist transiently in memory during config reads; never written to prefs in plaintext |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-TL | Information disclosure | Bearer token in Burp Preferences | mitigate | `SecretCipher.encrypt(json, KEY_EXT_MCP_SERVERS)` applied before `prefs.setString()`; stored value always has `ENC1:` prefix; test `externalServerBlob_isStoredEncrypted` asserts this |
| T-16-02-LOG | Information disclosure | Error logging path in loadExternalMcpServers | mitigate | Log only `KEY_EXT_MCP_SERVERS` constant on exception (never the decrypted/raw token value) — matches Phase 12 SecretCipher pattern at line 70 |
| T-16-02-VER | Tampering | Schema migration ladder — v5 branch | mitigate | `CURRENT_SETTINGS_SCHEMA_VERSION = 5` and `if (effectiveVersion < 5)` block committed atomically; idempotency test prevents double-encrypt on re-run |
</threat_model>

<verification>
- `./gradlew test --tests "*.ExternalMcpSettingsMigrationTest" --no-daemon` → 4 tests PASS
- `./gradlew test --no-daemon` → full suite still green
- `./gradlew ktlintCheck --no-daemon` → 0 violations
- `./gradlew detekt --no-daemon` → 0 new violations beyond baseline
- `grep "CURRENT_SETTINGS_SCHEMA_VERSION = 5" src/main/kotlin/com/six2dez/burp/aiagent/config/AgentSettings.kt` → matches
- `grep "ENC1:" src/test/kotlin/com/six2dez/burp/aiagent/config/ExternalMcpSettingsMigrationTest.kt` → matches (test asserts the prefix)
</verification>

<success_criteria>
1. `ExternalMcpServerConfig` + `ExternalMcpTransport` exist and compile
2. `McpSettings.externalMcpServers` field added with default `emptyList()`
3. Schema v5 migration branch + `CURRENT_SETTINGS_SCHEMA_VERSION = 5` committed atomically
4. All 4 `ExternalMcpSettingsMigrationTest` tests pass (token encrypted, round-trip, idempotency, schema version)
5. Full test suite green; ktlint + detekt clean
</success_criteria>

<output>
Create `.planning/phases/16-external-mcp-client/16-02-SUMMARY.md` when done.
</output>

---
gsd_plan_version: 1.0
quick_id: 260714-dp1
title: BApp Store code-review quick-wins (findings 3, 4, 6, 8 + verify 5)
status: in_progress
date: 2026-07-14
branch: fix/bapp-store-code-review
mode: quick
---

# Quick Task 260714-dp1 — BApp Store code-review quick-wins

## Context

PortSwigger's automated reviewer (issue [PortSwigger/extension-portal#231](https://github.com/PortSwigger/extension-portal/issues/231#issuecomment-4878007095), 2026-07-03) flagged 8 findings at the code-review stage of the BApp Store submission. This quick task addresses the **4 low-risk findings + verifies 1**. The 3 heavier findings (#1 site-map defer, #2 bounty-prompt cache, #7 collaborator async polling) are a separate later batch and are **out of scope here**.

All locations were verified against the current tree before planning. Milestone v0.9.0 is complete/verified; this runs as a standalone quick task on branch `fix/bapp-store-code-review`.

## Tasks (one atomic commit each)

### Task 1 — Finding 4: migrate deprecated `ScanCheck` → `ActiveScanCheck`
- **Files:** `src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt`, `src/main/kotlin/com/six2dez/burp/aiagent/App.kt`
- **Action:** Implement `burp.api.montoya.scanner.scancheck.ActiveScanCheck` instead of the deprecated `burp.api.montoya.scanner.ScanCheck`. Montoya 2026.2 signature: `checkName()`, `doCheck(HttpRequestResponse, AuditInsertionPoint, Http)`, default `consolidateIssues`. Add `checkName() = "AI Active Security Analysis"`; rename `activeAudit(rr, ip)` → `doCheck(rr, ip, http)`; delete `passiveAudit`; keep `consolidateIssues`. In `App.kt`, replace `registerScanCheck(aiScanCheck)` with `registerActiveScanCheck(aiScanCheck, ScanCheckType.PER_INSERTION_POINT)`.
- **Verify:** `compileKotlin` passes; `App.kt` registration uses `registerActiveScanCheck`.
- **Done:** No reference to the deprecated `registerScanCheck` / `ScanCheck` remains for the active check.

### Task 2 — Finding 3: pace scanner throttle instead of full `Thread.sleep`
- **File:** `src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt` (inside `doCheck`)
- **Action:** Track the timestamp when the last request finished; before the next request sleep only `max(0, delayMs − elapsed)` rather than the full `activeAiRequestDelayMs` on top of the round-trip. Keep `doCheck` synchronous (Burp scanner contract).
- **Verify:** `compileKotlin` + existing scanner tests pass.
- **Done:** No unconditional `Thread.sleep(delay)` after each payload; pacing counts the round-trip.

### Task 3 — Finding 6: guard the scheduled health-monitor task
- **File:** `src/main/kotlin/com/six2dez/burp/aiagent/supervisor/AgentSupervisor.kt` (init block, `scheduleAtFixedRate`)
- **Action:** Wrap the `{ checkHealth() }` body in `try/catch (Throwable)` that logs, so an uncaught exception can't make `scheduleAtFixedRate` silently cancel the recurring monitor.
- **Verify:** `compileKotlin` + `AgentSupervisorRestartPolicyTest` pass.
- **Done:** Health monitor keeps firing regardless of a single `checkHealth` failure.

### Task 4 — Finding 8: bound user-regex input over response bodies (ReDoS)
- **Files:** `src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolLegacy.kt`, `src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorImpl.kt` (both `response_body_search`); shared const in `McpToolHelpers.kt`
- **Action:** Cap the response body to `MAX_REGEX_SCAN_CHARS = 64 * 1024` before `compiledRegex.matcher(...)`. (Tools are gated out of the store build via `nativeTool`, but PortSwigger reviews the full repo.)
- **Verify:** `compileKotlin` + MCP tool tests pass.
- **Done:** Neither `response_body_search` site matches user regex against an unbounded body.

### Task 5 — Finding 5: verify requestExecutor unload + harden
- **Files:** verify `BurpAiAgentExtension.kt` → `App.shutdown()` → `ActiveAiScanner.shutdown()`; optional split in `App.kt`
- **Action:** Confirm `requestExecutor` is shut down on unload (it is: `registerUnloadingHandler { App.shutdown() }` → `activeAiScanner.shutdown()` → `requestExecutor.shutdown()` + `awaitTermination` + `shutdownNow`). Harden `App.shutdown()` by splitting `setEnabled(false)` and `shutdown()` into separate `safeShutdownStep` calls so a throw in the first can't skip the second. Do NOT touch `stopProcessing()` (that pool must survive enable/disable cycles).
- **Verify:** `compileKotlin` passes.
- **Done:** Verification documented; hardening committed.

## Gate

`JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew compileKotlin test` passes before the task is considered complete.
